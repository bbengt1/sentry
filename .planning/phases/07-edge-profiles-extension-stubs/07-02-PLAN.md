---
phase: 07-edge-profiles-extension-stubs
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - docs/export/README.md
  - docs/export/yolo26-onnx-tensorrt.md
  - docs/export/yoloe-export.md
  - docs/export/depth-anything-v2.md
  - docs/export/jetson-packaging.md
  - scripts/export/README.md
  - scripts/export/export_yolo.py
  - tests/test_export_docs.py
  - tests/test_export_script_cli.py
  - README.md
autonomous: true
requirements:
  - EDGE-03
user_setup: []
must_haves:
  truths:
    - "ONNX and TensorRT export recipes exist as docs + scripts without requiring Jetson in CI (EDGE-03)"
    - "Docs require on-device TensorRT engine builds and forbid copying .engine across JetPack/GPU SKUs"
    - "No prebuilt .engine files are shipped in the repo or wheel"
    - "YOLO26 export uses Ultralytics model.export (onnx and engine formats documented)"
    - "YOLOE export is documented as experimental with PyTorch open-vocab as supported edge fallback"
    - "Depth Anything V2 export section is feasibility notes + community links; live path remains HF Small"
    - "Jetson packaging notes cover JetPack/TensorRT on-device build, YOLO n + DAV2 Small, open-vocab off/on-demand, no unmeasured dual-model realtime claim"
    - "Pi/CPU messaging is spatial awareness lite / best-effort — no sustained dual-model realtime FPS claim"
    - "Export script argparse restricts weights to KNOWN_WEIGHTS basenames; --help works without GPU"
    - "Default pytest suite never runs real model.export or weight downloads"
  artifacts:
    - path: "docs/export/README.md"
      provides: "Export index + honesty notes"
    - path: "docs/export/yolo26-onnx-tensorrt.md"
      provides: "YOLO26 ONNX/TensorRT recipes"
    - path: "docs/export/yoloe-export.md"
      provides: "YOLOE export caveats + PyTorch fallback"
    - path: "docs/export/depth-anything-v2.md"
      provides: "Depth export feasibility notes"
    - path: "docs/export/jetson-packaging.md"
      provides: "Jetson/JetPack on-device packaging notes"
    - path: "scripts/export/export_yolo.py"
      provides: "Thin Ultralytics export CLI wrapper"
    - path: "scripts/export/README.md"
      provides: "How to run export scripts"
    - path: "tests/test_export_docs.py"
      provides: "Keyword/content assertions for export honesty"
    - path: "tests/test_export_script_cli.py"
      provides: "Argparse/--help without GPU export"
  key_links:
    - from: "scripts/export/export_yolo.py"
      to: "ultralytics YOLO.export"
      via: "format onnx|engine when executed by maker"
      pattern: "export\\(|format"
    - from: "scripts/export/export_yolo.py"
      to: "KNOWN_WEIGHTS"
      via: "basename allowlist"
      pattern: "KNOWN_WEIGHTS|yolo26"
    - from: "docs/export/jetson-packaging.md"
      to: "on-device engine build"
      via: "explicit non-portability language"
      pattern: "on-device|JetPack|engine"
    - from: "README.md"
      to: "docs/export/"
      via: "edge export link"
      pattern: "docs/export|export"
---

<objective>
Ship offline ONNX/TensorRT export recipes and Jetson packaging notes so makers can deploy edge engines without a full TensorRT runtime inside Sentry v1.

Purpose: Close EDGE-03 with docs + scripts only. Live inference remains the PyTorch paths from 07-01 profile wiring; export is an offline tool path.

Output: `docs/export/*`, `scripts/export/export_yolo.py`, content/CLI tests that never require Jetson or weight download in default CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-edge-profiles-extension-stubs/07-CONTEXT.md
@.planning/phases/07-edge-profiles-extension-stubs/07-RESEARCH.md
@.planning/phases/07-edge-profiles-extension-stubs/07-PATTERNS.md
@.planning/phases/07-edge-profiles-extension-stubs/07-VALIDATION.md
@.planning/phases/07-edge-profiles-extension-stubs/07-01-PLAN.md
@docs/camera-sources.md
@THIRD_PARTY_MODELS.md
@src/sentry_ai/models/cache.py

## Soft dependency on 07-01

07-02 is wave 2 and may run parallel with 07-03. Docs should reference profiles as shipped (`--profile jetson|desktop-gpu|cpu-fallback`) and preferred_backend honesty language from RESEARCH even if 07-01 lands same milestone. Do not import runtime code from incomplete profile helpers into export scripts beyond `KNOWN_WEIGHTS` if available — hardcode the same allowlist strings as fallback only if import path is awkward for scripts outside package install (prefer importing `sentry_ai.models.cache.KNOWN_WEIGHTS`).

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| Deliverable shape | Docs + scripts only | CONTEXT / EDGE-03 |
| Export API | Ultralytics `model.export` | RESEARCH verified 8.4.116 |
| TRT engines | Build on target device; never ship prebuilt multi-SKU engines | CONTEXT deferred + RESEARCH |
| CI | No Jetson, no real export, no weight download in default suite | VALIDATION |
| tensorrt pip extra | Do not add | RESEARCH package audit |
| Live path | Remains PyTorch; export is offline | CONTEXT |
| YOLOE | Experimental export; PyTorch OV supported on edge | RESEARCH A1 |
| Depth | HF Small live; community ONNX/TRT notes only | RESEARCH |
| Pi claims | No dual-model realtime FPS without measurement | CONTEXT honesty |

## Interfaces / APIs to use

Ultralytics (detect extra; maker machine):
- `from ultralytics import YOLO, YOLOE`
- `YOLO("yolo26n.pt").export(format="onnx", imgsz=640, simplify=True)`
- `YOLO(...).export(format="engine", imgsz=640, quantize=16, device=0)` on NVIDIA+TRT host

From `src/sentry_ai/models/cache.py`:
- `KNOWN_WEIGHTS` allowlist for script `--weights` basename validation

Doc style analog:
- `docs/camera-sources.md` honesty tables + deferred lists
- `tests/test_third_party_models_doc.py` keyword assertions

## Out of scope for 07-02

- Real TensorRT/ONNX Runtime InferenceBackend
- Prebuilt engines in GitHub Releases
- `tensorrt` or required `onnx` package extras
- Full ROS2 / voice (07-03)
- Profile application code (07-01)
- Measured Jetson FPS tables (document measure-on-device)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export documentation suite + Jetson packaging honesty</name>
  <files>docs/export/README.md, docs/export/yolo26-onnx-tensorrt.md, docs/export/yoloe-export.md, docs/export/depth-anything-v2.md, docs/export/jetson-packaging.md, README.md, tests/test_export_docs.py</files>
  <read_first>
    - .planning/phases/07-edge-profiles-extension-stubs/07-RESEARCH.md (EDGE-03 full section, pitfalls 1–3/7, hard rules)
    - .planning/phases/07-edge-profiles-extension-stubs/07-PATTERNS.md (docs honesty pattern)
    - docs/camera-sources.md
    - THIRD_PARTY_MODELS.md
    - README.md
    - tests/test_third_party_models_doc.py
  </read_first>
  <behavior>
    - docs/export/README.md indexes child docs; states export is offline; live serve remains PyTorch profiles
    - yolo26-onnx-tensorrt.md documents ONNX and TensorRT engine export via Ultralytics; bold on-device TRT build; forbid cross-SKU engine copy
    - yoloe-export.md documents experimental YOLOE export; PyTorch on-demand OV remains supported edge path; AGPL pointer
    - depth-anything-v2.md states Sentry live path is HF Transformers Small; community ONNX/TRT links only; preserve relative vs metric_estimated honesty (no silent meters)
    - jetson-packaging.md: JetPack/TensorRT "as of" research-date honesty; on-device engine build; profile jetson = n + DAV2 Small + OV off/on-demand; no Pi dual-model realtime claim; measure FPS on device; no prebuilt engines
    - README links to docs/export/ for edge deployment
    - tests/test_export_docs.py asserts files exist and keywords: ONNX, TensorRT, on-device, JetPack or Jetson, no engine-copy guidance, lite/best-effort for Pi/CPU, AGPL or THIRD_PARTY reference, depth_kind or relative honesty
  </behavior>
  <action>
    1. Create `docs/export/` with the five markdown files per RESEARCH layout. Copy honesty-matrix style from `docs/camera-sources.md`.

    2. Hard rules that must appear in prose (tests will keyword-check):
       - Build TensorRT engines on the target device (same GPU arch + TensorRT/JetPack)
       - Never copy `.engine` across JetPack SKUs or desktop→Jetson
       - Do not ship prebuilt engines in the repo
       - CI does not require Jetson
       - Live Sentry inference stays PyTorch unless a future backend ships
       - Jetson: YOLO n + DAV2 Small; open-vocab off or on-demand
       - CPU/Pi: spatial awareness lite / best-effort — no unmeasured dual-model realtime FPS
       - YOLOE export experimental; keep PyTorch OV fallback
       - Depth export must not rebrand relative outputs as meters
       - AGPL caution for Ultralytics YOLO/YOLOE (link THIRD_PARTY_MODELS.md)

    3. Jetson packaging doc: optional matrix table with "as of 2026-08 research; verify on device" — do not invent FPS numbers.

    4. README: short Edge / export subsection linking `docs/export/README.md` and noting primary desktop path still uses profiles (full desktop doc is 07-03). Do not claim Phase 7 complete features that 07-03 owns.

    5. `tests/test_export_docs.py` following third_party doc test pattern: pathlib read + asserts. Include a negative check that docs do not instruct copying engines between devices as a supported path (e.g. require phrases like "do not copy" / "on-device" / "not portable").
  </action>
  <verify>
    <automated>uv run pytest tests/test_export_docs.py -q</automated>
  </verify>
  <done>Export + Jetson docs exist with honesty constraints enforced by keyword tests; README links export docs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: export_yolo.py script + safe CLI tests (no GPU export in CI)</name>
  <files>scripts/export/export_yolo.py, scripts/export/README.md, tests/test_export_script_cli.py</files>
  <read_first>
    - .planning/phases/07-edge-profiles-extension-stubs/07-RESEARCH.md (export script layout, Ultralytics API, path traversal threat)
    - src/sentry_ai/models/cache.py (KNOWN_WEIGHTS)
    - docs/export/yolo26-onnx-tensorrt.md (after Task 1)
    - tests/test_model_cache.py
  </read_first>
  <behavior>
    - scripts/export/export_yolo.py is a thin CLI (argparse or typer) not imported by sentry_ai runtime package
    - Supports --weights (basename), --format onnx|engine, --imgsz default 640, optional --device for engine
    - Rejects weights not in KNOWN_WEIGHTS (or equivalent basename allowlist matching yolo26*.pt / yoloe-*.pt) with non-zero exit
    - Rejects path traversal attempts like ../../etc/passwd or absolute paths outside allowlist
    - --help exits 0 and mentions onnx and engine
    - Actual model.export only runs when invoked by user with detect extra; default unit tests never call export (mock or test parse-only helpers)
    - scripts/export/README.md shows example commands using uv run python scripts/export/export_yolo.py ...
    - No tensorrt pip dependency declared in pyproject
  </behavior>
  <action>
    1. Implement `scripts/export/export_yolo.py`:
       - Prefer `from sentry_ai.models.cache import KNOWN_WEIGHTS` when package importable; fail closed on unknown basenames
       - Parse args; validate weights basename only (Path(name).name == name and name in KNOWN_WEIGHTS)
       - For format onnx/engine call Ultralytics YOLO (or YOLOE if weight name starts with yoloe) `.export(...)` only in main path when not under unit-test dry mode
       - Structure code so tests can invoke a `parse_args` / `validate_weights` function without ultralytics

    2. `scripts/export/README.md`: how to run; requires `uv sync --extra detect`; engine needs NVIDIA+TensorRT on that machine; point to docs/export for Jetson on-device notes.

    3. `tests/test_export_script_cli.py`:
       - Import validate helpers or run subprocess `python scripts/export/export_yolo.py --help`
       - Assert --help success
       - Assert invalid weights rejected
       - Assert path traversal rejected
       - Do not call real export; do not download weights
       - Optional: mark a future full-export test with `@pytest.mark.export` skipped by default (only if useful)

    4. Do not add onnx/tensorrt to pyproject extras.
  </action>
  <verify>
    <automated>uv run pytest tests/test_export_docs.py tests/test_export_script_cli.py -q</automated>
  </verify>
  <done>Export script is safe, documented, and CI-testable without GPU; EDGE-03 recipes complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maker CLI → export script | Untrusted --weights paths on developer machine |
| Docs → operator trust | Misleading engine portability or FPS claims cause unsafe deploy assumptions |
| Optional pip packages | tensorrt/onnx wheels must not enter core product extras |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | export_yolo.py --weights | mitigate | KNOWN_WEIGHTS basename allowlist; reject path traversal |
| T-07-11 | Tampering | Engine portability guidance | mitigate | Docs + tests require on-device build / forbid cross-SKU copy |
| T-07-12 | Denial of service | CI running real export | mitigate | Tests never call model.export; no Jetson in CI |
| T-07-13 | Information disclosure | AGPL YOLO redistribution | mitigate | Export docs link THIRD_PARTY_MODELS.md AGPL caution |
| T-07-14 | Elevation of privilege | Depth export semantics | mitigate | Docs require preserving relative vs metric honesty |
| T-07-SC | Tampering | package installs | mitigate | No new required packages; no tensorrt extra |

</threat_model>

<verification>
- `uv run pytest tests/test_export_docs.py tests/test_export_script_cli.py -q` green
- Confirm no `.engine` files committed under repo
- Confirm pyproject has no tensorrt extra
</verification>

<success_criteria>
1. EDGE-03: ONNX and TensorRT recipes documented with on-device engine build notes
2. Jetson packaging notes honest (n + Small, OV off/on-demand, measure on device)
3. Export script help/validation works without GPU
4. No prebuilt engines; no Jetson CI requirement; no TRT runtime product code
</success_criteria>

<output>
Create `.planning/phases/07-edge-profiles-extension-stubs/07-02-SUMMARY.md` when done
</output>
