---
phase: 07-edge-profiles-extension-stubs
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/sentry_ai/extensions/__init__.py
  - src/sentry_ai/extensions/ros2/__init__.py
  - src/sentry_ai/extensions/ros2/bridge.py
  - src/sentry_ai/extensions/ros2/README.md
  - src/sentry_ai/plugins/builtins.py
  - src/sentry_ai/plugins/registry.py
  - pyproject.toml
  - docs/desktop-gpu.md
  - docs/safety-and-privacy.md
  - README.md
  - tests/test_camera_id_multi.py
  - tests/test_extensions_stubs.py
  - tests/test_plugins_registry.py
  - tests/test_desktop_docs.py
  - tests/test_safety_docs.py
autonomous: true
requirements:
  - EDGE-04
  - EDGE-01
user_setup: []
must_haves:
  truths:
    - "Multi-cam camera_id schema tests prove distinct camera identities (cam0 vs cam1) without multi-cam fusion product (EDGE-04)"
    - "Importable Ros2PerceptionBridge raises NotImplementedError on start/emit and does not depend on rclpy (EDGE-04)"
    - "VoiceNullSink (voice-null) is a no-op Sink registered in builtins and discoverable via entry points / health sinks list (EDGE-04)"
    - "Desktop GPU full pipeline is documented end-to-end as the primary maker path (EDGE-01)"
    - "Safety/privacy disclaimers finalize perception-only non-autonomy positioning, localhost default, free-space not a safety interlock, allow_cloud false"
    - "v1 remains single active source; multi-cam fusion deferred; camera_id is the extension key"
    - "No robot control / cmd_vel / motor fields introduced in stubs or docs"
  artifacts:
    - path: "src/sentry_ai/extensions/ros2/bridge.py"
      provides: "Ros2PerceptionBridge NotImplemented stub"
      exports: ["Ros2PerceptionBridge"]
    - path: "src/sentry_ai/extensions/ros2/README.md"
      provides: "ROS2 extension notes + deferred production scope"
    - path: "src/sentry_ai/plugins/builtins.py"
      provides: "VoiceNullSink no-op"
      exports: ["VoiceNullSink"]
    - path: "pyproject.toml"
      provides: "voice-null sinks entry point"
    - path: "docs/desktop-gpu.md"
      provides: "Primary maker desktop GPU E2E path"
    - path: "docs/safety-and-privacy.md"
      provides: "Finalized non-autonomy + privacy language"
    - path: "tests/test_camera_id_multi.py"
      provides: "Multi camera_id schema/store identity tests"
    - path: "tests/test_extensions_stubs.py"
      provides: "ROS2 + voice stub coverage"
    - path: "tests/test_desktop_docs.py"
      provides: "Desktop doc + README pointer assertions"
    - path: "tests/test_safety_docs.py"
      provides: "Safety/privacy keyword assertions"
  key_links:
    - from: "VoiceNullSink"
      to: "PluginRegistry sinks"
      via: "register_builtins + entry point voice-null"
      pattern: "voice-null|VoiceNullSink"
    - from: "Ros2PerceptionBridge"
      to: "extensions.ros2 README"
      via: "NotImplementedError message points to docs"
      pattern: "NotImplementedError|ros2"
    - from: "docs/desktop-gpu.md"
      to: "sentry serve --profile desktop-gpu"
      via: "primary path command"
      pattern: "desktop-gpu|detect|depth"
    - from: "docs/safety-and-privacy.md"
      to: "README + API-05"
      via: "perception-only / non-autonomy links"
      pattern: "perception-only|not a safety interlock|localhost"
---

<objective>
Leave clean post-v1 extension points (multi-cam camera_id tests, ROS2 bridge stub, voice no-op sink) and finalize release documentation for the desktop GPU primary path plus safety/privacy/non-autonomy positioning.

Purpose: Close EDGE-04 and EDGE-01. Stubs must import without heavy deps; docs make multi-target + ethical positioning explicit for v1 ship.

Output: `extensions/ros2`, VoiceNullSink registration, multi-id tests, `docs/desktop-gpu.md`, `docs/safety-and-privacy.md`, README links, doc/stub tests green without rclpy or Jetson.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-edge-profiles-extension-stubs/07-CONTEXT.md
@.planning/phases/07-edge-profiles-extension-stubs/07-RESEARCH.md
@.planning/phases/07-edge-profiles-extension-stubs/07-PATTERNS.md
@.planning/phases/07-edge-profiles-extension-stubs/07-VALIDATION.md
@.planning/phases/07-edge-profiles-extension-stubs/07-01-PLAN.md
@docs/camera-sources.md
@README.md
@THIRD_PARTY_MODELS.md

## Parallel with 07-02

Wave 2 parallel: no required file ownership overlap with 07-02 except `README.md`. Coordinate README edits carefully — 07-03 owns desktop GPU + safety sections; 07-02 owns export link section. If both touch README, merge non-conflicting subsections (Edge export vs Desktop primary path vs Safety). Prefer additive sections with distinct headings.

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| Stubs only | No full ROS2, multi-cam fusion, real voice | CONTEXT deferred |
| ROS2 shape | Importable NotImplemented bridge; no rclpy dep | RESEARCH discretion lock |
| ROS2 registration | Prefer importable without auto-register as sink (keep health clean); document entry-point snippet | RESEARCH |
| Voice | VoiceNullSink no-op; register sinks entry point voice-null | RESEARCH |
| Multi-cam | Schema/tests only; v1 single active source | CONTEXT |
| Desktop docs | Primary maker path EDGE-01 | ROADMAP success #1 |
| Safety docs | Perception-only, non-autonomy, privacy, free-space not interlock | RESEARCH + PROJECT |
| Local OSS | allow_cloud false remains | MODEL-01 |

## Interfaces (shipped — use these)

From schemas:
- `Frame.camera_id` required min_length=1
- `PerceptionFrame.camera_id` same
- Store products carry camera_id from loops

From plugins:
- `NullSink` pattern: name, emit, close
- `register_builtins` + entry points groups `sentry_ai.sinks`
- `discover()` skip-if-present

From docs analogs:
- `docs/camera-sources.md` honesty + deferred
- README privacy lines + free-space not safety interlock
- `tests/test_third_party_models_doc.py` content tests

## Out of scope for 07-03

- Production ROS2 package, launch files, bag recording
- Multi-cam fusion / calibration UX
- Real ASR/TTS voice I/O
- Authenticated remote API
- Export recipes (07-02)
- Profile runtime code (07-01)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extension stubs — multi camera_id tests, ROS2 bridge, VoiceNullSink</name>
  <files>src/sentry_ai/extensions/__init__.py, src/sentry_ai/extensions/ros2/__init__.py, src/sentry_ai/extensions/ros2/bridge.py, src/sentry_ai/extensions/ros2/README.md, src/sentry_ai/plugins/builtins.py, src/sentry_ai/plugins/registry.py, pyproject.toml, tests/test_camera_id_multi.py, tests/test_extensions_stubs.py, tests/test_plugins_registry.py</files>
  <read_first>
    - .planning/phases/07-edge-profiles-extension-stubs/07-RESEARCH.md (EDGE-04)
    - .planning/phases/07-edge-profiles-extension-stubs/07-PATTERNS.md (Pattern 4 plugins + multi-cam tests)
    - src/sentry_ai/plugins/builtins.py
    - src/sentry_ai/plugins/registry.py
    - src/sentry_ai/plugins/protocols.py
    - src/sentry_ai/schemas/frame.py
    - src/sentry_ai/schemas/perception.py
    - tests/test_schemas_frame.py
    - tests/test_schemas_perception.py
    - tests/test_plugins_registry.py
    - pyproject.toml (entry-points sinks)
  </read_first>
  <behavior>
    - Frame/PerceptionFrame with camera_id cam0 and cam1 validate as distinct identities; empty camera_id still rejected
    - Store product path: two DetectionProduct (or Frame) identities with different camera_id do not collapse to one id in schema sense
    - Docstring or README note: v1 single active source; multi-cam fusion is future; camera_id is extension key
    - Ros2PerceptionBridge importable from sentry_ai.extensions.ros2.bridge; name ros2_perception; start() and emit() raise NotImplementedError mentioning extension stub / README; close() no-op
    - Importing ros2 bridge does not import rclpy (assert "rclpy" not required)
    - VoiceNullSink name voice-null (or voice_null consistent with entry point voice-null); emit discards; close no-op
    - register_builtins registers voice-null when missing; pyproject entry point voice-null → VoiceNullSink
    - ROS2 stub not auto-registered as sink by default (health stays clean) unless docs show optional EP snippet
    - registry.discover remains idempotent with voice-null
  </behavior>
  <action>
    1. Multi-cam tests in `tests/test_camera_id_multi.py`:
       - Distinct Frame camera_ids cam_left/cam_right (or cam0/cam1)
       - Distinct PerceptionFrame camera_ids
       - Empty string rejected (reuse existing validation)
       - Optional: PerceptionStore set_detections with products carrying different camera_id fields independently (no fusion claim)
       - Module docstring states v1 is single active source

    2. Create `src/sentry_ai/extensions/` package + `ros2/bridge.py`:
       - Class `Ros2PerceptionBridge` per RESEARCH sketch
       - README.md: how integrators would map PerceptionFrame JSON to ROS2 messages later; deferred production scope (Humble/Jazzy package, lifecycle, bags); no rclpy install steps as required core deps; optional future entry-point example

    3. Add `VoiceNullSink` to builtins (export in `__all__`):
       - Twin NullSink; docstring EDGE-04 voice extension point — no ASR/TTS
       - register_builtins: register_sink("voice-null", VoiceNullSink) if missing
       - pyproject.toml: `voice-null = "sentry_ai.plugins.builtins:VoiceNullSink"` under sinks

    4. `tests/test_extensions_stubs.py`: import bridge; start/emit NotImplementedError; no rclpy; VoiceNullSink emit/close; optionally registry list includes voice-null after register_builtins.

    5. Extend `tests/test_plugins_registry.py` for voice-null presence and discover idempotency.

    6. Do not add rclpy to any extra. Do not implement real ROS publishers.
  </action>
  <verify>
    <automated>uv run pytest tests/test_camera_id_multi.py tests/test_extensions_stubs.py tests/test_plugins_registry.py tests/test_schemas_frame.py tests/test_schemas_perception.py -q</automated>
  </verify>
  <done>EDGE-04 stubs and multi-id tests land without rclpy; voice-null discoverable; ROS2 NotImplemented bridge importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Desktop GPU E2E docs + safety/privacy release docs + README</name>
  <files>docs/desktop-gpu.md, docs/safety-and-privacy.md, README.md, tests/test_desktop_docs.py, tests/test_safety_docs.py</files>
  <read_first>
    - .planning/phases/07-edge-profiles-extension-stubs/07-RESEARCH.md (EDGE-01, safety/privacy section)
    - .planning/phases/07-edge-profiles-extension-stubs/07-CONTEXT.md (success criteria 1 and 6)
    - README.md (install, serve, privacy, free-space, perception-only sections)
    - docs/camera-sources.md
    - THIRD_PARTY_MODELS.md
    - tests/test_third_party_models_doc.py
  </read_first>
  <behavior>
    - docs/desktop-gpu.md covers: Python 3.11+, uv sync --extra dev --extra detect --extra depth, sentry serve --profile desktop-gpu, USB example, model cache SENTRY_MODEL_CACHE / ~/.cache/sentry-ai, expected stages, UI URL http://127.0.0.1:8000/, AGPL caution + THIRD_PARTY_MODELS, optional open-vocab Run, robot clients /v1/snapshot + /v1/stream, headless --no-ui pointer, link export docs if present
    - Marks desktop GPU as primary maker path; cpu-fallback for CI/no-GPU; Jetson via export/profile notes
    - Does not invent measured FPS numbers ("depends on GPU/thermal")
    - docs/safety-and-privacy.md covers: perception-only (no motor/cmd_vel/path_plan); not autonomous driving/FSD; free-space not a safety interlock (STALE/incomplete); human/robot controller owns e-stop; privacy localhost default; LAN opt-in unauthenticated; allow_cloud false; headless ≠ auth; licenses summary pointer
    - README: primary path link to docs/desktop-gpu.md; safety link; profiles table or bullets; headless --no-ui; extension stubs note (ROS2/voice); consolidate without deleting existing good privacy language
    - tests assert file existence + keywords (desktop-gpu, detect, depth, perception-only, localhost, safety interlock or equivalent, not autonom)
  </behavior>
  <action>
    1. Write `docs/desktop-gpu.md` as the EDGE-01 primary path. Include both Live Preview and robot `/v1` consumers. Cross-link export recipes and safety doc. Reference profiles from 07-01 (`--profile desktop-gpu` opt-in; default cpu-fallback for non-GPU).

    2. Write `docs/safety-and-privacy.md` consolidating scattered README/cli warnings into one canonical page. Keep tone maker-honest, not legalistic boilerplate. Explicit non-autonomy positioning for monocular hobby perception.

    3. Update README:
       - Lead makers to desktop GPU path as primary
       - Profile examples (desktop-gpu / jetson / cpu-fallback)
       - Headless: `sentry serve --no-ui`
       - Links: desktop-gpu, export (if 07-02 merged), safety-and-privacy, camera-sources
       - Brief extension stubs mention (ROS2 bridge stub, voice-null, camera_id)
       - Keep API-05 perception-only denylist language

    4. `tests/test_desktop_docs.py` and `tests/test_safety_docs.py` content assertions per VALIDATION map. README must link desktop-gpu doc.

    5. Do not change REQUIREMENTS.md checkbox status (execute/verify owns that). Do not claim full ROS2/voice products.
  </action>
  <verify>
    <automated>uv run pytest tests/test_desktop_docs.py tests/test_safety_docs.py tests/test_camera_id_multi.py tests/test_extensions_stubs.py tests/test_plugins_registry.py -q</automated>
  </verify>
  <done>EDGE-01 desktop path documented; safety/privacy finalized; EDGE-04 stubs tested; phase doc/stub surface complete for wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extension stubs → integrators | Stubs must not imply production ROS2/voice safety |
| Docs → operator | Safety language must not overclaim autonomy or interlocks |
| Plugin entry points → process | Voice null sink must remain no-op |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Elevation of privilege | ROS2 bridge stub | mitigate | NotImplemented only; no cmd_vel fields; README non-autonomy |
| T-07-21 | Tampering | Voice sink | mitigate | No-op emit; no external I/O; no secrets |
| T-07-22 | Spoofing | Multi-cam camera_id | mitigate | Schema requires non-empty ids; tests distinct identities |
| T-07-23 | Information disclosure | Desktop/safety docs | mitigate | Localhost default + LAN unauth warning restated |
| T-07-24 | Denial of service | rclpy accidental dep | mitigate | No rclpy import; tests ensure stub imports without it |
| T-07-25 | Elevation of privilege | Free-space misuse | mitigate | Safety doc: not interlock; STALE/incomplete; e-stop external |
| T-07-SC | Tampering | package installs | accept | No new third-party packages for stubs |

</threat_model>

<verification>
- `uv run pytest tests/test_camera_id_multi.py tests/test_extensions_stubs.py tests/test_plugins_registry.py tests/test_desktop_docs.py tests/test_safety_docs.py -q` green
- `python -c "from sentry_ai.extensions.ros2.bridge import Ros2PerceptionBridge"` works without rclpy
- Full phase gate later: `uv run pytest -q` + ruff
</verification>

<success_criteria>
1. EDGE-04: multi-cam camera_id tests + ROS2 NotImplemented stub + VoiceNullSink
2. EDGE-01: desktop GPU E2E primary path documented and linked from README
3. Safety/privacy/non-autonomy positioning finalized in dedicated doc + README
4. No production ROS2/voice/multi-cam fusion scope creep
5. Phase 7 success criteria 1, 5, 6 from ROADMAP satisfied alongside 07-01/07-02
</success_criteria>

<output>
Create `.planning/phases/07-edge-profiles-extension-stubs/07-03-SUMMARY.md` when done
</output>
