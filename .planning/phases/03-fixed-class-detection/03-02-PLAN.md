---
phase: 03-fixed-class-detection
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/sentry_ai/models/detection/overlay.py
  - src/sentry_ai/api/routes_detection.py
  - src/sentry_ai/api/routes_preview.py
  - src/sentry_ai/api/app.py
  - src/sentry_ai/api/deps.py
  - src/sentry_ai/capture/status.py
  - src/sentry_ai/capture/loop.py
  - src/sentry_ai/cli.py
  - src/sentry_ai/ui/static/index.html
  - README.md
  - tests/test_detection_overlay.py
  - tests/test_api_detection.py
  - tests/test_api_preview.py
  - tests/test_cli_serve.py
autonomous: true
requirements:
  - DET-03
  - DET-04
user_setup: []
must_haves:
  truths:
    - "GET /api/snapshot returns PerceptionFrame JSON with completeness.detections true when store has a product (including empty list)"
    - "Snapshot detections list is the same store product used for MJPEG overlay draw (DET-04 parity)"
    - "PATCH /api/detection/config updates worker conf without process restart; GET returns current conf (DET-03)"
    - "Live Preview MJPEG shows server-drawn boxes + class + conf labels from store"
    - "Status/UI shows detection count, det latency ms, and current conf; conf control debounced ~100–200ms"
    - "sentry serve starts DetectionLoop when detect extra available; clear error/log when missing; still serves capture preview"
    - "Handlers never open cameras; inference never runs inside FastAPI request/MJPEG encode path"
    - "Localhost default bind preserved; no autonomy/safety language in UI"
  artifacts:
    - path: "src/sentry_ai/models/detection/overlay.py"
      provides: "draw_detections pure OpenCV helper"
      exports: ["draw_detections"]
    - path: "src/sentry_ai/api/routes_detection.py"
      provides: "snapshot + detection config routes"
      exports: ["router"]
    - path: "src/sentry_ai/api/routes_preview.py"
      provides: "MJPEG encodes bus frame + store overlay"
    - path: "src/sentry_ai/api/app.py"
      provides: "create_app injects perception_store + detection_worker"
      exports: ["create_app"]
    - path: "src/sentry_ai/ui/static/index.html"
      provides: "conf control + det telemetry (UI-SPEC Phase 3)"
      contains: "detection"
    - path: "src/sentry_ai/cli.py"
      provides: "serve starts DetectionLoop lifecycle"
    - path: "tests/test_api_detection.py"
      provides: "DET-03/DET-04 ASGI coverage"
    - path: "tests/test_detection_overlay.py"
      provides: "overlay pure-function coverage"
  key_links:
    - from: "src/sentry_ai/api/routes_preview.py"
      to: "PerceptionStore.snapshot"
      via: "draw_detections before imencode"
      pattern: "draw_detections|perception_store|get_latest"
    - from: "src/sentry_ai/api/routes_detection.py"
      to: "PerceptionStore + PerceptionFrame"
      via: "GET /api/snapshot assembly"
      pattern: "snapshot|PerceptionFrame|completeness"
    - from: "src/sentry_ai/api/routes_detection.py"
      to: "YoloDetectionWorker.set_conf"
      via: "PATCH /api/detection/config"
      pattern: "set_conf|detection/config"
    - from: "src/sentry_ai/ui/static/index.html"
      to: "/api/detection/config"
      via: "debounced PATCH on conf slider"
      pattern: "detection/config"
    - from: "src/sentry_ai/cli.py serve"
      to: "DetectionLoop.start/stop"
      via: "lifecycle with CaptureLoop"
      pattern: "DetectionLoop|det_loop"
---

<objective>
Expose fixed-class detections with UI/API parity: server-side OpenCV overlays on MJPEG, GET /api/snapshot PerceptionFrame JSON, runtime conf PATCH, status telemetry, Live Preview controls, and sentry serve wiring for DetectionLoop.

Purpose: Close DET-03 and DET-04; complete Phase 3 success criteria (boxes on dashboard, same truth on stream/snapshot, conf without restart) on top of 03-01 core.

Output: Routes + overlay + extended status/UI + serve lifecycle; tests prove parity and runtime conf without real YOLO weights.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-fixed-class-detection/03-CONTEXT.md
@.planning/phases/03-fixed-class-detection/03-RESEARCH.md
@.planning/phases/03-fixed-class-detection/03-PATTERNS.md
@.planning/phases/03-fixed-class-detection/03-UI-SPEC.md
@.planning/phases/03-fixed-class-detection/03-VALIDATION.md
@.planning/phases/03-fixed-class-detection/03-01-PLAN.md
@.planning/phases/02-camera-ingest-live-preview/02-03-SUMMARY.md

## Depends on 03-01

- `YoloDetectionWorker`, `DetectionLoop`, `PerceptionStore`, `results_to_detections`, `configure_model_cache`, `tier_to_weight`
- optional-dependencies.detect; AGPL/cache docs

## Locked decisions (do not re-litigate)

| Decision | Value |
|----------|-------|
| Overlay transport | **Option A**: server-side OpenCV draw on BGR before MJPEG JPEG encode (UI-SPEC recommended) |
| Drawing library | OpenCV only — no `supervision` |
| Snapshot path | `GET /api/snapshot` (not full `/v1` robot stream — Phase 5) |
| Conf API | `GET` + `PATCH /api/detection/config` with body `{"conf": float}` Field ge=0 le=1, extra=forbid |
| Single truth | MJPEG + snapshot + UI metrics all read PerceptionStore (no second model path) |
| Inference location | Still only DetectionLoop thread — never in handlers |
| create_app | Inject store/worker; do **not** start threads (caller owns lifecycle) |
| Out of scope | Depth overlays, free-space, open-vocab, multi-stage enable matrix (Phase 6), tracking |

## Interfaces from 03-01 / Phase 2

From PerceptionStore (03-01):
- `snapshot() -> DetectionProduct | None` with detections, frame_id, camera_id, t_capture, latency_ms, conf, model_name

From YoloDetectionWorker:
- `set_conf(conf: float)`, `get_conf() -> float`, `process(frame) -> list[Detection]`

From Phase 2 API:
- `create_app(bus=, capture_loop=, bind=)` → extend with optional `perception_store=`, `detection_worker=`
- `routes_preview._mjpeg_generator(bus)` → also needs store for draw
- `StatusSnapshot` capture fields — extend carefully with optional det fields (extra=forbid means all new fields must be declared)

From schemas:
- Assemble `PerceptionFrame` with `completeness=Completeness(detections=True, depth=False, free_space=False)` when store has product; empty detections list is valid complete stage

## UI-SPEC layout (footer)

- Source | FPS | Drops | Detections: n | Det ms
- Conf threshold: slider + numeric display (default 0.25)
- Bind unchanged; status pill preserved
- Note: model may download on first run
- Forbidden copy: autonomous, safe to drive, motor commands
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overlay helper + detection routes + create_app injection (DET-04 API + DET-03 PATCH)</name>
  <files>src/sentry_ai/models/detection/overlay.py, src/sentry_ai/api/routes_detection.py, src/sentry_ai/api/app.py, src/sentry_ai/api/deps.py, tests/test_detection_overlay.py, tests/test_api_detection.py</files>
  <read_first>
    - .planning/phases/03-fixed-class-detection/03-RESEARCH.md (Pattern 4 overlay, Pattern 5–6 API/snapshot, FastAPI PATCH sketch)
    - .planning/phases/03-fixed-class-detection/03-PATTERNS.md (overlay, routes_detection, app.py/deps.py)
    - .planning/phases/03-fixed-class-detection/03-UI-SPEC.md (overlay rules, label format)
    - src/sentry_ai/api/app.py
    - src/sentry_ai/api/deps.py
    - src/sentry_ai/api/routes_preview.py
    - src/sentry_ai/schemas/perception.py
    - tests/test_api_preview.py (TestClient + create_app pattern)
    - src/sentry_ai/state/perception_store.py (03-01 product)
    - src/sentry_ai/models/detection/yolo_worker.py (set_conf/get_conf)
  </read_first>
  <behavior>
    - draw_detections copies image; draws rectangle + label `{class_name} {confidence:.2f}`; empty list leaves image content equivalent (copy only)
    - GET /api/snapshot with empty store → 404 or 503 with clear body (pick one; document; prefer 404 "no detection product yet")
    - GET /api/snapshot after set_detections → 200 PerceptionFrame; completeness.detections true; detections match store
    - PATCH /api/detection/config {"conf": 0.4} → 200 {"conf": 0.4}; worker.get_conf()==0.4
    - PATCH conf out of range or extra fields → 422
    - PATCH/GET config when worker missing → 503
    - create_app without store/worker still serves Phase 2 preview routes (backward compatible defaults None)
  </behavior>
  <action>
    1. Implement pure `draw_detections(image_bgr, detections) -> np.ndarray` in `overlay.py`:
       - Copy input; for each Detection: int xyxy rectangle color high-contrast e.g. (0, 255, 180); putText label above box
       - No ultralytics; unit-testable with synthetic array + known bbox (pixel change near box corners optional)

    2. Implement `routes_detection.py` APIRouter:
       - Helpers `_store`, `_detection_worker` from `request.app.state` (same style as routes_preview)
       - `GET /api/snapshot`: build PerceptionFrame from store.snapshot() per RESEARCH Pattern 6; include stats det_latency_ms, det_conf, det_model when available; t_publish=time.time(); return model_dump()
       - Completeness: detections=True when product exists (including empty list)
       - `GET /api/detection/config`: conf, weights/model name if available, device if available
       - `PATCH /api/detection/config`: Pydantic body `DetectionConfigUpdate` with `conf: float = Field(ge=0.0, le=1.0)`, ConfigDict(extra="forbid"); call worker.set_conf; return applied conf
       - HTTPException 503 when worker/store missing as appropriate
       - Never run YOLO or open cameras in handlers

    3. Extend `AppState` + `create_app`:
       - Optional kwargs `perception_store=None`, `detection_worker=None` (and attach on app.state)
       - `app.include_router(detection_router)` next to preview
       - Keep docs_url/redoc_url None; factory still does not start threads

    4. Unskip/fill `tests/test_detection_overlay.py` and `tests/test_api_detection.py`:
       - Fake worker with set_conf/get_conf; pre-seed PerceptionStore; TestClient
       - Assert snapshot detections identity with store list content
       - Assert no VideoCapture in routes_detection module source
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_detection_overlay.py tests/test_api_detection.py &amp;&amp; uv run ruff check src/sentry_ai/models/detection/overlay.py src/sentry_ai/api/routes_detection.py src/sentry_ai/api/app.py src/sentry_ai/api/deps.py</automated>
  </verify>
  <acceptance_criteria>
    - DET-04 API surface: snapshot JSON matches store detections
    - DET-03 API: PATCH conf updates worker without reload
    - create_app injects optional store/worker; Phase 2 tests still constructable
    - Overlay pure function covered without YOLO
  </acceptance_criteria>
  <done>Snapshot + conf API + overlay helper + app injection landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MJPEG store overlay + status telemetry + sentry serve DetectionLoop</name>
  <files>src/sentry_ai/api/routes_preview.py, src/sentry_ai/capture/status.py, src/sentry_ai/capture/loop.py, src/sentry_ai/cli.py, tests/test_api_preview.py, tests/test_cli_serve.py, tests/test_api_detection.py</files>
  <read_first>
    - .planning/phases/03-fixed-class-detection/03-RESEARCH.md (architecture diagram, Pattern 4 temporal skew, Pitfall 1–2, cli serve sketch)
    - .planning/phases/03-fixed-class-detection/03-PATTERNS.md (routes_preview overlay, cli.py serve lifecycle, StatusSnapshot optional fields)
    - src/sentry_ai/api/routes_preview.py (_mjpeg_generator full)
    - src/sentry_ai/capture/status.py (StatusSnapshot extra=forbid)
    - src/sentry_ai/capture/loop.py (build_status)
    - src/sentry_ai/cli.py (serve command block ~192–281)
    - tests/test_api_preview.py
    - tests/test_cli_serve.py
  </read_first>
  <behavior>
    - MJPEG path still returns multipart JPEG; with store detections, encoded frame is drawn (smoke: generator callable / status still 200)
    - /api/status includes det fields when available: detections_count, det_latency_ms, det_conf, det_fps (names consistent across UI)
    - serve constructs PerceptionStore + worker (if importable) + DetectionLoop; starts capture then detection; stops detection then capture in finally
    - serve without detect extra: does not crash; logs clear message; preview still works
    - allow_cloud gate unchanged; host default 127.0.0.1 unchanged
    - No YOLO call inside routes_preview module
  </behavior>
  <action>
    1. Update `_mjpeg_generator` to accept store (or read from app.state via closure in route):
       - For each bus frame: if store has detections product, `draw_detections(item.image_bgr, product.detections)` then imencode
       - If no product, encode raw BGR (Phase 2 behavior)
       - Keep asyncio.sleep(MJPEG_SLEEP_S); never call worker.process here
       - Accept temporal skew (detection frame_id may lag RGB) — include det frame_id in status/snapshot only, not force match

    2. Extend status telemetry (DET-04 UI metrics / UI-05 partial):
       - Add optional fields to `StatusSnapshot` with defaults so Phase 2 callers remain valid, e.g.:
         `detections_count: int | None = None`, `det_latency_ms: float | None = None`, `det_conf: float | None = None`, `det_fps: float | None = None`, `det_frame_id: int | None = None`
       - Prefer merging det metrics in `api_status` handler (read store + worker) rather than forcing CaptureLoop to know about detection — keeps capture module clean
       - Alternative: extend `build_status` with optional store/worker kwargs — acceptable if cleaner for tests; do not couple CaptureLoop thread to detection
       - **Recommended:** in `routes_preview.api_status`, dump capture snapshot then update dict with store/worker fields when present

    3. Wire `cli.serve` lifecycle (DET-01 live path completion):
       - After bus + CaptureLoop: `store = PerceptionStore()`
       - Try import YoloDetectionWorker + configure_model_cache; map profile `detector_tier` via `tier_to_weight`; construct worker with conf 0.25 (or profile if ModelsConfig has detector_conf)
       - `det_loop = DetectionLoop(bus, worker, store)` when worker available else None
       - `create_app(..., perception_store=store, detection_worker=worker)`
       - `loop.start(); det_loop.start() if det_loop`
       - `finally: det_loop.stop() if det_loop; loop.stop()`
       - If ultralytics missing: log error with install hint (`uv sync --extra detect`); continue capture-only serve
       - Keep allow_cloud rejection; localhost warning on non-loopback host

    4. Update tests:
       - test_api_preview: create_app may pass store=None; MJPEG still 200; optional store overlay smoke
       - test_cli_serve: host default still 127.0.0.1; serve wiring does not require real YOLO (mock/patch worker construction if needed)
       - Extend test_api_detection for status det fields if asserted via TestClient

    5. Preserve architecture tests: routes never VideoCapture; no torch import on bare smoke path.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_api_preview.py tests/test_api_detection.py tests/test_cli_serve.py tests/test_cli_smoke.py &amp;&amp; uv run ruff check src/sentry_ai/api src/sentry_ai/capture/status.py src/sentry_ai/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - MJPEG draws from same PerceptionStore as snapshot (DET-04)
    - Status exposes det count/latency/conf for UI poll
    - serve starts/stops DetectionLoop when worker available; degrades cleanly without detect extra
    - MODEL-03 localhost default preserved; smoke still green without torch
  </acceptance_criteria>
  <done>Live overlay path + telemetry + serve detection lifecycle complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Live Preview conf control + telemetry UI + docs polish</name>
  <files>src/sentry_ai/ui/static/index.html, README.md, tests/test_api_preview.py, tests/test_api_detection.py</files>
  <read_first>
    - .planning/phases/03-fixed-class-detection/03-UI-SPEC.md (full layout, interaction, acceptance)
    - .planning/phases/03-fixed-class-detection/03-PATTERNS.md (index.html poll + PATCH debounce)
    - src/sentry_ai/ui/static/index.html (current status poll + footer)
    - tests/test_api_preview.py (HTML content assertions — no autonomous language)
  </read_first>
  <behavior>
    - HTML contains conf control (input range or number) and labels for Detections / Det ms
    - JS debounces conf PATCH (~100–200ms) to /api/detection/config
    - applyStatus maps detections_count, det_latency_ms, det_conf from /api/status
    - Still auto-connects /preview/mjpeg; no canvas overlay path
    - No strings: autonomous, safe to drive, motor, velocity command language
    - Optional first-run note about model download/cache
  </behavior>
  <action>
    1. Extend `index.html` per UI-SPEC Phase 3:
       - Footer metrics: Detections: n | Det ms (and conf display)
       - Conf threshold slider + value readout; default 0.25
       - Debounced PATCH on input (100–200ms); on success update displayed conf from response
       - Keep status pill + capture FPS/drops/bind
       - Single img src `/preview/mjpeg` (server-drawn boxes)
       - Short note: model may download on first run; cached thereafter (MODEL-02 UX)
       - No depth/free-space/open-vocab controls

    2. README polish:
       - Document `sentry serve` detection: optional `--extra detect`, conf API, snapshot path, SENTRY_MODEL_CACHE, AGPL pointer
       - Example: synthetic serve after detect install
       - Keep one-command local start for non-detect path working

    3. Tests:
       - HTML served at GET / still 200; assert key UI strings (Conf, Detections, detection/config or conf control markers)
       - Preserve forbidden-language assertions from Phase 2
       - Full suite green: `uv run pytest -q` + `uv run ruff check src tests`

    4. Manual-only (not automated gate): real YOLO on synthetic/USB after weight download — document in SUMMARY notes; do not fail CI on missing weights.
  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - DET-03: UI can change conf without restart via PATCH
    - DET-04: UI shows server overlays + metrics from same status/store truth
    - UI-SPEC acceptance items 1–4 covered by automation where possible + documented manual check for real YOLO
    - Full suite green; ruff clean; Phase 3 requirements DET-01..04 + MODEL-02 satisfied end-to-end with mocks
  </acceptance_criteria>
  <done>Phase 3 UI/API surface complete; fixed-class detection shippable for makers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI | Unauthenticated localhost control plane (conf PATCH, status) |
| FastAPI handlers → store/worker | Trusted in-process; no re-inference in handlers |
| MJPEG bytes → browser | Drawn overlays reflect store detections only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | weight load (serve path) | mitigate | Only tier_to_weight allowlisted names via 03-01 cache policy |
| T-03-02 | Elevation of Privilege | PATCH /api/detection/config | mitigate | Default bind 127.0.0.1 (MODEL-03); Pydantic conf [0,1] extra=forbid; LAN warn on 0.0.0.0 |
| T-03-03 | Tampering / Integrity | MJPEG vs snapshot | mitigate | Both read PerceptionStore only; single draw helper; tests assert list parity |
| T-03-04 | Denial of Service | conf slider thrash / huge encode | mitigate | UI debounce 100–200ms; keep-latest detection drops; MJPEG sleep cap; imgsz 640 in worker |
| T-03-05 | Information Disclosure | UI copy / first-run download | mitigate | No cloud upload; first-run download note; AGPL docs already |
| T-03-SC | Tampering | dependency installs | mitigate | No new packages in 03-02 beyond 03-01 detect extra; no supervision |
</threat_model>

<verification>
- `uv run pytest -q` full suite including test_api_detection, test_detection_overlay, test_api_preview, test_cli_serve
- `uv run ruff check src tests`
- Parity: snapshot detections == store product used for draw
- Conf: PATCH then GET reflects new value; worker.get_conf matches
- Architecture: no VideoCapture in api routes; no predict in routes_preview
</verification>

<success_criteria>
- Dashboard MJPEG shows labeled boxes from store detections
- GET /api/snapshot returns same detections with completeness.detections true
- Runtime conf changes via API/UI without restart (DET-03)
- Status shows detection count + latency + conf
- serve wires DetectionLoop with graceful degrade without detect extra
- Phase 3 roadmap success criteria 1–5 met (real YOLO manual optional for #1/#5 offline demo)
</success_criteria>

<output>
Create `.planning/phases/03-fixed-class-detection/03-02-SUMMARY.md` when done
</output>
