---
phase: 11-sticky-fallback-dual-model-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sentry_ai/config/models.py
  - src/sentry_ai/config/load.py
  - src/sentry_ai/config/profile_runtime.py
  - src/sentry_ai/models/detection/factory.py
  - src/sentry_ai/cli.py
  - tests/test_detection_factory.py
  - docs/configuration.md
  - docs/architecture.md
autonomous: true
requirements:
  - BACK-03
user_setup: []
must_haves:
  truths:
    - "When preferred ORT/TRT cannot go live and fallback_to_torch is true (default), factory returns a torch worker with backend_live=torch and a stable reason code (soft)"
    - "When preferred ORT/TRT cannot go live and fallback_to_torch is false (strict), factory returns worker=None, backend_live=None, and the same reason codes — never silent torch under ORT/TRT labels"
    - "Strict serve fails closed with non-zero exit after printing requested/reason (typer.Exit(1)); soft serve continues with torch detection"
    - "Resolve is sticky: build_detection_worker is called once at serve construct; DetectionLoop never re-resolves preferred backend"
    - "When backend_reason is set, factory emits exactly one structured log per construct call (warning soft / error strict)"
    - "Soft remains global default including jetson; jetson YAML field values are not flipped to strict"
    - "No new pip packages; factory still uses find_spec only (no module-level onnxruntime/tensorrt imports)"
  artifacts:
    - path: "src/sentry_ai/config/models.py"
      provides: "DeviceConfig.fallback_to_torch default True"
      contains: "fallback_to_torch"
    - path: "src/sentry_ai/config/load.py"
      provides: "SENTRY_FALLBACK_TO_TORCH env override (always-wins when set)"
      contains: "SENTRY_FALLBACK_TO_TORCH"
    - path: "src/sentry_ai/config/profile_runtime.py"
      provides: "ProfileRuntime.fallback_to_torch plumbed from DeviceConfig"
      contains: "fallback_to_torch"
    - path: "src/sentry_ai/models/detection/factory.py"
      provides: "soft/strict miss helper + once-per-construct reason log"
      exports: ["WorkerBuild", "build_detection_worker"]
    - path: "src/sentry_ai/cli.py"
      provides: "strict fail-closed Exit(1) when worker is None after factory"
    - path: "tests/test_detection_factory.py"
      provides: "strict matrix + soft regression + sticky + log-once coverage"
    - path: "docs/configuration.md"
      provides: "soft vs strict + SENTRY_FALLBACK_TO_TORCH + sticky contract"
    - path: "docs/architecture.md"
      provides: "fallback chain honesty (soft default, strict opt-in, sticky resolve)"
  key_links:
    - from: "load_config"
      to: "DeviceConfig.fallback_to_torch"
      via: "SENTRY_FALLBACK_TO_TORCH env always-wins parse"
      pattern: "SENTRY_FALLBACK_TO_TORCH"
    - from: "profile_runtime"
      to: "ProfileRuntime.fallback_to_torch"
      via: "cfg.device.fallback_to_torch"
      pattern: "fallback_to_torch"
    - from: "build_detection_worker"
      to: "rt.fallback_to_torch"
      via: "_miss soft torch vs strict None worker"
      pattern: "fallback_to_torch"
    - from: "cli.serve"
      to: "build_detection_worker once"
      via: "worker is None → typer.Exit(1) in strict"
      pattern: "build_detection_worker|typer\\.Exit"
    - from: "factory miss path"
      to: "logging.getLogger(__name__)"
      via: "warning soft / error strict when reason set"
      pattern: "soft-fallback|strict-fail"
---

<objective>
Codify sticky process-level ORT/TRT resolve and implement soft (default) vs strict (opt-in fail-closed) fallback policy with reason logged once at construct.

Purpose: Close BACK-03 — missing preferred artifacts/deps never thrash or silently lie; operators get documented soft torch fallback or strict refuse, with stable reason codes.

Output: `fallback_to_torch` config/env/ProfileRuntime plumbing, factory soft/strict miss paths + structured once-log, serve Exit(1) on strict miss, docs for sticky + soft/strict, green factory tests without Jetson/real engines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-sticky-fallback-dual-model-guardrails/11-RESEARCH.md
@.planning/phases/11-sticky-fallback-dual-model-guardrails/11-PATTERNS.md
@.planning/phases/11-sticky-fallback-dual-model-guardrails/11-VALIDATION.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-01-SUMMARY.md
@src/sentry_ai/models/detection/factory.py
@src/sentry_ai/config/models.py
@src/sentry_ai/config/load.py
@src/sentry_ai/config/profile_runtime.py
@src/sentry_ai/cli.py
@tests/test_detection_factory.py

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| Soft default | `fallback_to_torch=True` globally, including jetson | STATE + RESEARCH A1; do **not** flip jetson YAML values |
| Strict opt-in | `DeviceConfig.fallback_to_torch=false` and/or `SENTRY_FALLBACK_TO_TORCH=false` | RESEARCH + PATTERNS |
| Env name | `SENTRY_FALLBACK_TO_TORCH` — env always wins when set (mirror `SENTRY_ALLOW_CLOUD`) | RESEARCH discretion |
| Strict miss shape | `worker=None`, `backend_live=None`, reason set; **never** `backend_live=torch` under preferred ORT/TRT | RESEARCH Pattern 2 |
| Strict serve UX | After factory, if `worker is None` → loud stderr + `raise typer.Exit(code=1)` | RESEARCH open Q2 prefer Exit(1) |
| Sticky | Factory remains one-shot at serve; no DetectionLoop re-resolve | EDGE-RT-01 spine freeze |
| Reason codes | Unchanged vocabulary: `path_rejected`, `ort_artifact_missing`, `ort_dep_missing`, `trt_artifact_missing`, `trt_dep_missing`, `unsupported_backend` | Phase 9–10 |
| Log once | Structured `logger.warning` (soft) / `logger.error` (strict) in factory when `backend_reason is not None` | BACK-03 “logged once” |
| Residual corrupt-engine thrash | Document only this plan — do **not** rewrite DetectionLoop sticky-pause | RESEARCH A4 / Pitfall 3 optional |
| Packages | No new pip packages; keep `find_spec` only | Package audit N/A |

## Claude's discretion (locked by this plan)

| Item | Choice |
|------|--------|
| Config surface | Both profile field `device.fallback_to_torch` + env override |
| Factory API | Read policy from `rt.fallback_to_torch` (optional kwarg only if needed for tests; prefer rt field) |
| `WorkerBuild.worker` type | `Any \| None`; `backend_live: str \| None` |
| unsupported_backend under strict | Same strict miss shape (no silent torch claim when preferred is non-torch and fallback false) |
| jetson profile YAML | Comments-only optional note; **field values unchanged** (strict remains opt-in) |

## Interfaces (current — extend, do not break)

From `src/sentry_ai/config/models.py`:
```python
class DeviceConfig(BaseModel):
    model_config = ConfigDict(extra="forbid")
    preferred_backend: BackendName | str = BackendName.CPU
    device_id: str = "cpu"
    # ADD: fallback_to_torch: bool = True
```

From `src/sentry_ai/config/profile_runtime.py`:
```python
@dataclass(frozen=True)
class ProfileRuntime:
    profile: RuntimeProfile
    detector_weights: str
    open_vocab_weights: str
    depth_model_id: str
    depth_tier: str
    preferred_backend: str
    device: str | None
    device_id: str
    # ADD: fallback_to_torch: bool  # default True via profile_runtime()
```

From `src/sentry_ai/models/detection/factory.py` (today):
```python
@dataclass(frozen=True)
class WorkerBuild:
    worker: Any  # → Any | None
    backend_requested: str
    backend_live: str  # → str | None
    backend_reason: str | None = None

def build_detection_worker(rt, *, conf=0.25, model=None) -> WorkerBuild: ...
```

Serve construction today (`cli.py`): single `build = build_detection_worker(rt, conf=0.25)` then always `DetectionLoop(bus, worker, store)`.

## Out of scope for 11-01

- StatusSnapshot / Live Preview `fallback_to_torch` field (11-02)
- EDGE-RT-04 depth/OV torch lock tests + dual-model docs (11-02)
- DetectionLoop / FrameBus / PerceptionStore / `/v1` redesign
- Live ORT/TRT for depth or YOLOE
- New packages / Jetson hardware in CI
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plumb fallback_to_torch config + ProfileRuntime</name>
  <files>src/sentry_ai/config/models.py, src/sentry_ai/config/load.py, src/sentry_ai/config/profile_runtime.py, tests/test_detection_factory.py</files>
  <read_first>
    - .planning/phases/11-sticky-fallback-dual-model-guardrails/11-RESEARCH.md (soft vs strict recommendation, env name, A1)
    - .planning/phases/11-sticky-fallback-dual-model-guardrails/11-PATTERNS.md (DeviceConfig, load.py SENTRY_ALLOW_CLOUD mirror, ProfileRuntime plumb)
    - src/sentry_ai/config/models.py (DeviceConfig extra=forbid)
    - src/sentry_ai/config/load.py (_parse_bool, SENTRY_ALLOW_CLOUD always-wins)
    - src/sentry_ai/config/profile_runtime.py (ProfileRuntime + profile_runtime composition)
    - tests/test_detection_factory.py (hand-built ProfileRuntime constructors that must gain the new field)
  </read_first>
  <action>
    Implement BACK-03 config surface only (no factory policy yet if split carefully — but factory will break until Task 2 if it already reads the field; prefer add field first, factory consumption in Task 2).

    1. Add `fallback_to_torch: bool = True` to `DeviceConfig` in models.py. Keep `extra="forbid"`. Default True globally — soft maker default per locked decision (do not make jetson strict-by-default).

    2. In `load_config`, mirror `SENTRY_ALLOW_CLOUD` for device:
       - Ensure `device` mapping exists.
       - If `SENTRY_FALLBACK_TO_TORCH` is in `os.environ`, set `device["fallback_to_torch"] = _parse_bool(env, default=True)`.
       - Else `device.setdefault("fallback_to_torch", True)`.
       - Document env in `load_config` docstring next to `SENTRY_ALLOW_CLOUD`.
       - Invalid bool strings must raise ValueError via existing `_parse_bool` (no silent coerce).

    3. Add `fallback_to_torch: bool` to frozen `ProfileRuntime`. In `profile_runtime(cfg)`, set `fallback_to_torch=bool(getattr(cfg.device, "fallback_to_torch", True))`.

    4. Update every hand-built `ProfileRuntime(...)` in tests (only `tests/test_detection_factory.py` today) to pass `fallback_to_torch=True` so dataclass construction stays valid.

    5. Add focused tests in `tests/test_detection_factory.py` (or a small adjacent section):
       - default profile_runtime → `fallback_to_torch is True` for desktop-gpu, jetson, cpu-fallback
       - with `monkeypatch.setenv("SENTRY_FALLBACK_TO_TORCH", "false")` + load_config → profile_runtime → `fallback_to_torch is False`
       - with env `"true"` → True
       - Do **not** change jetson.yaml preferred_backend or add fallback_to_torch: false to package profiles.

    Avoid: inventing alternate env names; flipping jetson YAML defaults; putting factory soft/strict logic in this task beyond making the field available.
  </action>
  <acceptance_criteria>
    - `rg -n "fallback_to_torch" src/sentry_ai/config/models.py src/sentry_ai/config/load.py src/sentry_ai/config/profile_runtime.py` shows field + env + ProfileRuntime plumb
    - `rg -n "SENTRY_FALLBACK_TO_TORCH" src/sentry_ai/config/load.py` matches
    - `rg -n "fallback_to_torch:\\s*false" src/sentry_ai/config/profiles/` returns no matches (profiles stay soft-default)
    - `uv run pytest tests/test_detection_factory.py -q -k "fallback_to_torch or ProfileRuntime or soft_fallback or desktop_gpu or jetson" --tb=short` passes for existing soft tests and new config tests (factory may still always soft until Task 2)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_detection_factory.py -q --tb=short</automated>
  </verify>
  <done>DeviceConfig + env + ProfileRuntime expose fallback_to_torch defaulting True; jetson profiles remain soft; hand-built ProfileRuntime sites compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Factory soft/strict miss policy + once-log + sticky proof</name>
  <files>src/sentry_ai/models/detection/factory.py, tests/test_detection_factory.py</files>
  <read_first>
    - .planning/phases/11-sticky-fallback-dual-model-guardrails/11-PATTERNS.md (Pattern _miss helper, WorkerBuild optional worker/live, log-once analogs)
    - src/sentry_ai/models/detection/factory.py (full soft-fall ORT/TRT branches)
    - tests/test_detection_factory.py (soft matrix + live success monkeypatches)
    - src/sentry_ai/models/detection/loop.py (confirm no factory import — sticky contract)
  </read_first>
  <behavior>
    - Soft default (fallback_to_torch True / field default): ORT/TRT miss still returns torch worker + existing reason codes; live never claims onnxruntime/tensorrt without live worker
    - Strict miss (fallback_to_torch False): worker is None; backend_live is None; backend_reason is one of path_rejected|ort_artifact_missing|ort_dep_missing|trt_artifact_missing|trt_dep_missing|unsupported_backend; backend_live not in {torch, onnxruntime, tensorrt}
    - Strict live success (artifact+dep present, fallback false): still backend_live onnxruntime|tensorrt with real worker weights suffix
    - Soft miss logs one WARNING containing reason (logger name under factory module) per build_detection_worker call
    - Strict miss logs one ERROR containing reason per call
    - DetectionLoop source does not reference build_detection_worker
    - Factory module still has no top-level import onnxruntime/tensorrt
  </behavior>
  <action>
    Implement factory policy consuming `rt.fallback_to_torch` (default True if attribute missing for safety).

    1. Extend `WorkerBuild`: `worker: Any | None`, `backend_live: str | None`.

    2. Extract a private `_miss(...)` helper matching PATTERNS: if fallback_to_torch → torch worker + live torch + reason; else worker None + live None + reason. Use this for all ORT/TRT miss returns and for unsupported_backend when preferred is not torch/cpu.

    3. Keep live ORT/TRT success branches unchanged (strict does not affect live claim when path+dep resolve).

    4. When returning a build with `backend_reason is not None`, emit structured log once per construct call:
       - soft: `logger.warning("detection backend soft-fallback: requested=%s live=%s reason=%s", ...)`
       - strict: `logger.error("detection backend strict-fail: requested=%s live=%s reason=%s", ...)`
       Use `logging.getLogger(__name__)`. Do not log inside DetectionLoop. Do not log when reason is None.

    5. Torch/cpu preferred path unchanged (live torch, reason None, no fallback log).

    6. Tests in `tests/test_detection_factory.py` (Wave 0 coverage for BACK-03):
       - Keep all existing soft matrix tests green (default soft).
       - Strict TRT artifact miss (jetson profile + fallback_to_torch False via hand-built rt or setattr-equivalent): worker is None, live is None, reason trt_artifact_missing.
       - Strict ORT dep miss + path_rejected cases (monkeypatch like soft tests).
       - Strict live success still claims live TRT/ORT.
       - caplog: soft miss → WARNING with reason; strict miss → ERROR with reason.
       - Sticky contract: `inspect.getsource` on DetectionLoop module asserts `build_detection_worker` not present; optionally assert factory is only imported from cli among non-test production modules via a light source check on `loop.py` (not a full import-graph framework).
       - Module hygiene test remains.

    Avoid: renaming reason codes; re-resolving in loop; constructing torch worker on strict miss “for convenience”; adding tensorrt pip extra; FPS claims.
  </action>
  <acceptance_criteria>
    - `rg -n "soft-fallback|strict-fail|fallback_to_torch" src/sentry_ai/models/detection/factory.py` matches policy + log strings
    - `rg -n "build_detection_worker" src/sentry_ai/models/detection/loop.py` returns no matches
    - `uv run pytest tests/test_detection_factory.py -q --tb=short` all green including new strict/log/sticky cases
    - Soft jetson default without fixtures still: requested tensorrt, live torch, reason trt_artifact_missing, worker is YoloDetectionWorker
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_detection_factory.py -q --tb=short</automated>
  </verify>
  <done>Factory implements soft default + strict fail-closed miss paths with once-log; sticky and soft regressions proven in unit tests without Jetson.</done>
</task>

<task type="auto">
  <name>Task 3: Serve strict fail-closed + sticky/soft-strict docs</name>
  <files>src/sentry_ai/cli.py, docs/configuration.md, docs/architecture.md, tests/test_detection_factory.py</files>
  <read_first>
    - src/sentry_ai/cli.py (serve detection block ~470–630: factory once, banner, create_app)
    - .planning/phases/11-sticky-fallback-dual-model-guardrails/11-PATTERNS.md (strict Exit(1) wiring)
    - docs/configuration.md (env table + preferred_backend soft-fall language)
    - docs/architecture.md (profiles vs live inference / soft-fall paragraphs)
    - tests/test_cli_serve.py (inspect-source patterns for serve wiring — extend only if needed for Exit/strict keywords)
  </read_first>
  <action>
    Wire strict fail-closed at serve construct and document the sticky policy (BACK-03 docs half; dual-model left to 11-02).

    1. In `cli.py` serve, after `build = build_detection_worker(rt, conf=0.25)` and assigning backend_* locals:
       - If `worker is None` (strict miss): do **not** construct `DetectionLoop`; `typer.echo` a clear stderr line including requested + reason (and that fallback_to_torch is false / strict); then `raise typer.Exit(code=1)`.
       - Else: existing `DetectionLoop(bus, worker, store)`.
       - Keep single factory call site — never call factory again after loops start.
       - Soft path unchanged when worker is non-None.
       - Banner may print `fallback_to_torch: {rt.fallback_to_torch}` once near backend_* lines (optional but preferred for operator clarity). Status field wiring is 11-02 — do not expand StatusSnapshot here unless trivial; prefer leave create_app kwargs for 11-02 to avoid double-touch conflicts beyond Exit.

    2. Docs — configuration.md:
       - Document soft vs strict table: default soft torch fallback with reason; strict refuse when preferred ORT/TRT cannot go live.
       - Add `SENTRY_FALLBACK_TO_TORCH` to env table.
       - State sticky contract: resolve once at serve; reason logged once; no per-frame re-probe.
       - Note jetson/desktop remain soft-default; production robots opt into strict via env or `device.fallback_to_torch: false` in user config.

    3. Docs — architecture.md:
       - Fallback chain: preferred → live ORT/TRT when artifact+dep; else soft torch+reason or strict fail-closed.
       - Explicit: DetectionLoop never re-selects backend; factory is sole author of backend_live.
       - Residual risk note (one short paragraph): if live claim succeeds then YOLO engine load fails later, operators may still see load errors until process restart — not fixed by thrash-free factory resolve (document residual; no loop rewrite).

    4. Tests: add/adjust inspect-source assertion in test_cli_serve or test_detection_factory that serve source contains `typer.Exit` (or `Exit(code=1)`) near detection construction path, and still contains exactly one conceptual factory call pattern (`build_detection_worker`). Prefer not running full serve process for strict exit if unit-level factory coverage already proves policy — source inspect is enough for CLI wiring.

    Avoid: changing DetectionLoop; dual-model FPS tables; retiring Phase 11 dual-model deferred language (11-02 owns export dual-model docs); installing Jetson packages.
  </action>
  <acceptance_criteria>
    - `rg -n "typer\\.Exit|Exit\\(code=1\\)" src/sentry_ai/cli.py` matches strict path
    - `rg -n "build_detection_worker" src/sentry_ai/cli.py | wc -l` shows single production call site (one construct call)
    - `rg -n "SENTRY_FALLBACK_TO_TORCH|sticky|strict|fallback_to_torch" docs/configuration.md docs/architecture.md` shows soft/strict + sticky language
    - `uv run pytest tests/test_detection_factory.py tests/test_cli_serve.py -q --tb=short` green
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_detection_factory.py tests/test_cli_serve.py -q --tb=short</automated>
  </verify>
  <done>Strict serve exits non-zero without starting DetectionLoop; soft continues; configuration + architecture document sticky soft-default and strict opt-in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator config/env → factory | Untrusted profile YAML / env can set preferred_backend and fallback_to_torch |
| Factory → status/banner consumers | backend_* fields are the only honest live identity; routes must not recompute |
| Artifact path env → resolver | Existing BACK-04 allowlist; this plan does not weaken it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | factory soft/strict | mitigate | Never set backend_live to onnxruntime/tensorrt without live worker; strict never claims torch under preferred ORT/TRT; unit matrix proves |
| T-11-02 | Tampering | SENTRY_FALLBACK_TO_TORCH / DeviceConfig | mitigate | Pydantic extra=forbid; _parse_bool raises on garbage; env always-wins only for explicit set; document operator must monitor backend_live |
| T-11-03 | Information disclosure | reason log / banner | accept | Reason codes are operational, not secrets; stderr banner already public on serve host |
| T-11-04 | Denial of Service | strict Exit(1) | accept | Intentional fail-closed for robots; soft default preserves maker UX |
| T-11-05 | Denial of Service | residual corrupt engine load thrash | accept | Document residual; full sticky-pause hardening deferred (optional later); allowlist stems remain |
| T-11-06 | Elevation of privilege (ops) | preferred_backend injection | mitigate | Honesty fields + optional strict refuse; factory sole author of live; no silent “production TRT” under torch |
| T-11-SC | Tampering | package installs | accept | No new packages this plan — legitimacy gate N/A |
</threat_model>

<verification>
- Factory soft matrix unchanged for default profiles without fixtures
- Strict matrix: no silent torch live; worker None; reason set; Exit wiring present in serve
- Sticky: no build_detection_worker in DetectionLoop; single serve call site
- caplog proves once-per-construct reason log
- Docs name soft default, strict opt-in, sticky resolve, SENTRY_FALLBACK_TO_TORCH
- Full related suite: `uv run pytest tests/test_detection_factory.py tests/test_cli_serve.py -q`
</verification>

<success_criteria>
- BACK-03 behaviors implemented: sticky resolve, soft default torch fallback with reason, strict fail-closed opt-in, reason logged once
- Soft remains global default (including jetson YAML values)
- No Jetson/real engines required in CI
- Spine freeze intact (DetectionLoop/FrameBus/store/v1 untouched beyond non-use of factory)
</success_criteria>

<output>
Create `.planning/phases/11-sticky-fallback-dual-model-guardrails/11-01-SUMMARY.md` when done
</output>
