---
phase: 08-backend-selection-honesty
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/sentry_ai/capture/status.py
  - src/sentry_ai/api/app.py
  - src/sentry_ai/api/deps.py
  - src/sentry_ai/api/routes_preview.py
  - src/sentry_ai/cli.py
  - src/sentry_ai/ui/static/index.html
  - tests/test_backend_honesty_status.py
  - tests/test_api_preview.py
  - tests/test_cli_serve.py
autonomous: true
requirements:
  - BACK-02
  - EDGE-RT-01
  - EDGE-RT-03
user_setup: []
must_haves:
  truths:
    - "Status / serve banner expose both backend_requested and backend_live (BACK-02)"
    - "When preferred ORT/TRT soft-stubs to torch, backend_live is torch (never silent tensorrt/onnxruntime live claim)"
    - "Optional backend_reason is present on status when requested != live (or always when factory set a reason)"
    - "/api/status JSON includes backend fields when create_app/serve injects them; omitted/None when not injected (backward compatible)"
    - "Live Preview footer shows backend requested → live when /api/status provides fields (UI hint)"
    - "desktop-gpu banner path shows requested=torch and live=torch without mismatch reason"
    - "DetectionLoop / /v1 contracts unchanged (EDGE-RT-01)"
  artifacts:
    - path: "src/sentry_ai/capture/status.py"
      provides: "StatusSnapshot optional backend_requested/backend_live/backend_reason"
      contains: "backend_requested"
    - path: "src/sentry_ai/api/app.py"
      provides: "create_app kwargs → app.state backend fields"
    - path: "src/sentry_ai/api/routes_preview.py"
      provides: "/api/status merges backend_* from app.state"
    - path: "src/sentry_ai/cli.py"
      provides: "Banner prints backend_requested / backend_live / reason; passes fields into create_app"
    - path: "src/sentry_ai/ui/static/index.html"
      provides: "Footer backend metric from /api/status"
    - path: "tests/test_backend_honesty_status.py"
      provides: "StatusSnapshot + /api/status honesty coverage"
  key_links:
    - from: "cli.serve WorkerBuild"
      to: "create_app(... backend_requested=...)"
      via: "app.state injection"
      pattern: "backend_requested|backend_live"
    - from: "routes_preview.api_status"
      to: "app.state.backend_*"
      via: "getattr merge after model_dump"
      pattern: "backend_requested|backend_live|backend_reason"
    - from: "ui/static/index.html pollStatus"
      to: "/api/status"
      via: "footer metric-backend"
      pattern: "backend_requested|metric-backend"
    - from: "cli banner"
      to: "WorkerBuild fields"
      via: "typer.echo backend_requested/live/reason"
      pattern: "backend_requested|backend_live"
---

<objective>
Surface honest backend identity end-to-end: StatusSnapshot schema, `/api/status`, serve startup banner, and Live Preview footer show `backend_requested` vs `backend_live` (with reason when they differ) using factory metadata from 08-01.

Purpose: Close BACK-02 so operators and robots never see silent ORT/TRT claims while torch is running. Completes Phase 8 honesty success criteria on top of the factory plug-in.

Output: Extended status model + app injection + route merge + banner rewrite + small Live Preview footer update + automated honesty tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-backend-selection-honesty/08-CONTEXT.md
@.planning/phases/08-backend-selection-honesty/08-RESEARCH.md
@.planning/phases/08-backend-selection-honesty/08-PATTERNS.md
@.planning/phases/08-backend-selection-honesty/08-01-PLAN.md
@src/sentry_ai/capture/status.py
@src/sentry_ai/api/app.py
@src/sentry_ai/api/deps.py
@src/sentry_ai/api/routes_preview.py
@src/sentry_ai/cli.py
@src/sentry_ai/ui/static/index.html
@tests/test_api_preview.py
@tests/test_cli_serve.py

## Soft dependency on 08-01

Wave 2 requires:
- `WorkerBuild` with `backend_requested`, `backend_live`, `backend_reason`
- `cli.serve` already calling `build_detection_worker` and holding those fields in locals
- Soft-stub policy: ORT/TRT → live=torch + reason codes

If 08-01 SUMMARY differs on field names, use the shipped factory exports — do not re-litigate soft-stub vs hard-fail.

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| Status fields | `backend_requested` + `backend_live` (+ optional reason) | BACK-02 / user lock |
| No silent live TRT/ORT | Never claim live ORT/TRT when torch runs | CONTEXT |
| Factory is sole author of live label | Status/UI only display | RESEARCH Pattern 3 |
| CaptureLoop | Do not teach CaptureLoop about backends | RESEARCH open Q2 |
| Spine | DetectionLoop / FrameBus / store / /v1 frozen | EDGE-RT-01 |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| StatusSnapshot | Add optional `backend_requested`, `backend_live`, `backend_reason` (all `str \| None = None`) |
| Fill path | create_app kwargs → app.state; `api_status` merges into dumped dict (pipeline_state pattern) — do not require CaptureLoop changes |
| Banner | Print `backend_requested` / `backend_live`; if reason set, print `backend_reason` on stderr (replace v1 “export target only” prose with structured fields) |
| Live Preview footer | One metric line: `Backend: {requested} → {live}` when fields present; show reason muted when present and differs |
| Missing injection | Keys may be null/omitted — status must not raise |

## Interfaces from 08-01 (expected)

```python
@dataclass(frozen=True)
class WorkerBuild:
    worker: Any
    backend_requested: str
    backend_live: str
    backend_reason: str | None = None

def build_detection_worker(rt, *, conf=0.25, model=None) -> WorkerBuild: ...
```

From `routes_preview.api_status` enrichment pattern (pipeline_state):
```python
data = snapshot.model_dump()
# best-effort getattr on request.app.state — never raise
```

From `create_app` optional kwargs pattern:
```python
def create_app(..., open_vocab_loop: Any | None = None, serve_ui: bool = True) -> FastAPI:
    app.state.detection_worker = detection_worker
    ...
```

## Out of scope for 08-02

- Live ORT/TRT loaders (Phases 9–10)
- Sticky thrash-free fallback policy beyond displaying reason (Phase 11 / BACK-03)
- Depth/OV backend selection (EDGE-RT-04)
- Changing Detection wire schema
- New packages
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StatusSnapshot + create_app + /api/status honesty fields (BACK-02)</name>
  <files>src/sentry_ai/capture/status.py, src/sentry_ai/api/app.py, src/sentry_ai/api/deps.py, src/sentry_ai/api/routes_preview.py, tests/test_backend_honesty_status.py, tests/test_api_preview.py</files>
  <read_first>
    - .planning/phases/08-backend-selection-honesty/08-RESEARCH.md (Pattern 3, status merge, pitfall 1)
    - .planning/phases/08-backend-selection-honesty/08-PATTERNS.md (StatusSnapshot + api_status + create_app analogs)
    - src/sentry_ai/capture/status.py
    - src/sentry_ai/api/app.py
    - src/sentry_ai/api/deps.py
    - src/sentry_ai/api/routes_preview.py (api_status)
    - tests/test_api_preview.py (test_api_status_includes_pipeline_stage_flags)
  </read_first>
  <behavior>
    - StatusSnapshot accepts backend_requested, backend_live, backend_reason as optional None defaults; extra=forbid still holds
    - Existing StatusSnapshot constructions without new fields remain valid
    - create_app accepts optional backend_requested / backend_live / backend_reason kwargs and attaches to app.state
    - AppState dataclass mirrors the three optional fields for typed convenience
    - GET /api/status includes backend_requested and backend_live when set on app.state (values pass through exactly)
    - When backend_reason is set, it appears in JSON; when None/missing, key may be null or omitted without error
    - create_app without backend kwargs: backend fields None/absent — no forced fake values
    - Honesty fixture: backend_requested=tensorrt, backend_live=torch, backend_reason=trt_loader_not_implemented → JSON matches
    - Honesty fixture: backend_requested=onnxruntime, backend_live=torch, reason=ort_loader_not_implemented → JSON matches
    - Never invent backend_live=tensorrt|onnxruntime in the route layer
  </behavior>
  <action>
    1. Extend `StatusSnapshot` in `capture/status.py` with three optional fields at end (Phase 8 honesty / BACK-02):
       - `backend_requested: str | None = None`
       - `backend_live: str | None = None`
       - `backend_reason: str | None = None`
       - Keep `extra="forbid"`; do not break existing callers that omit fields

    2. Extend `create_app` + `AppState` with optional kwargs defaulting None; assign `app.state.backend_requested`, `app.state.backend_live`, `app.state.backend_reason`.

    3. In `api_status` after `data = snapshot.model_dump()` (and alongside pipeline_state enrichment), merge:
       - Read getattr(app.state, "backend_*", None)
       - If not None, set data[field] = value
       - Best-effort only; never raise; do not recompute live backend from preferred_backend strings

    4. Create `tests/test_backend_honesty_status.py`:
       - Unit: StatusSnapshot model_dump includes new keys when set; defaults None when unset
       - TestClient: create_app with tensorrt→torch honesty triple; GET /api/status asserts equality
       - TestClient: create_app with onnxruntime→torch honesty triple
       - TestClient: create_app without injection leaves get("backend_live") in {None} and does not crash
       - Do not start real YOLO / do not bind external ports beyond TestClient

    5. Optionally extend `tests/test_api_preview.py` only if a one-line assertion in existing status key tests is cleaner — prefer dedicated honesty file as primary coverage.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backend_honesty_status.py tests/test_api_preview.py -q</automated>
  </verify>
  <done>/api/status and StatusSnapshot expose backend_requested/live/reason when injected; backward compatible when absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serve banner + Live Preview footer honesty (BACK-02, EDGE-RT-03)</name>
  <files>src/sentry_ai/cli.py, src/sentry_ai/ui/static/index.html, tests/test_cli_serve.py, tests/test_backend_honesty_status.py</files>
  <read_first>
    - .planning/phases/08-backend-selection-honesty/08-RESEARCH.md (banner target, Live Preview footer discretion)
    - .planning/phases/08-backend-selection-honesty/08-PATTERNS.md (banner honesty + CLI inspect tests)
    - src/sentry_ai/cli.py (create_app call + banner block ~430–490)
    - src/sentry_ai/ui/static/index.html (footer metrics + pollStatus/applyStatus)
    - tests/test_cli_serve.py
    - 08-01 factory reason codes
  </read_first>
  <behavior>
    - create_app call in serve passes backend_requested/backend_live/backend_reason from WorkerBuild locals (None when detection disabled)
    - Banner prints backend_requested and backend_live when detection built (or always prints preferred vs live when available)
    - When backend_reason is not None, banner emits backend_reason (stderr ok) — replaces prose-only “export target” notes that claimed ORT/TRT without live field
    - inspect.getsource(serve) contains backend_requested, backend_live, and create_app kwargs wiring
    - preferred_backend line may remain for profile context; must not be the only backend identity
    - Live Preview footer includes an element (e.g. id=metric-backend) updated from /api/status backend_requested + backend_live
    - When fields missing, footer shows em dash — no JS throw
    - When reason present and requested != live, footer may append reason in muted style
    - desktop-gpu path still valid: requested=torch live=torch with no scare note required
  </behavior>
  <action>
    1. Wire `cli.serve` create_app invocation with backend_* kwargs from 08-01 locals (per BACK-02).

    2. Rewrite banner honesty section:
       - After profile/device lines, echo:
         - `backend_requested: {value}`
         - `backend_live: {value}`
         - if reason: `backend_reason: {value}` on stderr (same style as prior notes)
       - Remove or replace the old exclusive notes that only said “ORT is the export target” / “live path is still PyTorch” without structured live field — structured fields are the source of truth
       - Keep non-localhost bind warning and detection enabled/disabled lines

    3. Live Preview (`src/sentry_ai/ui/static/index.html`):
       - Add footer metric div near Bind: `Backend: <strong id="metric-backend">—</strong>`
       - In applyStatus/pollStatus path, set text from data.backend_requested and data.backend_live (format `torch → torch` or `tensorrt → torch`); if backend_reason present and requested != live, append ` (reason)` or set title attribute
       - Guard nulls; do not break existing metrics

    4. Update `tests/test_cli_serve.py`:
       - Assert serve source contains `backend_requested`, `backend_live`, and passes them into create_app
       - Assert `build_detection_worker` still present
       - Keep start/stop order tests green

    5. Extend honesty tests if useful: static HTML contains `metric-backend` and `backend_requested` JS reference (keyword assert — no browser driver required).

    6. Do not edit DetectionLoop, routes_v1 Detection schema, or depth/OV workers.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli_serve.py tests/test_backend_honesty_status.py tests/test_detection_factory.py -q</automated>
  </verify>
  <done>Operators see backend_requested vs backend_live on banner, /api/status, and Live Preview footer; torch-stub honesty preserved for jetson/cpu-fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Serve process → `/api/status` consumers | Robots/UI trust backend_live for deployment decisions |
| Browser Live Preview → status JSON | Untrusted display only; no write of backend identity from client |
| Operator terminal banner | Human trust boundary for edge deploy mistakes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Spoofing | `/api/status` backend_live | mitigate | Pass-through only from factory-authored app.state; never set live=ort/trt in route; tests for mismatch honesty |
| T-08-11 | Spoofing | CLI banner | mitigate | Print both requested and live; reason when differ; replace export-target-only wording |
| T-08-12 | Tampering | Live Preview JS | accept | Read-only display; cannot change server backend |
| T-08-13 | Information disclosure | backend_reason codes | accept | Non-sensitive operational codes (loader_not_implemented) |
| T-08-14 | Denial of service | status handler | mitigate | Best-effort getattr; status must not raise if fields missing |
| T-08-SC | Tampering | Package installs | mitigate | No new packages this phase |
</threat_model>

<verification>
- `uv run pytest tests/test_backend_honesty_status.py tests/test_cli_serve.py tests/test_api_preview.py tests/test_detection_factory.py -q` green
- `rg -n "backend_requested|backend_live" src/sentry_ai/cli.py src/sentry_ai/api/routes_preview.py src/sentry_ai/capture/status.py`
- `rg -n "metric-backend" src/sentry_ai/ui/static/index.html`
- Full suite optional gate: `uv run pytest -q` before phase verify
</verification>

<success_criteria>
1. `/api/status` exposes backend_requested and backend_live when serve injects factory metadata.
2. Banner prints both fields (and reason when set); never claims live ORT/TRT under torch soft-stub.
3. Live Preview footer shows backend pair when status provides fields.
4. desktop-gpu shows requested=torch live=torch; jetson/cpu-fallback selection remains visible as requested with live=torch in Phase 8.
5. No perception spine redesign; existing status clients without backend fields still work.
</success_criteria>

<output>
Create `.planning/phases/08-backend-selection-honesty/08-02-SUMMARY.md` when done
</output>
