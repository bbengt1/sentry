---
phase: 04-monocular-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/sentry_ai/models/cache.py
  - src/sentry_ai/models/depth/__init__.py
  - src/sentry_ai/models/depth/preprocess.py
  - src/sentry_ai/models/depth/mapping.py
  - src/sentry_ai/models/depth/worker.py
  - src/sentry_ai/models/depth/loop.py
  - src/sentry_ai/state/perception_store.py
  - src/sentry_ai/plugins/registry.py
  - THIRD_PARTY_MODELS.md
  - README.md
  - tests/test_model_cache.py
  - tests/test_depth_preprocess.py
  - tests/test_depth_mapping.py
  - tests/test_depth_worker.py
  - tests/test_depth_loop.py
  - tests/test_perception_store.py
  - tests/test_plugins_registry.py
  - tests/test_third_party_models_doc.py
  - tests/test_depth_colormap.py
  - tests/test_api_depth.py
autonomous: true
requirements:
  - DEPTH-01
user_setup: []
must_haves:
  truths:
    - "DepthAnythingWorker.process(ImageFrame) returns a depth map + DepthKind/unit without opening a camera (injectable fake model in CI)"
    - "Default path is DAV2 Small relative (depth-anything/Depth-Anything-V2-Small-hf); never default NC Base/Large"
    - "Relative mode sets DepthKind.RELATIVE and unit=None; metric_indoor/metric_outdoor set METRIC_ESTIMATED and unit='m' from configured mode (not float heuristics)"
    - "DepthLoop daemon reads FrameBus.get_latest(), skips same frame_id, writes PerceptionStore.set_depth; never VideoCapture"
    - "PerceptionStore keeps full float depth_map in-process; DepthProduct is keep-latest with isolated snapshot metadata"
    - "configure_model_cache sets HF_HOME under SENTRY_MODEL_CACHE/hf (sibling of weights/) before any real HF load"
    - "optional-dependencies.depth installs torch/transformers/hf-hub/pillow; default/dev path never downloads HF weights in unit tests"
    - "Preprocess BGR→RGB + depth_stats are pure golden-tested helpers"
  artifacts:
    - path: "src/sentry_ai/models/depth/worker.py"
      provides: "DepthAnythingWorker ModelWorker for DAV2 Small"
      exports: ["DepthAnythingWorker"]
    - path: "src/sentry_ai/models/depth/loop.py"
      provides: "DepthLoop FrameBus subscriber thread"
      exports: ["DepthLoop"]
    - path: "src/sentry_ai/models/depth/preprocess.py"
      provides: "BGR→RGB + depth_stats pure helpers"
      exports: ["bgr_to_rgb_uint8", "depth_stats"]
    - path: "src/sentry_ai/models/depth/mapping.py"
      provides: "mode→model id + kind/unit mapping"
      exports: ["MODE_TO_MODEL", "kind_for_mode"]
    - path: "src/sentry_ai/state/perception_store.py"
      provides: "DepthProduct + set_depth/snapshot_depth + depth metrics"
      exports: ["DepthProduct", "PerceptionStore"]
    - path: "src/sentry_ai/models/cache.py"
      provides: "HF_HOME under SENTRY_MODEL_CACHE"
      exports: ["configure_model_cache"]
    - path: "tests/test_depth_worker.py"
      provides: "DEPTH-01 mock worker coverage"
    - path: "tests/test_depth_loop.py"
      provides: "DEPTH-01 bus-only loop coverage"
    - path: "tests/test_depth_preprocess.py"
      provides: "preprocess golden coverage"
    - path: "tests/test_depth_mapping.py"
      provides: "kind/unit honesty mapping coverage"
  key_links:
    - from: "src/sentry_ai/models/depth/loop.py"
      to: "FrameBus.get_latest"
      via: "DepthLoop._run keep-latest read"
      pattern: "get_latest"
    - from: "src/sentry_ai/models/depth/loop.py"
      to: "PerceptionStore.set_depth"
      via: "publish after process"
      pattern: "set_depth"
    - from: "src/sentry_ai/models/depth/worker.py"
      to: "kind_for_mode / MODE_TO_MODEL"
      via: "configured depth_mode → DepthKind + unit"
      pattern: "kind_for_mode|MODE_TO_MODEL"
    - from: "src/sentry_ai/models/depth/worker.py"
      to: "configure_model_cache"
      via: "before from_pretrained on real load path"
      pattern: "configure_model_cache|HF_HOME"
    - from: "pyproject.toml"
      to: "DepthAnythingWorker"
      via: "optional-extra depth + optional worker entry point"
      pattern: "extra depth|depth-anything"
---

<objective>
Ship monocular depth core: Depth Anything V2 Small worker (HF Transformers), pure preprocess/mapping contracts, DepthLoop on FrameBus, PerceptionStore DepthProduct extension, and HF cache under SENTRY_MODEL_CACHE — with CI that never downloads weights.

Purpose: Close DEPTH-01 at the pipeline layer so 04-02 can attach stream payload, colormap UI, metric labeling, telemetry, and serve wiring without reinventing inference.

Output: Installable optional `depth` extra; `DepthAnythingWorker` + `DepthLoop` + store depth half + cache HF_HOME; unit tests green with mocks; docs note Apache Small default and offline cache.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-monocular-depth/04-CONTEXT.md
@.planning/phases/04-monocular-depth/04-RESEARCH.md
@.planning/phases/04-monocular-depth/04-PATTERNS.md
@.planning/phases/04-monocular-depth/04-VALIDATION.md
@.planning/phases/03-fixed-class-detection/03-01-SUMMARY.md
@.planning/phases/03-fixed-class-detection/03-02-SUMMARY.md

## Depends on Phase 3 (shipped)

- `DetectionLoop` / `YoloDetectionWorker` / `PerceptionStore` / `configure_model_cache` patterns
- FrameBus + ImageFrame; ModelWorker protocol
- Existing `DepthKind`, `DepthPayload`, validators (do not redesign)

## Locked decisions (do not re-litigate)

| Decision | Value |
|----------|-------|
| Model | HF Transformers `depth-anything/Depth-Anything-V2-Small-hf` (Apache-2.0) default |
| Load path | `AutoImageProcessor` + `AutoModelForDepthEstimation` (not native DAV2 git + timm) |
| Package | optional-extra `depth` = torch, transformers≥4.45,<6, huggingface-hub, pillow — never core deps |
| Inference placement | Dedicated `DepthLoop` thread; never in CaptureLoop or FastAPI handlers |
| Camera ownership | Workers never open cameras; only `FrameBus.get_latest()` / `ImageFrame.image_bgr` |
| Schema | Existing `DepthKind` / `DepthPayload` / `Completeness.depth` — map into, do not redesign |
| Store | **Extend** one `PerceptionStore` with `DepthProduct` (not a separate DepthStore) |
| In-process map | Full HxW float32 on product; JSON wire deferred to 04-02 (metadata only) |
| Relative default | `depth_mode="relative"` → `DepthKind.RELATIVE`, `unit=None` — no `depth_m` field ever |
| Metric optional | `metric_indoor` / `metric_outdoor` → `METRIC_ESTIMATED` + `unit="m"`; kind from **mode**, not float range |
| NC weights | Never default Base/Large/Giant (CC-BY-NC) |
| Cache | `HF_HOME` = `{SENTRY_MODEL_CACHE or ~/.cache/sentry-ai}/hf` sibling of `weights/` |
| API/UI/colormap | Deferred to 04-02 |
| Out of scope | Free-space, full `/v1` WS, TensorRT, stereo, stage toggles (Phase 5–7) |

## Package legitimacy (Phase 4 installs)

| Package | Disposition | Notes |
|---------|-------------|-------|
| transformers ≥4.45,<6 | Approved / [ASSUMED] (RESEARCH) | Apache-2.0; official DAV2 HF path |
| huggingface-hub ≥0.23,<2 | Approved / [ASSUMED] | Transitive control of cache |
| torch ≥2.2,<3 | Approved / [ASSUMED] | Explicit in depth extra; no CUDA pin in pyproject |
| pillow ≥10,<13 | Approved / [ASSUMED] | RGB for HF processor |
| timm / einops | **Not installed** | Native path rejected |

## Interfaces (Phase 1–3 contracts to use)

From `src/sentry_ai/schemas/enums.py`:
- `DepthKind.RELATIVE | METRIC_ESTIMATED | METRIC_CALIBRATED`

From `src/sentry_ai/schemas/perception.py`:
- `DepthPayload(kind, unit, width, height)` — no `depth_m`; relative forbids unit via validator

From `src/sentry_ai/plugins/protocols.py`:
- `ModelWorker`: `name: str`; `process(frame) -> object | None`

From `src/sentry_ai/bus/frame_bus.py`:
- `FrameBus.get_latest() -> ImageFrame | None`

From `src/sentry_ai/models/detection/loop.py` / `yolo_worker.py`:
- Structural twins for DepthLoop / injectable worker + lazy load

From `src/sentry_ai/state/perception_store.py`:
- Detection half must keep working; add parallel depth half under same lock pattern

From `src/sentry_ai/policy/models.py`:
- `DEFAULT_DEPTH_WEIGHT_KEY = "depth-anything-v2-small"` maps to relative Small HF id

## Discretion locked by research (implement as specified)

- HF transformers path (not native)
- Extend PerceptionStore (not separate store)
- Injectable `model=` + `processor=` for CI
- `resolve_device`: prefer extract to `sentry_ai.models.device` if both YOLO and depth import it; thin copy in depth worker is OK
- DepthResult dataclass: `depth_map` (HxW float32), `kind`, `unit`, optional `width`/`height`
- MODE_TO_MODEL allowlist only Small HF ids (relative + metric indoor/outdoor)
- Wave 0 skip stubs for 04-02 tests: colormap, api_depth

## Out of scope this plan

FastAPI routes, MJPEG colormap, Live Preview badges, `sentry serve` DepthLoop start, runtime depth config PATCH (04-02). Free-space / Spatial Post (Phase 5).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 depth extra, package skeleton, HF cache, docs, test stubs</name>
  <files>pyproject.toml, uv.lock, src/sentry_ai/models/cache.py, src/sentry_ai/models/depth/__init__.py, THIRD_PARTY_MODELS.md, README.md, tests/test_model_cache.py, tests/test_depth_preprocess.py, tests/test_depth_mapping.py, tests/test_depth_worker.py, tests/test_depth_loop.py, tests/test_depth_colormap.py, tests/test_api_depth.py, tests/test_third_party_models_doc.py</files>
  <read_first>
    - .planning/phases/04-monocular-depth/04-RESEARCH.md (Standard Stack install, Pattern 8 HF cache, Package Legitimacy, Wave 0 Gaps, Pitfall 3/7)
    - .planning/phases/04-monocular-depth/04-PATTERNS.md (cache.py, depth package __init__, pyproject depth extra)
    - .planning/phases/04-monocular-depth/04-VALIDATION.md
    - pyproject.toml ([project.optional-dependencies] detect style — use optional-dependencies not uv dependency-groups)
    - src/sentry_ai/models/cache.py
    - src/sentry_ai/models/detection/__init__.py (lazy __getattr__ pattern)
    - THIRD_PARTY_MODELS.md
    - tests/test_model_cache.py
    - tests/test_third_party_models_doc.py
  </read_first>
  <action>
    1. Add optional dependency group in pyproject.toml (locked: optional-extra `depth`, mirror detect):
       - Under `[project.optional-dependencies]` add:
         depth = ["torch>=2.2,<3", "transformers>=4.45,<6", "huggingface-hub>=0.23,<2", "pillow>=10,<13"]
       - Do **not** add torch/transformers to core `dependencies`
       - Do **not** pin CUDA-specific torch wheels
       - Run `uv lock` so lockfile records the optional extra; default `uv sync --extra dev` must still work without pulling depth stack for unit tests
       - Document in README: real depth needs `uv sync --extra dev --extra depth` (and combined with detect when both desired)
       - Optional entry point (mirror yolo-fixed): `depth-anything-v2-small = "sentry_ai.models.depth.worker:DepthAnythingWorker"` under `[project.entry-points."sentry_ai.workers"]` — only register when importable in registry later

    2. Create package skeleton `src/sentry_ai/models/depth/__init__.py`:
       - Future annotations; lazy `__getattr__` for `DepthAnythingWorker` and `DepthLoop` (avoid heavy imports on package touch — PATTERNS)
       - Sorted `__all__`

    3. Extend `configure_model_cache` in `cache.py` (MODEL-02 for HF):
       - After resolving root (arg → SENTRY_MODEL_CACHE → default_cache_root), keep existing `weights/` + YOLO settings path intact
       - Create `hf_home = root / "hf"`; mkdir parents
       - `os.environ.setdefault("HF_HOME", str(hf_home))`
       - Optionally setdefault `HUGGINGFACE_HUB_CACHE` under hf_home/hub (recommended, non-breaking)
       - Return value remains weights_dir for YOLO callers (backward compatible)
       - Pure path helpers must work without transformers/torch installed

    4. Docs:
       - THIRD_PARTY_MODELS.md: Phase 4 active wording — default DAV2 Small Apache-2.0; Base/Large NC never default; metric heads optional with license check note; HF cache under SENTRY_MODEL_CACHE/hf offline after first download
       - README: depth extra install, first-run HF download, offline re-run, relative-by-default honesty one-liner

    5. Wave 0 test modules (skip stubs where implementation lands later in this plan or 04-02):
       - Create or unskip shells: `tests/test_depth_preprocess.py`, `test_depth_mapping.py`, `test_depth_worker.py`, `test_depth_loop.py` — fill in Tasks 2–3 (can start as real tests in later tasks)
       - `tests/test_depth_colormap.py`, `tests/test_api_depth.py` — pytest.mark.skip with reason "04-02" (Wave 0 for next plan); use `raise AssertionError("should be skipped")` not `assert False` (ruff B011)
       - Extend `tests/test_model_cache.py`: assert after configure_model_cache(tmp_path), HF_HOME points under tmp_path/hf; YOLO weights_dir still under tmp_path/weights; idempotent
       - Extend `tests/test_third_party_models_doc.py` for Phase 4 depth active / Apache Small / NC never default wording as needed

    6. Do **not** implement worker/loop/store depth methods yet (Tasks 2–3). Do not wire API/serve.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_model_cache.py tests/test_third_party_models_doc.py &amp;&amp; uv run ruff check src/sentry_ai/models/cache.py src/sentry_ai/models/depth &amp;&amp; rg -n '\[project.optional-dependencies\]|depth =' pyproject.toml | head -20</automated>
  </verify>
  <acceptance_criteria>
    - pyproject has depth optional-extra with torch/transformers/huggingface-hub/pillow; not in core deps
    - configure_model_cache sets HF_HOME under cache root/hf; weights/ path still works
    - Wave 0 skip stubs for 04-02 colormap/api_depth exist and skip cleanly
    - THIRD_PARTY + README mention DAV2 Small Apache default and depth extra install
    - ruff clean on touched modules
  </acceptance_criteria>
  <done>Depth packaging + HF cache foundation ready; stubs in place for remaining tasks and 04-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preprocess + mapping contracts + PerceptionStore DepthProduct (golden tests)</name>
  <files>src/sentry_ai/models/depth/preprocess.py, src/sentry_ai/models/depth/mapping.py, src/sentry_ai/state/perception_store.py, tests/test_depth_preprocess.py, tests/test_depth_mapping.py, tests/test_perception_store.py</files>
  <read_first>
    - .planning/phases/04-monocular-depth/04-RESEARCH.md (Pattern 4 store, Code Examples preprocess/kind_for_mode, Pitfall 1/2/4)
    - .planning/phases/04-monocular-depth/04-PATTERNS.md (preprocess.py, mapping.py, perception_store DepthProduct)
    - src/sentry_ai/state/perception_store.py (DetectionProduct half — do not break)
    - src/sentry_ai/models/detection/mapping.py (pure transform conventions)
    - src/sentry_ai/schemas/enums.py
    - src/sentry_ai/schemas/perception.py
    - src/sentry_ai/schemas/validators.py
    - tests/test_perception_store.py
    - tests/test_detection_mapping.py
  </read_first>
  <behavior>
    - bgr_to_rgb_uint8 swaps channels; output shape HxWx3; does not mutate input (copy semantics via cvtColor)
    - depth_stats on synthetic ramp returns finite min/max/mean; empty/all-nan finite mask → zeros
    - kind_for_mode("relative") → (RELATIVE, None); metric_indoor/outdoor → (METRIC_ESTIMATED, "m"); unknown → ValueError
    - MODE_TO_MODEL maps only Small HF ids; relative key is depth-anything/Depth-Anything-V2-Small-hf
    - set_depth then snapshot_depth returns same frame_id/kind/unit/stats; keep-latest overwrite
    - snapshot_depth isolates product (new object); depth_map not required for metadata equality tests
    - record_depth_drop increments depth drop metric; metrics_snapshot includes depth_frames, depth_fps, last_depth_latency_ms
    - Existing detection set_detections/snapshot tests still pass (dual product coexistence)
    - Building DepthPayload(kind=product.kind, unit=product.unit, ...) validates; relative+unit m still rejected by schema
  </behavior>
  <action>
    1. Implement pure `preprocess.py` (no torch):
       - `bgr_to_rgb_uint8(image_bgr: np.ndarray) -> np.ndarray` via cv2.COLOR_BGR2RGB; assert ndim==3 and shape[2]==3
       - `depth_stats(depth: np.ndarray) -> dict[str, float]` with keys min/max/mean over finite values; empty finite → 0.0
       - Module docstring: pure helpers for worker + golden tests

    2. Implement pure `mapping.py` (no I/O):
       - `MODE_TO_MODEL` dict exactly:
         - relative → depth-anything/Depth-Anything-V2-Small-hf
         - metric_indoor → depth-anything/Depth-Anything-V2-Metric-Indoor-Small-hf
         - metric_outdoor → depth-anything/Depth-Anything-V2-Metric-Outdoor-Small-hf
       - `kind_for_mode(mode: str) -> tuple[DepthKind, str | None]` per RESEARCH Code Examples
       - Optional: `DEFAULT_MODEL_ID` alias for relative Small
       - Kind/unit **only** from mode string — never from array range heuristics (DEPTH-04 foundation / Pitfall 1)
       - No Base/Large model ids anywhere in defaults

    3. Extend `perception_store.py` (locked: single store, dual products):
       - Add `@dataclass DepthProduct` with: frame_id, camera_id, t_capture, kind (DepthKind), unit (Literal["m"]|None), width, height, latency_ms, depth_map (Any — HxW float32 in-process), min_value/max_value/mean_value optional, model_name, error
       - Extend `StoreMetrics` with depth_frames, depth_frames_dropped, depth_fps, last_depth_latency_ms (keep det_* fields)
       - `set_depth(...)`: build DepthProduct; compute stats via depth_stats when depth_map present and error is None; keep-latest under lock; update depth FPS window (separate window counters from det or shared wall clock — either OK if metrics accurate)
       - `snapshot_depth() -> DepthProduct | None`: isolated product copy under lock; metadata always copied; depth_map may share array reference if product is immutable after set (document); never mutate caller's array in set path without copy of input if needed
       - `record_depth_drop(n=1)`
       - `metrics_snapshot()` returns both det and depth metric fields
       - Detection methods unchanged in behavior
       - Export DepthProduct in `__all__`

    4. Tests (TDD RED→GREEN; no torch required):
       - test_depth_preprocess.py: channel swap on known BGR pixel; stats on ramp; nan handling
       - test_depth_mapping.py: all three modes; unknown mode; MODE_TO_MODEL ids contain Small and not Base/Large; DepthPayload validation for relative unit None vs m rejection
       - test_perception_store.py: extend with set_depth/snapshot_depth keep-latest, dual det+depth coexistence, drop metrics, error product with depth_map None

    5. Do not implement worker/loop yet.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_depth_preprocess.py tests/test_depth_mapping.py tests/test_perception_store.py &amp;&amp; uv run ruff check src/sentry_ai/models/depth/preprocess.py src/sentry_ai/models/depth/mapping.py src/sentry_ai/state/perception_store.py</automated>
  </verify>
  <acceptance_criteria>
    - Pure preprocess + mapping green without transformers
    - PerceptionStore depth half keep-latest + metrics; detection half unregressed
    - Relative mapping never yields unit="m"; metric modes yield METRIC_ESTIMATED + m
    - MODE_TO_MODEL allowlists only Small HF ids
  </acceptance_criteria>
  <done>Preprocess/mapping contracts and store DepthProduct ready for worker and loop consumers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DepthAnythingWorker + DepthLoop + plugin registration (DEPTH-01)</name>
  <files>src/sentry_ai/models/depth/worker.py, src/sentry_ai/models/depth/loop.py, src/sentry_ai/models/depth/__init__.py, src/sentry_ai/plugins/registry.py, pyproject.toml, tests/test_depth_worker.py, tests/test_depth_loop.py, tests/test_plugins_registry.py</files>
  <read_first>
    - .planning/phases/04-monocular-depth/04-RESEARCH.md (Pattern 1 DepthLoop, Pattern 2 injectable worker, Pattern 3 HF load, Pattern 9 device, Pitfall 3/4/6)
    - .planning/phases/04-monocular-depth/04-PATTERNS.md (worker.py, loop.py, registry)
    - src/sentry_ai/models/detection/yolo_worker.py (injectable model, resolve_device, lazy load, ImportError message)
    - src/sentry_ai/models/detection/loop.py (start/stop/_run structural twin)
    - src/sentry_ai/plugins/protocols.py
    - src/sentry_ai/plugins/registry.py
    - src/sentry_ai/models/depth/mapping.py (Task 2)
    - src/sentry_ai/models/depth/preprocess.py (Task 2)
    - src/sentry_ai/state/perception_store.py (Task 2)
    - tests/test_detection_worker.py
    - tests/test_detection_loop.py
  </read_first>
  <behavior>
    - Worker process with injectable fake model/processor returns DepthResult-like object with HxW depth_map matching input frame spatial size (or documented interpolate), kind/unit from depth_mode
    - depth_mode relative → unit is None; metric_indoor → unit "m" and kind metric_estimated
    - Missing image_bgr → empty/error safe result, never opens camera
    - DepthLoop: after bus.publish(frame), store.snapshot_depth eventually has that frame_id
    - DepthLoop: same frame_id not reprocessed; newer frame overwrites product
    - DepthLoop / worker source has no VideoCapture (inspect.getsource architecture assert)
    - Worker exceptions: loop stores product with error= and keeps thread alive
    - Real load path (model=None) calls configure_model_cache before from_pretrained; ImportError message mentions `uv sync --extra depth`
    - Unit tests never call network/HF hub (inject fakes only)
  </behavior>
  <action>
    1. Implement `DepthAnythingWorker` in `worker.py` (DEPTH-01, ModelWorker):
       - `name: str = "depth-anything-v2-small"`
       - `__init__(self, model_id: str | None = None, depth_mode: str = "relative", device: str | None = None, model=None, processor=None)`
         - Default model_id from MODE_TO_MODEL[depth_mode] when None
         - Validate depth_mode is one of MODE_TO_MODEL keys
         - Injectable model + processor for tests bypass HF entirely
       - Thread-safe optional `set_depth_mode` / `get_depth_mode` if cheap (foundation for 04-02 config); at minimum store mode used for kind_for_mode on each process
       - Lazy `_ensure_model`: if both injected present return them; else lock, configure_model_cache(), import transformers AutoImageProcessor + AutoModelForDepthEstimation, from_pretrained(model_id), to(device).eval(); clear ImportError pointing at `--extra depth`
       - `process(frame)`:
         - Read image_bgr; if missing log warning and return empty DepthResult (depth_map None or zeros + error path — document choice)
         - bgr_to_rgb_uint8 → PIL Image or processor-accepted RGB
         - Fake path: if injected model is callable or has a simple `predict`/`__call__` contract used by tests — define a minimal duck-type used by FakeModel in tests (e.g. model returns fixed tensor HxW; processor returns dict with pixel_values)
         - Real path: processor(images=..., return_tensors="pt"); model forward no_grad; interpolate predicted_depth to original H×W (F.interpolate or processor.post_process_depth_estimation when available)
         - kind, unit = kind_for_mode(self._depth_mode)
         - Return DepthResult dataclass (depth_map float32 HxW, kind, unit, width, height)
       - resolve_device: cuda → mps → cpu (shared util or copy from yolo_worker)
       - Never VideoCapture / open cameras
       - Never default model_id to Base/Large strings

    2. Implement `DepthLoop` in `loop.py` (structural twin of DetectionLoop):
       - `__init__(self, bus, worker, store)`
       - start/stop daemon thread name `"depth"`; join timeout 5.0; idempotent
       - `_run`: get_latest; if None or same frame_id → `_stop.wait(0.005)`; gap → store.record_depth_drop; else process, latency_ms via perf_counter, store.set_depth(...); update `_last_frame_id`
       - Broad except: log exception; set_depth with depth_map=None, error=str(exc), kind from worker mode or RELATIVE, unit=None; keep thread alive
       - Never source.read / VideoCapture

    3. Plugin registration:
       - Ensure entry point declared (Task 1)
       - register_builtins: register depth worker name when import succeeds; skip-if-present; graceful without torch
       - Extend tests/test_plugins_registry.py

    4. Tests (mock HF; no weight download):
       - test_depth_worker.py: FakeModel/FakeProcessor inject; relative vs metric_indoor kind/unit; missing image_bgr; protocol name+process; inspect no VideoCapture; optional ImportError message unit if import path mocked
       - test_depth_loop.py: FakeDepthWorker; FrameBus + store; _wait_until product; skip same frame_id; exception keeps alive with error product; no VideoCapture in loop module source
       - Full suite + ruff at plan end

    5. Do **not** wire create_app or sentry serve (04-02). Do not implement colormap.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_depth_worker.py tests/test_depth_loop.py tests/test_depth_preprocess.py tests/test_depth_mapping.py tests/test_perception_store.py tests/test_model_cache.py tests/test_plugins_registry.py &amp;&amp; uv run ruff check src/sentry_ai/models/depth src/sentry_ai/state/perception_store.py src/sentry_ai/plugins/registry.py &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - DEPTH-01: worker + loop process live bus frames into store without cameras
    - Default relative Small path; metric modes set honest kind/unit from config
    - CI never downloads HF weights; injectable fakes only
    - HF_HOME configured before real load; depth extra ImportError is actionable
    - Full pytest suite green; ruff clean; smoke path still no mandatory torch
  </acceptance_criteria>
  <done>Depth core (worker, loop, store, cache, preprocess, mapping) ready for 04-02 API/UI surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → HF/model load | Weights resolved under SENTRY_MODEL_CACHE/hf; allowlisted HF model ids |
| Optional third-party code | torch/transformers execution of model graphs |
| FrameBus → DepthLoop | Trusted in-process ImageFrame BGR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing / Integrity | depth kind/unit mapping | mitigate | kind_for_mode from configured depth_mode only; schema validators reject relative+unit; no depth_m field |
| T-04-02 | Tampering | models/cache.py HF paths | mitigate | HF_HOME under SENTRY_MODEL_CACHE; allowlist MODE_TO_MODEL Small ids only; no arbitrary URL/path weights |
| T-04-03 | Elevation / Compliance | default model selection | mitigate | Apache Small default only; never Base/Large NC as default |
| T-04-04 | Denial of Service | DepthLoop vs capture | mitigate | Dedicated depth thread + keep-latest skip; never infer in capture/handlers |
| T-04-05 | Information Disclosure | full depth arrays | accept deferred | Wire JSON bulk deferred to 04-02 policy (metadata only); in-process map for colormap/Phase 5 |
| T-04-SC | Tampering | pip install torch/transformers/hf-hub/pillow | mitigate | RESEARCH legitimacy Approved/[ASSUMED]; optional-extra only; no [SLOP]; no native timm path |
</threat_model>

<verification>
- `uv run pytest -q tests/test_depth_*.py tests/test_perception_store.py tests/test_model_cache.py tests/test_schemas_depth_kind.py`
- Full suite `uv run pytest -q` green without depth extra installed
- `uv run ruff check src tests`
- Architecture: no VideoCapture in `src/sentry_ai/models/depth/`
- No HF network in unit tests
</verification>

<success_criteria>
1. Injectable DepthAnythingWorker produces depth_map + honest DepthKind/unit (DEPTH-01)
2. DepthLoop is DetectionLoop twin: bus → process → set_depth; error-alive
3. PerceptionStore dual products (det + depth) keep-latest
4. HF cache under SENTRY_MODEL_CACHE; depth optional-extra documented
5. Golden preprocess/mapping tests lock BGR→RGB and mode honesty
6. 04-02 can attach snapshot/colormap/serve without redesign
</success_criteria>

<output>
Create `.planning/phases/04-monocular-depth/04-01-SUMMARY.md` when done
</output>
