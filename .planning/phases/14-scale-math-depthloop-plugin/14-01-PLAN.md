---
phase: 14-scale-math-depthloop-plugin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sentry_ai/spatial/calibration.py
  - src/sentry_ai/spatial/__init__.py
  - tests/test_calibration_fit.py
autonomous: true
requirements:
  - CAL-01
  - CAL-02
user_setup: []
must_haves:
  truths:
    - "fit_scale_median recovers scale from synthetic (observed_raw, known_meters) pairs via median(D_i/d_i)"
    - "fit_affine_lstsq recovers scale+offset when N>=2 via numpy.linalg.lstsq"
    - "Non-positive or non-finite observations are excluded; insufficient valid pairs reject the fit"
    - "Absurd scale outside (1e-4, 1e4) is rejected at fit time with reason absurd_scale"
    - "residual_rms above max(0.15*median(D), 0.05) is rejected at fit time with reason residual_rms_too_high"
    - "Rejected fits return ok=False and must not be treated as draft-ready params"
    - "No DepthLoop, CalibrationState.apply_map, wizard, YAML, or free-space meter changes in this plan"
  artifacts:
    - path: "src/sentry_ai/spatial/calibration.py"
      provides: "CalibrationFitResult, fit_scale_median, fit_affine_lstsq, residual/scale gate helpers"
      exports: ["CalibrationFitResult", "fit_scale_median", "fit_affine_lstsq", "MIN_SCALE", "MAX_SCALE"]
    - path: "tests/test_calibration_fit.py"
      provides: "Accept + reject matrix for CAL-01/CAL-02 synthetic pairs"
    - path: "src/sentry_ai/spatial/__init__.py"
      provides: "Optional lazy export of fit helpers (planner discretion; submodule import OK)"
  key_links:
    - from: "fit_scale_median / fit_affine_lstsq"
      to: "CalibrationFitResult"
      via: "ok/reason + scale/offset/residual_rms/sample_count"
      pattern: "CalibrationFitResult|fit_scale_median|fit_affine_lstsq"
    - from: "fit reject gates"
      to: "Phase 15 set_draft_params callers"
      via: "ok must be True before staging draft"
      pattern: "ok=False|absurd_scale|residual_rms_too_high"
    - from: "spatial/calibration.py"
      to: "control/calibration_state.py (plan 14-02)"
      via: "params fields only — no state mutation here"
      pattern: "scale|offset|residual_rms"
---

<objective>
Ship pure NumPy scale/affine fitters with fit-time reject gates so makers (and tests) can recover a global monocular scale from ground-truth pairs — and bad fits never look draft-ready.

Purpose: CAL-01 fit recovery + CAL-02 reject policy before DepthLoop wiring (14-02).

Output: `spatial/calibration.py`, synthetic `tests/test_calibration_fit.py`, optional spatial package export. Zero new pip packages. No DepthLoop, apply_map, wizard, YAML, or free-space meters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>


<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-scale-math-depthloop-plugin/14-RESEARCH.md
@.planning/phases/14-scale-math-depthloop-plugin/14-PATTERNS.md
@.planning/phases/14-scale-math-depthloop-plugin/14-VALIDATION.md
@src/sentry_ai/schemas/calibration.py
@src/sentry_ai/control/calibration_state.py
@src/sentry_ai/spatial/free_space.py
@src/sentry_ai/spatial/__init__.py
@tests/test_calibration_state.py
@tests/test_free_space_bands.py

## Locked decisions (ROADMAP + RESEARCH — must honor)

| Decision | Value | Source |
|----------|-------|--------|
| Fit default | Scale-only **median** of D_i/d_i | RESEARCH lock #1 |
| Optional affine | `numpy.linalg.lstsq` when N>=2; store scale+offset | RESEARCH lock #2 |
| Apply formula (document; implement in 14-02) | map' = scale*map + offset — not inverse-depth | RESEARCH lock #3 |
| Polarity / observations | No polarity flip; reject non-positive observations | RESEARCH lock #4 |
| Reject gates | residual_rms > max(0.15*median(D), 0.05); scale outside (1e-4, 1e4); **fit-time before draft** | RESEARCH lock #5 |
| Base kinds | Same apply later for relative + metric_estimated; fingerprint mode/model; no undo | RESEARCH lock #6 |
| Fitter core | `(observed_raw, known_meters)` pairs; height helper optional/minimal | RESEARCH lock #7 |
| Module | `src/sentry_ai/spatial/calibration.py` | RESEARCH lock #8 |
| Constraints | Zero new deps; freeze DetectionLoop/FrameBus/ORT-TRT; synthetic only; no wizard/YAML/free-space meters | RESEARCH lock #9 |
| Packages | Zero new pip deps | ROADMAP |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| Result API | Frozen `CalibrationFitResult` with `ok` + `reason` (not exception-only for gates) |
| Default entrypoint | `fit_scale_median` is the primary CAL-01 path; `fit_affine_lstsq` is explicit second function |
| Height helper | Optional thin helper **omitted** — pairs API is sufficient for CAL-01 |
| Package export | Lazy `__getattr__` for fit symbols **or** tests import submodule — either OK |
| Building CalibrationParams | Out of scope here — 14-02 / Phase 15 map FitResult → params; tests assert FitResult fields only |

## Interfaces (shipped — consume, do not redesign)

From `src/sentry_ai/schemas/calibration.py`:
```python
class CalibrationParams(BaseModel):
    scale: float
    offset: float = 0.0
    method: str = "known_distance"
    sample_count: int = 0
    residual_rms: float | None = None
    fingerprint: CalibrationFingerprint
```

From `src/sentry_ai/control/calibration_state.py` (do not edit in 14-01):
```python
def set_draft_params(self, params: CalibrationParams) -> CalibrationSnapshot: ...
def get_applied_params(self) -> CalibrationParams | None: ...
# apply_map lands in 14-02
```

## Target pure APIs (implement exactly)

```python
MIN_SCALE = 1e-4
MAX_SCALE = 1e4

@dataclass(frozen=True)
class CalibrationFitResult:
    ok: bool
    scale: float = 1.0
    offset: float = 0.0
    residual_rms: float | None = None
    sample_count: int = 0
    method: str = "known_distance"
    reason: str | None = None

def fit_scale_median(
    observed_raw: ArrayLike,
    known_meters: ArrayLike,
    *,
    method: str = "known_distance",
) -> CalibrationFitResult: ...

def fit_affine_lstsq(
    observed_raw: ArrayLike,
    known_meters: ArrayLike,
    *,
    method: str = "known_distance",
) -> CalibrationFitResult: ...
```

Module docstring must state apply formula for Phase 14-02 handoff:
`map_out = scale * map_in + offset` (float32 copy-on-write; not inverse-depth).

## Out of scope for 14-01

- `CalibrationState.apply_map` / DepthLoop / cli.py
- Wizard REST / index.html / YAML
- Free-space meter path / assemble units flip
- scipy or any new dependency
- Polarity flip heuristics
</context>


<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure scale/affine fitters + reject gates (CAL-01, CAL-02)</name>
  <files>src/sentry_ai/spatial/calibration.py, src/sentry_ai/spatial/__init__.py, tests/test_calibration_fit.py</files>
  <read_first>
    - .planning/phases/14-scale-math-depthloop-plugin/14-RESEARCH.md (Locked Decisions; Code Examples; Pitfalls 1–3)
    - .planning/phases/14-scale-math-depthloop-plugin/14-PATTERNS.md (spatial/calibration.py section)
    - .planning/phases/14-scale-math-depthloop-plugin/14-VALIDATION.md
    - src/sentry_ai/schemas/calibration.py
    - src/sentry_ai/spatial/free_space.py (pure NumPy module style)
    - src/sentry_ai/spatial/__init__.py
    - tests/test_free_space_bands.py (synthetic NumPy test style)
  </read_first>
  <behavior>
    - fit_scale_median on perfect scale*observed pairs → ok=True, scale≈true, offset=0, sample_count=N, residual_rms≈0
    - fit_scale_median ignores non-positive / non-finite pairs; if none remain → ok=False reason insufficient_valid_samples
    - Empty or all-invalid inputs reject (insufficient_valid_samples)
    - Scale ≤1e-4 or ≥1e4 (or non-finite) → ok=False reason absurd_scale
    - Construct pairs where median scale fits but residual_rms > max(0.15*median(D), 0.05) → residual_rms_too_high
    - fit_affine_lstsq with N=1 valid pair → ok=False reason affine_requires_n_ge_2
    - fit_affine_lstsq with N≥2 noisy-but-consistent affine data → ok=True, scale/offset approx true
    - No polarity flip API; negative observed_raw never contributes to ratios
    - Module imports only math/dataclasses/numpy (no FastAPI, no CalibrationState, no torch)
    - Existing honesty/state tests remain green (no edits to those modules)
  </behavior>
  <acceptance_criteria>
      - fit_scale_median on perfect scale*observed pairs → ok=True, scale≈true, offset=0, sample_count=N, residual_rms≈0
      - fit_scale_median ignores non-positive / non-finite pairs; if none remain → ok=False reason insufficient_valid_samples
      - Empty or all-invalid inputs reject (insufficient_valid_samples)
      - Scale ≤1e-4 or ≥1e4 (or non-finite) → ok=False reason absurd_scale
      - Construct pairs where median scale fits but residual_rms > max(0.15*median(D), 0.05) → residual_rms_too_high
      - fit_affine_lstsq with N=1 valid pair → ok=False reason affine_requires_n_ge_2
      - fit_affine_lstsq with N≥2 noisy-but-consistent affine data → ok=True, scale/offset approx true
      - No polarity flip API; negative observed_raw never contributes to ratios
      - Module imports only math/dataclasses/numpy (no FastAPI, no CalibrationState, no torch)
      - Existing honesty/state tests remain green (no edits to those modules)
  </acceptance_criteria>
  <action>
    1. Create `src/sentry_ai/spatial/calibration.py`:
       - Module docstring: CAL-01/02; locked apply formula for 14-02; zero new deps; no state mutation.
       - Constants `MIN_SCALE=1e-4`, `MAX_SCALE=1e4`.
       - `CalibrationFitResult` frozen dataclass with ok/scale/offset/residual_rms/sample_count/method/reason.
       - Private `_valid_pairs` filtering finite and strictly positive observed_raw + known_meters; length mismatch → ValueError.
       - `residual_rms_gate(rms, known_meters) -> bool` using `max(0.15 * median(D), 0.05)`.
       - `fit_scale_median`: ratios = k/o; scale = median(ratios); offset=0; compute RMS of (scale*o - k); apply absurd-scale + residual gates; return FitResult.
       - `fit_affine_lstsq`: require ≥2 valid pairs; `lstsq` on `[o, 1]`; same gates; return FitResult.
       - Do not build `CalibrationParams` here (fingerprint belongs to caller).
       - Do not implement apply_map.

    2. Optionally extend `src/sentry_ai/spatial/__init__.py` lazy exports for fit symbols; submodule import in tests is acceptable if exports skipped.

    3. Create `tests/test_calibration_fit.py` covering all behavior bullets (accept median, accept affine, reject matrix with reason codes). Use deterministic numpy RNG seeds. No HF, no DepthLoop, no CalibrationState required.

    4. Do not edit `models/depth/loop.py`, `control/calibration_state.py`, `cli.py`, free-space algorithm, routes, or UI.
  </action>
  <verify>
    <automated>uv run pytest tests/test_calibration_fit.py tests/test_calibration_state.py tests/test_calibration_validators.py -q</automated>
  </verify>
  <done>Pure fitters recover scale (and optional affine) from synthetic pairs; reject gates fire with stable reason codes; no DepthLoop/state apply yet.</done>
</task>

</tasks>


<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GT samples to fit result | Operator/wizard-provided pairs can include outliers or non-positive junk |
| Fit result to draft params (Phase 15) | Rejected fits must not become staged calibration |
| Scale magnitude | Absurd scales would lie about meters once applied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | Non-positive / garbage samples | mitigate | Filter positive finite pairs; insufficient rejects |
| T-14-02 | Tampering | Absurd scale | mitigate | Clamp reject outside (1e-4, 1e4) at fit time |
| T-14-03 | Tampering | High-residual fit staged as truth | mitigate | residual_rms gate before ok=True; no draft in this plan |
| T-14-04 | Spoofing | Inverse-depth / polarity flip inventing meters | mitigate | Locked affine-on-depth; no polarity flip API |
| T-14-05 | Tampering | Hidden scipy/new dep supply chain | mitigate | NumPy-only module; no new packages |
| T-14-SC | Tampering | Package installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `uv run pytest tests/test_calibration_fit.py tests/test_calibration_state.py tests/test_calibration_validators.py -q`
- Grep: no edits to `models/depth/loop.py`, `cli.py`, `spatial/free_space.py` algorithm, routes, or `index.html`
- Confirm `fit_scale_median` / `fit_affine_lstsq` importable for plan 14-02 / Phase 15
</verification>

<success_criteria>
1. Synthetic known-distance pairs recover correct median scale (CAL-01).
2. Affine path recovers scale+offset for N>=2 (CAL-01).
3. Invalid fits reject with explicit reasons and ok=False (CAL-02).
4. Zero new deps; no DepthLoop/wizard/YAML/free-space meter scope creep.
</success_criteria>

<output>
Create `.planning/phases/14-scale-math-depthloop-plugin/14-01-SUMMARY.md` when done
</output>
