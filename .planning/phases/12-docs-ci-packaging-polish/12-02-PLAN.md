---
phase: 12-docs-ci-packaging-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_edge_ci_workflow.py
  - tests/test_pyproject_onnx_extra.py
  - .gitignore
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - EDGE-CI-01
  - EDGE-CI-02
user_setup: []
must_haves:
  truths:
    - "Default GitHub Actions stays ubuntu-latest with uv sync --extra dev only — no Jetson, self-hosted, tensorrt, or GPU runner"
    - "Static tests lock ci.yml so future edits cannot silently add Jetson/GPU requirements"
    - "Backend selection, missing-artifact honesty, and factory wiring remain proven by existing unit suites without NVIDIA Jetson"
    - "No tensorrt optional extra; wheel force-include does not ship .engine/.onnx/.pt model artifacts"
    - "Local export artifacts *.engine and *.onnx are gitignored (with *.pt) so engines are not casually committed"
  artifacts:
    - path: "tests/test_edge_ci_workflow.py"
      provides: "EDGE-CI-02 static lock on .github/workflows/ci.yml + gitignore artifact hygiene"
    - path: "tests/test_pyproject_onnx_extra.py"
      provides: "No tensorrt extra + wheel force-include hygiene"
    - path: ".gitignore"
      provides: "*.engine and *.onnx ignored alongside *.pt"
      contains: "*.engine"
    - path: ".github/workflows/ci.yml"
      provides: "Default Jetson-free CI (content locked; edit only if test proves drift)"
    - path: "tests/test_detection_factory.py"
      provides: "EDGE-CI-01 selection/soft/strict matrix (verify-only reference)"
  key_links:
    - from: "tests/test_edge_ci_workflow.py"
      to: ".github/workflows/ci.yml"
      via: "Path.read_text static asserts"
      pattern: "ubuntu-latest|uv sync --extra dev"
    - from: "tests/test_pyproject_onnx_extra.py"
      to: "pyproject.toml"
      via: "tomllib optional-deps + force-include"
      pattern: "tensorrt|force-include|\\.engine"
    - from: "EDGE-CI-01 verification"
      to: "build_detection_worker matrix"
      via: "pytest factory/honesty/artifact/parity suites"
      pattern: "backend_live|backend_reason"
---

<objective>
Lock contributor CI and packaging hygiene so backend selection/honesty stay proven without Jetson hardware, and default GHA never requires TensorRT GPU.

Purpose: EDGE-CI-01 is largely implemented — harden with a green matrix gate. EDGE-CI-02 workflow is already compliant — lock it with static tests plus artifact gitignore / wheel hygiene.

Output: `tests/test_edge_ci_workflow.py`, extended packaging asserts, `.gitignore` for `*.engine`/`*.onnx`, full selection matrix verification green; no factory rewrite; no new packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-docs-ci-packaging-polish/12-RESEARCH.md
@.planning/phases/12-docs-ci-packaging-polish/12-PATTERNS.md
@.planning/phases/12-docs-ci-packaging-polish/12-VALIDATION.md
@.planning/phases/11-sticky-fallback-dual-model-guardrails/11-01-SUMMARY.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-02-SUMMARY.md
@.github/workflows/ci.yml
@.gitignore
@pyproject.toml
@tests/test_pyproject_onnx_extra.py
@tests/test_detection_factory.py
@tests/test_backend_honesty_status.py
@tests/test_artifact_paths.py
@tests/test_ort_parity.py
@tests/test_trt_parity.py
@tests/test_edge_rt04_torch_only.py

## Research locks

| Lock | Value |
|------|-------|
| EDGE-CI-01 | Already largely covered by factory/honesty/artifact/parity — **lock with tests, do not rewrite factory** |
| EDGE-CI-02 | Static test on ci.yml: ubuntu-latest, `uv sync --extra dev` only, no Jetson/GPU/self-hosted/tensorrt |
| Packaging | gitignore `*.engine`/`*.onnx` optional-but-planned; no tensorrt extra; no engines in wheel |
| Packages | No new runtime packages; no real YOLO engine loads in default CI |
| Parallelism | Zero file overlap with 12-01 — may run same wave |

## Current ci.yml contract (preserve)

Single job `lint-and-test` on `ubuntu-latest`: checkout → setup-uv → python 3.11 → `uv sync --extra dev` → ruff → pytest → `sentry health`.

## EDGE-CI-01 living matrix (verify only — do not re-implement)

| Case | Expect |
|------|--------|
| desktop-gpu default | live=torch, reason=None |
| jetson no artifact (soft) | live=torch, reason=trt_artifact_missing |
| cpu-fallback no artifact (soft) | live=torch, reason=ort_artifact_missing |
| ORT live mock | artifact + dep → live=onnxruntime |
| TRT live mock | artifact + dep → live=tensorrt |
| strict miss | worker=None, live=None, same reason codes |
| sticky | factory once at serve; not in DetectionLoop |

## Out of scope for 12-02

- Product markdown hubs / AGPL lineage (12-01)
- Self-hosted Jetson GHA runners
- Factory / DetectionLoop rewrites
- Adding detect/onnx/tensorrt extras to CI install
- Real model.export in default suite
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — CI workflow + packaging static locks</name>
  <files>tests/test_edge_ci_workflow.py, tests/test_pyproject_onnx_extra.py</files>
  <read_first>
    - .planning/phases/12-docs-ci-packaging-polish/12-RESEARCH.md (CI static lock example; packaging hygiene)
    - .planning/phases/12-docs-ci-packaging-polish/12-PATTERNS.md (test_edge_ci_workflow.py full shape; force-include assert)
    - .github/workflows/ci.yml (current content — already compliant)
    - .gitignore (ends with *.pt only today)
    - pyproject.toml (optional-dependencies; hatch force-include profiles + UI static only)
    - tests/test_pyproject_onnx_extra.py (test_no_tensorrt_optional_extra and tomllib patterns)
  </read_first>
  <behavior>
    - ci.yml must exist; contain ubuntu-latest; contain uv sync --extra dev; contain ruff and pytest and sentry health
    - ci.yml must not contain self-hosted, tensorrt, jetson, cuda, gpu labels, --extra detect, --extra onnx, --extra depth
    - .gitignore must list *.pt, *.engine, *.onnx
    - pyproject optional-dependencies must not include tensorrt; onnx extra must not pin onnxruntime-gpu
    - hatch wheel force-include sources/dests must not end with .engine/.onnx/.pt
  </behavior>
  <action>
    Write static tests first. Prefer plain Path.read_text asserts for YAML (no new PyYAML dependency).

    1. Create tests/test_edge_ci_workflow.py:
       - test_default_gha_no_jetson_or_tensorrt_gpu — assert file exists; ubuntu-latest present; self-hosted/tensorrt/jetson absent; `uv sync --extra dev` present; `--extra detect` / `--extra onnx` / `--extra depth` absent; ruff check or ruff present; pytest present; `sentry health` present
       - test_ci_single_job_no_gpu_labels — cuda and gpu absent from lowered yml; runs-on present
       - test_gitignore_ignores_engine_and_onnx — REPO_ROOT/.gitignore contains *.engine, *.onnx, *.pt

    2. Extend tests/test_pyproject_onnx_extra.py:
       - Keep test_no_tensorrt_optional_extra and onnx pin tests green
       - Add test_wheel_force_include_has_no_engines_or_onnx — parse pyproject via tomllib; force-include map must not have src/dst ending with .engine/.onnx/.pt; force-include should still cover profiles and UI static paths

    3. Run new tests: EDGE-CI-02 workflow asserts should PASS against current ci.yml; gitignore asserts should FAIL (RED) until Task 2 adds *.engine/*.onnx. Force-include assert should PASS if hatch block is already clean — if it fails, fix only in Task 2 after confirming.

    4. Do not modify factory modules. Optionally add a short module docstring on test_edge_ci_workflow.py documenting that EDGE-CI-01 matrix lives in test_detection_factory.py (living table in docstring is fine — not a new runtime module).

    Avoid: installing packages; rewriting ci.yml to add extras; using a YAML parser dependency; loading real engines.
  </action>
  <acceptance_criteria>
    - tests/test_edge_ci_workflow.py exists with the three tests named above (or equivalent coverage)
    - `uv run pytest tests/test_edge_ci_workflow.py tests/test_pyproject_onnx_extra.py -q --tb=short` shows RED only for missing gitignore lines (or all green if executor combines with Task 2 in same commit — prefer RED commit for gitignore first)
    - `rg -n "tensorrt" pyproject.toml` does not show an optional-dependencies.tensorrt table (comments about no tensorrt OK)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_edge_ci_workflow.py tests/test_pyproject_onnx_extra.py -q --tb=short</automated>
  </verify>
  <done>Static CI + packaging tests encode EDGE-CI-02 and wheel hygiene; gitignore gap is visible as RED if not yet fixed.</done>
</task>

<task type="auto">
  <name>Task 2: gitignore artifacts + confirm CI content lock (EDGE-CI-02)</name>
  <files>.gitignore, .github/workflows/ci.yml, tests/test_edge_ci_workflow.py</files>
  <read_first>
    - .gitignore (full file — extend weight artifact section)
    - .github/workflows/ci.yml (must remain Jetson-free)
    - tests/test_edge_ci_workflow.py (from Task 1)
    - git ls-files for any tracked .onnx/.engine (must be none — do not delete fixtures that do not exist)
  </read_first>
  <action>
    Close EDGE-CI-02 packaging hygiene without changing CI install surface.

    1. Extend .gitignore next to existing `*.pt`:
       - Add `*.engine` and `*.onnx` with a short comment that local model weights / export artifacts must not be committed
       - Confirm no tracked test fixtures use committed .onnx/.engine (research: none). If any tracked file exists, do not blanket-delete without plan escalation — report; expected state is zero tracked engines.

    2. Confirm .github/workflows/ci.yml still matches the locked contract (ubuntu-latest; uv sync --extra dev only; ruff; pytest; sentry health). Edit ci.yml **only** if a static test fails due to accidental drift — do not add runners, GPU labels, or ML extras. Prefer leave file byte-identical when already compliant.

    3. Re-run tests/test_edge_ci_workflow.py and packaging tests until green.

    Avoid: adding self-hosted Jetson jobs; `uv sync --extra detect/onnx/depth` in CI; committing sample engines; modifying factory.
  </action>
  <acceptance_criteria>
    - `rg -n "^\*\.engine$|^\*\.onnx$|^\*\.pt$" .gitignore` matches all three
    - `uv run pytest tests/test_edge_ci_workflow.py tests/test_pyproject_onnx_extra.py -q --tb=short` all green
    - `rg -n "self-hosted|tensorrt|jetson|cuda|--extra detect|--extra onnx" .github/workflows/ci.yml` returns no matches (except none expected)
    - `rg -n "ubuntu-latest|uv sync --extra dev|sentry health" .github/workflows/ci.yml` matches
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_edge_ci_workflow.py tests/test_pyproject_onnx_extra.py -q --tb=short</automated>
  </verify>
  <done>GHA remains Jetson/GPU-free and is keyword-locked; export artifacts are gitignored; packaging static gates green.</done>
</task>

<task type="auto">
  <name>Task 3: EDGE-CI-01 selection/fallback matrix gate (verify-only)</name>
  <files>tests/test_edge_ci_workflow.py</files>
  <read_first>
    - .planning/phases/12-docs-ci-packaging-polish/12-RESEARCH.md (EDGE-CI-01 existing coverage snapshot)
    - tests/test_detection_factory.py (soft/strict/live matrix — do not rewrite)
    - tests/test_backend_honesty_status.py
    - tests/test_artifact_paths.py
    - tests/test_ort_parity.py
    - tests/test_trt_parity.py
    - tests/test_edge_rt04_torch_only.py
  </read_first>
  <action>
    Prove EDGE-CI-01 without rewriting factory logic.

    1. Run the consolidated selection/honesty matrix command and ensure full green (no Jetson/GPU):
       - tests/test_detection_factory.py
       - tests/test_backend_honesty_status.py
       - tests/test_artifact_paths.py
       - tests/test_ort_parity.py
       - tests/test_trt_parity.py
       - tests/test_edge_rt04_torch_only.py

    2. If any failure is found, fix only if it is a trivial test drift from Phases 9–11 — do **not** redesign factory. Escalate real factory regressions as blockers rather than silent scope expansion.

    3. Document the living matrix in tests/test_edge_ci_workflow.py module docstring (or a short comment block) listing the EDGE-CI-01 cases table from plan context so future contributors know which suite owns selection/fallback coverage. No new product code.

    4. Optionally run `uv run ruff check src tests` and full `uv run pytest -q` if time allows before SUMMARY — at minimum the matrix command + CI/packaging tests must be green.

    Avoid: re-implementing build_detection_worker; adding real YOLO(*.engine) loads; new runtime modules; FPS claims.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_detection_factory.py tests/test_backend_honesty_status.py tests/test_artifact_paths.py tests/test_ort_parity.py tests/test_trt_parity.py tests/test_edge_rt04_torch_only.py tests/test_edge_ci_workflow.py tests/test_pyproject_onnx_extra.py -q --tb=short` all green
    - No modifications to src/sentry_ai/models/detection/factory.py unless a proven regression requires a minimal fix (default: zero factory edits)
    - test_edge_ci_workflow.py docstring references the EDGE-CI-01 matrix suites
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_detection_factory.py tests/test_backend_honesty_status.py tests/test_artifact_paths.py tests/test_ort_parity.py tests/test_trt_parity.py tests/test_edge_rt04_torch_only.py tests/test_edge_ci_workflow.py tests/test_pyproject_onnx_extra.py -q --tb=short</automated>
  </verify>
  <done>EDGE-CI-01 matrix verified green without Jetson; EDGE-CI-02 and packaging locks green; factory untouched by default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → repo secrets | CI must not pull GPU runners or secret-backed self-hosted labels casually |
| Packaging → redistributors | Wheel must not ship non-portable engines or AGPL weight dumps |
| Contributor machine → git | Local exports must not be committed as portable artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Elevation of privilege | .github/workflows/ci.yml | mitigate | Static test forbids self-hosted/jetson/tensorrt/cuda/gpu; install stays uv sync --extra dev only |
| T-12-07 | Tampering | Wheel force-include | mitigate | Assert hatch force-include excludes .engine/.onnx/.pt; packages only src/sentry_ai + profiles + UI static |
| T-12-08 | Tampering | Accidental engine commits | mitigate | .gitignore *.engine *.onnx *.pt + static test |
| T-12-09 | Denial of service | CI real export / GPU extras | mitigate | Forbid --extra detect/onnx/depth in default workflow; keep mock-only factory matrix |
| T-12-10 | Information disclosure | CI secrets via GPU runners | accept | No secrets used by current workflow; lock prevents new secret-bearing self-hosted labels without test failure |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; existing extras unchanged |
</threat_model>

<verification>
- Static CI + packaging + gitignore tests green
- Factory/honesty/artifact/parity/EDGE-RT-04 suite green without hardware
- ci.yml remains ubuntu-latest + dev extra only
- No factory rewrite unless proven regression
</verification>

<success_criteria>
1. EDGE-CI-02: Default GHA does not require Jetson or TensorRT GPU — locked by tests
2. EDGE-CI-01: Selection, missing-artifact honesty, factory wiring covered by unit tests without Jetson
3. Packaging: no tensorrt extra; no engines in wheel includes; *.engine/*.onnx gitignored
4. Matrix verification command green; ruff/tests remain CI-compatible
</success_criteria>

<output>
Create `.planning/phases/12-docs-ci-packaging-polish/12-02-SUMMARY.md` when done
</output>
