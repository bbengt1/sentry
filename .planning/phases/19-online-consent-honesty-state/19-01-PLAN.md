---
phase: 19-online-consent-honesty-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sentry_ai/control/calibration_state.py
  - src/sentry_ai/schemas/calibration.py
  - tests/test_calibration_state.py
  - tests/test_calibration_persist.py
autonomous: true
requirements:
  - ONL-01
  - ONL-02
user_setup: []
must_haves:
  truths:
    - "CalibrationState() and serve-shaped construction boot online=False; no env/CLI/YAML required to stay off"
    - "set_online(True) while not applied raises ValueError online_requires_applied and leaves applied/draft/kind unchanged"
    - "First metric_calibrated still requires apply() or apply_params (try_reapply match); set_online never calls apply/apply_params/apply_map"
    - "After apply() or matching try_reapply, set_online(True) succeeds; promote_kind_unit is calibrated only because applied+valid, not because online"
    - "apply() / apply_params() / try_reapply do not flip online on"
    - "clear_applied forces online=False (consent gone); set_online(False) is implemented but must not clear applied (full Cancel/Clear/disable matrix is 19-02)"
    - "CalibrationSnapshot.online defaults False; extra=forbid unchanged; persist_status plane untouched"
    - "No sampler, no auto-commit, no YAML online key, no DepthLoop/DetectionLoop/FrameBus/ORT-TRT/kind_for_mode, zero new deps"
  artifacts:
    - path: "src/sentry_ai/control/calibration_state.py"
      provides: "is_online + set_online first-scale lock; session flag default off"
      exports: ["CalibrationState"]
    - path: "src/sentry_ai/schemas/calibration.py"
      provides: "CalibrationSnapshot.online: bool = False"
    - path: "tests/test_calibration_state.py"
      provides: "ONL-01 default-off + ONL-02 first-scale lock"
    - path: "tests/test_calibration_persist.py"
      provides: "try_reapply match leaves online off"
  key_links:
    - from: "set_online(True)"
      to: "is_applied()"
      via: "refuse unless _applied_params is not None"
      pattern: "online_requires_applied"
    - from: "CalibrationSnapshot.online"
      to: "_online_enabled"
      via: "_snapshot_unlocked under lock"
      pattern: "online="
    - from: "try_reapply match"
      to: "apply_params"
      via: "unchanged; online stays False"
      pattern: "is_online"
    - from: "set_online"
      to: "apply / apply_params / apply_map"
      via: "must not call"
      pattern: "set_online"
---

<objective>
Add a session online-recal flag that boots off and cannot invent the first metric scale.

Purpose: ONL-01 (opt-in default off — flag half) and ONL-02 (first `metric_calibrated` still Apply / matching persist `try_reapply`).

Output: `CalibrationState.is_online` / `set_online` + `CalibrationSnapshot.online` + synthetic tests. Zero new packages. No REST toggle (19-02). No sampler / auto-commit / DepthLoop edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-online-consent-honesty-state/19-RESEARCH.md
@.planning/phases/19-online-consent-honesty-state/19-PATTERNS.md
@.planning/phases/19-online-consent-honesty-state/19-VALIDATION.md
@src/sentry_ai/control/calibration_state.py
@src/sentry_ai/control/calibration_persist.py
@src/sentry_ai/schemas/calibration.py
@tests/test_calibration_state.py
@tests/test_calibration_persist.py

## Locked decisions (ROADMAP + RESEARCH + user brief — must honor)

| Decision | Value | Source |
|----------|-------|--------|
| Default | Online **off** at `CalibrationState()` | Lock #1 / ONL-01 |
| First scale | Still `apply()` or matching `try_reapply` → `apply_params` | Lock #2 / ONL-02 |
| Enable unapplied | Raise `ValueError("online_requires_applied")`; stay off / unapplied | Lock #3 |
| Home | Fields on `CalibrationState`, not a new `OnlineRecalState` | Lock #11 |
| Flag persist | Session only — no YAML / env / CLI this phase | Lock #10 |
| Snapshot | Add `online: bool = False` now; `online_status` four-way is 19-02 | Plan split |
| `clear_applied` | Forces `online=False` (consent gone) | Lock #6 precursor |
| `set_online(False)` | Must not `clear_applied` (prove in unit test; REST/YAML matrix is 19-02) | Lock #7 |
| `apply` / `apply_params` | Do not enable online | Lock #2 |
| Constraints | Zero new deps; freeze DetectionLoop/FrameBus/ORT-TRT/`kind_for_mode`/`apply_map`; no sampler/auto-commit; no FSD | Lock #12 |
| REST / status enum / Cancel-Clear-disable matrix | 19-02 | Plan split |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| API | `is_online() -> bool` and `set_online(enabled: bool) -> CalibrationSnapshot` (mirrors persist helpers) |
| Internal storage | `_online_enabled: bool = False`. May also store `_online_status` default `online_off` so 19-02 only exposes it — do **not** add `online_status` to `CalibrationSnapshot` in this plan |
| Error token | Exact `ValueError` message contains `online_requires_applied` (REST 409 maps this in 19-02) |
| Idempotence | `set_online(True)` when already on after applied is a no-op success; `set_online(False)` when already off is a no-op success |
| `clear_applied` | Reset `_online_enabled = False` under the same lock (and `_online_status = "online_off"` if the field exists) |
| `clear_draft` | Unchanged — do not touch online |
| Tests | Extend existing modules; reuse `_params()` / `_fp()` helpers; `tmp_path` for persist cases |
| TDD | Task 1 RED (tests only) then Task 2 GREEN (state + schema) |

## Interfaces (shipped — consume, do not redesign)

```python
# CalibrationState today
def apply(self) -> CalibrationSnapshot: ...          # requires draft
def apply_params(self, params) -> CalibrationSnapshot: ...
def apply_map(self, depth_map): ...                  # FROZEN
def clear_draft(self) -> CalibrationSnapshot: ...
def clear_applied(self) -> CalibrationSnapshot: ...
def is_applied(self) -> bool: ...
def promote_kind_unit(self, base_kind, base_unit): ...
def set_persist_status(self, status, reason=None) -> None: ...

# persist
def try_reapply(state, path, live) -> ReapplyResult: ...  # match → apply_params

# CalibrationSnapshot today
applied, valid, draft_sample_count, has_draft_params
scale, method, fingerprint
persist_status: none|applied|ignored_mismatch|error
persist_reason: str | None
```

## Target APIs (implement)

```python
# schemas/calibration.py — CalibrationSnapshot
online: bool = False

# control/calibration_state.py
def is_online(self) -> bool: ...
def set_online(self, enabled: bool) -> CalibrationSnapshot: ...
# _snapshot_unlocked copies online=self._online_enabled
# clear_applied also sets _online_enabled = False
```

## Out of scope for 19-01

- `online_status` on the snapshot / `/api/status` / REST POST (19-02)
- Cancel/Clear/disable YAML matrix and route tests (19-02)
- Sampler, auto-commit, residual gate, `fingerprints_match` as an online conjunct
- DepthLoop `apply_map` edits; DetectionLoop / FrameBus / ORT-TRT / `kind_for_mode`
- Live Preview toggle; CLI/env; YAML persist of the flag
- New pip deps; `pyproject` version bump
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — online default-off + first-scale lock tests</name>
  <files>tests/test_calibration_state.py, tests/test_calibration_persist.py</files>
  <read_first>
    - .planning/phases/19-online-consent-honesty-state/19-RESEARCH.md (locks 1–3, 10–12)
    - .planning/phases/19-online-consent-honesty-state/19-PATTERNS.md (CalibrationState flag)
    - src/sentry_ai/control/calibration_state.py
    - src/sentry_ai/schemas/calibration.py
    - tests/test_calibration_state.py
    - tests/test_calibration_persist.py
  </read_first>
  <behavior>
    - CalibrationState() → is_online() is False; snapshot.online is False; is_applied() is False
    - CalibrationSnapshot() default online is False; extra=forbid still rejects unknown keys (existing test)
    - set_online(True) on a fresh state raises ValueError whose message includes online_requires_applied
    - after that raise: is_online() False, is_applied() False, promote_kind_unit(RELATIVE, None) stays RELATIVE / unit None
    - set_online(False) on a fresh state succeeds (idempotent) and stays not applied
    - apply() valid draft still promotes metric_calibrated; after apply, is_online() is still False
    - after apply(), set_online(True) returns a snapshot with online True and applied True; get_applied_params().scale unchanged
    - set_online(True) after apply does not change promote_kind_unit (still METRIC_CALIBRATED + m from applied, not from the flag)
    - apply_params(valid) leaves is_online() False; then set_online(True) ok
    - set_online(True) never creates applied params when none existed
    - clear_applied after online-on → is_online() False and is_applied() False
    - set_online(False) after online-on leaves is_applied() True and scale unchanged (disable ≠ Clear)
    - try_reapply matching YAML → result.status applied, state.is_applied() True, state.is_online() False
    - Existing apply/draft/apply_map/persist tests stay collected (will be GREEN once Task 2 lands; this task only adds failing new tests)
  </behavior>
  <acceptance_criteria>
    - New tests named for default-off, refuse-unapplied, apply-does-not-enable, try_reapply-does-not-enable, disable-does-not-clear-applied
    - `uv run pytest tests/test_calibration_state.py tests/test_calibration_persist.py -q --tb=line` is RED solely because is_online/set_online/snapshot.online are missing (or equivalent AttributeError / assert fail)
    - No src/sentry_ai edits in this task — tests only
    - No routes / DepthLoop / index.html / pyproject edits
  </acceptance_criteria>
  <action>
    Add failing unit tests first (TDD RED). Do not edit product modules in this task.

    1. Extend tests/test_calibration_state.py using existing `_params()` / `_fp()` helpers.
    2. Extend tests/test_calibration_persist.py: after a matching try_reapply, assert `state.is_online() is False` (and snapshot.online False if snapshot is used).
    3. Do not implement is_online/set_online yet.
    4. Run the two modules and confirm RED on the new tests.
    5. Commit tests-only if the workflow expects a RED commit.

    Avoid: REST tests (19-02); sampler; asserting auto_committed; writing YAML online keys; loading models.
  </action>
  <verify>
    <automated>uv run pytest tests/test_calibration_state.py tests/test_calibration_persist.py -q --tb=line</automated>
  </verify>
  <done>New ONL-01/ONL-02 tests exist and fail against current CalibrationState (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Session online flag + first-scale lock (ONL-01 / ONL-02 GREEN)</name>
  <files>src/sentry_ai/control/calibration_state.py, src/sentry_ai/schemas/calibration.py, tests/test_calibration_state.py, tests/test_calibration_persist.py</files>
  <read_first>
    - .planning/phases/19-online-consent-honesty-state/19-PATTERNS.md (flag shape)
    - src/sentry_ai/control/calibration_state.py
    - src/sentry_ai/schemas/calibration.py
    - tests/test_calibration_state.py (Task 1)
  </read_first>
  <action>
    Make ONL-01 (flag) and ONL-02 true. Satisfy Task 1 tests. No REST.

    1. CalibrationSnapshot: add `online: bool = False`. Keep extra=forbid. Do **not** add online_status yet.
    2. CalibrationState: `_online_enabled: bool = False`; `is_online()`; `set_online(enabled)` as in PATTERNS (raise `online_requires_applied` when enabling without `_applied_params`).
    3. `_snapshot_unlocked`: copy `online=self._online_enabled`.
    4. `clear_applied`: also set `_online_enabled = False` (and internal status off if present).
    5. `apply` / `apply_params` / `clear_draft` / `apply_map` / persist helpers: do **not** set online True. `apply_map` formula unchanged.
    6. Optional: store `_online_status` internally default `online_off` and flip it in `set_online` / `clear_applied` so 19-02 is a snapshot+REST job — do not expose it on CalibrationSnapshot yet.
    7. Do not edit routes_calibration.py, routes_preview.py, calibration_store.py, models/depth/loop.py, DetectionLoop, FrameBus, index.html, or pyproject.toml.

    Avoid: sampler; auto-commit; YAML online key; collapsing persist_status into online; FSD copy.
  </action>
  <acceptance_criteria>
    - Task 1 tests GREEN
    - `uv run pytest tests/test_calibration_state.py tests/test_calibration_persist.py tests/test_calibration_fit.py tests/test_api_calibration.py tests/test_cli_calibration_inject.py -q --tb=short` green
    - `rg -n "def apply_map" src/sentry_ai/control/calibration_state.py` still the sole apply_map; formula still scale*map+offset
    - No new files under src/sentry_ai/ besides the two listed edits
    - `rg -n "^version" pyproject.toml` still 0.1.0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_calibration_state.py tests/test_calibration_persist.py tests/test_calibration_fit.py tests/test_api_calibration.py tests/test_cli_calibration_inject.py -q --tb=short</automated>
  </verify>
  <done>Online flag boots off; enable refused until Apply / try_reapply; first scale not invented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Online flag → applied params | Enabling online must not become a silent first `apply_params` |
| Snapshot.online → operators | Default-on or missing field would look like consent |
| clear_applied → online | Leaving online True after wipe would let Phase 20 invent first scale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Spoofing | set_online invents first scale | mitigate | Refuse unless is_applied; test kind stays relative |
| T-19-02 | Tampering | apply/apply_params auto-enables online | mitigate | Tests assert online stays False after apply / try_reapply |
| T-19-03 | Tampering | Flag persisted to YAML | mitigate | No store/schema field; session only |
| T-19-04 | Elevation | Sampler / apply_map / spine edits in 19-01 | mitigate | files_modified + grep |
| T-19-05 | Spoofing | clear_applied leaves online True | mitigate | Reset in clear_applied; test |
| T-19-SC | Tampering | Package installs / version bump | accept | No installs; pyproject stays 0.1.0 |
</threat_model>

<verification>
- New state/persist tests green
- Existing apply/draft/apply_map/API calibration tests green
- No routes / DepthLoop / DetectionLoop / FrameBus diffs
- Snapshot.online defaults False; extra=forbid intact
</verification>

<success_criteria>
1. ONL-01 (flag): serve-shaped `CalibrationState()` is online-off.
2. ONL-02: enabling online while unapplied cannot create `metric_calibrated`; first scale still Apply / `try_reapply`.
3. Disable does not clear applied (unit-level; full YAML/REST matrix in 19-02).
4. Zero new deps; no sampler/auto-commit/spine creep.
</success_criteria>

<output>
Create `.planning/phases/19-online-consent-honesty-state/19-01-SUMMARY.md` when done
</output>
