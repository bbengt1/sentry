---
phase: 19-online-consent-honesty-state
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/sentry_ai/control/calibration_state.py
  - src/sentry_ai/schemas/calibration.py
  - src/sentry_ai/api/routes_calibration.py
  - src/sentry_ai/api/routes_preview.py
  - tests/test_calibration_state.py
  - tests/test_calibration_persist.py
  - tests/test_api_calibration.py
autonomous: true
requirements:
  - ONL-06
  - ONL-01
user_setup: []
must_haves:
  truths:
    - "Cancel / clear_draft still drops draft only; applied, YAML, online flag, and online_status stay put"
    - "Clear / clear_persisted still clears applied + deletes YAML; because consent is gone, online=False and online_status=online_off"
    - "set_online(False) / POST enabled=false is not Clear: applied params, persist_status, and YAML file remain"
    - "CalibrationSnapshot.online_status is online_off|online_draft|auto_committed|rejected, default online_off, extra=forbid"
    - "Enable after applied → online_status=online_draft; Phase 19 never sets auto_committed or rejected"
    - "GET /api/depth/calibration includes online + online_status from snapshot; POST /api/depth/calibration/online {enabled} extra=forbid; unapplied enable → 409"
    - "GET /api/status adds calibration_online + calibration_online_status without overwriting depth_kind or calibration_persist"
    - "No sampler, no auto-commit, no apply_map change, no YAML persist of the flag, zero new deps"
  artifacts:
    - path: "src/sentry_ai/schemas/calibration.py"
      provides: "CalibrationSnapshot.online_status four-way literal"
    - path: "src/sentry_ai/control/calibration_state.py"
      provides: "status transitions: off / draft-idle; clear_applied → online_off"
    - path: "src/sentry_ai/api/routes_calibration.py"
      provides: "POST /api/depth/calibration/online"
    - path: "src/sentry_ai/api/routes_preview.py"
      provides: "additive /api/status online fields"
    - path: "tests/test_api_calibration.py"
      provides: "ONL-06 REST matrix + 409 + status plane"
  key_links:
    - from: "POST /api/depth/calibration/online"
      to: "CalibrationState.set_online"
      via: "409 on online_requires_applied"
      pattern: "online_requires_applied|409"
    - from: "GET /api/depth/calibration"
      to: "snapshot.online + online_status"
      via: "_snapshot_payload model_dump"
      pattern: "online_status"
    - from: "GET /api/status"
      to: "calibration_online + calibration_online_status"
      via: "additive; do not set depth_kind"
      pattern: "calibration_online_status"
    - from: "clear_applied"
      to: "online_off"
      via: "consent gone"
      pattern: "online_off"
    - from: "set_online(False)"
      to: "YAML / applied"
      via: "must not delete_params or clear_applied"
      pattern: "set_online"
---

<objective>
Make Cancel, Clear, and disable-online three different operations, and expose a four-way online status plane that is not `depth.kind` and not persist status.

Purpose: ONL-06 (Cancel = draft; Clear = applied+YAML; disable-online ≠ Clear) plus the status half of ONL-01 (`online_off` representable and distinct).

Output: `online_status` on snapshot, thin POST toggle, `/api/status` additives, synthetic Cancel/Clear/disable matrix. No sampler / auto-commit / DepthLoop / YAML-flag persist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-online-consent-honesty-state/19-RESEARCH.md
@.planning/phases/19-online-consent-honesty-state/19-PATTERNS.md
@.planning/phases/19-online-consent-honesty-state/19-VALIDATION.md
@.planning/phases/19-online-consent-honesty-state/19-01-PLAN.md
@src/sentry_ai/control/calibration_state.py
@src/sentry_ai/control/calibration_persist.py
@src/sentry_ai/schemas/calibration.py
@src/sentry_ai/api/routes_calibration.py
@src/sentry_ai/api/routes_preview.py
@tests/test_calibration_state.py
@tests/test_calibration_persist.py
@tests/test_api_calibration.py

## Locked decisions (ROADMAP + RESEARCH + user brief — must honor)

| Decision | Value | Source |
|----------|-------|--------|
| Cancel | `clear_draft` only; applied + YAML + online unchanged | Lock #5 / ONL-06 |
| Clear | applied + YAML deleted; online forced off (`online_off`) because consent is gone | Lock #6 / ONL-06 |
| Disable | `set_online(False)` only — not `clear_applied`, not `delete_params` | Lock #7 / ONL-06 |
| Status enum | `online_off` / `online_draft` / `auto_committed` / `rejected` | Lock #8 / ONL-01 |
| Phase 19 transitions | off ↔ `online_draft` (idle after enable). Never set `auto_committed` or `rejected` | Lock #9 |
| Distinct planes | Do not write online into `depth.kind` or `persist_status` | Lock #8 |
| REST | Thin `POST /api/depth/calibration/online` `{enabled: bool}` extra=forbid; unapplied → 409 | RESEARCH + user brief |
| GET | Existing calibration GET dumps snapshot; `/api/status` adds `calibration_online` + `calibration_online_status` | PATTERNS |
| Flag persist | Still session-only | Lock #10 |
| Constraints | Zero new deps; freeze spine / `apply_map`; no sampler/auto-commit; no Live Preview toggle; no FSD | Lock #12 |
| 19-01 already shipped | `is_online` / `set_online` / `snapshot.online` / first-scale lock | depends_on |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| Snapshot field | `online_status: Literal["online_off","online_draft","auto_committed","rejected"] = "online_off"` |
| Internal | `_online_status` copied in `_snapshot_unlocked`; `set_online(True)` after applied sets `online_draft` if current is `online_off`; `set_online(False)` and `clear_applied` set `online_off` |
| Optional helper | `set_online_status(status)` that rejects unknown tokens (like `set_persist_status`). Phase 21 will use it for `auto_committed` / `rejected`. Phase 19 tests may construct a snapshot with those values but production paths must not set them |
| REST body | `CalibrationOnlineBody(enabled: bool)` extra=forbid; empty body → 422 (enabled required) |
| 409 detail | Pass through `online_requires_applied` (string or FastAPI detail) |
| `/api/status` keys | `calibration_online` (bool), `calibration_online_status` (str). Always emit when calibration_state exists (including `online_off`) |
| Clear implementation | Reset online **inside `clear_applied`** (already required by 19-01 for the flag) so `clear_persisted` / REST Clear / `refuse_if_mismatch` stay consistent |
| UI | No `index.html` edits |
| TDD | Task 1 RED (tests) then Task 2 GREEN (schema/state/routes) |

## Interfaces (shipped after 19-01 — consume)

```python
def is_online(self) -> bool: ...
def set_online(self, enabled: bool) -> CalibrationSnapshot: ...
# snapshot.online: bool
# Cancel route → clear_draft; Clear route → clear_persisted
# GET /api/depth/calibration → _snapshot_payload
# GET /api/status → calibration_active, calibration_persist, …
```

## Target APIs (implement)

```python
# CalibrationSnapshot
online_status: Literal["online_off", "online_draft", "auto_committed", "rejected"] = "online_off"

# CalibrationState._snapshot_unlocked
#   online_status=self._online_status

# POST /api/depth/calibration/online
class CalibrationOnlineBody(BaseModel):
    model_config = ConfigDict(extra="forbid")
    enabled: bool

# GET /api/status additives
data["calibration_online"] = bool(snap.online)
data["calibration_online_status"] = snap.online_status
```

## Out of scope for 19-02

- Sampler / N-window / throttle (20)
- Auto-commit / smoother reset-on-auto-commit / `apply_params` gate (21)
- Persist policy (session-only auto-commit YAML) (22)
- Live Preview toggle / operator docs hub refresh (22)
- DetectionLoop / FrameBus / ORT-TRT / `kind_for_mode` / `apply_map` formula
- CLI/env online flag; YAML persist of online
- New pip deps; pyproject bump
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — Cancel/Clear/disable matrix + status-plane tests</name>
  <files>tests/test_calibration_state.py, tests/test_calibration_persist.py, tests/test_api_calibration.py</files>
  <read_first>
    - .planning/phases/19-online-consent-honesty-state/19-RESEARCH.md (locks 5–9)
    - .planning/phases/19-online-consent-honesty-state/19-PATTERNS.md (matrix + REST)
    - src/sentry_ai/api/routes_calibration.py (cancel/clear)
    - src/sentry_ai/api/routes_preview.py (calibration status block)
    - tests/test_api_calibration.py (existing cancel/clear helpers)
    - tests/test_calibration_state.py (19-01 online tests)
  </read_first>
  <behavior>
    - CalibrationSnapshot() default online_status == "online_off"
    - CalibrationSnapshot(online_status="auto_committed") and "rejected" construct (enum exists); extra=forbid still rejects unknown keys / unknown status
    - After apply + set_online(True): snapshot.online is True and online_status == "online_draft"
    - clear_draft after that: has_draft_params False, is_applied True, is_online True, online_status still online_draft
    - clear_applied after that: is_applied False, is_online False, online_status == "online_off", persist_status == "none"
    - set_online(False) after apply+enable: is_applied True, scale unchanged, online False, online_status online_off
    - Production set_online / clear_applied / clear_draft never leave online_status in {auto_committed, rejected}
    - persist: after apply_params + persist_applied + set_online(True), set_online(False) leaves path.exists() True and load_params still valid; clear_persisted unlinks file and forces online off
    - REST GET /api/depth/calibration includes online and online_status keys
    - REST POST /api/depth/calibration/online {"enabled": true} while unapplied → 409; body online stays false / online_off
    - REST: apply (or apply_params via test fixture) then POST enabled true → 200, online true, online_status online_draft; applied scale unchanged
    - REST POST enabled false → 200, online false, online_status online_off, GET still shows applied
    - REST Cancel after enable: draft cleared, applied+online remain
    - REST Clear after enable: not applied, online off; if a YAML path was configured it is gone
    - REST POST extra field → 422; missing enabled → 422
    - GET /api/status includes calibration_online and calibration_online_status; does not require those strings to equal depth_kind or calibration_persist
    - Handlers still do not call apply_map
  </behavior>
  <acceptance_criteria>
    - New tests cover the PATTERNS Cancel/Clear/disable matrix at state, persist, and API layers
    - `uv run pytest tests/test_calibration_state.py tests/test_calibration_persist.py tests/test_api_calibration.py -q --tb=line` is RED against missing online_status / missing POST route
    - No src/sentry_ai edits in this task — tests only
  </acceptance_criteria>
  <action>
    Add failing tests first (TDD RED). Do not edit product modules.

    1. Extend test_calibration_state.py for online_status defaults + transitions + Cancel/disable vs clear_applied.
    2. Extend test_calibration_persist.py: disable leaves YAML; clear_persisted deletes YAML and forces online_off.
    3. Extend test_api_calibration.py using the existing create_app / client fixtures. Add POST /online cases and assert GET /api/status additives.
    4. Confirm RED (missing field / 404 on POST /online).
    5. Commit tests-only if the workflow expects a RED commit.

    Avoid: index.html; sampler; asserting auto-commit; DepthLoop tests; FSD copy.
  </action>
  <verify>
    <automated>uv run pytest tests/test_calibration_state.py tests/test_calibration_persist.py tests/test_api_calibration.py -q --tb=line</automated>
  </verify>
  <done>ONL-06 + status-plane tests exist and fail against 19-01 surfaces (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Status plane + Cancel/Clear/disable + thin REST (ONL-06 / ONL-01 GREEN)</name>
  <files>src/sentry_ai/control/calibration_state.py, src/sentry_ai/schemas/calibration.py, src/sentry_ai/api/routes_calibration.py, src/sentry_ai/api/routes_preview.py</files>
  <read_first>
    - .planning/phases/19-online-consent-honesty-state/19-PATTERNS.md (status + REST + /api/status)
    - src/sentry_ai/schemas/calibration.py
    - src/sentry_ai/control/calibration_state.py
    - src/sentry_ai/api/routes_calibration.py (cancel/clear, body models)
    - src/sentry_ai/api/routes_preview.py (calibration status block)
    - tests from Task 1
  </read_first>
  <action>
    Make ONL-06 and ONL-01 status true. Satisfy Task 1 tests.

    1. CalibrationSnapshot: add `online_status` Literal with default `online_off`. extra=forbid unchanged.
    2. CalibrationState: persist `_online_status`; copy into snapshot; `set_online` / `clear_applied` transitions per PATTERNS. Do not set `auto_committed` or `rejected` on any 19-02 path.
    3. Cancel route: still only `clear_draft` + drop freeze pin (no smoother reset, no set_online False).
    4. Clear route: still `clear_persisted` / `clear_applied` + smoother reset — online reset happens inside `clear_applied`.
    5. Add `POST /api/depth/calibration/online` with `CalibrationOnlineBody`. 409 on `online_requires_applied`. Return `_snapshot_payload`. Never YAML I/O, never `apply_map`.
    6. `/api/status`: add `calibration_online` and `calibration_online_status` in the existing calibration_state block. Do not assign `depth_kind` from online_status.
    7. Do not edit DepthLoop, DetectionLoop, FrameBus, ORT factory, `kind_for_mode`, index.html, calibration_store.py schema, or pyproject.toml.

    Avoid: sampler; auto-commit; writing online into YAML; collapsing three status planes; FSD language in comments.
  </action>
  <acceptance_criteria>
    - Task 1 tests GREEN
    - `uv run pytest tests/test_calibration_state.py tests/test_calibration_persist.py tests/test_api_calibration.py tests/test_cli_calibration_inject.py tests/test_calibration_fit.py -q --tb=short` green
    - Cancel does not delete YAML; disable does not delete YAML; Clear does
    - `rg -n "auto_committed|rejected" src/sentry_ai/control/calibration_state.py src/sentry_ai/api/routes_calibration.py` shows enum / comments only — no assignment of those statuses
    - No DetectionLoop / FrameBus / apply_map formula edits
    - `rg -n "^version" pyproject.toml` still 0.1.0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_calibration_state.py tests/test_calibration_persist.py tests/test_api_calibration.py tests/test_cli_calibration_inject.py tests/test_calibration_fit.py -q --tb=short</automated>
  </verify>
  <done>Cancel ≠ Clear ≠ disable-online; online_off is a distinct status plane; thin REST toggle cannot invent first scale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disable vs Clear | Operators who freeze scale must not lose YAML / applied meters |
| Cancel vs Clear | Draft discard must not wipe consented scale or online flag |
| online_status vs depth.kind | A rejected/draft online state must not relabel the map as relative or meters |
| REST enable | Unapplied POST must not apply_params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Tampering | Disable implemented as clear_persisted | mitigate | YAML-exists test after POST enabled=false |
| T-19-07 | Tampering | Cancel clears applied or flips online off | mitigate | State + REST tests |
| T-19-08 | Spoofing | online_status written into depth_kind | mitigate | /api/status test; grep depth_kind assignments |
| T-19-09 | Spoofing | POST enable unapplied auto-commits | mitigate | 409 test; is_applied False |
| T-19-10 | Elevation | Sampler / apply_map / UI / YAML-flag in 19-02 | mitigate | files_modified + grep |
| T-19-11 | Spoofing | Phase 19 sets auto_committed | mitigate | rg assignment forbid |
| T-19-SC | Tampering | Package installs | accept | No installs; pyproject 0.1.0 |
</threat_model>

<verification>
- State + persist + API matrix green
- Existing wizard apply/cancel/clear tests still green
- `/api/status` additives present and distinct from kind/persist
- No apply_map / DetectionLoop / FrameBus diffs
</verification>

<success_criteria>
1. ONL-06: Cancel = draft only; Clear = applied + YAML (online forced off); disable-online ≠ Clear.
2. ONL-01 status: `online_off` (and the four-way enum) is representable and not `depth.kind` / persist.
3. Thin REST toggle refuses first-scale invent (409).
4. Zero new deps; no sampler/auto-commit/spine creep.
</success_criteria>

<output>
Create `.planning/phases/19-online-consent-honesty-state/19-02-SUMMARY.md` when done
</output>
