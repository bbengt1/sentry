---
phase: 16-free-space-meters
plan: 02
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - src/sentry_ai/spatial/free_space.py
  - src/sentry_ai/spatial/loop.py
  - src/sentry_ai/state/perception_store.py
  - src/sentry_ai/api/assemble.py
  - src/sentry_ai/schemas/perception.py
  - src/sentry_ai/schemas/validators.py
  - src/sentry_ai/api/routes_calibration.py
  - tests/test_free_space_loop.py
  - tests/test_free_space_smoothing.py
  - tests/test_assemble_perception_frame.py
  - tests/test_calibration_validators.py
  - tests/test_free_space_bands.py
autonomous: true
requirements:
  - FS-03
  - FS-01
user_setup: []
must_haves:
  truths:
    - "FreeSpaceLoop consumes store depth kind+map and passes kind into compute_free_space; it never re-scales or calls apply_map"
    - "When depth.kind is METRIC_CALIBRATED the stored free-space product units are 'm'; relative and metric_estimated stay 'ordinal'"
    - "Ordinal set_near_cut/set_mid_cut remain [0,1] and are not passed as metric_near_cut_m"
    - "FreeSpaceLoop.reset_smoother() exists and OccupancySmoother.reset() runs on kind transition apply↔clear (relative↔calibrated)"
    - "assemble._units_for_depth_kind(METRIC_CALIBRATED) returns 'm'; else 'ordinal'"
    - "assert_free_space_units: calibrated must be 'm'; calibrated+ordinal is no longer the Phase 13 grace"
    - "Optional ObstacleCue.distance_m is mean scaled depth in the blob when calibrated; None/absent on relative and estimated"
    - "nearness_* stay 0..1; extra=forbid; no YAML; no wizard REST redesign; no DetectionLoop/FrameBus/ORT-TRT/kind_for_mode"
  artifacts:
    - path: "src/sentry_ai/spatial/loop.py"
      provides: "reset_smoother + kind-change detect + units passthrough"
    - path: "src/sentry_ai/api/assemble.py"
      provides: "_units_for_depth_kind METRIC_CALIBRATED → m"
    - path: "src/sentry_ai/schemas/validators.py"
      provides: "calibrated must emit units='m'"
    - path: "src/sentry_ai/schemas/perception.py"
      provides: "ObstacleCue.distance_m optional"
    - path: "src/sentry_ai/state/perception_store.py"
      provides: "FreeSpaceProduct.units"
    - path: "tests/test_free_space_loop.py"
      provides: "FS-03 kind-transition reset + units consume"
  key_links:
    - from: "FreeSpaceLoop._run"
      to: "compute_free_space(kind=depth.kind)"
      via: "consume snapshot_depth; never apply_map"
      pattern: "kind=depth.kind|reset_smoother"
    - from: "depth.kind change"
      to: "OccupancySmoother.reset"
      via: "FreeSpaceLoop.reset_smoother"
      pattern: "reset_smoother|_last_kind"
    - from: "assemble._units_for_depth_kind"
      to: "FreeSpacePayload.units"
      via: "METRIC_CALIBRATED → m"
      pattern: "_units_for_depth_kind"
    - from: "assert_free_space_units"
      to: "calibrated must be m"
      via: "Phase 13 grace removed"
      pattern: "must use units=.m."
    - from: "_extract_obstacles calibrated"
      to: "ObstacleCue.distance_m"
      via: "mean finite depth in blob"
      pattern: "distance_m"
---

<objective>
Wire the 16-01 metric compute path through FreeSpaceLoop, PerceptionStore, assemble, and honesty validators — and reset occupancy EMA on calibration apply↔clear — so `/v1` free-space meters match the scaled DepthLoop product.

Purpose: FS-03 smoother reset; FS-01 on the wire; optional additive `distance_m`.

Output: loop consume + `reset_smoother`, store `units`, assemble helper flip, validator tighten, optional `distance_m`. Depends on 16-01. Zero new packages. No YAML. No wizard REST redesign. No re-scale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-free-space-meters/16-RESEARCH.md
@.planning/phases/16-free-space-meters/16-PATTERNS.md
@.planning/phases/16-free-space-meters/16-VALIDATION.md
@.planning/phases/16-free-space-meters/16-01-PLAN.md
@src/sentry_ai/spatial/free_space.py
@src/sentry_ai/spatial/loop.py
@src/sentry_ai/spatial/smoothing.py
@src/sentry_ai/state/perception_store.py
@src/sentry_ai/api/assemble.py
@src/sentry_ai/schemas/perception.py
@src/sentry_ai/schemas/validators.py
@src/sentry_ai/api/routes_calibration.py
@src/sentry_ai/api/deps.py
@src/sentry_ai/models/depth/loop.py
@tests/test_free_space_loop.py
@tests/test_free_space_bands.py
@tests/test_assemble_perception_frame.py
@tests/test_calibration_validators.py
@tests/test_free_space_smoothing.py

## Locked decisions (must honor)

| Decision | Value | Source |
|----------|-------|--------|
| Consume, don't scale | Free-space reads DepthLoop scaled map + kind; never `apply_map` | RESEARCH #9 |
| Assemble helper | `METRIC_CALIBRATED` → `"m"`; else `"ordinal"` | RESEARCH #7 |
| Validator | Calibrated **must** emit `"m"`; Phase 13 calibrated+ordinal grace **ends** | RESEARCH #8 |
| Smoother | `OccupancySmoother.reset()` on kind apply↔clear; `FreeSpaceLoop.reset_smoother()`; loop detects kind change (`CalibrationState` has no listeners) | RESEARCH #6 |
| `distance_m` | Optional additive on cues **only when calibrated** (mean depth in blob); `nearness_*` stay 0..1 | RESEARCH #3 |
| Ordinal sliders | Stay `[0,1]`; not reused as 1.5/3.0 m | RESEARCH #4 |
| Belt-and-suspenders | Routes apply/clear **may** call `reset_smoother` if `app.state.free_space_loop` is present; **not required**. Cancel must not reset | RESEARCH #6 |
| Constraints | Zero new deps; freeze DetectionLoop/FrameBus/ORT-TRT/`kind_for_mode`; no YAML; no wizard redesign; no FSD/RANSAC | RESEARCH #10 |

## Claude's discretion

| Item | Choice |
|------|--------|
| Kind tracking | `FreeSpaceLoop._last_kind`; reset when `depth.kind != _last_kind` (after first frame). Treat calibrated↔not as apply/clear |
| Store units | Add `FreeSpaceProduct.units: str = "ordinal"`; `set_free_space(..., units=...)`; snapshot copies it |
| Assemble units | Prefer `free.units` if present, else `_units_for_depth_kind(free.depth_kind)` |
| Belt-and-suspenders | **Include**: POST apply and POST clear call `reset_smoother()` when `app.state.free_space_loop` has that method. POST cancel does **not**. Loop kind-detect remains the required path |
| `distance_m` fill | In `_extract_obstacles`, pass the depth array; when calibrated, `distance_m = float(mean(finite depth[component]))`; else omit/None |
| Store honesty gate | Do **not** require `assert_free_space_units` inside `set_free_space` (error products may still be ordinal). Wire validator is the gate |
| `reset_smoother` locking | Call smoother.reset() without requiring the cuts lock; document it is safe anytime |

## Interfaces (shipped — consume)

```python
# OccupancySmoother (already)
def reset(self) -> None: ...

# compute_free_space (16-01)
# kind=METRIC_CALIBRATED → units="m" via 1.5/3.0 m cuts

# AppState.free_space_loop: Any | None  # already
# CalibrationState has no listeners
```

## Target APIs (implement)

```python
# FreeSpaceLoop
def reset_smoother(self) -> None: ...

# OccupancySmoother.reset — already exists; just call it

# assemble
def _units_for_depth_kind(kind: DepthKind) -> str:
    if kind == DepthKind.METRIC_CALIBRATED:
        return "m"
    return "ordinal"

# ObstacleCue.distance_m: float | None = None

# assert_free_space_units: calibrated requires "m"
```

## Out of scope for 16-02

- Changing 16-01 cut math / polarity / min–max rules
- YAML persist / serve re-apply (Phase 17)
- Wizard REST redesign / index.html meter chrome (Phase 18 docs)
- DetectionLoop / FrameBus / ORT-TRT / `kind_for_mode`
- FSD / interlock / motor / RANSAC
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Loop consume kind/units, smoother reset, distance_m, store/wire, validator (FS-03 + FS-01 wire)</name>
  <files>src/sentry_ai/spatial/free_space.py, src/sentry_ai/spatial/loop.py, src/sentry_ai/state/perception_store.py, src/sentry_ai/api/assemble.py, src/sentry_ai/schemas/perception.py, src/sentry_ai/schemas/validators.py, src/sentry_ai/api/routes_calibration.py, tests/test_free_space_loop.py, tests/test_free_space_smoothing.py, tests/test_assemble_perception_frame.py, tests/test_calibration_validators.py, tests/test_free_space_bands.py</files>
  <read_first>
    - .planning/phases/16-free-space-meters/16-RESEARCH.md (locks 3, 6-10; Pitfalls 1, 5, 7)
    - .planning/phases/16-free-space-meters/16-PATTERNS.md (loop, store, assemble, schema, validator, optional routes)
    - .planning/phases/16-free-space-meters/16-01-PLAN.md
    - src/sentry_ai/spatial/loop.py
    - src/sentry_ai/spatial/free_space.py
    - src/sentry_ai/spatial/smoothing.py
    - src/sentry_ai/state/perception_store.py (FreeSpaceProduct, set_free_space)
    - src/sentry_ai/api/assemble.py (_units_for_depth_kind, _obstacle_to_wire)
    - src/sentry_ai/schemas/perception.py
    - src/sentry_ai/schemas/validators.py
    - src/sentry_ai/api/routes_calibration.py (apply/clear/cancel)
    - tests/test_free_space_loop.py
    - tests/test_calibration_validators.py (calibrated+ordinal currently OK)
    - tests/test_assemble_perception_frame.py
  </read_first>
  <behavior>
    - Loop on relative synthetic depth → stored product depth_kind relative; assemble units ordinal (existing)
    - Loop on calibrated synthetic **meter** map (0.5 m blob / 5 m bg, kind=METRIC_CALIBRATED, unit="m") → snapshot_free_space units=="m" (or assemble units=="m" if units only on wire); obstacle_count ≥ 1
    - Loop on metric_estimated kind + same array → units ordinal
    - set_near_cut(0.99) while kind calibrated does not empty a 0.5 m blob (sliders not used as meters)
    - reset_smoother() is public; OccupancySmoother.reset clears EMA so a subsequent empty frame is empty
    - Kind transition: process relative frames until occupied EMA is warm; then set_depth calibrated (or the reverse); next free-space frame must not keep the previous occupancy solely via EMA — reset ran. Implementation: call reset_smoother on kind != _last_kind
    - Loop source has no apply_map / CalibrationState / scale multiply
    - assemble METRIC_CALIBRATED free-space payload units=="m"; METRIC_ESTIMATED stays ordinal; relative stays ordinal
    - Wire ObstacleCue.distance_m is None/absent for relative dict obstacles; for calibrated cues with distance_m=1.2 it round-trips
    - compute_free_space calibrated obstacles have distance_m ≈ mean blob meters (e.g. ~0.5 for a 0.5 m blob); relative obstacles have distance_m None/absent
    - assert_free_space_units(METRIC_CALIBRATED, "ordinal") raises; (METRIC_CALIBRATED, "m") OK; relative/estimated + "m" still raise
    - FreeSpacePayload(calibrated, ordinal) ValidationError; FreeSpacePayload(calibrated, m) OK
    - Optional: POST apply/clear via existing calibration tests still pass; if belt-and-suspenders added, cancel does not require a loop
    - No torch/FrameBus imports in spatial/loop.py; extra=forbid still rejects unknown cue fields
    - Banned: no motor/FSD fields
  </behavior>
  <acceptance_criteria>
      - Loop consumes kind; calibrated store/wire units are "m"; estimated/relative ordinal
      - Ordinal sliders unused on calibrated frames
      - reset_smoother on kind transition; EMA does not ghost apply↔clear
      - No re-scale in FreeSpaceLoop
      - assemble helper METRIC_CALIBRATED → "m"
      - Validator: calibrated must be "m"; Phase 13 grace gone
      - distance_m only when calibrated (mean blob depth); nearness 0..1
      - extra=forbid; no YAML/wizard redesign/DetectionLoop/FrameBus/ORT/kind_for_mode
  </acceptance_criteria>
  <action>
    1. `ObstacleCue` dataclass in `free_space.py`: add `distance_m: float | None = None`. `_extract_obstacles(..., depth_map=None, calibrated=False)`: when calibrated, mean of finite `depth_map[component]` → `distance_m`; else None. `compute_free_space` passes the raw meter array on the calibrated branch.
    2. `_obstacles_for_store`: include `distance_m` when not None (always include the key with None is also OK if assemble treats missing/None the same — pick one and test).
    3. `FreeSpaceLoop`: `_last_kind`; `reset_smoother()` → `self._smoother.reset()`; in `_run` after a usable depth snapshot, if `_last_kind is not None and depth.kind != _last_kind`: `reset_smoother()`; then set `_last_kind`. Pass `kind=depth.kind`. Pass ordinal slider cuts only as `near_cut`/`mid_cut` (compute ignores them when calibrated). Pass `units=result.units` into `set_free_space` (including error path: result.units or "ordinal").
    4. `FreeSpaceProduct` + `set_free_space` + `snapshot_free_space`: add `units: str = "ordinal"` and copy it.
    5. `assemble._units_for_depth_kind`: calibrated → `"m"` else `"ordinal"`. Payload uses store `units` if set. `_obstacle_to_wire` copies `distance_m`.
    6. `assert_free_space_units`: if kind is METRIC_CALIBRATED and units != "m" → ValueError. Keep relative/estimated forbidding `"m"`. Flip tests that allowed calibrated+ordinal.
    7. `perception.ObstacleCue`: optional `distance_m: float | None = None`; extra=forbid unchanged. Update docstring.
    8. Optional belt-and-suspenders in `routes_calibration.py` apply + clear (not cancel): `loop = getattr(request.app.state, "free_space_loop", None); getattr(loop, "reset_smoother", lambda: None)()`.
    9. Tests per behavior bullets. Extend `_set_depth` in loop tests to accept kind/unit. Seed assemble tests with METRIC_CALIBRATED free-space. Keep dict-obstacle relative test green (`distance_m` absent OK).
    10. Grep guard: no DetectionLoop/FrameBus/ORT factory/`kind_for_mode`/YAML I/O/index.html algorithm changes. No `apply_map` in spatial/loop.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_free_space_bands.py tests/test_free_space_loop.py tests/test_free_space_smoothing.py tests/test_calibration_validators.py tests/test_assemble_perception_frame.py tests/test_api_calibration.py tests/test_depth_loop.py -q</automated>
  </verify>
  <done>Wire units follow calibrated metric cuts; smoother resets on kind apply↔clear; distance_m is additive and honest; Phase 13 ordinal grace is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DepthLoop scaled map → FreeSpaceLoop | Re-scale would double meters |
| Kind transition → EMA occupancy | Stale ordinal occupancy labeled as meters (or reverse) |
| Assemble helper vs compute units | Helper `"m"` without loop passing metric results would be label-only on the wire |
| Validator grace | Leaving calibrated+ordinal allowed after metric path ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Tampering | Double-scale in loop | mitigate | Consume snapshot map; grep apply_map/scale |
| T-16-12 | Tampering | EMA ghost on apply/clear | mitigate | reset_smoother on kind != _last_kind; loop test |
| T-16-13 | Spoofing | assemble always-ordinal stub | mitigate | helper flip + assemble tests |
| T-16-14 | Spoofing | calibrated+ordinal wire | mitigate | tighten assert_free_space_units; payload tests |
| T-16-15 | Spoofing | distance_m on relative cues | mitigate | fill only when calibrated; relative tests None |
| T-16-16 | Tampering | sliders as meters | mitigate | still [0,1]; compute ignores on calibrated |
| T-16-17 | Elevation | Cancel resetting applied occupancy | mitigate | belt-and-suspenders skips cancel |
| T-16-18 | Elevation | YAML / wizard / DetectionLoop / FSD | mitigate | out of scope; grep |
| T-16-SC | Tampering | Package installs | accept | No package installs |
</threat_model>

<verification>
- `uv run pytest tests/test_free_space_bands.py tests/test_free_space_loop.py tests/test_free_space_smoothing.py tests/test_calibration_validators.py tests/test_assemble_perception_frame.py tests/test_api_calibration.py tests/test_depth_loop.py -q`
- Confirm `_units_for_depth_kind` returns `"m"` only for `METRIC_CALIBRATED`
- Confirm `assert_free_space_units(METRIC_CALIBRATED, "ordinal")` raises
- Grep: no `apply_map` in `spatial/loop.py`; no DetectionLoop/FrameBus/ORT/`kind_for_mode`/YAML edits
</verification>

<success_criteria>
1. Free-space products on the wire use `units="m"` only when kind is `metric_calibrated` (FS-01).
2. Occupancy smoother resets on calibration apply and clear kind transitions (FS-03).
3. Optional `distance_m` is mean blob meters when calibrated; `nearness_*` stay 0..1.
4. Zero new deps; consume-only (no re-scale); no YAML/wizard/FSD scope creep.
</success_criteria>

<output>
Create `.planning/phases/16-free-space-meters/16-02-SUMMARY.md` when done
</output>
