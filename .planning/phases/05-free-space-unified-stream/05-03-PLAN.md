---
phase: 05-free-space-unified-stream
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/sentry_ai/api/routes_v1.py
  - src/sentry_ai/api/app.py
  - src/sentry_ai/api/routes_preview.py
  - src/sentry_ai/api/routes_detection.py
  - src/sentry_ai/capture/status.py
  - src/sentry_ai/cli.py
  - src/sentry_ai/ui/static/index.html
  - README.md
  - tests/test_api_v1.py
  - tests/test_api_perception_only.py
  - tests/test_api_preview.py
  - tests/test_cli_serve.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-05
  - SPACE-03
  - SPACE-04
  - UI-02
  - UI-06
user_setup: []
must_haves:
  truths:
    - "WS /v1/stream delivers JSON PerceptionFrame at ~10 Hz keep-latest using assemble_perception_frame"
    - "GET /v1/snapshot returns same assembler output as GET /api/snapshot (alias parity)"
    - "MJPEG draw order is depth blend → free-space → detection boxes from store products only"
    - "Live Preview footer shows free_space / obstacles / age and STALE/incomplete badge without safe-to-drive language"
    - "sentry serve always starts FreeSpaceLoop when PerceptionStore exists (no new ML extra)"
    - "API dumps never contain motor/velocity/path_plan/safe_to_drive fields (API-05)"
  artifacts:
    - path: "src/sentry_ai/api/routes_v1.py"
      provides: "GET /v1/snapshot + WS /v1/stream"
      exports: ["router"]
    - path: "src/sentry_ai/api/routes_preview.py"
      provides: "MJPEG free-space draw + status free_space_*"
    - path: "src/sentry_ai/cli.py"
      provides: "FreeSpaceLoop lifecycle in serve"
    - path: "src/sentry_ai/ui/static/index.html"
      provides: "Footer free-space metrics + STALE badge"
    - path: "tests/test_api_v1.py"
      provides: "API-01/API-02 stream+snapshot coverage"
    - path: "tests/test_api_perception_only.py"
      provides: "API-05 denylist on v1 envelopes"
    - path: "README.md"
      provides: "/v1 contract + stale consumer notes"
  key_links:
    - from: "src/sentry_ai/api/routes_v1.py"
      to: "assemble_perception_frame"
      via: "snapshot + stream send_json"
      pattern: "assemble_perception_frame"
    - from: "src/sentry_ai/api/routes_preview.py"
      to: "draw_free_space + snapshot_free_space"
      via: "MJPEG overlay order"
      pattern: "draw_free_space|snapshot_free_space"
    - from: "src/sentry_ai/cli.py"
      to: "FreeSpaceLoop"
      via: "serve start/stop order"
      pattern: "FreeSpaceLoop"
    - from: "src/sentry_ai/ui/static/index.html"
      to: "/api/status free_space fields"
      via: "footer poll"
      pattern: "obstacle_count|free_space|STALE"
---

<objective>
Ship versioned perception API (`GET /v1/snapshot`, `WS /v1/stream`), keep `/api/snapshot` alias parity, MJPEG free-space overlay + status/UI honesty (STALE), FreeSpaceLoop serve lifecycle, perception-only denylist tests, and README docs.

Purpose: Close the Phase 5 product thesis — robots consume unified `/v1` PerceptionFrame; UI matches API via single store (UI-06); free-space visible (SPACE-03/UI-02); stale visible (SPACE-04); no motor fields (API-05).

Output: Live `/v1` REST+WS; overlay parity; serve wires FreeSpaceLoop; docs + tests green without new ML packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-free-space-unified-stream/05-CONTEXT.md
@.planning/phases/05-free-space-unified-stream/05-RESEARCH.md
@.planning/phases/05-free-space-unified-stream/05-PATTERNS.md
@.planning/phases/05-free-space-unified-stream/05-UI-SPEC.md
@.planning/phases/05-free-space-unified-stream/05-VALIDATION.md
@.planning/phases/05-free-space-unified-stream/05-01-SUMMARY.md
@.planning/phases/05-free-space-unified-stream/05-02-SUMMARY.md
@.planning/phases/04-monocular-depth/04-02-SUMMARY.md
@.planning/phases/03-fixed-class-detection/03-02-SUMMARY.md

## Depends on 05-01 + 05-02

- FreeSpaceLoop, FreeSpaceProduct, draw_free_space
- assemble_perception_frame + expanded FreeSpacePayload
- /api/snapshot already calls assembler

## Locked decisions

| Decision | Value |
|----------|-------|
| REST | GET `/v1/snapshot` + keep `/api/snapshot` alias same assembler |
| WS | `/v1/stream` JSON PerceptionFrame ~10 Hz keep-latest; no per-client queue; honor shutdown_flag |
| Wire | Obstacles+bands on JSON; masks in-process for MJPEG only |
| Draw order | depth blend → free-space → boxes |
| Serve | FreeSpaceLoop always when store exists (CPU; no ImportError gate) |
| Start order | capture → det → depth → free_space; stop reverse |
| UI | Footer free_space/obstacles/age + STALE/incomplete; never safe-to-drive copy |
| Packages | None new |

## Interfaces

From 05-02:
- `assemble_perception_frame(store) -> PerceptionFrame | None`
- FreeSpacePayload wire shape

From 05-01:
- `draw_free_space`, `FreeSpaceLoop`, `store.snapshot_free_space()`

From Phase 4:
- `create_app` include_router pattern; `app.state.shutdown_flag`
- MJPEG `_mjpeg_generator` depth then boxes
- StatusSnapshot optional fields pattern
- cli serve DepthLoop lifecycle twin for FreeSpaceLoop (always-on variant)

## FastAPI WebSocket notes

- Use `fastapi.WebSocket` + `WebSocketDisconnect`
- `await websocket.accept()` then loop: assemble → `send_json(frame.model_dump())` → `asyncio.sleep(0.1)`
- Break on shutdown_flag; no Spatial Post inside handler
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /v1 snapshot + WebSocket stream + app wiring + API-05 tests</name>
  <files>src/sentry_ai/api/routes_v1.py, src/sentry_ai/api/app.py, src/sentry_ai/api/routes_detection.py, tests/test_api_v1.py, tests/test_api_perception_only.py</files>
  <behavior>
    - GET /v1/snapshot returns 200 PerceptionFrame when any product set; 404 when none; 503 without store
    - GET /api/snapshot body equals GET /v1/snapshot for same store state (alias parity)
    - WS /v1/stream accepts connection and yields at least one JSON message with schema_version, completeness, stats when products present
    - Stream uses assemble_perception_frame only (no free-space compute in route module)
    - model_dump keys of snapshot/stream messages disjoint FORBIDDEN motor/safety set
    - Handlers never import torch/ultralytics or call compute_free_space
  </behavior>
  <read_first>
    - src/sentry_ai/api/assemble.py
    - src/sentry_ai/api/routes_detection.py
    - src/sentry_ai/api/app.py
    - .planning/phases/05-free-space-unified-stream/05-RESEARCH.md (Pattern 5 WS, API-05 denylist)
    - .planning/phases/05-free-space-unified-stream/05-PATTERNS.md (routes_v1 section)
    - tests/test_api_detection.py (TestClient patterns)
  </read_first>
  <action>
    Create `routes_v1.py` with APIRouter:
    - GET `/v1/snapshot` → assemble_perception_frame → model_dump (API-02)
    - WS `/v1/stream` → accept, loop ~10 Hz keep-latest send_json, honor shutdown_flag and WebSocketDisconnect (API-01)
    - Optional GET `/v1/health` only if trivial; not required

    include_router(v1_router) in create_app. Keep `/api/snapshot` as alias (already assembler from 05-02; ensure docstring says alias of /v1).

    Tests with FastAPI TestClient + store seeding (det and/or depth and/or free_space). For WS use TestClient.websocket_connect("/v1/stream").

    tests/test_api_perception_only.py: dump FORBIDDEN_TOP_LEVEL isdisjoint keys on /v1/snapshot and one WS message; also nested free_space has no safe/go fields.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_v1.py tests/test_api_perception_only.py tests/test_api_detection.py -q</automated>
  </verify>
  <done>/v1 snapshot + stream live; alias parity; API-05 denylist green on wire envelopes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MJPEG free-space overlay + status telemetry + FreeSpaceLoop serve lifecycle</name>
  <files>src/sentry_ai/api/routes_preview.py, src/sentry_ai/capture/status.py, src/sentry_ai/cli.py, tests/test_api_preview.py, tests/test_cli_serve.py</files>
  <behavior>
    - MJPEG path calls draw_free_space from store free-space product between blend_depth and draw_detections when free product good
    - Never invents free-space from raw depth_map inside preview routes
    - /api/status includes free_space_fps, free_space_latency_ms, free_space_frame_id, obstacle_count when product present; free_space_error when error
    - StatusSnapshot optional free_space fields keep Phase 2–4 callers valid (defaults None)
    - cli serve constructs FreeSpaceLoop(store), starts after depth, stops before depth on shutdown; logs free-space enabled (CPU Spatial Post)
    - FreeSpaceLoop starts even when depth extra missing (degrades to no free product until depth appears)
  </behavior>
  <read_first>
    - src/sentry_ai/api/routes_preview.py
    - src/sentry_ai/capture/status.py
    - src/sentry_ai/cli.py (serve DepthLoop block)
    - src/sentry_ai/spatial/overlay.py
    - src/sentry_ai/spatial/loop.py
    - tests/test_api_preview.py
    - tests/test_cli_serve.py
    - .planning/phases/05-free-space-unified-stream/05-PATTERNS.md (preview + cli sections)
    - .planning/phases/05-free-space-unified-stream/05-UI-SPEC.md
  </read_first>
  <action>
    Preview MJPEG (SPACE-03 / UI-02 / UI-06): after blend_depth, if snapshot_free_space good → draw_free_space with free_mask/occupied_mask/obstacles; then draw_detections. Source-order tests assert draw_free_space imported/called and no compute_free_space in routes_preview.

    Status enrichment: mirror depth_* block with free_space_* + obstacle_count from store. Optionally surface free_space_age_ms / free_space_stale via assembler TTL helper if cheap — at minimum ages for UI can be computed in status from product t_capture vs time.time().

    StatusSnapshot: add optional free_space_latency_ms, free_space_fps, free_space_frame_id, obstacle_count, free_space_error (and stale/age if desired) with defaults None so extra=forbid models remain valid.

    cli serve: always `FreeSpaceLoop(store)` after store creation (no ML ImportError gate). Start order capture→det→depth→free_space; stop free_space→depth→det→capture. Echo "free-space: enabled (near-field bands Spatial Post)".

    Extend test_cli_serve to assert FreeSpaceLoop import/start/stop without requiring depth weights.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_preview.py tests/test_cli_serve.py -q</automated>
  </verify>
  <done>MJPEG free-space from store; status free_space metrics; serve always runs FreeSpaceLoop.</done>
</task>

<task type="auto">
  <name>Task 3: Live Preview UI footer/STALE + README /v1 docs</name>
  <files>src/sentry_ai/ui/static/index.html, README.md, tests/test_api_preview.py, tests/test_api_v1.py</files>
  <read_first>
    - src/sentry_ai/ui/static/index.html
    - README.md (existing API tables from Phase 3–4)
    - .planning/phases/05-free-space-unified-stream/05-UI-SPEC.md
    - .planning/phases/05-free-space-unified-stream/05-RESEARCH.md (stale contract, What NOT to Build language)
  </read_first>
  <action>
    Update Live Preview (UI-02 / SPACE-04 / UI-06):
    - Footer: free_space status, obstacles count, free_space age/latency alongside existing det/depth fields
    - Header/status area: yellow/red STALE or incomplete badge when free_space_stale / products_stale / missing completeness bits from status (or derived ages)
    - Forbidden copy: never "safe", "go", "clear to proceed", "safe to drive", "navigation cleared"
    - Keep depth "relative (not meters)" honesty

    README:
    - Document GET `/v1/snapshot`, WS `/v1/stream` (~10 Hz JSON keep-latest), `/api/snapshot` alias
    - Free-space method near_field_bands; ordinal units; consumers must honor stale/TTL — not a safety interlock
    - Perception-only boundary: no motor commands
    - Install remains core + optional detect/depth; free-space needs no new extra
    - Example robot client snippet optional (short)

    Add/extend automated string checks if practical (grep-style tests that index.html and README lack safe_to_drive / go_nogo phrases; status fields exist). Prefer tests/test_api_preview.py reading static HTML or a small language denylist test.

    Full suite + ruff before SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_v1.py tests/test_api_perception_only.py tests/test_api_preview.py tests/test_cli_serve.py tests/test_assemble_perception_frame.py tests/test_free_space_bands.py tests/test_free_space_loop.py -q</automated>
  </verify>
  <done>UI footer + STALE honesty; README /v1 docs; Phase 5 automated path green without ML weights.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Robot client ← WS/REST | Unauthenticated localhost perception stream; must not carry control |
| Browser ← MJPEG/status | Same store truth; UI must not invent free-space or safety claims |
| Network bind | Non-localhost still opt-in with existing warning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | free_space units on wire | mitigate | Payload units/depth_kind from assembler; honesty tests |
| T-05-02 | Spoofing | stale stream after stall | mitigate | ages + stale flags on every frame; UI STALE badge; docs consumers invalidate |
| T-05-03 | Elevation | motor commands on stream | mitigate | extra=forbid + test_api_perception_only denylist on /v1 snapshot+stream |
| T-05-04 | Information disclosure | LAN camera+occupancy | mitigate | Localhost default bind preserved; existing non-localhost warning |
| T-05-05 | Denial of service | WS backpressure | mitigate | Keep-latest; fixed 10 Hz sleep; no queue; no Spatial Post in WS task |
| T-05-06 | Repudiation/misuse | safe-to-drive UI | mitigate | UI-SPEC ban + string denylist tests on HTML/README |
| T-05-SC | Tampering | package installs | accept | No new packages |
</threat_model>

<verification>
uv run pytest tests/test_api_v1.py tests/test_api_perception_only.py tests/test_api_preview.py tests/test_cli_serve.py tests/test_assemble_perception_frame.py tests/test_free_space*.py tests/test_perception_store.py -q
uv run ruff check src tests
# Optional full: uv run pytest -q
</verification>

<success_criteria>
1. Robot can GET /v1/snapshot and WS /v1/stream for merged PerceptionFrame
2. Free-space overlay visible on MJPEG when FreeSpaceProduct present (same store as API)
3. Stale/incomplete visible in stats + UI; no safe-to-proceed language
4. FreeSpaceLoop always on in serve; no new ML deps
5. API-05 denylist holds on v1 envelopes
6. README documents /v1 contract and perception-only boundary
</success_criteria>

<output>
Create `.planning/phases/05-free-space-unified-stream/05-03-SUMMARY.md` when done
</output>
