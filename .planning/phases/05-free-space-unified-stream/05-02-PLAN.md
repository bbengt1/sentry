---
phase: 05-free-space-unified-stream
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/sentry_ai/schemas/perception.py
  - src/sentry_ai/schemas/__init__.py
  - src/sentry_ai/api/assemble.py
  - src/sentry_ai/api/routes_detection.py
  - tests/test_schemas_perception.py
  - tests/test_assemble_perception_frame.py
  - tests/test_api_detection.py
autonomous: true
requirements:
  - SPACE-02
  - SPACE-04
  - API-03
  - API-04
  - API-05
user_setup: []
must_haves:
  truths:
    - "FreeSpacePayload carries obstacles list, counts, bands, method, depth_kind, units=ordinal on relative — no distance_m"
    - "assemble_perception_frame is the single merge path for det + depth + free_space products"
    - "completeness.free_space is true only when FreeSpaceProduct present and error is None"
    - "stats include det/depth/free_space ages, stale flags vs TTL, stage latencies, fps/drops when available — no safe_to_drive language"
    - "GET /api/snapshot uses assemble_perception_frame (alias-ready for /v1); 404 only when all three products absent"
    - "PerceptionFrame.model_dump keys never include motor/velocity/path_plan/safe_to_drive/go_nogo"
  artifacts:
    - path: "src/sentry_ai/schemas/perception.py"
      provides: "Expanded FreeSpacePayload + ObstacleCue"
      exports: ["ObstacleCue", "FreeSpacePayload", "PerceptionFrame", "Completeness"]
    - path: "src/sentry_ai/api/assemble.py"
      provides: "Single PerceptionFrame merge + TTL/stale"
      exports: ["assemble_perception_frame", "TtlConfig", "DEFAULT_TTL_MS"]
    - path: "src/sentry_ai/api/routes_detection.py"
      provides: "/api/snapshot thin alias to assembler"
    - path: "tests/test_assemble_perception_frame.py"
      provides: "SPACE-04 / API-03 / API-04 merge coverage"
    - path: "tests/test_schemas_perception.py"
      provides: "FreeSpacePayload + API-05 denylist"
  key_links:
    - from: "src/sentry_ai/api/assemble.py"
      to: "PerceptionStore.snapshot / snapshot_depth / snapshot_free_space"
      via: "triple product read"
      pattern: "snapshot_free_space|snapshot_depth|snapshot\\("
    - from: "src/sentry_ai/api/assemble.py"
      to: "FreeSpacePayload"
      via: "obstacles+bands wire body without masks"
      pattern: "FreeSpacePayload|obstacles"
    - from: "src/sentry_ai/api/routes_detection.py"
      to: "assemble_perception_frame"
      via: "GET /api/snapshot"
      pattern: "assemble_perception_frame"
    - from: "src/sentry_ai/schemas/perception.py"
      to: "extra=forbid + no motor fields"
      via: "PerceptionFrame / FreeSpacePayload ConfigDict"
      pattern: "extra=.forbid."
---

<objective>
Expand FreeSpacePayload contracts, implement single assemble_perception_frame merge with completeness + TTL/stale ages/stats, and refactor GET /api/snapshot to call that assembler — no dual merge logic.

Purpose: Close SPACE-02 wire shape, SPACE-04 stale honesty, API-03 completeness, API-04 metadata, and API-05 perception-only schema so 05-03 can attach `/v1` REST/WS without reimplementing merge.

Output: `api/assemble.py`; expanded schemas; `/api/snapshot` uses assembler; pure unit tests with synthetic store products.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-free-space-unified-stream/05-CONTEXT.md
@.planning/phases/05-free-space-unified-stream/05-RESEARCH.md
@.planning/phases/05-free-space-unified-stream/05-PATTERNS.md
@.planning/phases/05-free-space-unified-stream/05-VALIDATION.md
@.planning/phases/05-free-space-unified-stream/05-01-SUMMARY.md
@.planning/phases/04-monocular-depth/04-02-SUMMARY.md

## Depends on 05-01

- FreeSpaceProduct + set_free_space / snapshot_free_space + free_space metrics
- FreeSpaceLoop produces products from synthetic depth (tests can also set_free_space directly)

## Locked decisions

| Decision | Value |
|----------|-------|
| Single assembler | One pure function for REST + future WS + parity (UI-06 foundation) |
| Wire free_space | Obstacles + bands + counts + method + depth_kind + units — NO full masks on JSON |
| Completeness | Presence + error is None (availability) |
| Stale | age_ms > TTL separate from completeness (freshness); defaults det 500 / depth 750 / free_space 750 ms |
| Identity | Latest t_capture among present products; free_space parent frame_id is depth's |
| Language | No safe_to_proceed / go_nogo / safe_to_drive fields or stats keys |
| Alias | Keep `/api/snapshot` working via assembler (canonical `/v1` in 05-03) |

## Interfaces from 05-01 / Phase 4

From store FreeSpaceProduct (05-01):
- obstacles, obstacle_count, bands, depth_kind, method, free_mask, occupied_mask, latency_ms, frame_id, t_capture, error

From current `routes_detection.api_snapshot` (extract, do not fork):
- Multi-product merge det+depth → extend to free_space
- DepthPayload metadata only (never depth_map)
- 404 when no products

From `schemas/perception.py` placeholder:
- FreeSpacePayload currently only obstacle_count optional — expand per RESEARCH Code Examples
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand FreeSpacePayload + ObstacleCue schemas</name>
  <files>src/sentry_ai/schemas/perception.py, src/sentry_ai/schemas/__init__.py, tests/test_schemas_perception.py</files>
  <behavior>
    - ObstacleCue requires bbox_xyxy, nearness_mean, nearness_max, area_px; band in near|mid|far; extra=forbid; no distance_m field
    - FreeSpacePayload: method near_field_bands, depth_kind, units ordinal|m default ordinal, obstacle_count, obstacles list, optional bands/width/height/roi_bottom_frac
    - PerceptionFrame.free_space accepts expanded payload; completeness.free_space still default false
    - API-05: PerceptionFrame and FreeSpacePayload reject extra motor/velocity/safe_to_drive fields; model_fields disjoint FORBIDDEN set including safe_to_drive, go_nogo, cmd_vel, twist, path_plan
    - Relative path convention: tests construct FreeSpacePayload with units=ordinal + DepthKind.RELATIVE
  </behavior>
  <read_first>
    - src/sentry_ai/schemas/perception.py
    - tests/test_schemas_perception.py
    - .planning/phases/05-free-space-unified-stream/05-RESEARCH.md (FreeSpacePayload expansion Code Example, API-05 denylist)
    - .planning/phases/05-free-space-unified-stream/05-PATTERNS.md (schema section)
  </read_first>
  <action>
    Expand FreeSpacePayload and add ObstacleCue exactly per RESEARCH target schema (user locked wire encoding: obstacle list + bands, not full masks). Keep Completeness.free_space. Keep PerceptionFrame.extra=forbid.

    Export ObstacleCue from schemas/__init__.py if other packages re-export public models.

    Extend tests: free_space payload round-trip, extra forbid, motor denylist expansion (add safe_to_drive, go_nogo, cmd_vel, twist to forbidden set), no distance_m on ObstacleCue.model_fields.
  </action>
  <verify>
    <automated>uv run pytest tests/test_schemas_perception.py -q</automated>
  </verify>
  <done>Expanded FreeSpacePayload/ObstacleCue green; API-05 schema denylist covers motor + safety language keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: assemble_perception_frame + TTL/stale/completeness/stats</name>
  <files>src/sentry_ai/api/assemble.py, tests/test_assemble_perception_frame.py</files>
  <behavior>
    - Returns None when det, depth, and free_space products all absent
    - completeness.detections/depth/free_space reflect presence and error is None
    - free_space payload includes obstacles/bands/counts; never includes free_mask or occupied_mask keys
    - When wall clock now is advanced past product t_capture by more than TTL, corresponding *_stale true and products_stale true while completeness may still be true
    - stats include free_space_latency_ms, free_space_frame_id, free_space_age_ms, det_age_ms, depth_age_ms, stage fps/drops from store metrics when provided
    - No stats keys named safe, go, nogo, safe_to_drive, clear_to_proceed
    - Primary identity = max t_capture among present products
  </behavior>
  <read_first>
    - src/sentry_ai/api/routes_detection.py (current multi-product merge body lines ~59–134)
    - src/sentry_ai/state/perception_store.py (snapshot APIs + metrics after 05-01)
    - .planning/phases/05-free-space-unified-stream/05-RESEARCH.md (Pattern 4 assembler, Stale/TTL contract, Pitfall 4/5)
    - .planning/phases/05-free-space-unified-stream/05-PATTERNS.md (assemble.py section)
  </read_first>
  <action>
    Create pure `assemble_perception_frame(store, *, now=None, ttl=None, bus_metrics=None) -> PerceptionFrame | None` (per D-assemble single path):

    1. Snapshot det, depth, free_space.
    2. If all None → return None (callers map to 404 / skip send).
    3. depth_good / free_good / det_present completeness rules.
    4. Build DepthPayload metadata only; FreeSpacePayload from product obstacles/bands (copy ObstacleCue fields) without masks.
    5. Ages: age_ms = (now - t_capture) * 1000 for each product present; compare to DEFAULT_TTL_MS {detections:500, depth:750, free_space:750}; set det_stale, depth_stale, free_space_stale, products_stale.
    6. Stats: latencies, frame_ids, depth min/max/mean if present, free_space_obstacle_count, store metrics free_space_fps / det_fps / depth_fps / drops when available. Optional bus_metrics for capture_fps / frames_dropped (API-04).
    7. TtlConfig dataclass or TypedDict for overrides; DEFAULT_TTL_MS module constant.

    Do not perform free-space compute or depth inference here. No motor fields.

    Unit tests construct PerceptionStore, set synthetic products with controlled t_capture, freeze `now`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_assemble_perception_frame.py tests/test_schemas_perception.py -q</automated>
  </verify>
  <done>Single assembler implements completeness, free_space wire payload, ages/TTL stale flags, and API-04 stats without dual truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor GET /api/snapshot to assembler</name>
  <files>src/sentry_ai/api/routes_detection.py, tests/test_api_detection.py</files>
  <behavior>
    - GET /api/snapshot returns PerceptionFrame JSON from assemble_perception_frame
    - 404 when assembler returns None; 503 without store
    - free_space completeness and payload appear when FreeSpaceProduct set
    - Existing multi-product det/depth snapshot tests still pass
    - Snapshot JSON has no free_mask / occupied_mask / depth_map bulk arrays
  </behavior>
  <read_first>
    - src/sentry_ai/api/routes_detection.py
    - tests/test_api_detection.py
    - tests/test_api_depth.py (multi-product snapshot expectations)
    - src/sentry_ai/api/assemble.py (Task 2 output)
  </read_first>
  <action>
    Replace inline merge in `api_snapshot` with call to `assemble_perception_frame(store)` (per D-alias keep /api/snapshot). Map None → HTTP 404 "no perception product yet". Keep detection config GET/PATCH handlers unchanged.

    Extend API tests: inject FreeSpaceProduct via store.set_free_space; assert free_space payload + completeness.free_space; assert no mask keys in JSON; assert existing det+depth cases still 200.

    Do not add /v1 routes here (05-03).
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_detection.py tests/test_assemble_perception_frame.py tests/test_schemas_perception.py -q</automated>
  </verify>
  <done>/api/snapshot is thin assembler client; free_space appears on snapshot when product present; no dual merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store products → PerceptionFrame JSON | Robot consumers trust completeness/stale semantics |
| Client-supplied extras on schema models | Must not inject control fields via extra= |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | FreeSpacePayload units | mitigate | units + depth_kind on payload; no distance_m field; schema tests |
| T-05-02 | Spoofing | stale free-space as live | mitigate | age_ms + per-product stale flags + products_stale; completeness ≠ freshness |
| T-05-03 | Elevation | motor/safety fields | mitigate | extra=forbid + expanded FORBIDDEN denylist tests (API-05) |
| T-05-04 | Information disclosure | bulk masks on wire | mitigate | assembler never attaches free_mask/occupied_mask/depth_map |
| T-05-05 | Denial of service | assemble in handler | accept | Pure snapshot merge is O(products); no inference — residual risk low |
| T-05-SC | Tampering | package installs | accept | No new packages |
</threat_model>

<verification>
uv run pytest tests/test_schemas_perception.py tests/test_assemble_perception_frame.py tests/test_api_detection.py -q
uv run ruff check src/sentry_ai/schemas src/sentry_ai/api/assemble.py src/sentry_ai/api/routes_detection.py
</verification>

<success_criteria>
1. Expanded FreeSpacePayload is the only free-space wire shape
2. assemble_perception_frame is sole merge implementation used by /api/snapshot
3. Stale flags independent of completeness; TTL defaults documented in code constants
4. Stats carry FPS/latency/drops/ages for free-space stage (API-04)
5. API-05 denylist tests pass on schema + dump keys
</success_criteria>

<output>
Create `.planning/phases/05-free-space-unified-stream/05-02-SUMMARY.md` when done
</output>
