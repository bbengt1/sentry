---
phase: 15-wizard-rest-live-preview-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - src/sentry_ai/ui/static/index.html
  - tests/test_api_preview.py
autonomous: true
requirements:
  - WIZ-03
  - OPS-01
user_setup: []
must_haves:
  truths:
    - "static wizard panel on existing index.html (no React)"
    - "shows sample count, residual/status, calibrated vs relative labels"
    - "Apply / Cancel / Clear call REST; Cancel does not wipe applied"
    - "JS never assigns metric_calibrated except from status depth_kind"
    - "Footer can show calibration_active / scale / method from status"
    - "Honesty copy: hobby monocular, not vehicle-grade; Cancel drops draft only"
    - "Banned strings absent: autonomous, safe_to_drive, go_nogo, motor, velocity"
    - "No YAML persist chrome, no free-space meter unit flip, no new deps"
  artifacts:
    - path: "src/sentry_ai/ui/static/index.html"
      provides: "Wizard panel + status wiring for OPS-01"
    - path: "tests/test_api_preview.py"
      provides: "HTML contract + honesty denylist"
  key_links:
    - from: "wizard buttons"
      to: "/api/depth/calibration/*"
      via: "fetch JSON"
      pattern: "api/depth/calibration"
    - from: "applyStatus"
      to: "depth_kind + calibration_active"
      via: "badge from status only"
      pattern: "calibration_active"
---

<objective>
Add a static Live Preview calibration wizard on existing index.html so makers can sample, preview a fit, and Apply/Cancel/Clear — without ever locally claiming metric_calibrated.

Purpose: WIZ-03 wizard feedback; OPS-01 UI (status/banner show active vs relative).

Output: index.html panel + HTML contract tests. Depends on 15-01 REST. Zero new packages. No YAML. No free-space meters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-wizard-rest-live-preview-ui/15-RESEARCH.md
@.planning/phases/15-wizard-rest-live-preview-ui/15-PATTERNS.md
@.planning/phases/15-wizard-rest-live-preview-ui/15-VALIDATION.md
@.planning/phases/15-wizard-rest-live-preview-ui/15-01-PLAN.md
@src/sentry_ai/ui/static/index.html
@src/sentry_ai/api/routes_calibration.py
@tests/test_api_preview.py

## Locked decisions (must honor)

| Decision | Value | Source |
|----------|-------|--------|
| Cancel vs Clear | Cancel = clear_draft REST; Clear = clear_applied REST | RESEARCH #1 |
| Live kind | Depth badge uses status depth_kind only | RESEARCH #6 #9 |
| Known distance | Primary known meters. Height optional FOV | RESEARCH #4 |
| Static UI | Extend existing index.html; no new frontend stack | RESEARCH #10 |
| Honesty | Hobby monocular; relative never meters | PITFALLS #5 #6 |
| Constraints | No persist UI this phase; no free-space meter chrome | RESEARCH #10 |

## Claude's discretion

| Item | Choice |
|------|--------|
| Panel id | #calibration-wizard control-row after open-vocab |
| Footer metric | #metric-calibration from status calibration fields |
| Click sample | Click #preview maps to naturalWidth/Height point_uv |
| Draft preview | #calib-draft labeled draft (not live) from compute JSON |
| 409 handling | Prompt to Clear applied first; do not auto-clear |
| 503 | Non-fatal note like OV unavailable |
| Persist button | Omit (Phase 17) |

## Out of scope for 15-02

- New REST routes (15-01)
- Persist / serve re-apply
- Free-space units=m
- New frontend stack, colormap legend rewrite
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Static wizard panel + honesty contracts (WIZ-03, OPS-01 UI)</name>
  <files>src/sentry_ai/ui/static/index.html, tests/test_api_preview.py</files>
  <read_first>
    - .planning/phases/15-wizard-rest-live-preview-ui/15-RESEARCH.md (locks 1,6,9,10; Pitfall 5)
    - .planning/phases/15-wizard-rest-live-preview-ui/15-PATTERNS.md (index.html section)
    - src/sentry_ai/ui/static/index.html (depth badge, OV control-row, applyStatus)
    - tests/test_api_preview.py (test_root_serves_live_preview_html, denylist)
  </read_first>
  <behavior>
    - #calibration-wizard exists with known-meters input, Sample, Compute, Apply, Cancel, Clear
    - Footer #metric-calibration (or equivalent) present
    - applyStatus uses calibration_active / sample_count / scale / method when present
    - Depth badge still driven only by data.depth_kind (relative not meters; estimated/calibrated + unit m)
    - Compute/draft numbers render in a draft-labeled element; do not assign metric_calibrated to #metric-depth-kind
    - Click on #preview can populate a pending UV used by Sample
    - Honesty note: hobby monocular, not vehicle-grade; Cancel drops draft only; Clear drops applied
    - GET / HTML includes api/depth/calibration and calibration-wizard
    - Denylist still holds (autonomous, safe_to_drive, go_nogo, motor, velocity)
    - Optional height field is labeled approximate FOV helper, not primary
  </behavior>
  <acceptance_criteria>
    - #calibration-wizard exists with known-meters input, Sample, Compute, Apply, Cancel, Clear
    - Footer #metric-calibration (or equivalent) present
    - applyStatus uses calibration_active / sample_count / scale / method when present
    - Depth badge still driven only by data.depth_kind
    - Draft numbers do not assign metric_calibrated to #metric-depth-kind
    - Click on #preview can populate a pending UV used by Sample
    - Honesty note: hobby monocular; Cancel drops draft only; Clear drops applied
    - GET / HTML includes api/depth/calibration and calibration-wizard
    - Denylist still holds (autonomous, safe_to_drive, go_nogo, motor, velocity)
    - Optional height field labeled approximate FOV helper, not primary
  </acceptance_criteria>
  <action>
    1. Add #calibration-wizard panel in index.html footer after open-vocab.
       Inputs: #calib-known-m, optional height with FOV note.
       Buttons: Sample, Compute, Apply, Cancel, Clear.
       Status: #calib-count, #calib-draft (draft not live), #calib-msg.
    2. Footer #metric-calibration filled from status calibration fields.
    3. Sample: POST /api/depth/calibration/sample with point_uv and known_meters. On 409, ask user to Clear first.
    4. Compute: POST /api/depth/calibration/compute; render in #calib-draft only.
    5. Apply/Cancel/Clear: POST apply / cancel / clear then pollStatus(). Cancel keeps applied.
    6. Click #preview: map to naturalWidth/naturalHeight for point_uv (not CSS box pixels).
    7. Do not set elDepthKind to metric_calibrated from wizard JS.
    8. Honesty sentence in .note: hobby monocular scale.
    9. Keep the existing static page; do not add a new UI framework.
    10. Extend tests/test_api_preview.py for wizard ids, routes, and denylist.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_preview.py tests/test_api_calibration.py -q</automated>
  </verify>
  <done>Static wizard shows count/residual/labels; badge stays honest to status; Cancel vs Clear copy is correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser JS to live labels | UI must not invent calibrated kind |
| Cancel button to applied state | Mis-labeled Cancel would look like Clear |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-11 | Spoofing | Local kind claim | mitigate | Badge only from status depth_kind |
| T-15-12 | Tampering | Cancel copy | mitigate | Button posts cancel; copy says draft only |
| T-15-13 | Tampering | CSS-pixel UV | mitigate | Map clicks via naturalWidth/Height |
| T-15-14 | Elevation | New UI stack | mitigate | Static HTML only |
| T-15-SC | Tampering | Package installs | accept | No package installs |
</threat_model>

<verification>
- uv run pytest tests/test_api_preview.py tests/test_api_calibration.py -q
- GET / HTML contains calibration-wizard and api/depth/calibration
- Denylist still green
</verification>

<success_criteria>
1. Wizard shows sample count, residual/status, calibrated vs relative (WIZ-03).
2. Live Preview shows whether calibration is active (OPS-01 UI).
3. UI never locally claims calibrated kind; Cancel vs Clear copy is correct.
</success_criteria>

<output>
Create `.planning/phases/15-wizard-rest-live-preview-ui/15-02-SUMMARY.md` when done
</output>
