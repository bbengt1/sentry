---
phase: 13-honesty-contracts-calibrationstate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sentry_ai/schemas/validators.py
  - src/sentry_ai/schemas/perception.py
  - src/sentry_ai/state/perception_store.py
  - tests/test_calibration_validators.py
  - tests/test_schemas_depth_kind.py
  - tests/test_perception_store_depth_honesty.py
  - tests/test_depth_mapping.py
autonomous: true
requirements:
  - CAL-04
  - CAL-05
user_setup: []
must_haves:
  truths:
    - "DepthPayload rejects relative + unit='m' and metric_calibrated + unit=None; accepts only honest pairs"
    - "metric_estimated depth requires unit='m' (never None) on wire and pure assert"
    - "FreeSpacePayload rejects units='m' unless depth_kind is metric_calibrated; relative and metric_estimated stay ordinal-only for meters"
    - "metric_calibrated + units='ordinal' remains allowed (Phase 16 owns real meter free-space path)"
    - "PerceptionStore.set_depth rejects dishonest kind/unit before any product write; store stays empty after reject"
    - "kind_for_mode never returns METRIC_CALIBRATED for any mode"
    - "promote_kind_unit pure helper returns (metric_calibrated, 'm') only when applied and valid; else base pair"
  artifacts:
    - path: "src/sentry_ai/schemas/validators.py"
      provides: "assert_depth_kind_unit, assert_free_space_units, promote_kind_unit; relative_depth_forbids_unit compatibility"
      exports: ["assert_depth_kind_unit", "assert_free_space_units", "promote_kind_unit", "relative_depth_forbids_unit"]
    - path: "src/sentry_ai/schemas/perception.py"
      provides: "DepthPayload + FreeSpacePayload model validators call shared asserts"
    - path: "src/sentry_ai/state/perception_store.py"
      provides: "set_depth honesty gate via assert_depth_kind_unit"
    - path: "tests/test_calibration_validators.py"
      provides: "Full kind↔unit + free-space units matrix + promote_kind_unit cases"
    - path: "tests/test_perception_store_depth_honesty.py"
      provides: "Store rejects relative+m and calibrated without m; accepts estimated+m"
    - path: "tests/test_schemas_depth_kind.py"
      provides: "Calibrated requires m; estimated requires m regression"
    - path: "tests/test_depth_mapping.py"
      provides: "kind_for_mode never calibrated guard"
  key_links:
    - from: "src/sentry_ai/schemas/perception.py DepthPayload"
      to: "assert_depth_kind_unit"
      via: "model_validator mode=after"
      pattern: "assert_depth_kind_unit"
    - from: "src/sentry_ai/schemas/perception.py FreeSpacePayload"
      to: "assert_free_space_units"
      via: "model_validator mode=after"
      pattern: "assert_free_space_units"
    - from: "src/sentry_ai/state/perception_store.py set_depth"
      to: "assert_depth_kind_unit"
      via: "call before DepthProduct construction"
      pattern: "assert_depth_kind_unit\\(kind, unit\\)"
    - from: "promote_kind_unit"
      to: "CalibrationState (plan 13-02)"
      via: "pure function re-exported / wrapped"
      pattern: "def promote_kind_unit"
---

<objective>
Close honesty contract holes so relative/uncalibrated depth cannot claim meters on wire or store, and `metric_calibrated` is only valid as the pair with `unit="m"`. Ship the pure `promote_kind_unit` gate for plan 13-02.

Purpose: CAL-04 pair enforcement on schemas + CAL-05 store/wire rejection foundation before CalibrationState exists.

Output: Extended `validators.py`, wire model validators, store gate, and green matrix tests. Zero new pip packages. No DepthLoop, wizard, free-space algorithm, or YAML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-honesty-contracts-calibrationstate/13-RESEARCH.md
@.planning/phases/13-honesty-contracts-calibrationstate/13-PATTERNS.md
@.planning/phases/13-honesty-contracts-calibrationstate/13-VALIDATION.md
@src/sentry_ai/schemas/validators.py
@src/sentry_ai/schemas/perception.py
@src/sentry_ai/schemas/enums.py
@src/sentry_ai/state/perception_store.py
@src/sentry_ai/models/depth/mapping.py
@tests/test_schemas_depth_kind.py
@tests/test_depth_mapping.py
@tests/test_perception_store.py
@tests/test_depth_kind_honesty.py
@tests/test_free_space_bands.py

## Locked decisions (ROADMAP + RESEARCH — no CONTEXT.md)

| Decision | Value | Source |
|----------|-------|--------|
| Kind↔unit matrix | relative→unit None; metric_estimated→`"m"`; metric_calibrated→`"m"` | RESEARCH A2 + Implementation Prescription |
| Free-space meters | `units="m"` only when `depth_kind=metric_calibrated` | RESEARCH + CAL-05 |
| Free-space calibrated+ordinal | **Allowed** until Phase 16 | RESEARCH Open Q4 |
| Promotion pure API | `promote_kind_unit(base_kind, base_unit, *, applied, valid)` | RESEARCH Pattern 1 |
| Store gate | `assert_depth_kind_unit` at start of `set_depth` | RESEARCH Pattern / Pitfall 3 |
| Mode mapping | `kind_for_mode` **unchanged** — never returns calibrated | RESEARCH Pattern 3 |
| Packages | Zero new pip deps | ROADMAP lock |
| Out of phase | DepthLoop, wizard REST/UI, free-space metric path, YAML persist, AppState injection | Phase boundary |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| `relative_depth_forbids_unit` | Keep exported; implement body as call into `assert_depth_kind_unit` (or subset) so existing imports keep working |
| DepthPayload validator name | Rename to `kind_unit_honesty` calling `assert_depth_kind_unit` |
| Estimated + unit=None | Reject (A2) — mapping always emits `"m"` |
| Test file split | New `test_calibration_validators.py` + `test_perception_store_depth_honesty.py`; extend existing depth_kind + mapping tests |

## Interfaces (shipped — use these)

From `src/sentry_ai/schemas/enums.py`:
```python
class DepthKind(StrEnum):
    RELATIVE = "relative"
    METRIC_ESTIMATED = "metric_estimated"
    METRIC_CALIBRATED = "metric_calibrated"
```

From `src/sentry_ai/schemas/validators.py` today:
```python
def relative_depth_forbids_unit(kind: DepthKind, unit: str | None) -> None: ...
```

From `src/sentry_ai/schemas/perception.py`:
```python
class DepthPayload(BaseModel):
    kind: DepthKind
    unit: Literal["m"] | None = None
    # model_validator calls relative_depth_forbids_unit today

class FreeSpacePayload(BaseModel):
    depth_kind: DepthKind
    units: Literal["ordinal", "m"] = "ordinal"
    # comment-only honesty today — no model_validator
```

From `src/sentry_ai/state/perception_store.py`:
```python
def set_depth(
    self, frame_id, camera_id, t_capture, depth_map,
    kind: DepthKind, unit: Literal["m"] | None, latency_ms, ...
) -> None: ...
```

From `src/sentry_ai/models/depth/mapping.py` (do not change production):
```python
def kind_for_mode(mode: str) -> tuple[DepthKind, str | None]:
    # relative → (RELATIVE, None); metric_* → (METRIC_ESTIMATED, "m")
```

## Target pure APIs (implement exactly)

```python
def assert_depth_kind_unit(kind: DepthKind, unit: str | None) -> None: ...
def assert_free_space_units(depth_kind: DepthKind, units: str) -> None: ...
def promote_kind_unit(
    base_kind: DepthKind,
    base_unit: str | None,
    *,
    applied: bool,
    valid: bool,
) -> tuple[DepthKind, str | None]: ...
```

## Out of scope for 13-01

- `CalibrationState` / `schemas/calibration.py` (13-02)
- DepthLoop / worker / free_space algorithm / assemble `_units_for_depth_kind` meter flip
- REST routes / index.html / YAML
- Residual RMS thresholds / scale clamps beyond structural honesty
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kind/unit + free-space honesty validators and wire models (CAL-04, CAL-05)</name>
  <files>src/sentry_ai/schemas/validators.py, src/sentry_ai/schemas/perception.py, tests/test_calibration_validators.py, tests/test_schemas_depth_kind.py</files>
  <read_first>
    - .planning/phases/13-honesty-contracts-calibrationstate/13-RESEARCH.md (Code Examples matrix; Pitfalls 1–2; Implementation Prescription items 1–3)
    - .planning/phases/13-honesty-contracts-calibrationstate/13-PATTERNS.md (validators.py + perception.py sections)
    - src/sentry_ai/schemas/validators.py
    - src/sentry_ai/schemas/perception.py
    - src/sentry_ai/schemas/enums.py
    - tests/test_schemas_depth_kind.py
    - tests/test_schemas_perception.py (FreeSpacePayload construction style ~186–206)
  </read_first>
  <behavior>
    - assert_depth_kind_unit(RELATIVE, None) ok; (RELATIVE, "m") raises ValueError matching relative/meters/unit
    - assert_depth_kind_unit(METRIC_ESTIMATED, "m") ok; (METRIC_ESTIMATED, None) raises
    - assert_depth_kind_unit(METRIC_CALIBRATED, "m") ok; (METRIC_CALIBRATED, None) raises with CAL-04 / meters messaging
    - DepthPayload mirrors pure assert via ValidationError on same dishonest pairs
    - assert_free_space_units(RELATIVE, "ordinal") ok; (RELATIVE, "m") raises
    - assert_free_space_units(METRIC_ESTIMATED, "m") raises; (METRIC_ESTIMATED, "ordinal") ok
    - assert_free_space_units(METRIC_CALIBRATED, "m") ok; (METRIC_CALIBRATED, "ordinal") ok
    - promote_kind_unit(base, base_unit, applied=False, valid=True) returns (base, base_unit)
    - promote_kind_unit(..., applied=True, valid=False) returns base pair
    - promote_kind_unit(..., applied=True, valid=True) returns (METRIC_CALIBRATED, "m") regardless of base
    - relative_depth_forbids_unit remains importable and rejects relative+m
    - Existing test_depth_payload_relative_unit_m_rejected and calibrated+m_ok stay green
  </behavior>
  <action>
    1. Extend `src/sentry_ai/schemas/validators.py`:
       - Add `assert_depth_kind_unit(kind, unit)` implementing the full matrix per RESEARCH (relative forbids any unit; estimated requires `"m"`; calibrated requires `"m"`; unknown kind raises).
       - Add `assert_free_space_units(depth_kind, units)` — `units=="m"` only if `depth_kind == METRIC_CALIBRATED`; allow only `"ordinal"` or `"m"`.
       - Add pure `promote_kind_unit(base_kind, base_unit, *, applied, valid)` — if applied and valid return `(DepthKind.METRIC_CALIBRATED, "m")` else return `(base_kind, base_unit)`. No I/O, no state.
       - Keep `relative_depth_forbids_unit` exported; implement via `assert_depth_kind_unit` or equivalent so FOUND-03 callers still work.
       - Module remains pure (enums + stdlib only; no FastAPI/numpy).

    2. Update `src/sentry_ai/schemas/perception.py`:
       - Import `assert_depth_kind_unit` and `assert_free_space_units`.
       - Replace DepthPayload validator body to call `assert_depth_kind_unit(self.kind, self.unit)` (rename method to `kind_unit_honesty` if desired).
       - Add FreeSpacePayload `@model_validator(mode="after")` calling `assert_free_space_units(self.depth_kind, self.units)`.
       - Update module docstring: relative forbids meters; calibrated requires meters; free-space meters only when calibrated.
       - Do not add motor/safety fields; keep `extra=forbid`.

    3. Create `tests/test_calibration_validators.py` covering pure helpers + FreeSpacePayload matrix + promote_kind_unit truth table (all three base kinds × applied/valid combos for promote).

    4. Extend `tests/test_schemas_depth_kind.py`:
       - `test_depth_payload_metric_calibrated_unit_none_rejected`
       - `test_depth_payload_metric_estimated_unit_none_rejected`
       - Keep existing relative + calibrated+m cases.

    5. Do not edit PerceptionStore, DepthLoop, assemble, mapping production code, or calibration state in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/test_calibration_validators.py tests/test_schemas_depth_kind.py tests/test_schemas_perception.py -q</automated>
  </verify>
  <done>Full kind↔unit and free-space units matrix enforced on pure helpers and wire models; promote_kind_unit pure gate green; existing relative honesty tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PerceptionStore depth honesty gate + never-calibrated mode guard (CAL-05)</name>
  <files>src/sentry_ai/state/perception_store.py, tests/test_perception_store_depth_honesty.py, tests/test_depth_mapping.py</files>
  <read_first>
    - .planning/phases/13-honesty-contracts-calibrationstate/13-RESEARCH.md (Store gate; Pitfall 3; Validation Architecture store rows)
    - .planning/phases/13-honesty-contracts-calibrationstate/13-PATTERNS.md (perception_store.py + test_perception_store_depth_honesty + mapping verify-only)
    - src/sentry_ai/state/perception_store.py (set_depth ~221–281)
    - src/sentry_ai/schemas/validators.py (Task 1 asserts)
    - tests/test_perception_store.py (set_depth happy path ~180–200)
    - tests/test_depth_mapping.py
    - tests/test_depth_kind_honesty.py (must stay green — honest fixtures only)
  </read_first>
  <behavior>
    - set_depth(kind=RELATIVE, unit="m") raises ValueError; snapshot_depth() remains None (no partial write)
    - set_depth(kind=METRIC_CALIBRATED, unit=None) raises ValueError; store empty
    - set_depth(kind=METRIC_ESTIMATED, unit="m") succeeds; snapshot kind/unit match
    - set_depth(kind=RELATIVE, unit=None) still succeeds (regression)
    - set_depth(kind=METRIC_CALIBRATED, unit="m") succeeds (store may hold calibrated pair once promote path exists)
    - Honesty assert runs before DepthProduct construction and outside the stats best-effort try/except
    - kind_for_mode for relative / metric_indoor / metric_outdoor never returns METRIC_CALIBRATED
    - Production mapping.py is unchanged (test-only guard)
  </behavior>
  <action>
    1. In `PerceptionStore.set_depth`:
       - Import `assert_depth_kind_unit` from `sentry_ai.schemas.validators`.
       - Call `assert_depth_kind_unit(kind, unit)` at the start of the method (before stats / DepthProduct / lock write).
       - Do not wrap the assert in the stats `except Exception` block.
       - Do not change FreeSpaceProduct or other store APIs.

    2. Create `tests/test_perception_store_depth_honesty.py`:
       - Reject relative+`m`, calibrated+None; accept estimated+`m`, relative+None, calibrated+`m`.
       - After each reject, assert `snapshot_depth()` is None (or prior product unchanged if testing overwrite — prefer empty store for pure reject cases).
       - Use small float32 numpy maps like existing store tests.

    3. Extend `tests/test_depth_mapping.py` with parametrized `test_kind_for_mode_never_calibrated` for modes relative, metric_indoor, metric_outdoor. Do not edit `mapping.py` production code.

    4. Regression: run depth honesty + free_space_bands tests — do not plant dishonest store fixtures; leave assemble free-space units path alone.
  </action>
  <verify>
    <automated>uv run pytest tests/test_perception_store_depth_honesty.py tests/test_perception_store.py tests/test_depth_mapping.py tests/test_depth_kind_honesty.py tests/test_free_space_bands.py tests/test_calibration_validators.py tests/test_schemas_depth_kind.py -q</automated>
  </verify>
  <done>Store rejects dishonest kind/unit pairs without partial write; mode mapping never yields calibrated; honesty regression suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → DepthPayload / FreeSpacePayload | Untrusted wire construction can invent kind/unit lies |
| Caller → PerceptionStore.set_depth | In-process products can bypass assemble if assert missing |
| Mode string → kind_for_mode | Config mode must never be treated as calibrated truth |
| Pure promote_kind_unit flags | applied/valid booleans are the sole promotion authority (state wired in 13-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Spoofing | DepthPayload kind/unit | mitigate | `assert_depth_kind_unit` in model_validator; reject relative+m and calibrated without m |
| T-13-02 | Spoofing | FreeSpacePayload units | mitigate | `assert_free_space_units` — meters only when metric_calibrated |
| T-13-03 | Tampering | PerceptionStore.set_depth | mitigate | Same assert before product write; no silent store of unit lies |
| T-13-04 | Spoofing | kind_for_mode | mitigate | Production unchanged; test guard asserts never METRIC_CALIBRATED |
| T-13-05 | Spoofing | promote_kind_unit | mitigate | Pure function promotes only when applied and valid both True |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `uv run pytest tests/test_calibration_validators.py tests/test_schemas_depth_kind.py tests/test_perception_store_depth_honesty.py tests/test_depth_mapping.py tests/test_depth_kind_honesty.py tests/test_free_space_bands.py tests/test_schemas_perception.py -q`
- Grep: no edits to `models/depth/loop.py`, `spatial/free_space.py` algorithm, routes, or `index.html`
- Confirm `promote_kind_unit` is importable for plan 13-02
</verification>

<success_criteria>
1. Relative and uncalibrated depth cannot claim meters on DepthPayload, FreeSpacePayload, or PerceptionStore.
2. metric_calibrated is only valid with unit="m" on wire and store.
3. promote_kind_unit is pure, tested, and ready for CalibrationState.
4. kind_for_mode remains non-calibrated; zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/13-honesty-contracts-calibrationstate/13-01-SUMMARY.md` when done
</output>
