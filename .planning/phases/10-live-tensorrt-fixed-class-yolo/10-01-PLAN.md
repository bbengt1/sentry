---
phase: 10-live-tensorrt-fixed-class-yolo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sentry_ai/models/detection/factory.py
  - tests/test_detection_factory.py
  - tests/test_trt_parity.py
  - tests/test_backend_honesty_status.py
autonomous: true
requirements:
  - TRT-01
  - TRT-04
user_setup: []
must_haves:
  truths:
    - "preferred_backend=tensorrt + allowlisted existing .engine + system tensorrt importable → backend_live=tensorrt, backend_reason=None, worker weights end with .engine (TRT-01)"
    - "Missing .engine → backend_live=torch with reason trt_artifact_missing; never claims live TRT"
    - "Missing system tensorrt dep → backend_live=torch with reason trt_dep_missing; never claims live TRT"
    - "path_rejected on explicit/env path → backend_live=torch with reason path_rejected"
    - "backend_live=tensorrt only when worker is constructed with resolved .engine weights (not .pt under a TRT label)"
    - "trt_loader_not_implemented is retired as the default TRT outcome"
    - "Factory live TRT path + injected FakeModel process() yields Detection with class_name, confidence, bbox_xyxy, source=fixed (TRT-04)"
    - "set_conf on TRT-path worker is reflected in the next predict conf kwarg when supported"
    - "Empty predict results yield [] (not None) on TRT factory path"
    - "Parity/factory suite uses mocks/fixtures only — no Jetson, no system TensorRT, no real YOLO(\"*.engine\") load, no weight download in default CI"
    - "Status honesty pass-through works when backend_live=tensorrt (requested=live, reason=None)"
    - "No tensorrt pip extra; no module-level import tensorrt; DetectionLoop/FrameBus/PerceptionStore//v1 frozen"
  artifacts:
    - path: "src/sentry_ai/models/detection/factory.py"
      provides: "Live TRT branch + soft fallbacks + _tensorrt_available probe"
      exports: ["WorkerBuild", "build_detection_worker", "normalize_backend"]
      contains: "backend_live=\"tensorrt\""
    - path: "tests/test_detection_factory.py"
      provides: "Live TRT success + soft-fallback reason matrix + import hygiene"
    - path: "tests/test_trt_parity.py"
      provides: "TRT-04 process + conf + source=fixed parity without hardware"
      contains: "backend_live"
    - path: "tests/test_backend_honesty_status.py"
      provides: "Live TRT triple + updated soft-stub reason fixtures"
  key_links:
    - from: "build_detection_worker tensorrt branch"
      to: "resolve_detector_artifact / _try_resolve_artifact"
      via: "consume Path for live weights; reject → path_rejected; None → trt_artifact_missing"
      pattern: "_try_resolve_artifact|resolve_detector_artifact"
    - from: "build_detection_worker live TRT success"
      to: "YoloDetectionWorker(weights=str(engine_path))"
      via: "Ultralytics-native YOLO(\"*.engine\") path; model= inject for tests"
      pattern: "YoloDetectionWorker\\(|weights=str\\("
    - from: "live TRT claim"
      to: "_tensorrt_available / find_spec"
      via: "probe before backend_live=tensorrt; no top-level import"
      pattern: "find_spec\\([\\\"']tensorrt|trt_dep_missing"
    - from: "tests/test_trt_parity.py"
      to: "build_detection_worker live TRT branch"
      via: "resolve/dep monkeypatch + model=FakeModel; never real engine load"
      pattern: "build_detection_worker|backend_live.*tensorrt"
    - from: "YoloDetectionWorker.process"
      to: "results_to_detections"
      via: "same predict → Results → Detection path as torch/ORT"
      pattern: "results_to_detections"
---

<objective>
Flip the Phase 8 TensorRT soft-stub into a live Ultralytics-native TensorRT path at the serve factory (system/JetPack `tensorrt` only — no pip extra), prove Detection contract parity with mocks, and extend status honesty for live TRT.

Purpose: Close TRT-01 (live when preferred + allowlisted `.engine` + system tensorrt) and TRT-04 (same Detection contract + runtime conf when supported). On-device engine lifecycle docs and Jetson packaging honesty are 10-02.

Output: Live TRT factory branch with honest soft-fallback reason codes, green factory + parity + status tests without Jetson/system TensorRT/real `.engine` loads or weight downloads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-RESEARCH.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-PATTERNS.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-VALIDATION.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-01-SUMMARY.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-02-SUMMARY.md
@src/sentry_ai/models/detection/factory.py
@src/sentry_ai/models/detection/yolo_worker.py
@src/sentry_ai/models/detection/mapping.py
@src/sentry_ai/config/artifact_paths.py
@src/sentry_ai/config/profile_runtime.py
@src/sentry_ai/schemas/perception.py
@tests/test_detection_factory.py
@tests/test_ort_parity.py
@tests/test_backend_honesty_status.py
@tests/test_detection_mapping.py
@tests/test_detection_worker.py

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| Loader path | Ultralytics-native `YOLO("*.engine")` via `YoloDetectionWorker(weights=engine_path)` — **no** custom TRT Runtime/bindings/NMS decoder | RESEARCH / STATE |
| Live claim | `backend_live=tensorrt` **only** when resolved `.engine` path + `find_spec("tensorrt")` True + worker constructed with that engine path | RESEARCH |
| Soft fallback | `trt_artifact_missing` \| `trt_dep_missing` \| `path_rejected` → torch worker; never silent TRT claim | VALIDATION reason-code contract |
| Packaging | **No** `tensorrt` pip extra; system/JetPack only (TRT-03 owned by 10-02 but 10-01 must not add extra) | TRT-03 / STATE |
| Worker class | **Reuse** `YoloDetectionWorker` — no thin TRT wrapper class | RESEARCH |
| Dep probe | `importlib.util.find_spec("tensorrt")` only; **no** module-level `import tensorrt`; **never** call Ultralytics `check_tensorrt()` (auto-pip risk) | RESEARCH pitfalls |
| Artifact resolve | Existing `resolve_detector_artifact` + env `SENTRY_DETECTOR_ENGINE` / `SENTRY_ARTIFACT_ROOT` — do not reimplement | Phase 8 BACK-04 |
| Factory sole author | Only factory writes `backend_*`; status/banner/routes pass-through unchanged | Phase 8 |
| Spine freeze | DetectionLoop / FrameBus / PerceptionStore / `/v1` unmodified | EDGE-RT-01 |
| CI hardware | No Jetson / no system TensorRT / no real `.engine` load / no weight download; inject `model=` + mock resolve/dep | RESEARCH / VALIDATION |
| Retire Phase 8 default | `trt_loader_not_implemented` is **not** the default TRT outcome after this plan | VALIDATION |
| ORT branch | Leave Phase 9 live ORT path untouched | RESEARCH |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| CUDA probe at factory | **No** extra CUDA check — mirror ORT `find_spec` only; document CUDA requirement in 10-02 |
| Parity module | New `tests/test_trt_parity.py` (mirror `test_ort_parity.py`) |
| Live path setup | Monkeypatch `_try_resolve_artifact` → allowlisted Path + `_tensorrt_available` → True + `model=` FakeModel |
| Real engine load | **Not** in default suite; do not add required hardware integration tests |
| Honesty status | Extend with live TRT triple + rewrite soft-stub fixtures from `trt_loader_not_implemented` → `trt_artifact_missing` (or any VALIDATION soft reason) |
| Profile for live tests | Prefer `jetson` profile runtime (`preferred_backend=tensorrt`); live path must not hard-code profile name in factory |
| yolo_worker.py / mapping.py | **No production change expected** (format-agnostic load + shared mapper) |

## Interfaces (shipped — use these)

From `src/sentry_ai/models/detection/factory.py` (Phase 9 — rewrite TRT branch only):
```python
@dataclass(frozen=True)
class WorkerBuild:
    worker: Any
    backend_requested: str
    backend_live: str
    backend_reason: str | None = None

def build_detection_worker(
    rt: ProfileRuntime, *, conf: float = 0.25, model: Any | None = None
) -> WorkerBuild: ...

def _try_resolve_artifact(rt, *, preferred: str) -> tuple[Path | None, str | None]: ...
# returns (path, None) | (None, "path_rejected") | (None, None) when missing
# preferred=="tensorrt" already reads SENTRY_DETECTOR_ENGINE
```

Current TRT soft-stub to replace (factory.py ~181–189):
```python
if requested == "tensorrt":
    _path, reject = _try_resolve_artifact(rt, preferred="tensorrt")
    reason = reject or "trt_loader_not_implemented"
    return WorkerBuild(
        worker=_torch_worker(rt, conf=conf, model=model),
        backend_requested="tensorrt",
        backend_live="torch",
        backend_reason=reason,
    )
```

From `src/sentry_ai/models/detection/yolo_worker.py`:
```python
class YoloDetectionWorker:
    def __init__(self, weights=..., conf=0.25, device=None, model=None) -> None: ...
    def process(self, frame) -> list[Detection]: ...
    def get_conf(self) -> float: ...
    def set_conf(self, conf: float) -> None: ...
    # YOLO(self._weights) — .engine suffix selects Ultralytics TensorRTBackend when system TRT installed
```

From `src/sentry_ai/config/artifact_paths.py`:
- `resolve_detector_artifact(...)` — allowlisted existing `.engine` for preferred tensorrt; env `SENTRY_DETECTOR_ENGINE`
- Stems: `yolo26n|s|m`; no reimplementation of path logic in factory

From `src/sentry_ai/config/profile_runtime.py`:
- `device_for_backend("tensorrt", "0")` → `"cuda:0"`
- jetson profile: `preferred_backend: tensorrt`

From `src/sentry_ai/schemas/perception.py`:
```python
class Detection(BaseModel):
    class_name: str
    confidence: float
    bbox_xyxy: tuple[float, float, float, float] | list[float]
    source: Literal["fixed", "open_vocab"] = "fixed"
```

## Reason-code contract (assertable — VALIDATION)

| Condition | backend_requested | backend_live | backend_reason |
|-----------|-------------------|--------------|----------------|
| preferred TRT + valid `.engine` + tensorrt importable | tensorrt | **tensorrt** | None |
| preferred TRT + no artifact | tensorrt | torch | `trt_artifact_missing` |
| preferred TRT + no system tensorrt | tensorrt | torch | `trt_dep_missing` |
| preferred TRT + path_rejected | tensorrt | torch | `path_rejected` |

**Retired:** `trt_loader_not_implemented` as default TRT outcome.

## Package legitimacy

| Package | Disposition | Notes |
|---------|-------------|-------|
| *(none new)* | No install step | Phase 10 adds no pip deps |
| `tensorrt` / `tensorrt-cu*` | **OMITTED** from project extras | Locked TRT-03; system/JetPack only |

## Out of scope for 10-01

- On-device engine lifecycle docs + Jetson packaging keyword honesty (10-02 / TRT-02 / TRT-03)
- Sticky thrash-free fallback overhaul (Phase 11 / BACK-03)
- Dual-model VRAM first-class claims (Phase 11)
- Custom TRT session + decoder
- Depth / YOLOE TRT
- Real `YOLO("*.engine")` as phase merge gate
- Editing DetectionLoop / FrameBus / PerceptionStore / `/v1` / routes honesty recompute
- Adding `tensorrt` to `pyproject.toml` extras
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live factory TRT branch + honesty matrix tests (TRT-01)</name>
  <files>src/sentry_ai/models/detection/factory.py, tests/test_detection_factory.py</files>
  <read_first>
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-RESEARCH.md (Pattern 1 live branch, reason codes, pitfalls 1/3/6/7)
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-PATTERNS.md (factory recommended live-TRT branch shape)
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-VALIDATION.md (reason-code contract table)
    - src/sentry_ai/models/detection/factory.py (ORT live branch lines 144–179; TRT soft-stub 181–189)
    - src/sentry_ai/models/detection/yolo_worker.py (constructor + model= inject)
    - src/sentry_ai/config/artifact_paths.py (resolve order; do not reimplement)
    - tests/test_detection_factory.py (ORT live matrix + test_jetson_tensorrt_soft_stub)
  </read_first>
  <behavior>
    - preferred tensorrt + resolve Path + _tensorrt_available True → backend_live=tensorrt, backend_reason=None, worker is YoloDetectionWorker with _weights ending .engine (not rt.detector_weights .pt)
    - preferred tensorrt + resolve None (no reject) → live=torch, reason=trt_artifact_missing, worker still torch weights path
    - preferred tensorrt + path_rejected → live=torch, reason=path_rejected
    - preferred tensorrt + path present + available False → live=torch, reason=trt_dep_missing
    - Never set backend_live=tensorrt while worker._weights ends with .pt
    - trt_loader_not_implemented must not remain the default jetson-without-fixture outcome
    - desktop-gpu / torch / cpu preferred paths unchanged
    - Phase 9 ORT live branch behavior unchanged
    - factory module source still has no `import tensorrt` / `from tensorrt` / `import onnxruntime` / `from onnxruntime` (find_spec string "tensorrt" is allowed)
    - model= still forwarded on live TRT construction so tests never download or load real engines
    - Default jetson without fixture artifact still soft-falls (not live TRT)
  </behavior>
  <action>
    1. Update `factory.py` module docstring: Phase 10 live TRT when allowlisted `.engine` + system tensorrt importable; soft-fall otherwise; still no module-level ORT/TRT imports; ORT remains Phase 9 live path.

    2. Add private helper `_tensorrt_available() -> bool` using `importlib.util.find_spec("tensorrt") is not None` only (mirror `_onnxruntime_available`). Keep probe inside helper called from the TRT branch. Do **not** hard-import tensorrt. Do **not** call Ultralytics `check_tensorrt()`.

    3. Replace the tensorrt soft-stub block in `build_detection_worker` with the locked condition chain per RESEARCH/PATTERNS (order matters — mirror ORT 1:1):
       - `_try_resolve_artifact(rt, preferred="tensorrt")` → if `reject`: return torch soft-stub with `backend_reason=reject` (`path_rejected`)
       - if `path is None`: return torch soft-stub with `backend_reason="trt_artifact_missing"`
       - if not `_tensorrt_available()`: return torch soft-stub with `backend_reason="trt_dep_missing"`
       - else LIVE: construct `YoloDetectionWorker(weights=str(path), conf=conf, device=rt.device, model=model)` and return `WorkerBuild(..., backend_requested="tensorrt", backend_live="tensorrt", backend_reason=None)`
       - Do **not** use `_torch_worker` / `rt.detector_weights` on the live success path
       - Do **not** pre-build a torch worker and then only flip the live string
       - Do **not** hard-code profile name `jetson` in the factory branch

    4. Update `_try_resolve_artifact` docstring: Phase 10 consumes the path for live TRT (alongside Phase 9 ORT).

    5. Leave torch / cpu / onnxruntime / unsupported branches behaviorally unchanged.

    6. Rewrite `tests/test_detection_factory.py` TRT coverage (mirror ORT matrix patterns already in that file):
       - Replace `test_jetson_tensorrt_soft_stub` to expect `trt_artifact_missing` when no artifact (default jetson, no fixture file) — **not** `trt_loader_not_implemented`
       - Keep `test_backend_live_not_ort_or_trt_without_fixtures`: without live fixtures, backend_live still not ort/trt; separate success case **must** allow live=tensorrt
       - Add live TRT success test: monkeypatch `_try_resolve_artifact` to return `(tmp_path / "yolo26n.engine", None)` (touch fake bytes); monkeypatch `_tensorrt_available` → True; `build_detection_worker(jetson rt, model=FakeModel())`; assert live=tensorrt, reason is None, `str(worker._weights).endswith(".engine")`, not `.pt`
       - Add `test_trt_soft_fallback_dep_missing`: artifact present (or resolve monkeypatch), available=False → live=torch, reason=trt_dep_missing
       - Add path_rejected coverage for TRT (resolve returns reject or env outside allowlist) → live=torch, reason=path_rejected
       - Keep desktop torch live, ORT live fixtures, duck-type conf, factory import hygiene tests
       - Never call real Ultralytics YOLO load; always inject FakeModel on factory calls
       - Grep gate after rewrite: factory source must not contain `trt_loader_not_implemented` as the implemented-branch default reason string used on soft-fall paths (retire it)

    7. Do not edit DetectionLoop, cli serve wiring (already uses factory), mapping, yolo_worker, pyproject extras, or docs (docs are 10-02).
  </action>
  <acceptance_criteria>
    - factory.py defines `_tensorrt_available` using only `importlib.util.find_spec("tensorrt")`
    - factory.py TRT branch returns `backend_live="tensorrt"` only after path + dep success with `YoloDetectionWorker(weights=str(path), ...)`
    - Soft reasons exactly: `trt_artifact_missing`, `trt_dep_missing`, `path_rejected` (reject passthrough)
    - `rg -n 'trt_loader_not_implemented' src/sentry_ai/models/detection/factory.py` is empty
    - `rg -n 'import tensorrt|from tensorrt' src/sentry_ai/models/detection/factory.py` is empty
    - `rg -n 'check_tensorrt' src/sentry_ai` is empty (Sentry never calls it)
    - tests/test_detection_factory.py covers live TRT success + three soft-fall cases + import hygiene
    - No edits to loop.py, frame_bus, perception_store, routes_v1, pyproject.toml extras
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_detection_factory.py tests/test_artifact_paths.py -q</automated>
  </verify>
  <done>Factory claims live TRT only on path+dep success with .engine worker weights; soft-fallback reason codes match VALIDATION contract; no top-level TRT imports; ORT path unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TRT process parity + conf golden tests (TRT-04)</name>
  <files>tests/test_trt_parity.py</files>
  <read_first>
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-RESEARCH.md (Pattern 2 parity; Detection contract examples)
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-PATTERNS.md (parity section)
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-VALIDATION.md (TRT-04 rows + conf caveat)
    - src/sentry_ai/models/detection/factory.py (post-Task-1 live TRT branch)
    - tests/test_ort_parity.py (exact structure to mirror — FakeModel, _live_*_build, contract tests)
    - tests/test_detection_worker.py (FakeModel / conf / process asserts)
    - tests/test_detection_mapping.py (box golden fields)
    - tests/test_detection_factory.py (live TRT fixture patterns from Task 1)
  </read_first>
  <behavior>
    - Factory TRT live path (jetson or ProfileRuntime preferred=tensorrt) + FakeModel with one person box → process returns list[Detection] with class_name=="person", confidence≈expected, bbox_xyxy==expected, source=="fixed"
    - After set_conf(0.5), next process passes conf≈0.5 into FakeModel.predict kwargs
    - FakeModel returning [] → process returns [] (not None)
    - Assert build.backend_live=="tensorrt" and worker._weights endswith ".engine" for the parity setup (guards against testing torch soft-stub by mistake)
    - No import of real tensorrt required; no YOLO("*.engine"); no network/weight download
    - No Jetson markers required to pass
  </behavior>
  <action>
    1. Create `tests/test_trt_parity.py` by mirroring `tests/test_ort_parity.py` structure with TRT-specific names:
       - Local `FakeModel` + `_FakeBoxes` (copy structure from ORT parity / detection worker tests)
       - Helper `_live_trt_build(monkeypatch, tmp_path, model, conf=0.25)` that:
         - builds jetson `ProfileRuntime` via `load_config(profile="jetson")` + `profile_runtime` (or minimal ProfileRuntime with preferred_backend=tensorrt)
         - monkeypatches factory resolve to return an allowlisted `tmp_path / "yolo26n.engine"` path (touch empty/fake bytes)
         - monkeypatches `_tensorrt_available` → True
         - calls `build_detection_worker(rt, conf=conf, model=model)`
         - asserts live=tensorrt before returning build (fail loud if Task 1 regression)

    2. Tests to include:
       - `test_trt_process_detection_contract` — known box → Detection fields + `source == "fixed"`
       - `test_trt_set_conf_applies_on_next_process` — set_conf then process; assert predict conf kwarg
       - `test_trt_empty_predict_returns_empty_list` — FakeModel returns [] → `[]`
       - Optional tight honesty guard: if `backend_live == "tensorrt"` then weights suffix `.engine` (and inverse: `.pt` weights never paired with live trt in this module’s setups)

    3. Use `image_frame_factory` from conftest for frames (same as ORT/worker tests).

    4. Do **not** add a default-suite test that constructs real `ultralytics.YOLO` on an engine file. If an opt-in real-TRT test is added, it must skip without system tensorrt/CUDA/engine — prefer omit entirely this plan.

    5. Do not modify production code unless a genuine Task-1 gap blocks parity (then fix minimally in factory only and note in SUMMARY — prefer report-back over scope creep).
  </action>
  <acceptance_criteria>
    - tests/test_trt_parity.py exists and is collected by pytest
    - Contract test asserts class_name, confidence, bbox_xyxy, source=="fixed" on live TRT factory path
    - set_conf test asserts next predict conf kwarg
    - Empty predict returns []
    - Live setup asserts backend_live=="tensorrt" and weights endswith ".engine" before process asserts
    - File does not call real YOLO("*.engine") or require tensorrt package import
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_trt_parity.py tests/test_detection_mapping.py tests/test_detection_worker.py -q</automated>
  </verify>
  <done>Parity suite proves Detection wire contract + conf on factory TRT live path with mocks only; mapping/worker baselines still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Status honesty fixture for live TRT + phase-01 suite gate</name>
  <files>tests/test_backend_honesty_status.py</files>
  <read_first>
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-VALIDATION.md (status pass-through + reason-code contract)
    - tests/test_backend_honesty_status.py (existing TRT soft-stub fixtures using trt_loader_not_implemented + live ORT triple)
    - src/sentry_ai/api (status snapshot construction — only as needed for fixture style)
  </read_first>
  <behavior>
    - When create_app / status path is given backend_requested=tensorrt, backend_live=tensorrt, backend_reason=None, /api/status (or StatusSnapshot builder under test) round-trips those fields without recomputing live from preferred
    - Soft-stub honesty cases still green but use retired-reason-safe soft codes (e.g. trt_artifact_missing) instead of trt_loader_not_implemented as the only TRT soft fixture
    - Existing live ORT honesty cases remain green
    - No production route changes required if Phase 8 pass-through already generic
  </behavior>
  <action>
    1. Extend `tests/test_backend_honesty_status.py`:
       - Add live TRT triple fixture/case: `backend_requested="tensorrt"`, `backend_live="tensorrt"`, `backend_reason=None` — assert JSON/status fields match (same accessors existing tests use for live ORT)
       - Update existing TRT soft-stub fixtures that hard-require `trt_loader_not_implemented` to use a VALIDATION soft reason such as `trt_artifact_missing` (or keep as illustrative pass-through of any reason string — but do not require the retired code as the only valid soft reason)
       - Do not re-derive live from preferred in the test double

    2. If the honesty test module already parametrizes backend triples, add the live TRT triple to the matrix instead of a wholly separate style.

    3. Run the TRT-focused suite as the plan gate:
       - factory matrix, trt parity, mapping, worker, backend honesty
       - Confirm with `rg` that spine freeze files were not modified this plan (`loop.py`, frame bus, perception store, routes_v1)
       - Confirm factory has no `trt_loader_not_implemented` and no top-level tensorrt import

    4. Do not edit production status routes unless a bug blocks pass-through of live=tensorrt (unexpected after Phase 8 — only fix if proven).
  </action>
  <acceptance_criteria>
    - Live TRT status triple round-trips requested=tensorrt, live=tensorrt, reason=None
    - Soft TRT honesty fixtures no longer require trt_loader_not_implemented as sole soft reason
    - Live ORT honesty cases still pass
    - Spine freeze files unmodified this plan
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_trt_parity.py tests/test_detection_factory.py tests/test_backend_honesty_status.py tests/test_detection_mapping.py -q</automated>
  </verify>
  <done>Live TRT status triple round-trips; factory+parity+honesty suite green without Jetson/system TensorRT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env/config → artifact resolver | Untrusted `SENTRY_DETECTOR_ENGINE` / `SENTRY_ARTIFACT_ROOT` paths |
| Factory → worker weights | Live TRT must only load allowlisted resolved `.engine` paths |
| Operator status trust | Claiming `backend_live=tensorrt` while running `.pt` is spoofing |
| Test doubles → factory | Mocks must not invent production live claims without path+dep coupling asserts |
| Package install | No new pip packages; system TRT is out-of-band on device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing | `build_detection_worker` live label | mitigate | Set `backend_live=tensorrt` only on path+dep+engine weights path; unit assert `_weights.endswith(".engine")` when live=trt |
| T-10-02 | Tampering | `SENTRY_DETECTOR_ENGINE` path | mitigate | Existing `resolve_detector_artifact` allowlist + `path_rejected` soft-fall; keep artifact path tests green; no path traversal |
| T-10-03 | Denial of service | Missing TRT dep / artifact | mitigate | Soft torch fallback with stable reason; serve still starts |
| T-10-04 | Tampering | Module-level TRT import / Ultralytics auto-pip | mitigate | `find_spec` probe only; never call `check_tensorrt()`; source inspect forbids `import tensorrt` / `from tensorrt` |
| T-10-05 | Elevation of privilege | Arbitrary model path / weight download | mitigate | Stem/suffix allowlist only; no URL fetch; no invent path; no arbitrary weight download |
| T-10-06 | Spoofing | Parity tests accidentally soft-stub | mitigate | Assert `backend_live=="tensorrt"` + `.engine` weights before process asserts |
| T-10-07 | Denial of service | Real truncated .engine load in CI | mitigate | Always inject FakeModel; forbid real YOLO engine load in default suite |
| T-10-08 | Spoofing | Status recomputes live from preferred | mitigate | Honesty fixture asserts pass-through of live=tensorrt without recompute |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | **No new packages** this plan; do not add `tensorrt` extra (TRT-03); keep `test_no_tensorrt_optional_extra` green in 10-02 |
</threat_model>

<verification>
- `uv run pytest tests/test_detection_factory.py tests/test_trt_parity.py tests/test_backend_honesty_status.py tests/test_detection_mapping.py tests/test_detection_worker.py tests/test_artifact_paths.py -q` green
- `uv run ruff check src/sentry_ai/models/detection/factory.py tests/test_detection_factory.py tests/test_trt_parity.py tests/test_backend_honesty_status.py` green (if ruff available)
- `rg -n 'backend_live=.tensorrt.|backend_live=\"tensorrt\"' src/sentry_ai/models/detection/factory.py` matches live success return only
- `rg -n 'trt_artifact_missing|trt_dep_missing' src/sentry_ai/models/detection/factory.py` matches
- `rg -n 'trt_loader_not_implemented' src/sentry_ai/models/detection/factory.py` is empty
- `rg -n 'import tensorrt|from tensorrt|import onnxruntime|from onnxruntime' src/sentry_ai/models/detection/factory.py` is empty
- `rg -n 'check_tensorrt' src/sentry_ai` is empty
- No edits to `models/detection/loop.py`, `bus/frame_bus.py`, `state/perception_store.py`, `api/routes_v1.py`
- Default suite never requires Jetson, system TensorRT, or real `.engine` load
</verification>

<success_criteria>
1. jetson (or preferred=tensorrt) + valid allowlisted `.engine` + dep probe True → `backend_live=tensorrt` and worker weights are that `.engine` path.
2. Missing artifact / missing dep / path_rejected → torch soft-fallback with exact reason codes; never live TRT.
3. TRT factory live path process produces Detection fields identical in shape to torch/ORT path golden (class, conf, bbox, source=fixed).
4. Runtime conf adjustment works on TRT-path worker (predict conf kwarg).
5. Status honesty surfaces live=tensorrt when factory authored it.
6. Parity proven with mocks only — no Jetson / system TensorRT / real engine load required.
7. Perception spine freeze intact; factory remains sole `backend_live` author; no tensorrt pip extra added.
</success_criteria>

<output>
Create `.planning/phases/10-live-tensorrt-fixed-class-yolo/10-01-SUMMARY.md` when done
</output>
