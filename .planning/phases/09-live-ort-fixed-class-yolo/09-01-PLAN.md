---
phase: 09-live-ort-fixed-class-yolo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sentry_ai/models/detection/factory.py
  - pyproject.toml
  - docs/export/yolo26-onnx-tensorrt.md
  - docs/export/README.md
  - docs/architecture.md
  - docs/configuration.md
  - src/sentry_ai/config/profiles/cpu-fallback.yaml
  - tests/test_detection_factory.py
  - tests/test_export_docs.py
  - tests/test_pyproject_onnx_extra.py
autonomous: true
requirements:
  - ORT-01
  - ORT-03
user_setup: []
must_haves:
  truths:
    - "preferred_backend=onnxruntime + allowlisted existing .onnx + onnxruntime importable → backend_live=onnxruntime, backend_reason=None, worker weights end with .onnx (ORT-01)"
    - "Missing .onnx → backend_live=torch with reason ort_artifact_missing; never claims live ORT"
    - "Missing onnxruntime dep → backend_live=torch with reason ort_dep_missing; never claims live ORT"
    - "path_rejected on explicit/env path → backend_live=torch with reason path_rejected"
    - "backend_live=onnxruntime only when worker is constructed with resolved .onnx weights (not .pt under an ORT label)"
    - "TRT branch remains Phase 8 soft-stub (trt_loader_not_implemented); no tensorrt pip extra"
    - "Optional onnx extra pins onnxruntime>=1.20,<1.29; CI does not require GPU ORT / Jetson (ORT-03)"
    - "Export/docs no longer claim fixed-class live is always PyTorch-only; document live ORT conditions + uv sync --extra onnx"
    - "Factory remains sole author of backend_live; no module-level import onnxruntime/tensorrt; DetectionLoop/bus/store//v1 frozen"
  artifacts:
    - path: "src/sentry_ai/models/detection/factory.py"
      provides: "Live ORT branch + soft fallbacks + _onnxruntime_available probe"
      exports: ["WorkerBuild", "build_detection_worker", "normalize_backend"]
      contains: "backend_live=\"onnxruntime\""
    - path: "pyproject.toml"
      provides: "optional onnx extra"
      contains: "onnxruntime>=1.20,<1.29"
    - path: "tests/test_detection_factory.py"
      provides: "Live ORT success + soft-fallback reason matrix + import hygiene"
    - path: "tests/test_pyproject_onnx_extra.py"
      provides: "Static pin assertion for onnx extra"
    - path: "docs/export/yolo26-onnx-tensorrt.md"
      provides: "Live ORT conditions + install path honesty"
    - path: "tests/test_export_docs.py"
      provides: "Keyword asserts for live ORT + onnx extra"
  key_links:
    - from: "build_detection_worker onnxruntime branch"
      to: "resolve_detector_artifact / _try_resolve_artifact"
      via: "consume Path for live weights; reject → path_rejected; None → ort_artifact_missing"
      pattern: "_try_resolve_artifact|resolve_detector_artifact"
    - from: "build_detection_worker live ORT success"
      to: "YoloDetectionWorker(weights=str(onnx_path))"
      via: "Ultralytics-native YOLO(\"*.onnx\") path; model= inject for tests"
      pattern: "YoloDetectionWorker\\(|weights=str\\("
    - from: "live ORT claim"
      to: "_onnxruntime_available / find_spec"
      via: "probe before backend_live=onnxruntime; no top-level import"
      pattern: "find_spec\\([\\\"']onnxruntime|ort_dep_missing"
    - from: "pyproject onnx extra"
      to: "docs/export install guidance"
      via: "uv sync --extra detect --extra onnx"
      pattern: "--extra onnx|onnxruntime"
---

<objective>
Flip the Phase 8 ORT soft-stub into a live Ultralytics-native ONNX Runtime path at the serve factory, add the optional `onnx` packaging extra, and update operator docs so makers can install and run fixed-class YOLO via ORT when artifact + dep conditions are met.

Purpose: Close ORT-01 (live when preferred + `.onnx` + dep) and ORT-03 (documented optional extra; CI free of GPU ORT). Detection contract parity (ORT-02/04) is proven in 09-02 on top of this factory branch.

Output: Live ORT factory branch with honest soft-fallback reason codes, `onnx` extra pin, revised export/architecture/configuration honesty, green factory + docs + pin tests without Jetson/GPU ORT or weight downloads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-CONTEXT.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-RESEARCH.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-PATTERNS.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-VALIDATION.md
@.planning/phases/08-backend-selection-honesty/08-01-SUMMARY.md
@src/sentry_ai/models/detection/factory.py
@src/sentry_ai/models/detection/yolo_worker.py
@src/sentry_ai/config/artifact_paths.py
@src/sentry_ai/config/profile_runtime.py
@src/sentry_ai/config/profiles/cpu-fallback.yaml
@pyproject.toml
@docs/export/yolo26-onnx-tensorrt.md
@docs/export/README.md
@docs/architecture.md
@docs/configuration.md
@tests/test_detection_factory.py
@tests/test_export_docs.py
@tests/test_artifact_paths.py

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| Loader path | Ultralytics-native `YOLO("*.onnx")` via `YoloDetectionWorker(weights=onnx_path)` — **no** custom `InferenceSession` / hand-written YOLO26 decoder | CONTEXT |
| Live claim | `backend_live=onnxruntime` **only** when resolved `.onnx` path + onnxruntime importable + worker constructed with that onnx path | CONTEXT / RESEARCH |
| Soft fallback | `ort_artifact_missing` \| `ort_dep_missing` \| `path_rejected` → torch worker; never silent ORT claim | CONTEXT + user lock |
| Packaging | Optional `onnx` extra: `onnxruntime>=1.20,<1.29` (prefer 1.28.x within range); **no** `tensorrt` extra; **no** `onnxruntime-gpu` in extra | CONTEXT |
| Factory sole author | Only factory writes `backend_*`; status/banner/routes pass-through unchanged | Phase 8 |
| Spine freeze | DetectionLoop / FrameBus / PerceptionStore / `/v1` unmodified | CONTEXT / EDGE-RT-01 |
| Artifact resolve | Existing `resolve_detector_artifact` + `SENTRY_DETECTOR_ONNX` / `SENTRY_ARTIFACT_ROOT` | CONTEXT |
| CI hardware | No Jetson / no GPU ORT required; default tests inject `model=` and mock dep probe | ORT-03 / ORT-04 |
| Retire Phase 8 default | `ort_loader_not_implemented` is **not** the default ORT outcome after this plan | RESEARCH / VALIDATION |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| Worker class | **Reuse** `YoloDetectionWorker` with `weights=str(onnx_path)` — no thin ORT wrapper class |
| Device | Pass `rt.device` (cpu-fallback already forces `"cpu"` via `device_for_backend`) |
| Dep probe | `importlib.util.find_spec("onnxruntime")` helper `_onnxruntime_available()` — soft-fall `ort_dep_missing` when False |
| Missing artifact reason | `ort_artifact_missing` when resolve returns `None` (not `ort_loader_not_implemented`) |
| `model=` injection | Still forwarded on live ORT branch so unit tests never call real `YOLO("*.onnx")` |
| yolo_worker.py | **No production change required** this plan (suffix dispatch already format-agnostic) |
| Docs scope | Export docs + architecture/configuration honesty paragraphs + `cpu-fallback.yaml` comment; TRT remains non-live |

## Interfaces (shipped — use these)

From `src/sentry_ai/models/detection/factory.py` (Phase 8 — rewrite ORT branch only):
```python
@dataclass(frozen=True)
class WorkerBuild:
    worker: Any
    backend_requested: str
    backend_live: str
    backend_reason: str | None = None

def build_detection_worker(
    rt: ProfileRuntime, *, conf: float = 0.25, model: Any | None = None
) -> WorkerBuild: ...

def _try_resolve_artifact(rt, *, preferred: str) -> tuple[Path | None, str | None]: ...
# returns (path, None) | (None, "path_rejected") | (None, None) when missing
```

From `src/sentry_ai/models/detection/yolo_worker.py`:
```python
class YoloDetectionWorker:
    def __init__(self, weights=..., conf=0.25, device=None, model=None) -> None: ...
    def process(self, frame) -> list[Detection]: ...
    def get_conf(self) -> float: ...
    def set_conf(self, conf: float) -> None: ...
    # YOLO(self._weights) — .onnx suffix selects Ultralytics ONNXBackend when ORT installed
```

From `src/sentry_ai/config/artifact_paths.py`:
- `resolve_detector_artifact(...)` — allowlisted existing `.onnx` / `.engine`; env `SENTRY_DETECTOR_ONNX`
- Stems: `yolo26n|s|m`; no reimplementation of path logic in factory

From `src/sentry_ai/config/profile_runtime.py`:
- `device_for_backend("onnxruntime", ...)` → `"cpu"`
- cpu-fallback profile: `preferred_backend: onnxruntime`

## Package legitimacy

| Package | Disposition | Notes |
|---------|-------------|-------|
| `onnxruntime` | Approved (registry-verified) | Microsoft ORT; pin `>=1.20,<1.29`; RESEARCH Package Legitimacy Audit — not [SLOP]/[SUS] |
| `onnxruntime-gpu` | Not installed | Docs-only mention allowed |
| `tensorrt*` | Omitted | Locked |

## Out of scope for 09-01

- Detection parity / golden process tests (09-02 / ORT-02 / ORT-04)
- Live TensorRT (Phase 10)
- Sticky thrash-free fallback overhaul (Phase 11)
- Custom ORT session + YOLO26 decoder
- Depth / YOLOE ORT
- Eager construct-time corrupt-`.onnx` validation (stay lazy load like torch)
- Editing DetectionLoop / FrameBus / PerceptionStore / `/v1` / routes honesty recompute
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live factory ORT branch + honesty matrix tests (ORT-01)</name>
  <files>src/sentry_ai/models/detection/factory.py, tests/test_detection_factory.py</files>
  <read_first>
    - .planning/phases/09-live-ort-fixed-class-yolo/09-RESEARCH.md (Pattern 1 live branch, reason codes, pitfalls 1–4/6)
    - .planning/phases/09-live-ort-fixed-class-yolo/09-PATTERNS.md (factory recommended live-branch shape)
    - .planning/phases/09-live-ort-fixed-class-yolo/09-VALIDATION.md (reason-code contract table)
    - src/sentry_ai/models/detection/factory.py (Phase 8 soft-stub lines 131–139)
    - src/sentry_ai/models/detection/yolo_worker.py (constructor + model= inject)
    - src/sentry_ai/config/artifact_paths.py (resolve order; do not reimplement)
    - tests/test_detection_factory.py (current matrix + never-ORT assert)
    - tests/test_artifact_paths.py (tmp_path allowlisted .onnx fixture style)
  </read_first>
  <behavior>
    - preferred onnxruntime + resolve Path + _onnxruntime_available True → backend_live=onnxruntime, backend_reason=None, worker is YoloDetectionWorker with _weights ending .onnx (not rt.detector_weights .pt)
    - preferred onnxruntime + resolve None (no reject) → live=torch, reason=ort_artifact_missing, worker still torch weights path
    - preferred onnxruntime + path_rejected → live=torch, reason=path_rejected
    - preferred onnxruntime + path present + available False → live=torch, reason=ort_dep_missing
    - Never set backend_live=onnxruntime while worker._weights ends with .pt
    - preferred tensorrt still soft-stub: live=torch, reason=trt_loader_not_implemented (or path_rejected if reject)
    - desktop-gpu / torch / cpu preferred paths unchanged
    - factory module source still has no `import onnxruntime` / `from onnxruntime` / `import tensorrt` / `from tensorrt` (find_spec string "onnxruntime" is allowed)
    - model= still forwarded on live ORT construction so tests never download or load real graphs
    - Default cpu-fallback without fixture artifact still soft-falls (not live ORT)
  </behavior>
  <action>
    1. Update `factory.py` module docstring: Phase 9 live ORT when artifact+dep; soft-fall otherwise; still no module-level ORT/TRT imports; TRT remains soft-stub.

    2. Add private helper `_onnxruntime_available() -> bool` using `importlib.util.find_spec("onnxruntime") is not None` only (no hard import that loads the package into factory import graph as a dependency of the module). Keep probe inside helper called from the ORT branch.

    3. Replace the onnxruntime soft-stub block in `build_detection_worker` with the locked condition chain per CONTEXT/RESEARCH (order matters):
       - `_try_resolve_artifact(rt, preferred="onnxruntime")` → if `reject`: return torch soft-stub with `backend_reason=reject` (`path_rejected`)
       - if `path is None`: return torch soft-stub with `backend_reason="ort_artifact_missing"`
       - if not `_onnxruntime_available()`: return torch soft-stub with `backend_reason="ort_dep_missing"`
       - else LIVE: construct `YoloDetectionWorker(weights=str(path), conf=conf, device=rt.device, model=model)` and return `WorkerBuild(..., backend_requested="onnxruntime", backend_live="onnxruntime", backend_reason=None)`
       - Do **not** use `_torch_worker` / `rt.detector_weights` on the live success path
       - Do **not** pre-build a torch worker and then only flip the live string

    4. Update `_try_resolve_artifact` docstring: Phase 9 consumes the path for live ORT (no longer “future loaders only”).

    5. Leave tensorrt / torch / cpu / unsupported branches behaviorally unchanged (still soft-stub TRT).

    6. Rewrite `tests/test_detection_factory.py`:
       - Replace `test_cpu_fallback_ort_soft_stub` to expect `ort_artifact_missing` when no artifact (default profile, no fixture file) — **not** `ort_loader_not_implemented`
       - Remove or narrow `test_backend_live_never_ort_or_trt`: without live fixtures, backend_live still not ort/trt; separate success case **must** allow live=onnxruntime
       - Add live ORT success test: place allowlisted `yolo26n.onnx` under tmp weights/cwd or monkeypatch `_try_resolve_artifact` to return `(path, None)`; monkeypatch `_onnxruntime_available` → True; `build_detection_worker(cpu-fallback rt, model=FakeModel())`; assert live=onnxruntime, reason is None, `str(worker._weights).endswith(".onnx")`
       - Add `test_ort_soft_fallback_dep_missing`: artifact present (or resolve monkeypatch), available=False → live=torch, reason=ort_dep_missing
       - Keep path_rejected coverage (env outside allowlist or resolve raise path) → live=torch, reason=path_rejected
       - Keep jetson TRT soft-stub, desktop torch live, duck-type conf, factory import hygiene tests
       - Never call real Ultralytics YOLO load; always inject FakeModel on factory calls

    7. Do not edit DetectionLoop, cli serve wiring (already uses factory), mapping, or yolo_worker unless a compile issue forces a zero-behavior import tweak (prefer zero change).
  </action>
  <verify>
    <automated>uv run pytest tests/test_detection_factory.py tests/test_artifact_paths.py -q</automated>
  </verify>
  <done>Factory claims live ORT only on path+dep success with .onnx worker weights; soft-fallback reason codes match VALIDATION contract; TRT still soft-stub; no top-level ORT/TRT imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Optional onnx extra pin (ORT-03 packaging)</name>
  <files>pyproject.toml, tests/test_pyproject_onnx_extra.py</files>
  <read_first>
    - .planning/phases/09-live-ort-fixed-class-yolo/09-RESEARCH.md (Package Legitimacy Audit + pin block)
    - .planning/phases/09-live-ort-fixed-class-yolo/09-PATTERNS.md (extras analog)
    - pyproject.toml `[project.optional-dependencies]` detect/depth blocks
  </read_first>
  <behavior>
    - pyproject.toml has optional-dependencies key `onnx` containing a pin matching onnxruntime>=1.20,<1.29
    - No tensorrt package appears under optional-dependencies
    - No onnxruntime-gpu pin in the onnx extra
    - Comment block documents Phase 9 live ORT + install one-liner including detect + onnx
  </behavior>
  <action>
    1. Add to `pyproject.toml` under `[project.optional-dependencies]`, matching detect/depth comment style:

       - Comment: Live ONNX Runtime path for fixed-class YOLO (Phase 9); CPU ORT enough for CI/makers; GPU ORT is optional system install — not required in CI
       - Install line: `uv sync --extra dev --extra detect --extra onnx`
       - `onnx = ["onnxruntime>=1.20,<1.29"]`

    2. Do **not** add `tensorrt`, `onnxruntime-gpu`, or change required dependencies / detect extra.

    3. Create `tests/test_pyproject_onnx_extra.py` that reads `pyproject.toml` text (or tomllib) and asserts:
       - `onnx` extra exists
       - pin string contains `onnxruntime>=1.20` and upper bound `<1.29` (or equivalent single pin string)
       - `tensorrt` not present as an optional extra name
       - onnx extra body does not include `onnxruntime-gpu`

    4. Do not run `uv lock` / `uv sync --extra onnx` as a required gate in this task (soft-fallback works without the package; CI remains mock-based). If lockfile update is needed for project policy, keep it optional and non-blocking for unit tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pyproject_onnx_extra.py -q</automated>
  </verify>
  <done>Optional onnx extra documented and pin-tested; no tensorrt/gpu-ort extra added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Docs honesty for live ORT + export keyword tests (ORT-03)</name>
  <files>docs/export/yolo26-onnx-tensorrt.md, docs/export/README.md, docs/architecture.md, docs/configuration.md, src/sentry_ai/config/profiles/cpu-fallback.yaml, tests/test_export_docs.py</files>
  <read_first>
    - .planning/phases/09-live-ort-fixed-class-yolo/09-RESEARCH.md (pitfall 5 docs still export-only)
    - .planning/phases/09-live-ort-fixed-class-yolo/09-PATTERNS.md (export docs update pattern + keyword list)
    - docs/export/yolo26-onnx-tensorrt.md (lines 1–5, 87–94)
    - docs/export/README.md (lines 1–18, 42–51)
    - docs/architecture.md (live inference / preferred_backend honesty)
    - docs/configuration.md (profile table + live path notes)
    - src/sentry_ai/config/profiles/cpu-fallback.yaml
    - tests/test_export_docs.py
  </read_first>
  <behavior>
    - Export docs state live fixed-class ORT when preferred_backend=onnxruntime + valid .onnx + onnx extra installed
    - Docs mention install path with `--extra onnx` (and detect)
    - Soft-fallback honesty: missing artifact/deps → torch + reason (not silent ORT claim)
    - TRT remains non-live / on-device / never-copy engines; no multi-SKU engine ship
    - CI still does not require Jetson or GPU ORT
    - Absolute “live always PyTorch” language removed or qualified with ORT exception for fixed-class
    - cpu-fallback.yaml comment no longer says ORT is export-target-only
    - Existing EDGE-03 keyword tests still pass; new keywords cover live ORT conditions
  </behavior>
  <action>
    1. Update `docs/export/yolo26-onnx-tensorrt.md`:
       - Replace intro “live still PyTorch only / offline packaging only” with accurate split: live ORT for fixed-class when conditions met; export recipes still offline for producing `.onnx` / on-device `.engine`
       - Document prerequisites: `uv sync --extra detect --extra onnx` for live ORT; env `SENTRY_DETECTOR_ONNX` / optional `SENTRY_ARTIFACT_ROOT`
       - Profile section: cpu-fallback preferred onnxruntime can be **live** with artifact+dep; jetson tensorrt still export/device policy until Phase 10
       - Remove or rewrite “Deferred: First-class ONNX Runtime InferenceBackend” so it does not deny the Ultralytics-native live ORT path that Phase 9 ships
       - Keep AGPL, on-device TRT rules, no invented FPS

    2. Update `docs/export/README.md`:
       - Qualify “Live inference stays PyTorch” → torch default / TRT not live yet; fixed-class ORT live when preferred+artifact+`onnx` extra
       - Table row for live serve reflects honesty (not “always PyTorch profiles only”)
       - Point makers at detect+onnx install for ORT path; keep offline export rules for TRT

    3. Update `docs/architecture.md` and `docs/configuration.md` honesty paragraphs that still say preferred onnxruntime/tensorrt is **only** policy/export target — ORT may now be live; TRT remains policy/export until Phase 10.

    4. Update `src/sentry_ai/config/profiles/cpu-fallback.yaml` header comments: preferred_backend=onnxruntime selects live ORT when `.onnx` + onnxruntime available; otherwise soft torch fallback with reason (do not change YAML keys/values).

    5. Extend `tests/test_export_docs.py`:
       - Assert yolo26 or README text mentions live onnxruntime conditions (keywords: `onnxruntime` or `onnx runtime`, live language, and `onnx` extra / `uv sync` with onnx)
       - Assert soft-fallback / missing artifact or dependency honesty appears somewhere in export docs for ORT
       - Keep TRT on-device / never-copy / no Jetson-required-CI asserts green
       - Do not assert absolute pytorch-only live forever

    6. Do not rewrite jetson packaging beyond what’s needed if it still claims ORT can never be live — only fix false absolute claims about fixed-class ORT.
  </action>
  <verify>
    <automated>uv run pytest tests/test_export_docs.py tests/test_pyproject_onnx_extra.py tests/test_detection_factory.py -q</automated>
  </verify>
  <done>Docs and profile comments honestly describe live ORT conditions and onnx extra install; keyword tests green; TRT honesty preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env/config → artifact resolver | Untrusted `SENTRY_DETECTOR_ONNX` / `SENTRY_ARTIFACT_ROOT` paths |
| Factory → worker weights | Live ORT must only load allowlisted resolved `.onnx` paths |
| Operator status trust | Claiming `backend_live=onnxruntime` while running `.pt` is spoofing |
| Package install | Optional `onnxruntime` wheel supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | `build_detection_worker` live label | mitigate | Set `backend_live=onnxruntime` only on path+dep+onnx weights path; unit assert `_weights.endswith(".onnx")` when live=ort |
| T-09-02 | Tampering | `SENTRY_DETECTOR_ONNX` path | mitigate | Existing `resolve_detector_artifact` allowlist + `path_rejected` soft-fall; keep artifact path tests green |
| T-09-03 | Denial of service | Missing ORT dep / artifact | mitigate | Soft torch fallback with stable reason; serve still starts |
| T-09-04 | Tampering | Module-level ORT import side effects | mitigate | `find_spec` probe only; source inspect forbids `import onnxruntime` / `from onnxruntime` |
| T-09-05 | Elevation of privilege | Arbitrary model path load | mitigate | Stem/suffix allowlist only; no URL fetch; no invent path |
| T-09-06 | Information disclosure | Artifact paths in docs/logs | accept | Local operator tool; model paths only |
| T-09-SC | Tampering | `onnxruntime` pip install | mitigate | Pin band from RESEARCH legitimacy audit (Approved registry-verified); no postinstall scripts expected; no [SLOP] packages |
</threat_model>

<verification>
- `uv run pytest tests/test_detection_factory.py tests/test_pyproject_onnx_extra.py tests/test_export_docs.py tests/test_artifact_paths.py -q` green
- `rg -n 'backend_live=.onnxruntime.|backend_live=\"onnxruntime\"' src/sentry_ai/models/detection/factory.py` matches live success return only
- `rg -n 'ort_artifact_missing|ort_dep_missing' src/sentry_ai/models/detection/factory.py` matches
- `rg -n 'ort_loader_not_implemented' src/sentry_ai/models/detection/factory.py` is empty (retired from live factory path)
- `rg -n 'import onnxruntime|from onnxruntime|import tensorrt|from tensorrt' src/sentry_ai/models/detection/factory.py` is empty
- `rg -n 'onnxruntime>=1.20,<1.29' pyproject.toml` matches
- No edits to `models/detection/loop.py`, `bus/frame_bus.py`, `state/perception_store.py`, `api/routes_v1.py`
- Default suite never requires Jetson or `onnxruntime-gpu`
</verification>

<success_criteria>
1. cpu-fallback + valid allowlisted `.onnx` + dep probe True → `backend_live=onnxruntime` and worker weights are that `.onnx` path.
2. Missing artifact / missing dep / path_rejected → torch soft-fallback with exact reason codes; never live ORT.
3. `pyproject.toml` ships optional `onnx` extra with locked pin; static test proves it.
4. Export/architecture/configuration docs describe live ORT conditions and install path; TRT still non-live/on-device.
5. Perception spine freeze intact; factory remains sole `backend_live` author.
</success_criteria>

<output>
Create `.planning/phases/09-live-ort-fixed-class-yolo/09-01-SUMMARY.md` when done
</output>
