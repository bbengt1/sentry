---
phase: 09-live-ort-fixed-class-yolo
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - tests/test_ort_parity.py
  - tests/test_backend_honesty_status.py
autonomous: true
requirements:
  - ORT-02
  - ORT-04
user_setup: []
must_haves:
  truths:
    - "Factory live ORT path + injected FakeModel process() yields Detection with class_name, confidence, bbox_xyxy, source=fixed (ORT-02)"
    - "set_conf on ORT-path worker is reflected in the next predict conf kwarg (runtime conf still works)"
    - "Empty predict results yield [] (not None) on ORT factory path"
    - "Parity/golden suite uses mocks/fixtures only — no Jetson, no GPU ORT, no real YOLO(\"*.onnx\") load in default CI (ORT-04)"
    - "Existing mapping golden tests remain green (shared postprocess path)"
    - "Status honesty pass-through still works when backend_live=onnxruntime (requested=live, reason=None)"
    - "DetectionLoop / FrameBus / PerceptionStore / /v1 remain unmodified"
  artifacts:
    - path: "tests/test_ort_parity.py"
      provides: "ORT-02/04 process + conf + source=fixed parity without hardware"
      contains: "backend_live"
    - path: "tests/test_backend_honesty_status.py"
      provides: "Optional live=onnxruntime status fixture coverage"
    - path: "tests/test_detection_mapping.py"
      provides: "Unchanged mapping golden (shared torch/ORT postprocess)"
  key_links:
    - from: "tests/test_ort_parity.py"
      to: "build_detection_worker live ORT branch"
      via: "resolve/dep monkeypatch + model=FakeModel; never real ONNX load"
      pattern: "build_detection_worker|backend_live.*onnxruntime"
    - from: "YoloDetectionWorker.process"
      to: "results_to_detections"
      via: "same predict → Results → Detection path as torch"
      pattern: "results_to_detections"
    - from: "Detection schema"
      to: "source=fixed default"
      via: "mapper omits source; schema default supplies ORT-02"
      pattern: "source.*=.*fixed|source == \"fixed\""
---

<objective>
Prove that the live ORT factory path produces the same Detection wire contract as PyTorch and that conf remains runtime-adjustable — entirely with mocks/fixtures so CI never needs Jetson, GPU ORT, or real ONNX graphs.

Purpose: Close ORT-02 (schema-identical detections) and ORT-04 (golden/parity without edge hardware) on top of the 09-01 live factory branch.

Output: `tests/test_ort_parity.py` (and honesty status fixture extension) green under default pytest; mapping tests still green; spine freeze intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-CONTEXT.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-RESEARCH.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-PATTERNS.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-VALIDATION.md
@.planning/phases/09-live-ort-fixed-class-yolo/09-01-PLAN.md
@src/sentry_ai/models/detection/factory.py
@src/sentry_ai/models/detection/yolo_worker.py
@src/sentry_ai/models/detection/mapping.py
@src/sentry_ai/schemas/perception.py
@tests/test_detection_worker.py
@tests/test_detection_mapping.py
@tests/test_detection_factory.py
@tests/test_backend_honesty_status.py

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| Detection contract | Same as torch: `class_name`, `confidence`, `bbox_xyxy`, `source="fixed"` | ORT-02 / CONTEXT |
| Test strategy | Inject FakeModel on factory live ORT path; mock resolve + dep probe — no Jetson | ORT-04 / CONTEXT |
| Custom decoder | Forbidden — reuse `results_to_detections` | CONTEXT |
| conf | Runtime `set_conf` / `get_conf` must work on ORT-path worker (predict kwarg) | user lock |
| Optional real CPU ORT | Opt-in only if desired; **not** merge gate | RESEARCH / VALIDATION |
| Spine freeze | No DetectionLoop / bus / store / `/v1` edits | CONTEXT |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| New test module | `tests/test_ort_parity.py` (VALIDATION name) |
| Live path setup | Monkeypatch `_try_resolve_artifact` → allowlisted Path + `_onnxruntime_available` → True + `model=` FakeModel with boxes |
| Real ORT load | **Not** in default suite; do not add required `@pytest.mark` integration that fails without onnxruntime |
| Honesty status | Extend `tests/test_backend_honesty_status.py` with one fixture: requested=onnxruntime, live=onnxruntime, reason=None |
| Mapping file | Do not rewrite `mapping.py`; re-run existing mapping tests only |

## Interfaces from 09-01 (executor must use)

From factory after 09-01:
```python
# Live success only when path + dep OK:
build = build_detection_worker(rt, conf=0.25, model=fake)
# build.backend_live == "onnxruntime"
# build.backend_reason is None
# str(build.worker._weights).endswith(".onnx")
```

Helpers (names as shipped in 09-01 — use actual private names if slightly different):
- `factory._try_resolve_artifact` / monkeypatch return `(Path(".../yolo26n.onnx"), None)`
- `factory._onnxruntime_available` → True/False

From `tests/test_detection_worker.py` patterns:
```python
class FakeModel:
    def predict(self, **kwargs) -> list[Any]: ...
# returns SimpleNamespace(boxes=_FakeBoxes(...), names={0: "person"})
```

From `src/sentry_ai/schemas/perception.py`:
```python
class Detection(BaseModel):
    class_name: str
    confidence: float
    bbox_xyxy: tuple[float, float, float, float] | list[float]
    source: Literal["fixed", "open_vocab"] = "fixed"
```

## Depends on 09-01

This plan assumes:
- Live ORT factory branch exists and can return `backend_live="onnxruntime"`
- Soft reasons `ort_artifact_missing` / `ort_dep_missing` / `path_rejected` already unit-tested in factory matrix
- `onnx` extra + docs already landed (not re-tested here except suite remains green)

## Out of scope for 09-02

- Re-implementing factory live branch (09-01)
- Live TensorRT parity (Phase 10)
- Sticky fallback policy (Phase 11)
- Real exported `yolo26n.onnx` end-to-end as phase gate
- Changing Detection schema or mapper implementation
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ORT process parity + conf golden tests (ORT-02, ORT-04)</name>
  <files>tests/test_ort_parity.py</files>
  <read_first>
    - .planning/phases/09-live-ort-fixed-class-yolo/09-RESEARCH.md (Pattern 3 golden/parity; factory test sketch)
    - .planning/phases/09-live-ort-fixed-class-yolo/09-PATTERNS.md (parity section + FakeModel patterns)
    - .planning/phases/09-live-ort-fixed-class-yolo/09-VALIDATION.md (ORT-02/04 rows + conf row)
    - src/sentry_ai/models/detection/factory.py (post-09-01 live branch)
    - tests/test_detection_worker.py (FakeModel, conf, process asserts)
    - tests/test_detection_mapping.py (box golden fields)
    - tests/test_detection_factory.py (live ORT fixture patterns from 09-01)
  </read_first>
  <behavior>
    - Factory ORT live path (cpu-fallback or ProfileRuntime preferred=onnxruntime) + FakeModel with one person box → process returns list[Detection] with class_name=="person", confidence≈expected, bbox_xyxy==expected, source=="fixed"
    - After set_conf(0.5), next process passes conf≈0.5 into FakeModel.predict kwargs
    - FakeModel returning [] → process returns [] (not None)
    - Assert build.backend_live=="onnxruntime" and worker._weights endswith ".onnx" for the parity setup (guards against testing torch soft-stub by mistake)
    - No import of real onnxruntime required; no YOLO("*.onnx"); no network/weight download
    - No Jetson markers required to pass
  </behavior>
  <action>
    1. Create `tests/test_ort_parity.py` with helpers local to the file (or small shared fakes — prefer local to avoid churn):
       - `FakeModel` recording `predict` kwargs and returning configurable Results-like `SimpleNamespace` + `_FakeBoxes` (copy structure from `test_detection_worker.py`)
       - Fixture or helper `_live_ort_build(monkeypatch, tmp_path, model, conf=0.25)` that:
         - builds cpu-fallback `ProfileRuntime` via `load_config(profile="cpu-fallback")` + `profile_runtime` (or minimal ProfileRuntime with preferred_backend=onnxruntime)
         - monkeypatches factory resolve to return an allowlisted `tmp_path / "yolo26n.onnx"` path (touch empty file if resolve is not fully mocked)
         - monkeypatches `_onnxruntime_available` → True
         - calls `build_detection_worker(rt, conf=conf, model=model)`
         - asserts live=onnxruntime before returning build (fail loud if 09-01 regression)

    2. Tests to include:
       - `test_ort_process_detection_contract` — known box → Detection fields + `source == "fixed"`
       - `test_ort_set_conf_applies_on_next_process` — set_conf then process; assert predict conf kwarg
       - `test_ort_empty_predict_returns_empty_list` — FakeModel returns [] → `[]`
       - Optional tight honesty guard: if `backend_live == "onnxruntime"` then weights suffix `.onnx` (and inverse: `.pt` weights never paired with live ort in this module’s setups)

    3. Use `image_frame_factory` from conftest for frames (same as worker tests).

    4. Do **not** add a default-suite test that constructs real `ultralytics.YOLO` on an onnx file. If an opt-in real-ORT test is added, it must `pytest.importorskip("onnxruntime")` and skip when no real model — prefer omit entirely this plan.

    5. Do not modify production code unless a genuine 09-01 gap blocks parity (then fix minimally in factory only and note in SUMMARY — prefer report-back over scope creep).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ort_parity.py tests/test_detection_mapping.py tests/test_detection_worker.py -q</automated>
  </verify>
  <done>Parity suite proves Detection wire contract + conf on factory ORT live path with mocks only; mapping/worker baselines still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Status honesty fixture for live ORT + phase suite gate</name>
  <files>tests/test_backend_honesty_status.py</files>
  <read_first>
    - .planning/phases/09-live-ort-fixed-class-yolo/09-VALIDATION.md (status pass-through optional row)
    - tests/test_backend_honesty_status.py (existing inject patterns for backend_requested/live/reason)
    - src/sentry_ai/api (status snapshot construction — only as needed for fixture style)
    - .planning/phases/08-backend-selection-honesty/08-02-SUMMARY.md if present for honesty field names
  </read_first>
  <behavior>
    - When create_app / status path is given backend_requested=onnxruntime, backend_live=onnxruntime, backend_reason=None, /api/status (or StatusSnapshot builder under test) round-trips those fields without recomputing live from preferred
    - Existing soft-stub honesty cases remain green
    - No production route changes required if Phase 8 pass-through already generic
  </behavior>
  <action>
    1. Extend `tests/test_backend_honesty_status.py` with one focused case:
       - Inject or construct status with `backend_requested="onnxruntime"`, `backend_live="onnxruntime"`, `backend_reason=None`
       - Assert JSON/status fields match (same accessors existing tests use for torch/soft-stub cases)
       - Do not re-derive live from preferred in the test double

    2. If the honesty test module already parametrizes backend triples, add the live ORT triple to the matrix instead of a wholly separate style.

    3. Run the ORT-focused suite as the phase gate for this plan:
       - factory matrix, ort parity, mapping, worker, export docs, pyproject onnx extra, backend honesty
       - Confirm with `rg` that spine freeze files were not modified this phase (`loop.py`, frame bus, perception store, routes_v1)

    4. Do not edit production status routes unless a bug blocks pass-through of live=onnxruntime (unexpected after Phase 8 — only fix if proven).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ort_parity.py tests/test_detection_factory.py tests/test_backend_honesty_status.py tests/test_detection_mapping.py tests/test_export_docs.py tests/test_pyproject_onnx_extra.py -q</automated>
  </verify>
  <done>Live ORT status triple round-trips; full Phase 9 automated dimension set green without Jetson/GPU ORT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test doubles → factory | Mocks must not invent production live claims without path+dep coupling asserts |
| Status API → operator | Live ORT label must match factory-authored fields only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Spoofing | Parity tests accidentally soft-stub | mitigate | Assert `backend_live=="onnxruntime"` + `.onnx` weights before process asserts |
| T-09-08 | Denial of service | Real truncated .onnx load in CI | mitigate | Always inject FakeModel; forbid real YOLO onnx load in default suite |
| T-09-09 | Spoofing | Status recomputes live from preferred | mitigate | Honesty fixture asserts pass-through of live=onnxruntime without recompute |
| T-09-10 | Tampering | Custom decoder drift | accept | Out of scope — parity uses shared mapper; no custom ORT path code in this plan |
</threat_model>

<verification>
- `uv run pytest tests/test_ort_parity.py tests/test_detection_factory.py tests/test_backend_honesty_status.py tests/test_detection_mapping.py tests/test_detection_worker.py tests/test_export_docs.py tests/test_pyproject_onnx_extra.py -q` green
- `uv run ruff check src tests` green (if ruff available in env)
- `rg -n "InferenceSession|onnxruntime.InferenceSession" src/sentry_ai` is empty (no custom ORT decoder)
- Spine freeze: `git diff --name-only` for this phase work must not include `models/detection/loop.py`, `bus/frame_bus.py`, `state/perception_store.py`, `api/routes_v1.py`
- Default suite does not require `onnxruntime` package installed
</verification>

<success_criteria>
1. ORT factory live path process produces Detection fields identical in shape to torch path golden (class, conf, bbox, source=fixed).
2. Runtime conf adjustment works on ORT-path worker.
3. Parity proven with mocks only — no Jetson / GPU ORT / real onnx load required.
4. Status honesty surfaces live=onnxruntime when factory authored it.
5. Phase 9 ORT-01..04 automated dimensions from VALIDATION are green under the combined suite.
</success_criteria>

<output>
Create `.planning/phases/09-live-ort-fixed-class-yolo/09-02-SUMMARY.md` when done
</output>
