---
phase: 02-camera-ingest-live-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/sentry_ai/capture/__init__.py
  - src/sentry_ai/capture/image_frame.py
  - src/sentry_ai/capture/status.py
  - src/sentry_ai/sources/__init__.py
  - src/sentry_ai/sources/errors.py
  - src/sentry_ai/sources/synthetic.py
  - src/sentry_ai/sources/opencv_source.py
  - src/sentry_ai/plugins/protocols.py
  - src/sentry_ai/plugins/builtins.py
  - src/sentry_ai/plugins/registry.py
  - src/sentry_ai/cli.py
  - src/sentry_ai/config/models.py
  - tests/conftest.py
  - tests/test_sources_synthetic.py
  - tests/test_sources_file.py
  - tests/test_sources_opencv.py
  - tests/test_sources_rtsp.py
  - tests/test_frame_bus.py
  - tests/test_capture_loop_reconnect.py
  - tests/test_api_preview.py
  - tests/test_cli_serve.py
  - tests/test_plugins_registry.py
  - tests/test_cli_smoke.py
  - tests/fixtures/.gitkeep
autonomous: true
requirements:
  - CAM-01
  - CAM-02
  - CAM-03
user_setup: []
must_haves:
  truths:
    - "Synthetic source yields ImageFrame with HxWx3 uint8 BGR and monotonic frame_id without hardware"
    - "USB OpenCV source constructs VideoCapture(index) with CAP_PROP_BUFFERSIZE=1 (mockable without hardware)"
    - "File OpenCV source reads video frames with increasing frame_id and optional loop-on-EOF"
    - "CameraSource protocol read() returns ImageFrame; Pydantic Frame stays identity-only (no numpy)"
    - "SourceDisconnected / SourceError exist for disconnect and open failures"
    - "Plugin registry lists synthetic, usb, and file sources; smoke CLI still validates PerceptionFrames"
  artifacts:
    - path: "src/sentry_ai/capture/image_frame.py"
      provides: "Runtime ImageFrame dataclass (meta Frame + image_bgr ndarray)"
      exports: ["ImageFrame"]
    - path: "src/sentry_ai/capture/status.py"
      provides: "SourceStatus StrEnum for capture lifecycle"
      exports: ["SourceStatus"]
    - path: "src/sentry_ai/sources/synthetic.py"
      provides: "Real patterned-BGR SyntheticSource implementing CameraSource"
      exports: ["SyntheticSource"]
    - path: "src/sentry_ai/sources/opencv_source.py"
      provides: "OpenCVSource for USB index and file path targets"
      exports: ["OpenCVSource"]
    - path: "src/sentry_ai/sources/errors.py"
      provides: "SourceError and SourceDisconnected hierarchy"
      exports: ["SourceError", "SourceDisconnected"]
    - path: "src/sentry_ai/plugins/protocols.py"
      provides: "CameraSource.read() -> ImageFrame"
      contains: "ImageFrame"
    - path: "tests/test_sources_synthetic.py"
      provides: "CAM-03 automated coverage"
    - path: "tests/test_sources_file.py"
      provides: "CAM-02 automated coverage"
    - path: "tests/test_sources_opencv.py"
      provides: "CAM-01 mock VideoCapture coverage"
  key_links:
    - from: "src/sentry_ai/plugins/protocols.py"
      to: "src/sentry_ai/capture/image_frame.py"
      via: "CameraSource.read return type"
      pattern: "read\\(\\)\\s*->\\s*ImageFrame"
    - from: "src/sentry_ai/sources/synthetic.py"
      to: "src/sentry_ai/schemas/frame.py"
      via: "ImageFrame.meta = Frame(...)"
      pattern: "Frame\\("
    - from: "src/sentry_ai/plugins/builtins.py"
      to: "src/sentry_ai/sources/synthetic.py"
      via: "re-export SyntheticSource for entry-point stability"
      pattern: "SyntheticSource"
    - from: "src/sentry_ai/plugins/registry.py"
      to: "usb/file source classes"
      via: "register_builtins skip-if-present"
      pattern: "register_source\\(\"(usb|file)\""
    - from: "src/sentry_ai/cli.py smoke"
      to: "ImageFrame.meta fields"
      via: "PerceptionFrame construction uses meta.frame_id/camera_id/t_capture"
      pattern: "\\.meta\\.|image_frame|frame\\.meta"
---

<objective>
Add runtime ImageFrame + SourceStatus/errors, evolve CameraSource to return ImageFrame, and ship real synthetic + OpenCV USB/file sources with Wave 0 test scaffolding for the rest of Phase 2.

Purpose: Prove CAM-01/02/03 “any camera works” at the adapter layer without ML, without putting numpy on Pydantic Frame, and without unbounded queues (bus lands in 02-02).

Output: Installable deps (opencv-python-headless, numpy); synthetic/usb/file plugins; protocol + smoke migration; Wave 0 skip-or-real tests for later plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-camera-ingest-live-preview/02-CONTEXT.md
@.planning/phases/02-camera-ingest-live-preview/02-RESEARCH.md
@.planning/phases/02-camera-ingest-live-preview/02-PATTERNS.md
@.planning/phases/02-camera-ingest-live-preview/02-VALIDATION.md
@.planning/phases/01-foundations-contracts/01-01-SUMMARY.md
@.planning/phases/01-foundations-contracts/01-02-SUMMARY.md
@.planning/phases/01-foundations-contracts/01-03-SUMMARY.md

## Locked decisions (do not re-litigate)

| Decision | Value |
|----------|-------|
| Image payload type | Runtime `@dataclass` `ImageFrame` with `meta: Frame` + `image_bgr: np.ndarray` — **not** numpy on Pydantic Frame |
| Protocol | `CameraSource.read() -> ImageFrame` |
| Capture stack | opencv-python-headless + numpy; no PyAV in Phase 2 |
| Sources | USB + file + synthetic here; RTSP wiring/docs in 02-03 |
| Reconnect foundation | `SourceError` / `SourceDisconnected` + `SourceStatus` enum; capture thread in 02-02 |
| Forbidden | torch, ultralytics, FastAPI server (02-03), FrameBus (02-02) |

## Package legitimacy (Phase 2 installs)

| Package | Disposition |
|---------|-------------|
| opencv-python-headless | Approved (RESEARCH audit) |
| numpy | Approved |
| fastapi / uvicorn / httpx | Deferred to 02-03 |

## Interfaces (Phase 1 contracts to evolve)

From `src/sentry_ai/schemas/frame.py`:
- `Frame`: frame_id, camera_id, t_capture, optional t_ingest/width/height; `extra="forbid"`; identity only

From `src/sentry_ai/plugins/protocols.py` (before):
- `CameraSource`: name, open(), read() -> Frame, close()

From `src/sentry_ai/plugins/builtins.py`:
- `SyntheticSource` currently returns bare Frame — move implementation to sources/, re-export

## Out of scope this plan

FrameBus, CaptureLoop thread, FastAPI/MJPEG, RTSP entry-point + docs (02-03), detection/depth.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 deps, ImageFrame, status/errors, protocol evolution, test stubs</name>
  <files>pyproject.toml, uv.lock, src/sentry_ai/capture/__init__.py, src/sentry_ai/capture/image_frame.py, src/sentry_ai/capture/status.py, src/sentry_ai/sources/__init__.py, src/sentry_ai/sources/errors.py, src/sentry_ai/plugins/protocols.py, tests/conftest.py, tests/test_sources_synthetic.py, tests/test_sources_file.py, tests/test_sources_opencv.py, tests/test_sources_rtsp.py, tests/test_frame_bus.py, tests/test_capture_loop_reconnect.py, tests/test_api_preview.py, tests/test_cli_serve.py, tests/fixtures/.gitkeep</files>
  <read_first>
    - .planning/phases/02-camera-ingest-live-preview/02-RESEARCH.md (Pattern 1 ImageFrame, Standard Stack deps, Wave 0 Gaps, Package Legitimacy)
    - .planning/phases/02-camera-ingest-live-preview/02-PATTERNS.md (image_frame, status, errors, package __init__ layout)
    - .planning/phases/02-camera-ingest-live-preview/02-VALIDATION.md (Wave 0 Requirements, T-2 threat refs)
    - src/sentry_ai/schemas/frame.py
    - src/sentry_ai/plugins/protocols.py
    - src/sentry_ai/backend/__init__.py (package export style)
    - src/sentry_ai/schemas/enums.py (StrEnum style)
    - pyproject.toml
  </read_first>
  <action>
    1. Add runtime dependencies to pyproject.toml (keep existing pydantic/typer/pyyaml):
       - opencv-python-headless&gt;=4.10,&lt;6
       - numpy&gt;=2.0,&lt;2.5
       Do NOT add fastapi/uvicorn/httpx yet (02-03). Do NOT add torch/ultralytics.
       Run `uv lock` and `uv sync --extra dev` so the env imports cv2 and numpy.

    2. Create `src/sentry_ai/capture/image_frame.py` per RESEARCH Pattern 1 / PATTERNS:
       - `@dataclass(slots=True) class ImageFrame` with fields `meta: Frame` and `image_bgr: np.ndarray`
       - Convenience properties `frame_id` and `camera_id` delegating to meta
       - Module docstring: hot-path runtime container; not a Pydantic/wire model
       - Do not put image bytes or ndarray on `schemas.frame.Frame`

    3. Create `src/sentry_ai/capture/status.py` with `SourceStatus(StrEnum)` values exactly:
       starting, streaming, reconnecting, error, stopped
       (string values lowercase as listed). Export from `capture/__init__.py` with `__all__`.

    4. Create `src/sentry_ai/sources/errors.py`:
       - `SourceError(RuntimeError)` base
       - `SourceDisconnected(SourceError)` for failed read / EOF / disconnect
       Export from `sources/__init__.py` (minimal `__all__` for now).

    5. Evolve `CameraSource` in `plugins/protocols.py`:
       - Import ImageFrame from capture.image_frame
       - Change `read() -> ImageFrame`
       - Update module docstring: Phase 2 sources return ImageFrame; Frame remains identity schema
       - Leave ModelWorker.process signature accepting Frame or widen to object/ImageFrame only if needed for isinstance tests; prefer accepting ImageFrame or object so NoopWorker still type-checks when smoke passes ImageFrame

    6. Wave 0 Nyquist stubs (suite must stay green):
       - `tests/fixtures/.gitkeep` (clip generated later in Task 3 or fixture helper)
       - Create collectable test modules listed in VALIDATION Wave 0:
         test_sources_synthetic.py, test_sources_file.py, test_sources_opencv.py — will be filled this plan (can be empty placeholders only if Task 2–3 same wave fills them; prefer skip markers with reason "implemented in plan 02-01" only if Task 1 commits alone)
         test_sources_rtsp.py, test_frame_bus.py, test_capture_loop_reconnect.py, test_api_preview.py, test_cli_serve.py — `pytest.mark.skip(reason="implemented in plan 02-0N")` so suite stays green
       - Extend `tests/conftest.py` with `make_image_frame(frame_id, camera_id=..., width=64, height=48)` helper + fixture that builds ImageFrame with small uint8 zeros or patterned BGR for later bus/API tests

    7. Package init files follow backend/ style: future annotations, explicit imports, sorted `__all__`.
  </action>
  <verify>
    <automated>uv sync --extra dev &amp;&amp; uv run python -c "import cv2, numpy; from sentry_ai.capture.image_frame import ImageFrame; from sentry_ai.capture.status import SourceStatus; from sentry_ai.sources.errors import SourceDisconnected; assert SourceStatus.STREAMING == 'streaming'" &amp;&amp; uv run pytest -q tests/test_schemas_frame.py &amp;&amp; uv run ruff check src/sentry_ai/capture src/sentry_ai/sources/errors.py src/sentry_ai/plugins/protocols.py</automated>
  </verify>
  <acceptance_criteria>
    - pyproject lists opencv-python-headless and numpy; no torch/fastapi yet
    - ImageFrame has meta + image_bgr; Frame schema tests still pass without numpy fields
    - SourceStatus has starting|streaming|reconnecting|error|stopped
    - CameraSource.read annotated to return ImageFrame
    - Wave 0 skip-marked tests for 02-02/02-03 paths exist under tests/
    - make_image_frame fixture available from conftest
  </acceptance_criteria>
  <done>Runtime ImageFrame + status/errors + protocol evolution landed; capture deps installed; Wave 0 stubs present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real SyntheticSource (CAM-03) + migrate Phase 1 consumers</name>
  <files>src/sentry_ai/sources/synthetic.py, src/sentry_ai/plugins/builtins.py, src/sentry_ai/plugins/protocols.py, src/sentry_ai/cli.py, tests/test_sources_synthetic.py, tests/test_plugins_registry.py, tests/test_cli_smoke.py</files>
  <read_first>
    - .planning/phases/02-camera-ingest-live-preview/02-RESEARCH.md (Synthetic source code example, Protocol evolution, Pitfall 6)
    - .planning/phases/02-camera-ingest-live-preview/02-PATTERNS.md (synthetic.py section, Protocol/Test Migration Checklist)
    - src/sentry_ai/plugins/builtins.py
    - src/sentry_ai/cli.py (smoke unwrap lines)
    - tests/test_plugins_registry.py
    - tests/test_cli_smoke.py
  </read_first>
  <behavior>
    - open() then read() returns ImageFrame; meta is valid Frame; image_bgr shape (H,W,3) dtype uint8
    - frame_id starts at 0 after open and increments by 1 each read
    - read() before open raises RuntimeError (or SourceError) with clear message
    - width/height on meta match image dimensions
    - Registry/smoke still green after ImageFrame migration
  </behavior>
  <action>
    1. Implement `src/sentry_ai/sources/synthetic.py` class `SyntheticSource`:
       - `name: str = "synthetic"`
       - `__init__(camera_id="synthetic0", width=640, height=480, fps=0.0)` — default fps=0.0 so unit tests do not sleep; serve demos may pass fps=30 later
       - open/close with `_open` guard; reset `_next_frame_id` on open
       - read(): if not open raise RuntimeError/SourceError with message containing "not open"
       - Build patterned BGR ndarray (e.g. moving green bar keyed by frame_id) — deterministic for tests
       - Construct Frame meta with frame_id, camera_id, time.time() t_capture and t_ingest, width, height
       - Return ImageFrame(meta=..., image_bgr=...)
       - Optional sleep only when fps &gt; 0

    2. Re-export from `plugins/builtins.py`: `from sentry_ai.sources.synthetic import SyntheticSource` (and keep NoopWorker/NullSink). Update builtins module docstring: synthetic is real image source; no model inference.

    3. Keep entry point `synthetic = "sentry_ai.plugins.builtins:SyntheticSource"` stable (no breaking change).

    4. Update NoopWorker.process to accept ImageFrame (or object) so smoke can pass ImageFrame without type friction; still return None.

    5. Migrate consumers (PATTERNS Protocol/Test Migration Checklist):
       - `cli.py` smoke: `image = source.read()` then use `image.meta` (or properties) for PerceptionFrame fields; do not assume bare Frame
       - `tests/test_plugins_registry.py`: assert isinstance ImageFrame; validate Frame via image.meta.model_dump(); assert frame_id via property or meta
       - `tests/test_cli_smoke.py`: still exit 0 and validate PerceptionFrames

    6. Write real `tests/test_sources_synthetic.py` covering CAM-03 behaviors above (no skip).

    7. Do not change Frame schema tests — identity-only Frame construction must remain valid.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_sources_synthetic.py tests/test_plugins_registry.py tests/test_cli_smoke.py tests/test_schemas_frame.py &amp;&amp; uv run sentry smoke &amp;&amp; uv run ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - SyntheticSource.read returns ImageFrame with HxWx3 uint8
    - frame_id monotonic from 0 after open
    - test_plugins_registry and smoke green
    - test_schemas_frame still identity-only (no image on Frame)
    - Entry point path still plugins.builtins:SyntheticSource
  </acceptance_criteria>
  <done>CAM-03 synthetic image source live; Phase 1 consumers migrated to ImageFrame.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OpenCVSource USB + file (CAM-01, CAM-02) + registry registration</name>
  <files>src/sentry_ai/sources/opencv_source.py, src/sentry_ai/sources/__init__.py, src/sentry_ai/plugins/registry.py, src/sentry_ai/config/models.py, pyproject.toml, tests/test_sources_opencv.py, tests/test_sources_file.py, tests/fixtures/.gitkeep, tests/conftest.py</files>
  <read_first>
    - .planning/phases/02-camera-ingest-live-preview/02-RESEARCH.md (OpenCV multi-source adapter, CAP_PROP_BUFFERSIZE, file loop default, T-2-01 path notes)
    - .planning/phases/02-camera-ingest-live-preview/02-PATTERNS.md (opencv_source, registry registration, entry points)
    - src/sentry_ai/plugins/registry.py
    - src/sentry_ai/config/models.py (SourceConfig)
    - src/sentry_ai/sources/synthetic.py (lifecycle pattern)
  </read_first>
  <behavior>
    - OpenCVSource(target=0, camera_id=..., name="usb") open() calls VideoCapture(0) and sets BUFFERSIZE=1
    - OpenCVSource(target=path, name="file") reads successive frames with increasing frame_id
    - Failed read raises SourceDisconnected
    - Failed open raises SourceError/RuntimeError with target in message
    - File with loop_file=True rewinds on EOF and continues
    - Registry lists usb and file after register_builtins
  </behavior>
  <action>
    1. Implement `OpenCVSource` in `sources/opencv_source.py` per RESEARCH adapter:
       - Constructor: target: int | str, camera_id: str, name: str = "opencv", loop_file: bool = True
       - Detect file vs stream URL: string targets not starting with rtsp://, rtsps://, http://, https:// are files (for loop policy). URL schemes reserved for RTSP plan 02-03 but OpenCVSource may accept them without special entry point yet.
       - open(): VideoCapture(target); if not isOpened raise SourceError/RuntimeError; set CAP_PROP_BUFFERSIZE=1 best-effort; reset frame id
       - read(): cap.read(); on failure if file+loop_file seek POS_FRAMES=0 and retry once; still failing → SourceDisconnected
       - Build Frame meta with shape-derived width/height, epoch timestamps, monotonic frame_id (source-owned)
       - close(): release capture; clear handle
       - Never open cameras from FastAPI (N/A this plan) — source is pure adapter

    2. Optional thin factory helpers or subclasses for plugin class attributes if needed for entry points:
       - Prefer registering callables/classes that set name="usb" / name="file" — e.g. thin subclasses UsbSource(OpenCVSource) with name="usb" and default device wiring, or factory classes with fixed name class attr matching Protocol
       - register_builtins must register "usb" and "file" with skip-if-present (same pattern as synthetic)
       - Add pyproject entry points under sentry_ai.sources for usb and file pointing at the public classes

    3. Extend SourceConfig (config/models.py) with optional fields keeping extra="forbid":
       - device: int | None = None
       - path: str | None = None
       - url: str | None = None
       - camera_id: str | None = None
       Do not break existing profile YAML loads (defaults only).

    4. Tests (mock cv2 for USB; real short clip for file):
       - `tests/test_sources_opencv.py`: mock VideoCapture so open uses index 0, BUFFERSIZE set, read returns ImageFrame; disconnect path raises SourceDisconnected
       - `tests/test_sources_file.py`: generate short fixture clip in session/tmp via cv2.VideoWriter (or write once under tests/fixtures/sample_clip.mp4) — open file source, read ≥2 frames, assert increasing frame_id and valid shapes; with loop_file True, reading past end does not raise permanently
       - Path validation (T-2-01): document that file target is a path string passed to OpenCV only (no shell=True); reject empty path with ValueError if practical

    5. Export OpenCVSource (and any UsbSource/FileSource wrappers) from sources/__init__.py.

    6. Ensure `uv run sentry health` lists synthetic,usb,file among sources after register_builtins+discover.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_sources_opencv.py tests/test_sources_file.py tests/test_sources_synthetic.py tests/test_plugins_registry.py tests/test_cli_smoke.py &amp;&amp; uv run sentry health &amp;&amp; uv run ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - CAM-01: mock proves VideoCapture(index) + buffersize path
    - CAM-02: file fixture yields multiple ImageFrames with increasing frame_id
    - Failed read raises SourceDisconnected
    - register_builtins exposes synthetic, usb, file
    - SourceConfig optional device/path/url fields load without breaking profiles
    - Full Phase 1 suite still green
  </acceptance_criteria>
  <done>USB and file OpenCV adapters + registry registration complete; CAM-01/02/03 satisfied at source layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/config → OpenCVSource target | Untrusted path / device index / future URL crosses into capture |
| Capture adapter → process memory | BGR frames allocated per read; must not unbounded-queue (bus later) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | OpenCVSource file path / target | mitigate | Pass path only to VideoCapture (no shell); empty path ValueError; document no arbitrary command execution (Task 3) |
| T-2-02 | Denial of Service | Frame backlog | accept (N/A this plan) | No queue yet; sources return one frame per read — bus depth-1 in 02-02 |
| T-2-03 | Information Disclosure | Bind address | accept (N/A this plan) | No server in 02-01; MODEL-03 in 02-03 |
| T-2-04 | Denial of Service / Integrity | Silent freeze | mitigate (foundation) | SourceDisconnected + SourceStatus enums for loop in 02-02; raise on failed open/read (Tasks 1–3) |
| T-2-SC | Tampering | opencv/numpy installs | mitigate | RESEARCH Approved packages only; lock via uv.lock |
</threat_model>

<verification>
- [ ] `uv sync --extra dev` imports cv2 and numpy
- [ ] ImageFrame + SourceStatus + SourceError importable
- [ ] CameraSource.read returns ImageFrame (protocol + implementations)
- [ ] `pytest tests/test_sources_synthetic.py tests/test_sources_file.py tests/test_sources_opencv.py -q` green
- [ ] Phase 1 tests (schemas, registry, smoke, config) green
- [ ] `sentry health` lists synthetic,usb,file
- [ ] No fastapi/torch in deps yet
- [ ] `ruff check src tests` green
</verification>

<success_criteria>
CAM-01, CAM-02, and CAM-03 satisfied at the source-adapter layer: USB (mocked), file (fixture), and synthetic produce ImageFrames with stable frame_ids; protocol and smoke migrated; reconnect error types ready for 02-02.
</success_criteria>

<output>
Create `.planning/phases/02-camera-ingest-live-preview/02-01-SUMMARY.md` when done
</output>
