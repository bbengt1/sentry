---
phase: 05-free-space-unified-stream
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sentry_ai/spatial/__init__.py
  - src/sentry_ai/spatial/free_space.py
  - src/sentry_ai/spatial/smoothing.py
  - src/sentry_ai/spatial/overlay.py
  - src/sentry_ai/spatial/loop.py
  - src/sentry_ai/state/perception_store.py
  - tests/test_free_space_bands.py
  - tests/test_free_space_smoothing.py
  - tests/test_free_space_overlay.py
  - tests/test_free_space_loop.py
  - tests/test_perception_store.py
autonomous: true
requirements:
  - SPACE-01
  - SPACE-02
user_setup: []
must_haves:
  truths:
    - "Synthetic depth maps produce near-field occupied blobs and free ROI without any ML package import"
    - "Free-space is image-space ordinal occupancy (method=near_field_bands); never invents meters on relative depth"
    - "Temporal smoothing (morphology + EMA) stabilizes occupancy across frames on FreeSpaceLoop-owned state"
    - "FreeSpaceLoop polls PerceptionStore.snapshot_depth(), skips same frame_id / missing map, writes FreeSpaceProduct keep-latest"
    - "Full free_mask/occupied_mask stay in-process on FreeSpaceProduct; obstacle list + band fractions are machine-readable product fields"
    - "draw_free_space returns a copy with free/occupied tint and does not mutate inputs"
  artifacts:
    - path: "src/sentry_ai/spatial/free_space.py"
      provides: "Near-field percentile band free-space pure algorithm"
      exports: ["compute_free_space", "FreeSpaceResult"]
    - path: "src/sentry_ai/spatial/smoothing.py"
      provides: "Morphology + EMA occupancy smoother"
      exports: ["smooth_occupancy", "OccupancySmoother"]
    - path: "src/sentry_ai/spatial/overlay.py"
      provides: "Server-side free-space draw helper"
      exports: ["draw_free_space"]
    - path: "src/sentry_ai/spatial/loop.py"
      provides: "FreeSpaceLoop daemon Spatial Post owner"
      exports: ["FreeSpaceLoop"]
    - path: "src/sentry_ai/state/perception_store.py"
      provides: "FreeSpaceProduct triple-product store"
      exports: ["FreeSpaceProduct", "PerceptionStore"]
    - path: "tests/test_free_space_bands.py"
      provides: "SPACE-01 pure algorithm coverage"
    - path: "tests/test_free_space_loop.py"
      provides: "FreeSpaceLoop synthetic DepthProduct coverage"
  key_links:
    - from: "src/sentry_ai/spatial/loop.py"
      to: "PerceptionStore.snapshot_depth"
      via: "FreeSpaceLoop._run polls depth product"
      pattern: "snapshot_depth"
    - from: "src/sentry_ai/spatial/loop.py"
      to: "PerceptionStore.set_free_space"
      via: "publish FreeSpaceProduct after compute"
      pattern: "set_free_space"
    - from: "src/sentry_ai/spatial/free_space.py"
      to: "cv2.morphologyEx / connectedComponentsWithStats"
      via: "occupied seed → obstacles"
      pattern: "morphologyEx|connectedComponentsWithStats"
    - from: "src/sentry_ai/spatial/loop.py"
      to: "compute_free_space + OccupancySmoother"
      via: "Spatial Post sole free-space owner"
      pattern: "compute_free_space|OccupancySmoother"
---

<objective>
Ship Spatial Post core: near-field percentile-band free-space from monocular depth maps, temporal occupancy smoothing, FreeSpaceProduct on PerceptionStore, FreeSpaceLoop daemon, and pure draw_free_space helper — all CI-safe with synthetic depth (no DAV2/HF).

Purpose: Close SPACE-01 (derivation + smoothing) and produce machine-readable obstacle product fields (SPACE-02 foundation) so 05-02 can assemble wire payloads and 05-03 can stream/overlay without reinventing free-space math.

Output: `sentry_ai.spatial` package; store triple product; unit tests green without depth/detect extras.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-free-space-unified-stream/05-CONTEXT.md
@.planning/phases/05-free-space-unified-stream/05-RESEARCH.md
@.planning/phases/05-free-space-unified-stream/05-PATTERNS.md
@.planning/phases/05-free-space-unified-stream/05-VALIDATION.md
@.planning/phases/04-monocular-depth/04-01-SUMMARY.md
@.planning/phases/04-monocular-depth/04-02-SUMMARY.md

## Depends on Phase 4 (shipped)

- `DepthProduct` with in-process `depth_map` HxW float32 + `kind` / `unit` / `error`
- `PerceptionStore.set_depth` / `snapshot_depth` / depth metrics dual half
- `DepthLoop` lifecycle pattern (daemon, start/stop, keep-latest, exception → product error)
- Handlers never run inference; Spatial Post must follow same purity

## Locked decisions (do not re-litigate)

| Decision | Value |
|----------|-------|
| Algorithm | Near-field percentile bands (image-space ordinal) — NOT ground-plane / BEV v1 |
| Placement | Separate `FreeSpaceLoop` (not inline in DepthLoop) |
| Smoothing | Morphology open 3×3 + close 5×5, then EMA α=0.35 on occupancy float, re-threshold 0.5 |
| Units | `units="ordinal"` when relative; never `distance_m` on relative path |
| Wire masks | Full masks in-process only this plan; wire encoding deferred to 05-02/05-03 (obstacles+bands) |
| Packages | No new ML packages; NumPy + OpenCV only |
| Out of scope | `/v1` routes, assemble_perception_frame, MJPEG wiring, UI footer, serve start (05-02/05-03) |

## Interfaces (existing contracts)

From `src/sentry_ai/state/perception_store.py`:
- `DepthProduct(frame_id, camera_id, t_capture, kind, unit, width, height, latency_ms, depth_map, error, ...)`
- `PerceptionStore.set_depth(...)` / `snapshot_depth()` / `metrics_snapshot()` — extend with free-space twin under same lock

From `src/sentry_ai/models/depth/loop.py`:
- Structural twin for FreeSpaceLoop lifecycle (daemon thread, Event wait, exception keeps thread alive)
- FreeSpaceLoop does NOT take FrameBus or ModelWorker — only `PerceptionStore`

From `src/sentry_ai/models/detection/overlay.py`:
- Pure copy-in/copy-out `draw_detections` pattern for `draw_free_space`

From `src/sentry_ai/schemas/enums.py`:
- `DepthKind.RELATIVE | METRIC_ESTIMATED | METRIC_CALIBRATED` — copy onto free-space result for honesty

## Discretion locked by research (implement as specified)

- ROI bottom fraction default `roi_bottom_frac=0.55`
- Nearness polarity: config default `auto` (bottom 20% vs top 20% median heuristic); also support `higher_is_farther` / `higher_is_nearer`
- Band cuts: near ≥ 0.72, mid ≥ 0.45 (nearness ∈ [0,1], 1 = nearer)
- Min obstacle area: 0.15% of ROI pixels
- Optional persistence N=2 consecutive occupied frames before publishing obstacle blobs (implement if cheap; morphology+EMA is required)
- FreeSpaceResult fields: obstacles (list of cue-like dicts/dataclasses with bbox_xyxy, nearness_mean/max, area_px, band), bands dict (near_frac/mid_frac/far_frac), free_mask, occupied_mask, method, depth_kind, units, width, height, error
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Near-field free-space algorithm + temporal smoothing (pure)</name>
  <files>src/sentry_ai/spatial/__init__.py, src/sentry_ai/spatial/free_space.py, src/sentry_ai/spatial/smoothing.py, tests/test_free_space_bands.py, tests/test_free_space_smoothing.py</files>
  <behavior>
    - Synthetic depth with near blob in lower-center (higher_is_farther map, low values = near) yields obstacle_count ≥ 1 and occupied pixels inside ROI
    - Sky/upper FOV outside ROI is not counted as free-space for ground robots (free_mask only meaningful in ROI)
    - relative DepthKind → units="ordinal"; method="near_field_bands"; no distance_m field on results
    - higher_is_nearer and higher_is_farther explicit polarities both produce nearer-bottom occupancy when maps are inverted appropriately
    - auto polarity chooses orientation so bottom strip is nearer than top on typical robot FOV synthetic
    - Morphology+EMA: single-pixel salt noise suppressed; static near blob persists across frames
    - compute_free_space and smoothing modules do not import torch/transformers/ultralytics
  </behavior>
  <read_first>
    - .planning/phases/05-free-space-unified-stream/05-RESEARCH.md (Pattern 1 near-field bands, Pattern 2 smoothing, Code Examples synthetic depth, Recommended Defaults)
    - .planning/phases/05-free-space-unified-stream/05-PATTERNS.md (spatial/free_space.py and smoothing.py sections)
    - src/sentry_ai/models/depth/preprocess.py (pure helper style)
    - src/sentry_ai/schemas/enums.py (DepthKind)
  </read_first>
  <action>
    Create package `src/sentry_ai/spatial/` with pure CPU Spatial Post (per D-algorithm near-field bands; research locked).

    free_space.py — implement `compute_free_space(depth_map, *, kind: DepthKind, nearness_polarity="auto", roi_bottom_frac=0.55, near_cut=0.72, mid_cut=0.45, min_area_frac=0.0015, ...)` returning `FreeSpaceResult` dataclass:
    1. Map depth → nearness ∈ [0,1] using polarity (auto = bottom-vs-top median heuristic per RESEARCH).
    2. Restrict occupancy evaluation to bottom `roi_bottom_frac` of image.
    3. Seed occupied from near band; optional mid as soft (do not invent meters).
    4. Apply morphology helpers from smoothing (open 3×3 then close 5×5) when called without external smoother, OR accept pre-smoothed occupancy from caller — prefer: compute raw occupied, then caller/loop runs OccupancySmoother.
    5. Connected components (`cv2.connectedComponentsWithStats`) → obstacle cues with bbox_xyxy, nearness_mean, nearness_max, area_px, band="near". Filter min area.
    6. free_mask = ROI pixels not occupied; occupied_mask = occupied uint8.
    7. bands dict: near_frac, mid_frac, far_frac over ROI.
    8. Set method="near_field_bands", depth_kind=kind, units="ordinal" always for v1 even if kind is metric_estimated (research open Q4 — no meters without calibration). Never emit distance_m.

    smoothing.py — `OccupancySmoother` (or module functions + class) holding last EMA float mask:
    - `smooth_occupancy(occupied_u8, *, alpha=0.35)` → morphology then EMA then re-threshold 0.5
    - Loop-owned state only (not store)

    Wave 0 tests first (RED) then GREEN. Use synthetic maps only — no model weights.
  </action>
  <verify>
    <automated>uv run pytest tests/test_free_space_bands.py tests/test_free_space_smoothing.py -q</automated>
  </verify>
  <done>Pure free-space + smoothing pass synthetic golden tests; no ML imports; ordinal honesty locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FreeSpaceProduct store half + FreeSpaceLoop</name>
  <files>src/sentry_ai/state/perception_store.py, src/sentry_ai/spatial/loop.py, tests/test_perception_store.py, tests/test_free_space_loop.py</files>
  <behavior>
    - set_free_space + snapshot_free_space keep-latest; snapshot isolates obstacles list and bands dict; masks may share array ref (immutable after set)
    - metrics: free_space_frames, free_space_frames_dropped, free_space_fps, last_free_space_latency_ms
    - FreeSpaceLoop starts/stops idempotently; processes synthetic DepthProduct from store without FrameBus
    - Skips when depth None / error / depth_map None / same frame_id; records free_space drop on frame_id gaps
    - Exception in compute keeps thread alive and writes product with error string and empty obstacles
    - Parent depth frame_id/camera_id/t_capture copied onto FreeSpaceProduct
  </behavior>
  <read_first>
    - src/sentry_ai/state/perception_store.py (DepthProduct dual half)
    - src/sentry_ai/models/depth/loop.py (daemon lifecycle twin)
    - .planning/phases/05-free-space-unified-stream/05-PATTERNS.md (FreeSpaceLoop + FreeSpaceProduct sections)
    - tests/test_depth_loop.py (wait_until / start-stop patterns)
    - tests/test_perception_store.py (depth store tests to mirror)
  </read_first>
  <action>
    Extend PerceptionStore to triple product (per D-store extend one store):
    - FreeSpaceProduct dataclass: frame_id, camera_id, t_capture, t_compute, latency_ms, depth_kind, obstacle_count, obstacles, bands, free_mask, occupied_mask, method="near_field_bands", error
    - set_free_space / record_free_space_drop / snapshot_free_space under same `_lock` as det/depth
    - StoreMetrics gains free_space_* fields; metrics_snapshot copies them
    - Det/depth APIs must remain backward compatible (existing tests green)

    FreeSpaceLoop(store) — structural twin of DepthLoop but polls snapshot_depth only (per D-FreeSpaceLoop separate):
    - Thread name "free-space"
    - On new depth: OccupancySmoother + compute_free_space → set_free_space
    - Never opens cameras, never reads FrameBus, never imports torch/HF
    - Tests inject store.set_depth with synthetic depth_map arrays only

    SPACE-02 foundation: FreeSpaceProduct.obstacles + obstacle_count are machine-readable in-process fields ready for 05-02 wire payload.
  </action>
  <verify>
    <automated>uv run pytest tests/test_perception_store.py tests/test_free_space_loop.py -q</automated>
  </verify>
  <done>Store free-space half + FreeSpaceLoop process synthetic depth into FreeSpaceProduct; existing det/depth store tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: draw_free_space pure overlay helper</name>
  <files>src/sentry_ai/spatial/overlay.py, tests/test_free_space_overlay.py</files>
  <behavior>
    - draw_free_space returns ndarray same shape/dtype as input; does not mutate image or masks
    - free_mask pixels tint cool/green semi-transparent; occupied/near tint amber-red
    - Empty masks / None free still returns a copy (content-equivalent when both masks None)
    - No safe/go/nogo text painted on image
  </behavior>
  <read_first>
    - src/sentry_ai/models/detection/overlay.py
    - src/sentry_ai/models/depth/colormap.py (addWeighted alpha pattern)
    - .planning/phases/05-free-space-unified-stream/05-UI-SPEC.md (visual rules)
    - .planning/phases/05-free-space-unified-stream/05-PATTERNS.md (draw_free_space section)
  </read_first>
  <action>
    Implement pure `draw_free_space(image_bgr, free_mask=None, occupied_mask=None, obstacles=None, *, alpha=0.35)` per UI-SPEC:
    - Semi-transparent green/cool free; amber-red occupied
    - Optional obstacle bbox outlines from obstacle cues (no confidence labels that imply safety)
    - Copy-in/copy-out only; resize masks to image if shape mismatch (like blend_depth)
    - Module must not import models or run free-space math — draw only

    MJPEG wiring deferred to 05-03; this task only ships the pure helper + unit tests so 05-03 can call it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_free_space_overlay.py tests/test_free_space_bands.py tests/test_free_space_smoothing.py tests/test_free_space_loop.py tests/test_perception_store.py -q</automated>
  </verify>
  <done>draw_free_space pure helper green; full 05-01 test set green; ruff clean on spatial + store changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DepthProduct.depth_map → Spatial Post | Untrusted numeric map (noise, polarity mistakes) becomes occupancy semantics |
| FreeSpaceProduct → later wire/UI | Obstacle cues may be consumed by robots; honesty fields must travel with product |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | free_space.py units/depth_kind | mitigate | units=ordinal + depth_kind copy; forbid distance_m on relative path; tests lock honesty |
| T-05-02 | Spoofing | FreeSpaceProduct age (later) | accept | Freshness/TTL is 05-02 assembler; this plan only produces t_compute timestamps |
| T-05-03 | Elevation | FreeSpaceLoop exceptions | mitigate | try/except writes error product; thread stays alive; never crashes serve |
| T-05-04 | Information disclosure | free_mask bulk | mitigate | masks stay in-process; no serialization in this plan |
| T-05-05 | Denial of service | FreeSpaceLoop | mitigate | keep-latest skip same frame_id; short Event.wait; no unbounded queue |
| T-05-SC | Tampering | package installs | accept | No new packages; reuse numpy/opencv already in core |
</threat_model>

<verification>
uv run pytest tests/test_free_space_bands.py tests/test_free_space_smoothing.py tests/test_free_space_overlay.py tests/test_free_space_loop.py tests/test_perception_store.py -q
uv run ruff check src/sentry_ai/spatial src/sentry_ai/state/perception_store.py tests/test_free_space*.py tests/test_perception_store.py
</verification>

<success_criteria>
1. Synthetic depth → FreeSpaceProduct with obstacles + masks without ML weights
2. FreeSpaceLoop is sole Spatial Post owner (separate thread; not DepthLoop)
3. Temporal smoothing reduces single-frame speckles (tests prove morphology/EMA path)
4. Ordinal honesty: no distance_m / meter claims from relative depth free-space
5. Existing detection + depth store tests remain green
</success_criteria>

<output>
Create `.planning/phases/05-free-space-unified-stream/05-01-SUMMARY.md` when done
</output>
