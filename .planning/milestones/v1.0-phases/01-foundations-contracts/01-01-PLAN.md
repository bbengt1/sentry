---
phase: 01-foundations-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - README.md
  - LICENSE
  - uv.lock
  - src/sentry_ai/__init__.py
  - src/sentry_ai/__main__.py
  - src/sentry_ai/cli.py
  - tests/conftest.py
  - tests/test_cli_smoke.py
  - tests/test_schemas_frame.py
  - tests/test_schemas_depth_kind.py
  - tests/test_schemas_perception.py
  - tests/test_config_profiles.py
  - tests/test_plugins_registry.py
  - tests/test_backend_protocols.py
  - tests/test_third_party_models_doc.py
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - FOUND-01
user_setup: []
must_haves:
  truths:
    - "Developer can install the package with uv and import sentry_ai"
    - "Developer can run `uv run sentry health` and get exit code 0 with version printed"
    - "README documents one-command local start (`uv sync` then `uv run sentry smoke`)"
    - "Distribution name is sentry-ai (not PyPI sentry); import package is sentry_ai"
    - "Wave 0 pytest files exist so later plans can turn stubs green without inventing paths"
  artifacts:
    - path: "pyproject.toml"
      provides: "Installable package metadata, scripts, deps, ruff/pytest config"
      contains: "name = \"sentry-ai\""
    - path: "src/sentry_ai/__init__.py"
      provides: "Import package with __version__"
      exports: ["__version__"]
    - path: "src/sentry_ai/cli.py"
      provides: "Typer app with health and smoke subcommands"
      exports: ["app", "main"]
    - path: "README.md"
      provides: "One-command install/start and getsentry disambiguation"
      contains: "uv run sentry"
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures placeholder for synthetic frames"
    - path: ".github/workflows/ci.yml"
      provides: "CI smoke: Python 3.11, uv sync, ruff, pytest"
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "sentry_ai.cli:app"
      via: "console_scripts entry"
      pattern: "sentry\\s*=\\s*\"sentry_ai\\.cli"
    - from: "src/sentry_ai/__main__.py"
      to: "sentry_ai.cli.main"
      via: "python -m sentry_ai"
      pattern: "main"
    - from: "README.md"
      to: "uv run sentry smoke"
      via: "documented one-command start"
      pattern: "uv run sentry"
---

<objective>
Create the installable Sentry AI package skeleton: `sentry-ai` / `sentry_ai` layout, uv+hatchling tooling, Typer CLI health/smoke stubs, README one-command start, Wave 0 test file stubs, and CI.

Purpose: FOUND-01 foundation so every later plan imports a real installed package and runs automated checks without hardware or ML deps.

Output: Editable install works; `uv run sentry health` exits 0; pytest discovers Wave 0 stubs; CI workflow present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-contracts/01-CONTEXT.md
@.planning/phases/01-foundations-contracts/01-RESEARCH.md
@.planning/phases/01-foundations-contracts/01-VALIDATION.md

## Locked naming (do not re-litigate)

| Surface | Value |
|---------|-------|
| PyPI / dist name | `sentry-ai` (NOT `sentry` — collision with getsentry) |
| Import package | `sentry_ai` under `src/sentry_ai/` |
| CLI entry | `sentry` (also `python -m sentry_ai`) |
| Python | 3.11 (`.python-version`) |
| Build | hatchling |
| Phase 1 deps only | pydantic≥2.13, pydantic-settings≥2.15, pyyaml≥6, typer≥0.27 |
| Dev deps | pytest≥8, ruff≥0.8 |
| Forbidden Phase 1 deps | torch, opencv, fastapi, numpy, ultralytics |

## Package legitimacy

All Phase 1 packages are mature ecosystem libs marked `[ASSUMED]` in RESEARCH (slopcheck unavailable). No exotic packages. Proceed with install; keep supply-chain threat T-1-SC documented.

## Out of scope this plan

Schemas, config YAML, plugin registry, THIRD_PARTY_MODELS.md, real synthetic Frame validation in smoke (stubs only — full smoke in 01-03).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package scaffold and tooling config</name>
  <files>pyproject.toml, .python-version, LICENSE, src/sentry_ai/__init__.py</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md (Standard Stack, Pattern 1 pyproject illustrative block, Package naming table)
    - .planning/phases/01-foundations-contracts/01-CONTEXT.md (product identity, stack direction)
    - .planning/REQUIREMENTS.md (FOUND-01)
  </read_first>
  <action>
    Create an installable src-layout package with these exact values (per research + FOUND-01):

    1. `.python-version` containing `3.11`.
    2. `LICENSE` — Apache-2.0 text for application code (research recommendation A1; product SPDX not user-locked; use Apache-2.0).
    3. `pyproject.toml`:
       - build-system: hatchling≥1.26, backend `hatchling.build`
       - `[project] name = "sentry-ai"`, version `0.1.0`, `requires-python = ">=3.11"`, license SPDX Apache-2.0
       - description: camera-only perception for maker robotics
       - dependencies ONLY: `pydantic>=2.13,<3`, `pydantic-settings>=2.15,<3`, `pyyaml>=6.0.3`, `typer>=0.27`
       - optional-dependencies.dev: `pytest>=8`, `ruff>=0.8`
       - `[project.scripts] sentry = "sentry_ai.cli:app"` — Typer app must be invokable as console script (use `typer.run` pattern or expose `app` as Typer instance with `if __name__` / callback so hatch entry works; prefer `app = typer.Typer(...)` and set script to a `main` function if needed for Click compatibility — research shows `sentry = "sentry_ai.cli:app"`; if hatchling requires a callable, use `sentry = "sentry_ai.cli:main"` and implement `main()` that invokes the Typer app)
       - `[tool.hatch.build.targets.wheel] packages = ["src/sentry_ai"]` (or hatchling standard packages discovery for src layout)
       - `[tool.pytest.ini_options] testpaths = ["tests"]` — do NOT add `pythonpath = ["src"]` hacks; rely on editable install
       - `[tool.ruff]` target py311, src = ["src", "tests"]
    4. `src/sentry_ai/__init__.py` exports `__version__ = "0.1.0"` (keep in sync with pyproject version for Phase 1).
    5. Do NOT create schemas/, config/, plugins/, backend/, policy/ modules yet (plans 01-02 and 01-03).
    6. Do NOT add torch/opencv/fastapi/numpy.

    Then run `uv lock` / `uv sync --all-extras` (or `uv sync --extra dev`) from repo root so `uv.lock` is generated.
  </action>
  <verify>
    <automated>test -f pyproject.toml &amp;&amp; test -f .python-version &amp;&amp; test -f src/sentry_ai/__init__.py &amp;&amp; grep -q 'name = "sentry-ai"' pyproject.toml &amp;&amp; ! grep -E 'torch|opencv|fastapi' pyproject.toml &amp;&amp; uv sync --extra dev &amp;&amp; uv run python -c "import sentry_ai; assert sentry_ai.__version__"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml project name is exactly `sentry-ai` (not `sentry`)
    - `import sentry_ai` works after `uv sync --extra dev`
    - No torch, opencv, or fastapi in project dependencies
    - `.python-version` is `3.11`
    - `uv.lock` exists after sync
  </acceptance_criteria>
  <done>Installable `sentry-ai` package imports as `sentry_ai` with version set; tooling configured; lockfile present.</done>
</task>

<task type="auto">
  <name>Task 2: CLI skeleton, README, and Wave 0 test stubs</name>
  <files>src/sentry_ai/cli.py, src/sentry_ai/__main__.py, README.md, tests/conftest.py, tests/test_cli_smoke.py, tests/test_schemas_frame.py, tests/test_schemas_depth_kind.py, tests/test_schemas_perception.py, tests/test_config_profiles.py, tests/test_plugins_registry.py, tests/test_backend_protocols.py, tests/test_third_party_models_doc.py</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md (Pattern 6 Smoke CLI, Validation Architecture Wave 0 Gaps)
    - .planning/phases/01-foundations-contracts/01-VALIDATION.md (Wave 0 Requirements, Manual-Only Verifications)
    - src/sentry_ai/__init__.py (after Task 1)
    - pyproject.toml (scripts entry after Task 1)
  </read_first>
  <action>
    1. Implement `src/sentry_ai/cli.py` with Typer app name `sentry`, help text "Sentry AI — camera-only perception":
       - Command `health`: print package version from `sentry_ai.__version__`, default profile name `cpu-fallback` (string only; no config load yet), and a short "ok" status; exit 0.
       - Command `smoke`: Phase 1 skeleton only — print that synthetic smoke will validate frames (or "smoke skeleton ok") and exit 0. Full Frame/PerceptionFrame validation is plan 01-03; do not import missing schema modules.
       - Expose `main()` that runs the Typer app so `[project.scripts]` and `__main__` work.
    2. `src/sentry_ai/__main__.py` calls `main()` so `python -m sentry_ai` works.
    3. `README.md` minimum sections:
       - Product one-liner: camera-only perception for maker robotics (perception stream only — no motor commands)
       - Naming note: PyPI package is `sentry-ai`; this is not the getsentry error-tracking product; import `sentry_ai`
       - Requirements: Python 3.11+, `uv`
       - One-command local start:
         ```
         uv sync --extra dev
         uv run sentry smoke
         ```
         Also document `uv run sentry health` and `python -m sentry_ai health`
       - Phase 1 scope note: contracts + smoke; camera/models come later
    4. Wave 0 test scaffolding (Nyquist) — create all files from VALIDATION.md Wave 0 list:
       - `tests/conftest.py`: empty or comment that synthetic Frame fixtures land in 01-02; may define a placeholder pytest fixture later plans extend.
       - `tests/test_cli_smoke.py`: real tests using Typer CliRunner or subprocess:
         - `uv run sentry health` / CliRunner invoke health → exit 0, output contains version
         - smoke command exits 0 (skeleton acceptable)
       - Remaining test modules as **collectable stubs** that will be filled by 01-02/01-03 — each file must either:
         (a) contain a single `pytest.mark.skip(reason="implemented in plan 01-0N")` test, OR
         (b) empty module with a docstring listing intended coverage (prefer skip markers so suite stays green)
       Stub map:
       - `test_schemas_frame.py`, `test_schemas_depth_kind.py`, `test_schemas_perception.py` → skip until 01-02
       - `test_config_profiles.py` → skip until 01-02
       - `test_plugins_registry.py`, `test_backend_protocols.py`, `test_third_party_models_doc.py` → skip until 01-03
    5. Ensure `uv run pytest -q` is green after this task (skips OK; health/smoke tests must pass).
  </action>
  <verify>
    <automated>uv run sentry health &amp;&amp; uv run sentry smoke &amp;&amp; uv run python -m sentry_ai health &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run sentry health` exits 0 and prints version string matching `0.1.0`
    - `uv run sentry smoke` exits 0
    - `python -m sentry_ai health` works
    - README contains `uv run sentry smoke` and disambiguation of `sentry-ai` vs getsentry
    - All Wave 0 test paths listed in VALIDATION.md exist under `tests/`
    - `uv run pytest -q` exits 0 (passing + skipped only)
  </acceptance_criteria>
  <done>CLI health/smoke skeleton works; README documents one-command start; Wave 0 test files exist; full pytest green.</done>
</task>

<task type="auto">
  <name>Task 3: CI workflow for ruff + pytest on Python 3.11</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md (Validation Architecture, CI recommendation)
    - pyproject.toml
    - tests/test_cli_smoke.py
  </read_first>
  <action>
    Create `.github/workflows/ci.yml`:
    - Trigger: push and pull_request
    - Job on ubuntu-latest
    - Setup Python 3.11
    - Install uv (astral-sh/setup-uv or official install script)
    - `uv sync --extra dev`
    - `uv run ruff check src tests`
    - `uv run pytest -q`
    - Optionally `uv run sentry health` as smoke step
    - No GPU, no model download, no camera
    - Do not require secrets

    Fix any ruff issues introduced by Tasks 1–2 so CI would pass locally: `uv run ruff check src tests`.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml &amp;&amp; uv run ruff check src tests &amp;&amp; uv run pytest -q &amp;&amp; uv run sentry health</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists and references Python 3.11, uv, ruff, pytest
    - Local `ruff check src tests` exits 0
    - Local full pytest still green
  </acceptance_criteria>
  <done>CI workflow committed content-ready; local lint+tests match CI commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer shell → installed package | Untrusted environment; only local CLI in Phase 1 |
| PyPI / lockfile → runtime | Dependency supply chain for install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Spoofing | Package naming / README | mitigate | Dist name `sentry-ai` only; README disambiguates from getsentry `sentry` (Task 1–2) |
| T-1-05 | Elevation of privilege (product misuse) | README product language | mitigate | Perception-only wording; no motor/control claims (Task 2 README) |
| T-1-SC | Tampering | uv/pypi installs | accept | Well-known deps only (`[ASSUMED]` audit); pin via `uv.lock`; no exotic packages |
| T-1-01 | Tampering | YAML config | accept (N/A this plan) | Deferred to 01-02 config loader |
| T-1-03 | Tampering | Model licenses | accept (N/A this plan) | Deferred to 01-03 THIRD_PARTY_MODELS.md |
| T-1-04 | Information disclosure | Cloud inference flag | accept (N/A this plan) | Deferred to 01-02 `allow_cloud` default false |
</threat_model>

<verification>
- [ ] `uv sync --extra dev` succeeds on Python 3.11
- [ ] `import sentry_ai` works
- [ ] `uv run sentry health` exit 0
- [ ] `uv run sentry smoke` exit 0
- [ ] `uv run pytest -q` green (skips allowed for later plans)
- [ ] `uv run ruff check src tests` green
- [ ] README documents one-command start
- [ ] Dist name is `sentry-ai`, not `sentry`
</verification>

<success_criteria>
FOUND-01 satisfied for scaffold: installable package, CLI entry, documented one-command path, CI smoke skeleton, Wave 0 tests present. No ML/camera dependencies introduced.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-contracts/01-01-SUMMARY.md` when done
</output>
