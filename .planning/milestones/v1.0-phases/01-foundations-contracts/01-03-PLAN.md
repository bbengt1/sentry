---
phase: 01-foundations-contracts
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - pyproject.toml
  - src/sentry_ai/plugins/__init__.py
  - src/sentry_ai/plugins/protocols.py
  - src/sentry_ai/plugins/registry.py
  - src/sentry_ai/plugins/builtins.py
  - src/sentry_ai/backend/__init__.py
  - src/sentry_ai/backend/protocols.py
  - src/sentry_ai/backend/null.py
  - src/sentry_ai/policy/models.py
  - src/sentry_ai/cli.py
  - THIRD_PARTY_MODELS.md
  - tests/test_plugins_registry.py
  - tests/test_backend_protocols.py
  - tests/test_third_party_models_doc.py
  - tests/test_cli_smoke.py
autonomous: true
requirements:
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - MODEL-01
user_setup: []
must_haves:
  truths:
    - "Plugin registry lists at least one source, one worker, and one sink stub"
    - "Entry-point groups sentry_ai.sources|workers|sinks are declared for discovery"
    - "InferenceBackend Protocol and NullBackend stub are importable without torch"
    - "THIRD_PARTY_MODELS.md documents DAV2 Small Apache-2.0 as default and AGPL/NC as non-default"
    - "uv run sentry smoke builds synthetic Frames, validates PerceptionFrame, exits 0"
    - "Core path remains local OSS only (no cloud required for smoke)"
  artifacts:
    - path: "src/sentry_ai/plugins/registry.py"
      provides: "PluginRegistry register/get/discover"
      exports: ["PluginRegistry"]
    - path: "src/sentry_ai/plugins/protocols.py"
      provides: "CameraSource, ModelWorker, Sink Protocols"
    - path: "src/sentry_ai/plugins/builtins.py"
      provides: "SyntheticSource, NoopWorker, NullSink"
    - path: "src/sentry_ai/backend/protocols.py"
      provides: "InferenceBackend Protocol, DeviceInfo"
    - path: "src/sentry_ai/backend/null.py"
      provides: "NullBackend stub"
      exports: ["NullBackend"]
    - path: "THIRD_PARTY_MODELS.md"
      provides: "License table for third-party model weights"
      contains: "Apache-2.0"
    - path: "src/sentry_ai/cli.py"
      provides: "Full smoke command using synthetic frames"
  key_links:
    - from: "pyproject.toml entry-points"
      to: "sentry_ai.plugins.builtins"
      via: "sentry_ai.sources|workers|sinks groups"
      pattern: "sentry_ai\\.(sources|workers|sinks)"
    - from: "cli.smoke"
      to: "schemas.Frame / PerceptionFrame"
      via: "construct synthetic frames and validate"
      pattern: "PerceptionFrame|make_synthetic|smoke"
    - from: "tests/test_third_party_models_doc.py"
      to: "THIRD_PARTY_MODELS.md"
      via: "file presence + license string asserts"
      pattern: "Apache-2.0|AGPL|CC-BY-NC"
---

<objective>
Ship plugin registry stubs with hybrid entry-point discovery, device/backend protocol stubs, model license documentation, and complete the one-command synthetic smoke path.

Purpose: FOUND-04, FOUND-05, FOUND-06 (device/backend stubs), MODEL-01 documentation — extensibility and license honesty without implementing cameras or inference.

Output: Registry + builtins + NullBackend; THIRD_PARTY_MODELS.md; `uv run sentry smoke` validates synthetic PerceptionFrames and exits 0; full phase test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-contracts/01-CONTEXT.md
@.planning/phases/01-foundations-contracts/01-RESEARCH.md
@.planning/phases/01-foundations-contracts/01-VALIDATION.md
@.planning/phases/01-foundations-contracts/01-01-PLAN.md
@.planning/phases/01-foundations-contracts/01-02-PLAN.md

## Depends on 01-01 and 01-02

- Installable CLI package
- Frame, PerceptionFrame, DepthKind, Completeness, RuntimeProfile
- Config profiles + allow_cloud false
- Policy constants

## Locked plugin/backend design

- Hybrid registry: in-tree register + `importlib.metadata.entry_points` discovery
- Entry point groups: `sentry_ai.sources`, `sentry_ai.workers`, `sentry_ai.sinks`
- Builtins: `synthetic` source, `noop` worker, `null` sink
- `InferenceBackend` Protocol + `NullBackend` only — no torch/onnxruntime execution
- Smoke: synthetic frames only, no OpenCV/camera

## Out of scope

USB/RTSP capture, Frame Bus, FastAPI, real YOLO/DAV2, TensorRT, ROS2 implementation.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plugin protocols, registry, builtins, entry points</name>
  <files>src/sentry_ai/plugins/__init__.py, src/sentry_ai/plugins/protocols.py, src/sentry_ai/plugins/registry.py, src/sentry_ai/plugins/builtins.py, pyproject.toml, tests/test_plugins_registry.py</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md (Pattern 3 Hybrid plugin registry, entry point pyproject block)
    - .planning/REQUIREMENTS.md (FOUND-04)
    - src/sentry_ai/schemas/frame.py
    - pyproject.toml
    - tests/test_plugins_registry.py
  </read_first>
  <behavior>
    - PluginRegistry after register_builtins (or discover) has source named "synthetic"
    - Registry has worker "noop" and sink "null"
    - get_source("synthetic") returns a class/type that can open/read/close without hardware
    - Synthetic read returns a schema-valid Frame (or object convertible to Frame) with camera_id set
    - Duplicate register of same name raises ValueError
    - discover() loads entry points from groups without crashing when builtins already registered (idempotent or skip duplicates — document choice; prefer skip-if-present for entry points that re-declare builtins)
  </behavior>
  <action>
    1. `plugins/protocols.py` — typing.Protocol (runtime_checkable optional):
       - `CameraSource`: name: str; open() -> None; read() -> Frame (or Frame-compatible); close() -> None
       - `ModelWorker`: name: str; process(frame: Frame) -> object | None (Phase 1 noop)
       - `Sink`: name: str; emit(item: object) -> None; close() -> None

    2. `plugins/builtins.py`:
       - `SyntheticSource`: yields/increments frame_id; camera_id default "synthetic0"; constructs real `Frame` via schemas (uses time.time()); no OpenCV/numpy image payload required
       - `NoopWorker`: process returns None or empty PerceptionFrame shell without models
       - `NullSink`: emit discards

    3. `plugins/registry.py` — `PluginRegistry` class:
       - Internal dicts for sources, workers, sinks
       - register_source/worker/sink(name, cls) with duplicate ValueError
       - get_source/get_worker/get_sink
       - list_sources/list_workers/list_sinks
       - discover() using `importlib.metadata.entry_points` for groups `sentry_ai.sources`, `sentry_ai.workers`, `sentry_ai.sinks` (Python 3.11 API: entry_points().select(group=...) or entry_points(group=...))
       - `register_builtins(registry)` helper that registers synthetic/noop/null without requiring install entry points

    4. Update `pyproject.toml`:
       ```
       [project.entry-points."sentry_ai.sources"]
       synthetic = "sentry_ai.plugins.builtins:SyntheticSource"
       [project.entry-points."sentry_ai.workers"]
       noop = "sentry_ai.plugins.builtins:NoopWorker"
       [project.entry-points."sentry_ai.sinks"]
       null = "sentry_ai.plugins.builtins:NullSink"
       ```

    5. Tests: replace skip stubs; cover register, list, synthetic Frame validity, duplicate error, discover after `uv sync` (entry points available in editable install).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_plugins_registry.py</automated>
  </verify>
  <acceptance_criteria>
    - Registry exposes synthetic, noop, null
    - SyntheticSource.read() returns Frame with camera_id and frame_id
    - pyproject.toml declares all three entry-point groups
    - Duplicate registration raises ValueError
  </acceptance_criteria>
  <done>FOUND-04 plugin registry stubs with hybrid discovery and synthetic source producing valid Frames.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: InferenceBackend Protocol, DeviceInfo, NullBackend stubs</name>
  <files>src/sentry_ai/backend/__init__.py, src/sentry_ai/backend/protocols.py, src/sentry_ai/backend/null.py, tests/test_backend_protocols.py</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md (Pattern 5 Device/backend protocols)
    - .planning/REQUIREMENTS.md (FOUND-06)
    - src/sentry_ai/schemas/enums.py (BackendName, RuntimeProfile)
    - tests/test_backend_protocols.py
  </read_first>
  <behavior>
    - InferenceBackend protocol is importable; NullBackend implements load/infer/close
    - NullBackend.infer records call or returns None without importing torch
    - DeviceInfo model accepts profile=cpu-fallback, backend=cpu, device_id="cpu", available=False
    - isinstance(NullBackend(), InferenceBackend) works if runtime_checkable; otherwise structural use is documented and methods exist
  </behavior>
  <action>
    1. `backend/protocols.py`:
       - Reuse or import `BackendName`, `RuntimeProfile` from schemas.enums
       - `@runtime_checkable class InferenceBackend(Protocol)`: name: BackendName; load() -> None; infer(tensor: Any) -> Any; close() -> None
       - `DeviceInfo` Pydantic model: profile: RuntimeProfile, backend: BackendName, device_id: str, available: bool = False
       - Optional helper `probe_device(...)` stub that returns DeviceInfo with available=False without touching CUDA

    2. `backend/null.py` — `NullBackend`:
       - name = BackendName.CPU (or dedicated null — use CPU)
       - load/close no-op
       - infer returns None and increments a call counter attribute for tests
       - No torch/onnx imports

    3. Export from `backend/__init__.py`

    4. Tests: import paths, NullBackend lifecycle, DeviceInfo validation, assert torch not required (`importlib.util.find_spec` not needed if simply never imported).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_backend_protocols.py &amp;&amp; uv run python -c "from sentry_ai.backend import NullBackend; from sentry_ai.backend.protocols import InferenceBackend, DeviceInfo; b=NullBackend(); b.load(); assert b.infer(None) is None; b.close()"</automated>
  </verify>
  <acceptance_criteria>
    - NullBackend and InferenceBackend import without ML deps
    - DeviceInfo works for all three RuntimeProfile values with available=False
    - test_backend_protocols.py fully green (no skips)
  </acceptance_criteria>
  <done>FOUND-06 device/backend abstraction stubs exist for desktop-gpu/jetson/cpu-fallback profiles without real inference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: THIRD_PARTY_MODELS.md, policy polish, full smoke CLI</name>
  <files>THIRD_PARTY_MODELS.md, src/sentry_ai/policy/models.py, src/sentry_ai/cli.py, tests/test_third_party_models_doc.py, tests/test_cli_smoke.py, README.md</files>
  <read_first>
    - .planning/phases/01-foundations-contracts/01-RESEARCH.md (Pattern 7 THIRD_PARTY_MODELS.md table, Pattern 6 smoke, MODEL-01 policy text)
    - .planning/REQUIREMENTS.md (FOUND-05, MODEL-01)
    - src/sentry_ai/cli.py
    - src/sentry_ai/plugins/builtins.py
    - src/sentry_ai/schemas/perception.py
    - tests/test_third_party_models_doc.py
    - tests/test_cli_smoke.py
  </read_first>
  <behavior>
    - THIRD_PARTY_MODELS.md exists at repo root
    - File contains Apache-2.0 and Depth Anything V2 Small as default Yes
    - File mentions AGPL (Ultralytics) and NC or CC-BY-NC as non-default / research-only
    - sentry smoke with N frames exits 0
    - smoke constructs PerceptionFrame instances that validate
    - health lists version and can mention profile/plugins without crashing
    - No cloud API key required for smoke
  </behavior>
  <action>
    1. Write `THIRD_PARTY_MODELS.md` at repo root with minimum columns: Model/weights | Role | License | Default? | Notes
       Required rows/content (FOUND-05):
       - Depth Anything V2 **Small** — Depth — Apache-2.0 — **Yes** — commercially friendly default
       - Depth Anything V2 Base/Large/Giant — Depth — CC-BY-NC-4.0 — **No** — research-only
       - YOLO26 via Ultralytics — Fixed detect — AGPL-3.0 — Planned Phase 3 / non-default commercial caution
       - YOLOE — Open-vocab — AGPL-3.0 — Planned Phase 6
       - Policy section: core inference path local OSS only; no cloud API keys for default install/smoke; allow_cloud false default

    2. Align `policy/models.py` with doc: default commercially friendly weight id for depth small; `DEFAULT_ALLOW_CLOUD = False`; optional tuple of non-default license tags ("AGPL-3.0", "CC-BY-NC-4.0") for documentation hooks — no downloads.

    3. Upgrade `cli.py`:
       - `health`: print version, active/default profile (cpu-fallback or --profile), list registered plugin names (sources/workers/sinks via PluginRegistry + register_builtins), schema_version=1
       - `smoke`: options `--frames` default 3, `--profile` default cpu-fallback
         - load profile config (assert allow_cloud is false; do not call cloud)
         - open SyntheticSource, read N frames, wrap each into PerceptionFrame with completeness all false (or depth false), model_validate path
         - optional: pass through NoopWorker + NullSink
         - print summary count; exit 0 on success; non-zero on ValidationError
       - Still no camera, no torch

    4. Tests:
       - `test_third_party_models_doc.py`: path exists; content assertions for "Apache-2.0", "Small", "AGPL", and NC/CC-BY-NC; Default Yes only for commercially friendly small depth
       - `test_cli_smoke.py`: CliRunner/subprocess smoke exits 0; health exits 0; optional assert "synthetic" appears in health plugin list

    5. README: ensure one-command start still accurate; note smoke validates synthetic PerceptionFrames; link to THIRD_PARTY_MODELS.md for licenses.

    6. Run full suite: `uv run pytest -q`, `uv run ruff check src tests`, `uv run sentry smoke`, `uv run sentry health`.
  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run ruff check src tests &amp;&amp; uv run sentry smoke &amp;&amp; uv run sentry health &amp;&amp; test -f THIRD_PARTY_MODELS.md</automated>
  </verify>
  <acceptance_criteria>
    - THIRD_PARTY_MODELS.md documents DAV2 Small Apache-2.0 as default and AGPL/NC as non-default
    - `uv run sentry smoke` exits 0 using synthetic Frames → PerceptionFrame validation
    - `uv run pytest -q` fully green with no remaining plan-skip stubs for Phase 1 Wave 0 files
    - Full phase requirements FOUND-04, FOUND-05, FOUND-06 device stubs, MODEL-01 docs satisfied
    - No torch/opencv/fastapi added to dependencies
  </acceptance_criteria>
  <done>Phase 1 contracts complete: plugins, backends, licenses, and synthetic smoke one-command path all verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entry point discovery → plugin load | Third-party installed packages could register plugins later |
| CLI args → smoke loop | Untrusted frame count/profile strings |
| Docs → operator trust | License mislabeling could cause commercial misuse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Tampering / legal | THIRD_PARTY_MODELS.md + policy | mitigate | Document Apache default; AGPL/NC non-default; tests for key strings (Task 3) |
| T-1-04 | Information disclosure | smoke/health config | mitigate | Smoke loads profile with allow_cloud false; no cloud calls (Task 3) |
| T-1-05 | Elevation / misuse | schemas + CLI language | mitigate | Perception-only smoke; no command fields; health text stays perception product |
| T-1-01 | Tampering | profile load in CLI | mitigate | Reuse 01-02 safe_load + Pydantic (Task 3 smoke) |
| T-1-02 | Spoofing | package identity | accept (done 01-01) | Unchanged |
| Plugin entry points | Tampering | discover() | accept (Phase 1) | Only load declared groups; builtins registered in-tree; future phases may pin trust |
| T-1-SC | Tampering | deps | accept | No new exotic packages; still pydantic/typer/pyyaml only |
</threat_model>

<verification>
- [ ] `uv run pytest -q` all Phase 1 tests green (no Wave 0 skips left)
- [ ] `uv run sentry smoke` exit 0
- [ ] `uv run sentry health` shows version + plugins
- [ ] THIRD_PARTY_MODELS.md present with Apache default + AGPL/NC notes
- [ ] Plugin registry has synthetic/noop/null
- [ ] NullBackend works without torch
- [ ] ruff clean
</verification>

<success_criteria>
Phase 1 goal achieved: installable package with schemas, plugins, licenses, multi-profile hooks, backend stubs, and synthetic smoke. All FOUND-01..06 and MODEL-01 satisfied across plans 01-01..01-03.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-contracts/01-03-SUMMARY.md` when done
</output>
