---
phase: 06-developer-controls-open-vocab
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/sentry_ai/schemas/perception.py
  - src/sentry_ai/models/detection/yoloe_worker.py
  - src/sentry_ai/models/detection/open_vocab_loop.py
  - src/sentry_ai/models/detection/overlay.py
  - src/sentry_ai/models/cache.py
  - src/sentry_ai/state/perception_store.py
  - src/sentry_ai/api/assemble.py
  - src/sentry_ai/api/routes_open_vocab.py
  - src/sentry_ai/api/app.py
  - src/sentry_ai/api/deps.py
  - src/sentry_ai/api/routes_preview.py
  - src/sentry_ai/capture/status.py
  - src/sentry_ai/cli.py
  - src/sentry_ai/ui/static/index.html
  - THIRD_PARTY_MODELS.md
  - README.md
  - tests/test_yoloe_worker.py
  - tests/test_open_vocab_loop.py
  - tests/test_api_open_vocab.py
  - tests/test_assemble_open_vocab.py
  - tests/test_detection_overlay.py
  - tests/test_model_cache.py
  - tests/test_third_party_models_doc.py
  - tests/test_cli_serve.py
  - tests/test_api_preview.py
  - tests/test_api_perception_only.py
autonomous: true
requirements:
  - OVD-01
  - OVD-02
  - OVD-03
user_setup: []
must_haves:
  truths:
    - "Open-vocab YOLOE worker accepts text prompts via set_prompt_classes and returns Detection list with source=open_vocab (OVD-01)"
    - "Open-vocab runs off (default), on_demand (Run one-shot), or continuous every_n=3 without sharing DetectionLoop thread (OVD-02)"
    - "Fixed-class DetectionLoop process path remains independent when OV is idle or running"
    - "OpenVocabProduct is a separate store slot; assemble merges fixed then OV detections with source tags (OVD-03)"
    - "MJPEG and /v1 stream/snapshot show open-vocab boxes when OV product present; distinct color/prefix vs fixed-class"
    - "Default mode is off; continuous is opt-in; no dual writers on set_detections"
    - "Unit tests inject FakeModel — never download YOLOE weights in CI"
    - "THIRD_PARTY_MODELS.md marks YOLOE AGPL active with yoloe-26s-seg.pt default"
  artifacts:
    - path: "src/sentry_ai/models/detection/yoloe_worker.py"
      provides: "YoloeOpenVocabWorker with injectable model"
      exports: ["YoloeOpenVocabWorker"]
    - path: "src/sentry_ai/models/detection/open_vocab_loop.py"
      provides: "OpenVocabLoop modes off/on_demand/continuous"
      exports: ["OpenVocabLoop"]
    - path: "src/sentry_ai/api/routes_open_vocab.py"
      provides: "GET/PATCH /api/open-vocab/config + POST /api/open-vocab/run"
      exports: ["router"]
    - path: "src/sentry_ai/state/perception_store.py"
      provides: "OpenVocabProduct + set/snapshot/metrics/clear"
    - path: "src/sentry_ai/api/assemble.py"
      provides: "Merge OV into wire detections with source tags"
    - path: "src/sentry_ai/schemas/perception.py"
      provides: "Detection.source Literal fixed|open_vocab default fixed"
    - path: "src/sentry_ai/ui/static/index.html"
      provides: "Open-vocab prompt + Run + continuous control"
    - path: "THIRD_PARTY_MODELS.md"
      provides: "YOLOE AGPL Phase 6 active"
    - path: "tests/test_yoloe_worker.py"
      provides: "Mock YOLOE set_classes/predict coverage"
    - path: "tests/test_open_vocab_loop.py"
      provides: "Mode scheduling + non-blocking fixed path"
    - path: "tests/test_assemble_open_vocab.py"
      provides: "Merge + completeness coverage"
  key_links:
    - from: "OpenVocabLoop"
      to: "PerceptionStore.set_open_vocab"
      via: "only OV writer — never set_detections"
      pattern: "set_open_vocab"
    - from: "YoloeOpenVocabWorker"
      to: "YOLOE.set_classes + predict"
      via: "dirty flag once per prompt change"
      pattern: "set_classes|set_prompt_classes"
    - from: "assemble_perception_frame"
      to: "snapshot + snapshot_open_vocab"
      via: "fixed first then OV merge"
      pattern: "snapshot_open_vocab|open_vocab"
    - from: "draw_detections / MJPEG"
      to: "Detection.source"
      via: "magenta OV boxes vs cyan fixed"
      pattern: "open_vocab|source"
    - from: "index.html"
      to: "/api/open-vocab/*"
      via: "Run + continuous + prompt"
      pattern: "open-vocab"
---

<objective>
Ship open-vocabulary detection as a secondary local path: YOLOE worker (text prompts), OpenVocabLoop (off / on_demand / continuous every_n=3), separate OpenVocabProduct store slot, assembler merge with Detection.source tags, dual-color overlays, open-vocab API + Live Preview prompt UX, AGPL docs, and serve lifecycle.

Purpose: Close OVD-01, OVD-02, OVD-03 on top of 06-01 control plane without blocking fixed-class detection, depth, or capture.

Output: YOLOE integration via existing detect extra; stream/UI parity for OV detections; default off; tests mock all weights.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-developer-controls-open-vocab/06-CONTEXT.md
@.planning/phases/06-developer-controls-open-vocab/06-RESEARCH.md
@.planning/phases/06-developer-controls-open-vocab/06-PATTERNS.md
@.planning/phases/06-developer-controls-open-vocab/06-UI-SPEC.md
@.planning/phases/06-developer-controls-open-vocab/06-VALIDATION.md
@.planning/phases/06-developer-controls-open-vocab/06-01-PLAN.md
@.planning/phases/05-free-space-unified-stream/05-02-SUMMARY.md
@.planning/phases/05-free-space-unified-stream/05-03-SUMMARY.md
@.planning/phases/03-fixed-class-detection/03-01-SUMMARY.md
@.planning/phases/03-fixed-class-detection/03-02-SUMMARY.md

## Depends on 06-01

- PipelineState + enable gates pattern (OV uses modes, not the three stage flags)
- create_app injection pattern for optional workers/loops
- Live Preview control-row + debounced PATCH patterns
- PerceptionStore clear_* methods (extend with clear_open_vocab)

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| Model | Ultralytics YOLOE via existing `detect` extra — no new package | RESEARCH lock |
| Default weights | `yoloe-26s-seg.pt` desktop; document n for edge | RESEARCH lock |
| Worker pattern | Injectable model; mirror YoloDetectionWorker | PATTERNS |
| Loop | Separate OpenVocabLoop — never dual-write set_detections | RESEARCH lock |
| Modes | off (default) / on_demand / continuous with every_n=3 | RESEARCH lock |
| Store | Fourth product OpenVocabProduct + assembler merge | RESEARCH lock |
| Schema | Additive `Detection.source: Literal["fixed","open_vocab"] = "fixed"` | RESEARCH lock |
| Overlay | Distinct color (magenta BGR 255,0,255); optional `ov:` label prefix | RESEARCH |
| Wire merge | fixed detections first, then OV; completeness.detections true if either present | RESEARCH |
| Boxes only | Ignore YOLOE masks for v1 | RESEARCH |
| set_classes | Only when prompt dirty — not every frame | RESEARCH |
| Prompt limits | ≤32 classes; ≤64 chars each; strip empties | Security domain |
| AGPL | THIRD_PARTY_MODELS + UI first-run note | RESEARCH lock |
| Tests | Mock YOLOE; never download weights | VALIDATION |

## Interfaces to extend

From `YoloDetectionWorker` (mirror structure):
- conf lock, `_ensure_model`, `process(frame) -> list[Detection]`, `resolve_device`
- Use `from ultralytics import YOLOE` and `model.set_classes(classes)` then `predict(...)`
- Reuse `results_to_detections` from mapping.py; re-tag source after map

From DetectionLoop:
- Thread name, keep-latest, error keep-alive → OpenVocabLoop twin writing set_open_vocab only

From assemble_perception_frame:
- snapshot det/depth/free; extend with snapshot_open_vocab
- DEFAULT_TTL_MS — add open_vocab 500.0 or share detections TTL
- stats: ov_latency_ms, ov_fps, ov_age_ms, ov_stale

From overlay:
- `_BOX_COLOR = (0, 255, 180)` fixed; add OV color branch on `source`

From cli serve (after 06-01):
- Start order: capture → det → depth → free_space → open_vocab
- Stop reverse; OV uses same detect extra ImportError soft-disable as fixed YOLO when ultralytics missing
- Default OpenVocabLoop mode off (thread sleeps)

## Out of scope

- Visual-prompt YOLOE / prompt-free PF weights
- Segmentation mask overlays
- React rewrite; source switcher UI
- TensorRT export (Phase 7)
- Always-on continuous OV as default
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Detection.source + YOLOE worker + OpenVocabProduct store + cache/AGPL docs</name>
  <files>src/sentry_ai/schemas/perception.py, src/sentry_ai/models/detection/yoloe_worker.py, src/sentry_ai/state/perception_store.py, src/sentry_ai/models/cache.py, THIRD_PARTY_MODELS.md, tests/test_yoloe_worker.py, tests/test_model_cache.py, tests/test_third_party_models_doc.py</files>
  <read_first>
    - .planning/phases/06-developer-controls-open-vocab/06-RESEARCH.md (Patterns 4–6, YOLOE API, AGPL checklist)
    - .planning/phases/06-developer-controls-open-vocab/06-PATTERNS.md (yoloe_worker, OpenVocabProduct, cache, Detection.source)
    - src/sentry_ai/models/detection/yolo_worker.py
    - src/sentry_ai/models/detection/mapping.py
    - src/sentry_ai/schemas/perception.py
    - src/sentry_ai/state/perception_store.py
    - src/sentry_ai/models/cache.py
    - THIRD_PARTY_MODELS.md
    - tests/test_detection_worker.py (FakeModel pattern)
  </read_first>
  <behavior>
    - Detection.source defaults to "fixed"; accepts "open_vocab"; extra=forbid still holds; existing fixed tests need no class_name changes
    - YoloeOpenVocabWorker.name == "yoloe-open-vocab"; DEFAULT_WEIGHTS yoloe-26s-seg.pt
    - set_prompt_classes strips empties; empty classes → process returns [] without predict
    - set_classes called once when dirty, not on subsequent process with same classes
    - process tags every returned Detection with source="open_vocab"; conf lock applies next process
    - Injected model never downloads weights; missing ultralytics raises clear ImportError mentioning detect extra
    - Store: OpenVocabProduct; set_open_vocab; snapshot_open_vocab isolated copy; clear_open_vocab; ov_fps / last_ov_latency_ms metrics + record_open_vocab_drop
    - KNOWN_WEIGHTS includes yoloe-26s-seg.pt and yoloe-26n-seg.pt
    - THIRD_PARTY_MODELS YOLOE row: Phase 6 active, AGPL-3.0, default weight, non-default commercial caution
  </behavior>
  <action>
    1. Schema: add optional `source: Literal["fixed", "open_vocab"] = "fixed"` on Detection (additive; keep extra=forbid). Do not add motor fields.

    2. Implement YoloeOpenVocabWorker per PATTERNS twin of YoloDetectionWorker:
       - Injectable model= for tests
       - prompt lock + classes list + dirty flag; set_prompt_classes(list[str])
       - conf lock set_conf/get_conf
       - _ensure_model loads YOLOE(weights) after configure_model_cache; lazy on worker thread only
       - process: apply set_classes when dirty; empty → []; predict boxes only; map via results_to_detections; re-wrap with source open_vocab
       - Cap is enforced at API layer (Task 2); worker may still strip empties

    3. PerceptionStore: OpenVocabProduct dataclass (mirror DetectionProduct + optional prompt field). set_open_vocab / snapshot_open_vocab / clear_open_vocab / record_open_vocab_drop. Extend StoreMetrics with ov_frames, ov_frames_dropped, ov_fps, last_ov_latency_ms. FPS window twin of det path. Docstring: DetectionLoop owns set_detections; OpenVocabLoop owns set_open_vocab only.

    4. cache.py: add yoloe-26s-seg.pt and yoloe-26n-seg.pt to KNOWN_WEIGHTS.

    5. THIRD_PARTY_MODELS.md: activate YOLOE row for Phase 6; document default yoloe-26s-seg.pt; offline-after-first-download; AGPL caution. Do not claim Apache for YOLOE.

    6. tests/test_yoloe_worker.py with FakeModel exposing set_classes + predict; cover dirty-once, empty prompt, conf, source tags. Extend model_cache + third_party doc tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_yoloe_worker.py tests/test_model_cache.py tests/test_third_party_models_doc.py -q</automated>
  </verify>
  <done>YOLOE worker + schema source + OV store product + AGPL/cache docs landed; unit tests green with injected FakeModel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OpenVocabLoop + open-vocab API + assemble merge + overlay + serve wiring</name>
  <files>src/sentry_ai/models/detection/open_vocab_loop.py, src/sentry_ai/api/routes_open_vocab.py, src/sentry_ai/api/assemble.py, src/sentry_ai/models/detection/overlay.py, src/sentry_ai/api/app.py, src/sentry_ai/api/deps.py, src/sentry_ai/api/routes_preview.py, src/sentry_ai/capture/status.py, src/sentry_ai/cli.py, tests/test_open_vocab_loop.py, tests/test_api_open_vocab.py, tests/test_assemble_open_vocab.py, tests/test_detection_overlay.py, tests/test_cli_serve.py, tests/test_api_perception_only.py</files>
  <read_first>
    - .planning/phases/06-developer-controls-open-vocab/06-RESEARCH.md (Patterns 5–7, dual-writer anti-pattern, Pitfall 5)
    - .planning/phases/06-developer-controls-open-vocab/06-PATTERNS.md (open_vocab_loop, routes_open_vocab, assemble, overlay, cli)
    - src/sentry_ai/models/detection/loop.py
    - src/sentry_ai/api/assemble.py
    - src/sentry_ai/api/routes_detection.py
    - src/sentry_ai/api/routes_preview.py
    - src/sentry_ai/models/detection/overlay.py
    - src/sentry_ai/cli.py
    - src/sentry_ai/api/app.py
    - tests/test_detection_loop.py
    - tests/test_assemble_perception_frame.py
  </read_first>
  <behavior>
    - OpenVocabLoop default mode off: no process calls; thread alive
    - on_demand: arm() or POST run processes exactly one latest frame then idles
    - continuous every_n=3: process roughly every N new frame_ids; never writes set_detections
    - Fixed DetectionLoop process_calls independent when OV runs (separate threads/fakes)
    - GET/PATCH /api/open-vocab/config: prompt and/or classes, mode, conf, every_n; extra=forbid; 503 without worker
    - POST /api/open-vocab/run arms one-shot without calling worker.process on request thread; returns armed/mode/classes
    - Prompt validation: strip empties; reject &gt;32 classes or &gt;64 char strings with 422
    - assemble: fixed first then OV; each OV det source open_vocab; completeness.detections true if either product present; ov_* stats/stale
    - draw_detections uses distinct color for open_vocab; fixed path unchanged for source fixed/default
    - MJPEG draws OV boxes from store (merged list or second draw) after free-space
    - status exposes ov_fps, ov_latency_ms, ov_count, ov_mode when available
    - serve: construct worker+loop when detect extra importable; start after free_space; stop before free_space stop; default mode off
    - Handlers never run inference or download weights on the request path
  </behavior>
  <action>
    1. OpenVocabLoop(bus, worker, store): thread name open-vocab; modes off|on_demand|continuous behind lock; every_n default 3; arm_once for on_demand; enable Event optional but mode off is sufficient default. On mode off, clear_open_vocab once. Write only set_open_vocab with latency/model/prompt metadata. Error path keep-alive like DetectionLoop.

    2. routes_open_vocab.py:
       - OpenVocabConfigUpdate: optional prompt str or classes list, mode Literal, conf Field, every_n Field(ge=1, le=60); extra=forbid
       - Parse prompt as comma-separated classes when prompt string provided
       - Enforce class count ≤32 and length ≤64
       - PATCH applies to worker.set_prompt_classes + loop.set_mode/set_every_n/set_conf
       - POST /api/open-vocab/run: set classes if provided, set mode on_demand, arm one-shot; return 200 with armed true (do not process on request thread — first-run weight load stays on loop thread)
       - GET returns current mode, classes, conf, every_n

    3. assemble_perception_frame: snapshot_open_vocab; merge detections fixed then OV; completeness.detections = det_present or ov_present; extend TTL/stats for ov; never attach masks/bulk arrays.

    4. overlay.py: color branch on source == open_vocab (magenta); optional ov: label prefix for readability. Keep pure OpenCV.

    5. routes_preview: MJPEG include OV product detections; status ov_* fields from store + open_vocab loop/worker if on app.state.

    6. AppState/create_app: open_vocab_worker, open_vocab_loop optional; include open_vocab router.

    7. cli serve: after det setup (same detect extra), construct YoloeOpenVocabWorker(weights=yoloe-26s-seg.pt) + OpenVocabLoop; start after free_space; stop reverse. Soft-disable on ImportError with clear stderr message. Do not enable continuous by default.

    8. Tests: test_open_vocab_loop modes; test_api_open_vocab (Fake worker raises if process called from handler); test_assemble_open_vocab merge order + source + completeness; overlay color/source tests; cli source wiring; perception-only denylist still passes on v1 envelopes including source field (source is allowed perception field).
  </action>
  <verify>
    <automated>uv run pytest tests/test_open_vocab_loop.py tests/test_api_open_vocab.py tests/test_assemble_open_vocab.py tests/test_yoloe_worker.py tests/test_detection_overlay.py tests/test_cli_serve.py tests/test_api_perception_only.py -q</automated>
  </verify>
  <done>OV loop/API/assemble/overlay/serve integrated; dual-writer race impossible; fixed-class independent; tests green without weight downloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Live Preview open-vocab prompt UX + telemetry + docs note</name>
  <files>src/sentry_ai/ui/static/index.html, README.md, tests/test_api_preview.py</files>
  <read_first>
    - .planning/phases/06-developer-controls-open-vocab/06-UI-SPEC.md
    - .planning/phases/06-developer-controls-open-vocab/06-PATTERNS.md (index.html OV section)
    - src/sentry_ai/ui/static/index.html (after 06-01 controls)
    - README.md (perception API / model notes)
  </read_first>
  <behavior>
    - Open-vocab row: text input placeholder e.g. person, red cup, toolbox; Run button; continuous lower-rate checkbox
    - Run → PATCH/POST open-vocab config+run with parsed classes; does not block fixed-class UI
    - Continuous checkbox → mode continuous every_n=3; uncheck → off or on_demand idle
    - Footer shows ov_ms and/or ov_fps when status provides them
    - Note: first open-vocab Run may download YOLOE weights (AGPL Ultralytics) into SENTRY_MODEL_CACHE
    - No safe-to-drive / motor / autonomy language
    - Distinct box color documented in note or implicit via server draw
  </behavior>
  <action>
    1. Extend static index.html per UI-SPEC open-vocab row without React. Wire Run to POST /api/open-vocab/run (or PATCH config then run) with prompt string. Continuous checkbox PATCHes mode continuous vs off.

    2. Status poll: display ov_fps / ov_latency_ms; sync continuous checkbox from ov_mode with fromServer guard.

    3. Expand model note for YOLOE first-run download + AGPL (alongside existing YOLO/depth note).

    4. README: brief open-vocab usage (prompt, on-demand vs continuous, default off, AGPL pointer to THIRD_PARTY_MODELS.md). Keep perception-only boundary language.

    5. Tests: HTML contract asserts open-vocab controls + open-vocab API path strings; denylist remains clean.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_open_vocab.py tests/test_api_preview.py tests/test_assemble_open_vocab.py tests/test_open_vocab_loop.py tests/test_yoloe_worker.py tests/test_third_party_models_doc.py -q</automated>
  </verify>
  <done>Open-vocab prompt UX live on dashboard; OVD-01/02/03 end-to-end via store truth; docs note AGPL; full phase OV tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → open-vocab config/run | Untrusted prompt strings and mode flags |
| OpenVocabLoop → PerceptionStore | Secondary detection producer must not thrash fixed path |
| Model weights → disk cache | YOLOE AGPL weights via Ultralytics download |
| Stream consumers → PerceptionFrame | Additive source field; still perception-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Denial of service | Prompt / class list | mitigate | Cap ≤32 classes, ≤64 chars; strip empties; Pydantic extra=forbid |
| T-06-11 | Denial of service | Continuous OV GPU thrash | mitigate | Default mode off; continuous every_n≥3 opt-in only |
| T-06-12 | Tampering | Dual writer on detections | mitigate | OpenVocabProduct only; assemble merges; never set_detections from OV loop |
| T-06-13 | Tampering | Weights path injection | mitigate | KNOWN_WEIGHTS allowlist includes yoloe-*.pt only |
| T-06-14 | Information disclosure | AGPL undisclosed | mitigate | THIRD_PARTY_MODELS active row + UI/README first-run note |
| T-06-15 | Elevation of privilege | Wire schema | mitigate | Detection.source only; no motor fields; API-05 tests remain |
| T-06-16 | Denial of service | First-run download on HTTP thread | mitigate | Lazy load inside worker/loop thread; POST run only arms |
| T-06-SC | Tampering | package installs | accept | No new package names; existing detect extra only |
</threat_model>

<verification>
- `uv run pytest tests/test_yoloe_worker.py tests/test_open_vocab_loop.py tests/test_api_open_vocab.py tests/test_assemble_open_vocab.py tests/test_detection_overlay.py tests/test_model_cache.py tests/test_third_party_models_doc.py tests/test_api_preview.py tests/test_cli_serve.py -q` green
- Full suite `uv run pytest -q` green before phase complete
- Optional manual: synthetic serve → Run prompt "person" → magenta/ov boxes on preview and `/v1/snapshot` detections with source open_vocab; fixed conf slider still works; continuous off by default
</verification>

<success_criteria>
1. OVD-01: Text prompts produce open-vocab detections via local YOLOE
2. OVD-02: on_demand and continuous every_n without blocking fixed-class path; default off
3. OVD-03: OV results on dashboard MJPEG and `/v1` stream/snapshot when enabled
4. AGPL documented; tests never download weights
5. Phase 6 success criteria 4–5 from ROADMAP true alongside 06-01 controls
</success_criteria>

<output>
Create `.planning/phases/06-developer-controls-open-vocab/06-02-SUMMARY.md` when done
</output>
