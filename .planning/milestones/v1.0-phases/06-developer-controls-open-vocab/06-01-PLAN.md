---
phase: 06-developer-controls-open-vocab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sentry_ai/control/__init__.py
  - src/sentry_ai/control/pipeline_state.py
  - src/sentry_ai/models/detection/loop.py
  - src/sentry_ai/models/depth/loop.py
  - src/sentry_ai/spatial/loop.py
  - src/sentry_ai/state/perception_store.py
  - src/sentry_ai/api/routes_pipeline.py
  - src/sentry_ai/api/app.py
  - src/sentry_ai/api/deps.py
  - src/sentry_ai/api/routes_preview.py
  - src/sentry_ai/capture/status.py
  - src/sentry_ai/cli.py
  - src/sentry_ai/ui/static/index.html
  - tests/test_pipeline_config.py
  - tests/test_loop_enable_gates.py
  - tests/test_free_space_runtime_cuts.py
  - tests/test_api_preview.py
  - tests/test_cli_serve.py
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05
user_setup: []
must_haves:
  truths:
    - "Developer can enable/disable detection, depth, and free-space stages live without restarting sentry serve (UI-03)"
    - "Disabled stages skip worker/Spatial Post compute (not overlay-hide only) and clear that stage product so completeness/overlays drop"
    - "Detection conf remains adjustable via existing PATCH /api/detection/config; free-space near/mid cutoffs adjust via PATCH /api/pipeline/config without restart (UI-04)"
    - "Invalid free-space cuts (out of range or near_cut <= mid_cut) return 422"
    - "Dashboard footer shows capture + stage FPS/latency telemetry from /api/status (UI-05)"
    - "Stage toggles and free-space sliders work from Live Preview static HTML without process restart"
    - "CaptureLoop and serve stay up when stages are toggled; enable uses loop flags not stop/start threads"
  artifacts:
    - path: "src/sentry_ai/control/pipeline_state.py"
      provides: "Thread-safe stage flags + free-space cutoffs"
      exports: ["PipelineState"]
    - path: "src/sentry_ai/api/routes_pipeline.py"
      provides: "GET/PATCH /api/pipeline/config"
      exports: ["router"]
    - path: "src/sentry_ai/models/detection/loop.py"
      provides: "set_enabled enable gate"
    - path: "src/sentry_ai/models/depth/loop.py"
      provides: "set_enabled enable gate"
    - path: "src/sentry_ai/spatial/loop.py"
      provides: "set_enabled + near/mid cut runtime knobs"
    - path: "src/sentry_ai/ui/static/index.html"
      provides: "Stage toggles, free-space sliders, stage FPS telemetry"
    - path: "tests/test_pipeline_config.py"
      provides: "Pipeline config API + validation coverage"
    - path: "tests/test_loop_enable_gates.py"
      provides: "Loops skip process when disabled"
    - path: "tests/test_free_space_runtime_cuts.py"
      provides: "near/mid cuts applied next free-space frame"
  key_links:
    - from: "src/sentry_ai/api/routes_pipeline.py"
      to: "PipelineState + loop.set_enabled + FreeSpaceLoop cuts"
      via: "PATCH side effects"
      pattern: "pipeline/config|set_enabled|set_near_cut|set_mid_cut"
    - from: "DetectionLoop/DepthLoop/FreeSpaceLoop"
      to: "worker.process / compute_free_space"
      via: "enable Event gate at top of _run"
      pattern: "_enabled|set_enabled"
    - from: "src/sentry_ai/ui/static/index.html"
      to: "/api/pipeline/config + /api/status"
      via: "checkbox/slider PATCH + status poll"
      pattern: "pipeline/config|detection_enabled|near_cut|det_fps"
    - from: "src/sentry_ai/cli.py"
      to: "PipelineState + loops on create_app"
      via: "serve injects control plane refs"
      pattern: "PipelineState|pipeline_state"
---

<objective>
Ship the developer control plane: thread-safe stage enable/disable, free-space near/mid cutoffs, GET/PATCH `/api/pipeline/config`, loop enable gates (pause without teardown), Live Preview stage toggles + cutoff sliders, and full stage FPS/latency telemetry on the dashboard.

Purpose: Close UI-03, UI-04, UI-05 so makers can interactively control the perception pipeline without restarting serve. Open-vocab is deferred to 06-02.

Output: `PipelineState`, pipeline routes, enable gates on Detection/Depth/FreeSpace loops, runtime free-space cuts, extended status + static UI console, tests green without new packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-developer-controls-open-vocab/06-CONTEXT.md
@.planning/phases/06-developer-controls-open-vocab/06-RESEARCH.md
@.planning/phases/06-developer-controls-open-vocab/06-PATTERNS.md
@.planning/phases/06-developer-controls-open-vocab/06-UI-SPEC.md
@.planning/phases/06-developer-controls-open-vocab/06-VALIDATION.md
@.planning/phases/05-free-space-unified-stream/05-03-SUMMARY.md
@.planning/phases/04-monocular-depth/04-02-SUMMARY.md
@.planning/phases/03-fixed-class-detection/03-02-SUMMARY.md

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| Pause model | Enable flags inside loops — **not** `stop()`/`start()` for UI toggles | RESEARCH + user lock |
| Control plane | Unified `GET/PATCH /api/pipeline/config` for stages + free-space cuts | RESEARCH |
| Keep existing | `PATCH /api/detection/config` conf; `PATCH /api/depth/config` depth_mode | CONTEXT / RESEARCH |
| Disable semantics | Skip compute + **clear stage product once** (honest completeness) | RESEARCH A4 |
| Free-space defaults | `near_cut=0.72`, `mid_cut=0.45` from `spatial/free_space.py` | VERIFIED |
| Validation | Both cuts in [0,1]; require `near_cut > mid_cut` else 422 | RESEARCH |
| Source switcher | CLI-only — **not** in Phase 6 UI | Deferred / discretion |
| Frontend | Extend static `index.html` only — no React rewrite | CONTEXT |
| Packages | None new | RESEARCH |

## Interfaces (shipped — use these)

From `src/sentry_ai/api/routes_detection.py`:
- `DetectionConfigUpdate` with `conf: float = Field(ge=0.0, le=1.0)`, `extra=forbid`
- GET/PATCH `/api/detection/config` pattern: `_require_worker`, never call `process`

From `src/sentry_ai/api/routes_depth.py`:
- `DepthConfigUpdate` with `depth_mode` Literal, `extra=forbid`

From `src/sentry_ai/spatial/free_space.py`:
- `DEFAULT_NEAR_CUT = 0.72`, `DEFAULT_MID_CUT = 0.45`
- `compute_free_space(..., near_cut=..., mid_cut=...)` already accepts cuts

From loops:
- `DetectionLoop(bus, worker, store)`, `DepthLoop(bus, worker, store)`, `FreeSpaceLoop(store)`
- `_run` keep-latest + `_stop.wait(0.005)` sleep; exception keep-alive

From `PerceptionStore`:
- `set_detections` / `snapshot`, `set_depth` / `snapshot_depth`, `set_free_space` / `snapshot_free_space`
- `metrics_snapshot()` with `det_fps`, `depth_fps`, `free_space_fps`, last latencies
- No `clear_*` yet — add in this plan

From `create_app` / `AppState`:
- Optional `perception_store`, `detection_worker`, `depth_worker`
- Routers: preview, detection, depth, v1

From status / UI:
- `/api/status` already exposes `det_fps`, `depth_fps`, `free_space_fps`, latencies
- Live Preview conf slider debounces PATCH 150ms with `confFromServer` feedback guard

## Out of scope for 06-01

- Open-vocab worker/loop/routes/UI (06-02)
- Detection.source schema field (06-02)
- Source switcher UI
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PipelineState + loop enable gates + free-space runtime cuts + store clear</name>
  <files>src/sentry_ai/control/__init__.py, src/sentry_ai/control/pipeline_state.py, src/sentry_ai/models/detection/loop.py, src/sentry_ai/models/depth/loop.py, src/sentry_ai/spatial/loop.py, src/sentry_ai/state/perception_store.py, tests/test_loop_enable_gates.py, tests/test_free_space_runtime_cuts.py</files>
  <read_first>
    - .planning/phases/06-developer-controls-open-vocab/06-RESEARCH.md (Patterns 1–3, Pitfalls 1/3/4)
    - .planning/phases/06-developer-controls-open-vocab/06-PATTERNS.md (pipeline_state, enable gates, FreeSpaceLoop cuts, store clear)
    - src/sentry_ai/models/detection/loop.py
    - src/sentry_ai/models/depth/loop.py
    - src/sentry_ai/spatial/loop.py
    - src/sentry_ai/spatial/free_space.py (DEFAULT_NEAR_CUT/MID_CUT, compute_free_space kwargs)
    - src/sentry_ai/state/perception_store.py
    - tests/test_detection_loop.py (_wait_until, FakeDetectionWorker patterns)
  </read_first>
  <behavior>
    - PipelineState.snapshot returns detection_enabled, depth_enabled, free_space_enabled, near_cut, mid_cut with defaults True/True/True/0.72/0.45
    - PipelineState.update partial fields under lock; rejects near_cut &lt;= mid_cut with ValueError; returns full snapshot
    - DetectionLoop/DepthLoop/FreeSpaceLoop default enabled; when disabled, process/compute is not called and thread stays alive
    - set_enabled(False) clears that stage product once via store clear_*; set_enabled(True) resumes producing
    - FreeSpaceLoop set_near_cut/set_mid_cut (or set_cuts) apply to next compute_free_space call; invalid ordering raises ValueError
    - clear_detections / clear_depth / clear_free_space set product slot to None under lock; snapshot returns None after clear
  </behavior>
  <action>
    1. Create package `src/sentry_ai/control/` with `PipelineState` dataclass per RESEARCH Pattern 1: threading.Lock; fields detection_enabled/depth_enabled/free_space_enabled (default True); near_cut=DEFAULT_NEAR_CUT; mid_cut=DEFAULT_MID_CUT. Methods: snapshot() -&gt; dict; update(**kwargs) -&gt; dict that validates optional bools and optional cuts in [0,1], requires effective near_cut &gt; mid_cut after merge, returns full snapshot. No FastAPI imports.

    2. PerceptionStore: add clear_detections, clear_depth, clear_free_space that set the corresponding `_latest*` slot to None under the existing lock (do not reset FPS counters). Prefer explicit clear over writing empty error products for stage-off honesty (RESEARCH A4).

    3. DetectionLoop and DepthLoop: add `_enabled = threading.Event()` set True at init; public `set_enabled(enabled: bool)` and `is_enabled() -&gt; bool`. At top of `_run` after stop check: if not enabled, wait 0.01s and continue (do not call worker.process). On disable path in set_enabled, call store.clear_detections / clear_depth once. Do **not** call stop()/start() from set_enabled.

    4. FreeSpaceLoop: same enable gate before depth poll; clear_free_space on disable. Hold near_cut/mid_cut behind a lock (defaults from free_space constants). Public getters/setters or set_cuts(near_cut=, mid_cut=) with same near&gt;mid validation. Pass near_cut/mid_cut into existing compute_free_space kwargs each frame. Do not reset OccupancySmoother on cut changes.

    5. Tests (inject fakes; no real models):
       - test_loop_enable_gates: Fake worker process_calls counter; disable → process stays 0; re-enable → process_calls increases; store product cleared after disable.
       - test_free_space_runtime_cuts: FreeSpaceLoop with synthetic depth product in store; change cuts; assert next product/bands reflect new thresholds (or assert compute receives cuts via thin spy if easier). Invalid mid&gt;=near raises.
  </action>
  <verify>
    <automated>uv run pytest tests/test_loop_enable_gates.py tests/test_free_space_runtime_cuts.py -q</automated>
  </verify>
  <done>PipelineState exists; three loops honor enable gates without teardown; free-space cuts apply next frame; store clear_* works; unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET/PATCH /api/pipeline/config + create_app/cli wiring + status stage flags</name>
  <files>src/sentry_ai/api/routes_pipeline.py, src/sentry_ai/api/app.py, src/sentry_ai/api/deps.py, src/sentry_ai/api/routes_preview.py, src/sentry_ai/capture/status.py, src/sentry_ai/cli.py, tests/test_pipeline_config.py, tests/test_cli_serve.py, tests/test_api_preview.py</files>
  <read_first>
    - .planning/phases/06-developer-controls-open-vocab/06-PATTERNS.md (routes_pipeline, app.py/deps.py, cli lifecycle)
    - src/sentry_ai/api/routes_detection.py
    - src/sentry_ai/api/routes_depth.py
    - src/sentry_ai/api/app.py
    - src/sentry_ai/api/deps.py
    - src/sentry_ai/api/routes_preview.py
    - src/sentry_ai/cli.py (serve block: store/loops/create_app/start-stop)
    - tests/test_api_detection.py (TestClient + Fake worker + create_app inject)
    - tests/test_api_depth.py (process must not be called from handlers)
  </read_first>
  <behavior>
    - GET /api/pipeline/config returns full snapshot when pipeline_state present; 503 when missing
    - PATCH partial body applies only provided fields; returns full snapshot
    - PATCH invalid cuts (range or near&lt;=mid) → 422; extra fields → 422
    - PATCH detection_enabled=false calls detection_loop.set_enabled(False) when loop injected; same for depth/free_space
    - PATCH near_cut/mid_cut pushes to FreeSpaceLoop when present
    - Handlers never call worker.process or open cameras
    - Existing /api/detection/config and /api/depth/config still work unchanged
    - /api/status includes detection_enabled, depth_enabled, free_space_enabled, near_cut, mid_cut when pipeline_state present; keeps existing det/depth/free_space fps+latency fields
    - create_app accepts optional pipeline_state, detection_loop, depth_loop, free_space_loop; does not start threads
    - sentry serve constructs PipelineState, injects into create_app with loop refs; stage toggles never stop CaptureLoop
  </behavior>
  <action>
    1. Implement `routes_pipeline.py` APIRouter mirroring detection/depth style:
       - Pydantic `PipelineConfigUpdate` with optional bools detection_enabled/depth_enabled/free_space_enabled and optional near_cut/mid_cut Field(ge=0, le=1); ConfigDict(extra="forbid")
       - GET `/api/pipeline/config`: require pipeline_state; return snapshot()
       - PATCH `/api/pipeline/config`: merge via pipeline_state.update; on ValueError → HTTP 422; then side-effect push: if detection_enabled in body and detection_loop present → set_enabled; same for depth_loop / free_space_loop; if cut fields present and free_space_loop present → apply cuts. Return full snapshot.
       - Never run inference in handlers.

    2. Extend AppState + create_app kwargs: pipeline_state, detection_loop, depth_loop, free_space_loop (all optional, default None). Attach on app.state. include_router(pipeline_router). Keep docs_url/redoc_url None.

    3. routes_preview `/api/status`: when pipeline_state on app.state, merge stage flags + near_cut/mid_cut into status dict. Ensure det_fps/depth_fps/free_space_fps and latencies remain present from store metrics (already mostly there). If StatusSnapshot is used with extra=forbid, add optional fields there too so validation does not drop them.

    4. cli serve: construct PipelineState(); pass pipeline_state and loop objects into create_app. Start order unchanged: capture → det → depth → free_space (no open-vocab yet). Stage disable must not stop capture or free_space thread teardown.

    5. tests/test_pipeline_config.py: TestClient; Fake loops with set_enabled recording; assert GET/PATCH; 422 paths; 503 without state; assert FakeDetectionWorker.process never called from PATCH. Extend test_cli_serve for PipelineState source wiring if pattern exists. Extend test_api_preview for new status fields.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline_config.py tests/test_loop_enable_gates.py tests/test_free_space_runtime_cuts.py tests/test_api_preview.py tests/test_cli_serve.py tests/test_api_detection.py tests/test_api_depth.py -q</automated>
  </verify>
  <done>Pipeline control plane live on API; serve wires state+loops; status exposes stage flags; existing conf/depth routes unbroken; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Live Preview stage toggles, free-space sliders, and stage FPS telemetry</name>
  <files>src/sentry_ai/ui/static/index.html, tests/test_api_preview.py</files>
  <read_first>
    - .planning/phases/06-developer-controls-open-vocab/06-UI-SPEC.md
    - .planning/phases/06-developer-controls-open-vocab/06-PATTERNS.md (index.html debounced PATCH + status poll)
    - src/sentry_ai/ui/static/index.html (conf slider, applyStatus, pollStatus)
    - tests/test_api_preview.py (static fixture / language denylist patterns if any)
  </read_first>
  <behavior>
    - Footer/controls include stage checkboxes for detection, depth, free-space that PATCH /api/pipeline/config on change
    - Free-space near/mid range sliders (0–1) debounced ~150ms PATCH pipeline config; client may warn if near&lt;=mid but server 422 is source of truth
    - Existing conf slider still PATCHes /api/detection/config only
    - Footer shows det_fps, depth_fps, free_space_fps in addition to existing latencies (UI-05)
    - Status poll syncs checkbox/slider state from server flags using a fromServer guard (same pattern as confFromServer) to avoid feedback loops
    - No motor / safe-to-drive / autonomous language; no open-vocab UI yet (06-02)
    - Capture-only path still works when pipeline endpoints 503 (graceful no-op)
  </behavior>
  <action>
    1. Extend static Live Preview per 06-UI-SPEC layout (Stages row + free-space near/mid + telemetry). Keep single HTML file; no React/Vite.

    2. Wire stage checkboxes → immediate or lightly debounced PATCH `/api/pipeline/config` with the changed bool. On failure, non-fatal (status poll refreshes).

    3. Wire near/mid sliders with CONF_DEBOUNCE_MS=150 style debounce to PATCH near_cut/mid_cut. Display numeric values. Reuse fromServer flag pattern when applyStatus writes server values.

    4. Telemetry: render det_fps, depth_fps, free_space_fps from status (fields already on API). Keep det/depth/free-space latency displays. Prefer compact footer labels matching UI-SPEC.

    5. GET pipeline config once on load (optional) or rely on status fields for initial checkbox state — if using status, ensure Task 2 status fields are consumed.

    6. Tests: extend test_api_preview or HTML contract checks so index.html contains pipeline/config, stage control markers, and fps metric ids; keep language denylist (no safe-to-drive / motor). Do not require a browser.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline_config.py tests/test_api_preview.py tests/test_loop_enable_gates.py tests/test_free_space_runtime_cuts.py -q</automated>
  </verify>
  <done>Live Preview is a full stage/threshold control console with visible stage FPS telemetry; UI-03/UI-04/UI-05 satisfied without open-vocab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI control routes | Untrusted JSON for stage flags and cutoffs on localhost |
| Control plane → stage loops | Cold-path flags affect hot-path compute skip only |
| UI copy → operator | Must not imply autonomy or motor control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | PATCH /api/pipeline/config | mitigate | Pydantic extra=forbid; Field ge/le on cuts; near&gt;mid 422 before apply |
| T-06-02 | Denial of service | Rapid stage toggle thrash | mitigate | Enable flags only (no thread join/reload); short Event.wait when disabled |
| T-06-03 | Elevation of privilege | New control JSON | mitigate | No motor/path_plan fields; perception-only; API-05 denylist remains |
| T-06-04 | Information disclosure | Remote bind | accept | Default 127.0.0.1; existing serve warning; v1 has no auth |
| T-06-05 | Spoofing | N/A (no auth v1) | accept | Localhost-only default; out of scope for Phase 6 |
| T-06-SC | Tampering | package installs | accept | No new packages in 06-01 |
</threat_model>

<verification>
- `uv run pytest tests/test_pipeline_config.py tests/test_loop_enable_gates.py tests/test_free_space_runtime_cuts.py tests/test_api_preview.py tests/test_cli_serve.py -q` green
- Manual (optional): `sentry serve --source synthetic` → uncheck detection → boxes disappear and det work stops; adjust near/mid → free-space bands change; footer shows stage fps
- Existing detection/depth config tests still pass
</verification>

<success_criteria>
1. UI-03: Three stage toggles disable compute without serve restart
2. UI-04: Det conf (existing) + free-space near/mid cuts adjustable live
3. UI-05: Dashboard shows capture + stage FPS/latency telemetry
4. Control plane GET/PATCH `/api/pipeline/config` is source of truth for flags/cuts
5. No open-vocab scope creep; no React rewrite; no source switcher UI
</success_criteria>

<output>
Create `.planning/phases/06-developer-controls-open-vocab/06-01-SUMMARY.md` when done
</output>
