---
phase: 03-fixed-class-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/sentry_ai/models/__init__.py
  - src/sentry_ai/models/cache.py
  - src/sentry_ai/models/detection/__init__.py
  - src/sentry_ai/models/detection/mapping.py
  - src/sentry_ai/models/detection/yolo_worker.py
  - src/sentry_ai/models/detection/loop.py
  - src/sentry_ai/state/__init__.py
  - src/sentry_ai/state/perception_store.py
  - src/sentry_ai/plugins/registry.py
  - src/sentry_ai/config/models.py
  - src/sentry_ai/config/profiles/desktop-gpu.yaml
  - THIRD_PARTY_MODELS.md
  - README.md
  - tests/conftest.py
  - tests/test_detection_mapping.py
  - tests/test_perception_store.py
  - tests/test_detection_worker.py
  - tests/test_detection_loop.py
  - tests/test_model_cache.py
  - tests/test_detection_overlay.py
  - tests/test_api_detection.py
  - tests/test_third_party_models_doc.py
  - tests/test_plugins_registry.py
autonomous: true
requirements:
  - DET-01
  - DET-02
  - MODEL-02
user_setup: []
must_haves:
  truths:
    - "YoloDetectionWorker.process(ImageFrame) returns list[Detection] without opening a camera (mock YOLO in CI)"
    - "Detection mapping produces class_name, confidence, and bbox_xyxy in image pixel coordinates (DET-02)"
    - "DetectionLoop daemon thread reads FrameBus.get_latest(), skips same frame_id, writes PerceptionStore; never VideoCapture"
    - "Thread-safe worker conf is read on each process() and passed to predict (foundation for DET-03)"
    - "configure_model_cache points Ultralytics weights_dir at SENTRY_MODEL_CACHE or ~/.cache/sentry-ai/weights (MODEL-02)"
    - "optional-dependencies.detect installs ultralytics-opencv-headless; default/dev path does not require torch for unit tests"
    - "THIRD_PARTY_MODELS documents YOLO26 AGPL and offline cache after first download"
    - "yolo-fixed worker entry point is discoverable; InferenceBackend remains stubs (not forced around Ultralytics)"
  artifacts:
    - path: "src/sentry_ai/models/detection/yolo_worker.py"
      provides: "YoloDetectionWorker ModelWorker for YOLO26"
      exports: ["YoloDetectionWorker"]
    - path: "src/sentry_ai/models/detection/mapping.py"
      provides: "Ultralytics Results → list[Detection]"
      exports: ["results_to_detections"]
    - path: "src/sentry_ai/models/detection/loop.py"
      provides: "DetectionLoop FrameBus subscriber thread"
      exports: ["DetectionLoop"]
    - path: "src/sentry_ai/state/perception_store.py"
      provides: "Keep-latest detection product store (single truth)"
      exports: ["PerceptionStore", "DetectionProduct"]
    - path: "src/sentry_ai/models/cache.py"
      provides: "Sentry-owned Ultralytics weights_dir setup"
      exports: ["configure_model_cache", "tier_to_weight"]
    - path: "tests/test_detection_mapping.py"
      provides: "DET-02 automated coverage"
    - path: "tests/test_detection_worker.py"
      provides: "DET-01 mock worker + conf coverage"
    - path: "tests/test_detection_loop.py"
      provides: "DET-01 bus-only loop coverage"
    - path: "tests/test_model_cache.py"
      provides: "MODEL-02 cache path coverage"
  key_links:
    - from: "src/sentry_ai/models/detection/loop.py"
      to: "FrameBus.get_latest"
      via: "DetectionLoop._run keep-latest read"
      pattern: "get_latest"
    - from: "src/sentry_ai/models/detection/loop.py"
      to: "PerceptionStore.set_detections"
      via: "publish after process"
      pattern: "set_detections"
    - from: "src/sentry_ai/models/detection/yolo_worker.py"
      to: "results_to_detections"
      via: "map predict output to Detection schema"
      pattern: "results_to_detections"
    - from: "src/sentry_ai/models/cache.py"
      to: "ultralytics.settings.weights_dir"
      via: "configure_model_cache before YOLO load"
      pattern: "weights_dir|SENTRY_MODEL_CACHE"
    - from: "pyproject.toml"
      to: "YoloDetectionWorker"
      via: "sentry_ai.workers yolo-fixed entry point"
      pattern: "yolo-fixed"
---

<objective>
Ship the fixed-class detection core: YOLO26 worker (ModelWorker), Ultralytics→Detection mapping, DetectionLoop on FrameBus, PerceptionStore keep-latest product, and Sentry-owned model cache — with CI that never downloads weights.

Purpose: Close DET-01/DET-02/MODEL-02 at the pipeline layer so 03-02 can attach overlays, snapshot JSON, runtime conf API, and UI telemetry without reinventing inference.

Output: Installable optional `detect` extra; `YoloDetectionWorker` + `DetectionLoop` + `PerceptionStore` + `configure_model_cache`; unit tests green with mocks; AGPL/cache docs updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-fixed-class-detection/03-CONTEXT.md
@.planning/phases/03-fixed-class-detection/03-RESEARCH.md
@.planning/phases/03-fixed-class-detection/03-PATTERNS.md
@.planning/phases/03-fixed-class-detection/03-VALIDATION.md
@.planning/phases/02-camera-ingest-live-preview/02-01-SUMMARY.md
@.planning/phases/02-camera-ingest-live-preview/02-02-SUMMARY.md
@.planning/phases/02-camera-ingest-live-preview/02-03-SUMMARY.md

## Locked decisions (do not re-litigate)

| Decision | Value |
|----------|-------|
| Detector | YOLO26 via Ultralytics (`yolo26n` default edge/CPU; `yolo26s` desktop) |
| Package | `ultralytics-opencv-headless` 8.4.x optional-extra `detect` — never plain `ultralytics` (OpenCV GUI conflict) |
| Inference placement | Dedicated `DetectionLoop` thread; never in CaptureLoop or FastAPI handlers |
| Camera ownership | Workers never open cameras; only `FrameBus.get_latest()` / `ImageFrame` |
| Schema | Existing `Detection` / `PerceptionFrame` / `Completeness.detections` — map into, do not redesign |
| Backend protocol | Keep `InferenceBackend` + `NullBackend` stubs; do **not** wrap Ultralytics in `infer(tensor)` this phase |
| Drawing | Deferred to 03-02 (OpenCV only; no `supervision`) |
| API/UI | Deferred to 03-02 (`/api/snapshot`, PATCH conf, MJPEG overlay) |
| Out of scope | Depth, free-space, open-vocab, full `/v1` stream, TensorRT |

## Package legitimacy (Phase 3 installs)

| Package | Disposition | Notes |
|---------|-------------|-------|
| ultralytics-opencv-headless ≥8.4.33,&lt;9 | Approved (RESEARCH; treat [ASSUMED] without slopcheck) | Prefer over `ultralytics` |
| torch / torchvision | Transitive via ultralytics | Do not pin CUDA wheel in pyproject core; mock in CI |
| supervision | **Not installed** | OpenCV draw in 03-02 |

## Interfaces (Phase 1–2 contracts to use)

From `src/sentry_ai/schemas/perception.py`:
- `Detection(class_name, confidence, bbox_xyxy)` extra=forbid
- `Completeness(depth, detections, free_space)`
- `PerceptionFrame` with `detections: list[Detection] | None`, `stats: dict | None`

From `src/sentry_ai/plugins/protocols.py`:
- `ModelWorker`: `name: str`; `process(frame: ImageFrame | object) -> object | None`

From `src/sentry_ai/bus/frame_bus.py`:
- `FrameBus.get_latest() -> ImageFrame | None` (non-consuming)
- `FrameBus.publish` / `metrics_snapshot` (CaptureLoop owns publish)

From `src/sentry_ai/capture/loop.py`:
- `CaptureLoop` start/stop daemon pattern — structural template for `DetectionLoop`

From `src/sentry_ai/plugins/builtins.py`:
- `NoopWorker` shape for worker class attrs

## Discretion locked by research (implement as specified)

- Default conf **0.25**; `imgsz=640`; `verbose=False`, `save=False`
- Device auto: `cuda` > `mps` > `cpu` (best-effort probe)
- Weight map: `n`→`yolo26n.pt`, `s`→`yolo26s.pt`, `m`→`yolo26m.pt`; change `desktop-gpu.yaml` `detector_tier` from `m` to `s`
- Cache: `SENTRY_MODEL_CACHE` or `~/.cache/sentry-ai`; `weights_dir = cache_root/weights`; `YOLO_CONFIG_DIR` setdefault under cache; `settings.update({"weights_dir": ..., "sync": False})`
- Completeness: empty list + `detections=True` means stage ran (product assembly used in 03-02; store still records empty lists)
- Optional warm-up predict on zeros after load (log “model ready”) — do in worker load path

## Out of scope this plan

FastAPI routes, MJPEG overlay draw, Live Preview conf slider, `sentry serve` DetectionLoop start (03-02). Depth/open-vocab/free-space.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 detect extra, package skeleton, model cache, AGPL docs, test stubs</name>
  <files>pyproject.toml, uv.lock, src/sentry_ai/models/__init__.py, src/sentry_ai/models/cache.py, src/sentry_ai/models/detection/__init__.py, src/sentry_ai/state/__init__.py, THIRD_PARTY_MODELS.md, README.md, tests/test_model_cache.py, tests/test_detection_mapping.py, tests/test_detection_worker.py, tests/test_detection_loop.py, tests/test_detection_overlay.py, tests/test_api_detection.py, tests/test_third_party_models_doc.py</files>
  <read_first>
    - .planning/phases/03-fixed-class-detection/03-RESEARCH.md (Standard Stack install, Pattern 3 model cache, Package Legitimacy, Wave 0 Gaps, Pitfall 3/4/7/8)
    - .planning/phases/03-fixed-class-detection/03-PATTERNS.md (cache.py section, package __init__ analogs, test table)
    - .planning/phases/03-fixed-class-detection/03-VALIDATION.md (Wave 0 Requirements, T-3 threats)
    - pyproject.toml (optional-dependencies + entry points style — use [project.optional-dependencies] not uv dependency-groups)
    - THIRD_PARTY_MODELS.md
    - tests/test_third_party_models_doc.py
    - src/sentry_ai/backend/__init__.py (package export style)
  </read_first>
  <action>
    1. Add optional dependency group in pyproject.toml (per D RESEARCH / MODEL-02 packaging):
       - Under `[project.optional-dependencies]` add `detect = ["ultralytics-opencv-headless&gt;=8.4.33,&lt;9"]`
       - Keep `dev` as-is; do **not** add ultralytics/torch to core `dependencies`
       - Do **not** install plain `ultralytics` (OpenCV GUI conflict — Pitfall 3)
       - Run `uv lock` so lockfile records the optional extra; default `uv sync --extra dev` must still work without pulling torch if the resolver allows (unit tests mock YOLO). Document in README that real detection needs `uv sync --extra dev --extra detect` (or equivalent).

    2. Create package skeletons with future annotations + sorted `__all__`:
       - `src/sentry_ai/models/__init__.py`, `models/detection/__init__.py`, `state/__init__.py`
       - Leave exports minimal until Task 2–3 fill modules

    3. Implement `src/sentry_ai/models/cache.py` (RESEARCH Pattern 3 / MODEL-02):
       - `configure_model_cache(cache_root: Path | None = None) -> Path` returns weights_dir
       - Resolve root: arg → env `SENTRY_MODEL_CACHE` → `Path.home() / ".cache" / "sentry-ai"`
       - `weights_dir = root / "weights"`; `mkdir(parents=True, exist_ok=True)`
       - `os.environ.setdefault("YOLO_CONFIG_DIR", str(root / "ultralytics"))`
       - Lazy-import ultralytics settings only inside the function (or guard ImportError with clear message) so non-detect installs do not break import of sentry_ai.models.cache path helpers
       - Prefer splitting: pure path resolution always works; settings.update only when ultralytics importable
       - Call `settings.update({"weights_dir": str(weights_dir), "sync": False})` when ultralytics present
       - Also export pure helpers: `default_cache_root() -> Path`, `tier_to_weight(tier: str | None) -> str` mapping n/s/m → yolo26{n,s,m}.pt; default missing/unknown tier to `yolo26n.pt`
       - Security (T-3-01): only known weight filenames from tier map / allowlist (`yolo26n.pt`, `yolo26s.pt`, `yolo26m.pt`); reject path components with `..` if any user path API is added (tier map only for v1)

    4. Update `THIRD_PARTY_MODELS.md` (T-3-05 / FOUND-05 continuity):
       - Change YOLO26 row status from “Planned Phase 3” to active Phase 3 language (still **non-default for commercial** AGPL caution)
       - Add short note: weights download once into Sentry cache (`SENTRY_MODEL_CACHE` / `~/.cache/sentry-ai/weights`); offline re-run after first pull (MODEL-02)
       - Keep DAV2 Small Apache-2.0 as commercially friendly default depth

    5. README: one short section or bullet — detection optional extra, first-run download, AGPL pointer to THIRD_PARTY_MODELS, offline after cache. No autonomy/safety language.

    6. Wave 0 Nyquist stubs (suite stays green):
       - `tests/test_model_cache.py` — implement real path/env tests this task (tmp_path, env monkeypatch; never network)
       - `tests/test_detection_mapping.py`, `tests/test_detection_worker.py`, `tests/test_detection_loop.py` — collectable modules; either empty placeholders filled in Task 2–3 same plan, or skip with reason only if committing Task 1 alone
       - `tests/test_detection_overlay.py`, `tests/test_api_detection.py` — `pytest.mark.skip(reason="implemented in plan 03-02")`
       - Extend `tests/test_third_party_models_doc.py` to assert YOLO/AGPL + cache/offline wording present for Phase 3

    7. Do not add create_app/routes/UI changes here.
  </action>
  <verify>
    <automated>uv lock &amp;&amp; uv sync --extra dev &amp;&amp; uv run pytest -q tests/test_model_cache.py tests/test_third_party_models_doc.py &amp;&amp; uv run ruff check src/sentry_ai/models src/sentry_ai/state tests/test_model_cache.py tests/test_third_party_models_doc.py &amp;&amp; uv run python -c "from sentry_ai.models.cache import default_cache_root, tier_to_weight; assert tier_to_weight('n')=='yolo26n.pt'"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject has optional-dependencies.detect with ultralytics-opencv-headless; core deps unchanged (no torch in core)
    - configure_model_cache / tier_to_weight / default_cache_root exist and unit tests pass without network
    - THIRD_PARTY_MODELS YOLO row is Phase 3 active + AGPL + cache offline note
    - Wave 0 skip stubs for 03-02 test modules exist; suite remains green for existing tests
  </acceptance_criteria>
  <done>Detect optional extra + model cache helper + AGPL/cache docs + Wave 0 stubs landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detection mapping (DET-02) + PerceptionStore keep-latest</name>
  <files>src/sentry_ai/models/detection/mapping.py, src/sentry_ai/models/detection/__init__.py, src/sentry_ai/state/perception_store.py, src/sentry_ai/state/__init__.py, tests/test_detection_mapping.py, tests/test_perception_store.py, tests/conftest.py</files>
  <read_first>
    - .planning/phases/03-fixed-class-detection/03-RESEARCH.md (Pattern 2 mapping, Pattern 6 completeness semantics, Pitfall 6)
    - .planning/phases/03-fixed-class-detection/03-PATTERNS.md (mapping.py, perception_store.py, FrameBus analog)
    - src/sentry_ai/schemas/perception.py (Detection fields)
    - src/sentry_ai/bus/frame_bus.py (lock + isolated snapshot pattern)
    - tests/test_frame_bus.py (overwrite + isolation patterns)
    - tests/conftest.py (make_image_frame)
  </read_first>
  <behavior>
    - results_to_detections: empty/None boxes → []
    - results_to_detections: known xyxy/conf/cls/names → Detection with float tuple bbox and class_name from names
    - PerceptionStore: snapshot() is None before any set_detections
    - set_detections overwrites latest; second product replaces first (keep-latest)
    - snapshot() returns isolated copy (mutating returned list/product must not corrupt store)
    - concurrent set + snapshot does not raise (smoke with threads)
  </behavior>
  <action>
    1. Implement pure `results_to_detections(result) -> list[Detection]` in `mapping.py`:
       - Duck-type Ultralytics result: `result.boxes`, `result.names`
       - If boxes is None or len 0 → return []
       - Read xyxy/conf/cls (support tensor-like `.cpu().numpy()` OR plain numpy/list for fakes)
       - Build `Detection(class_name=str(names.get(int(cls_id), str(cls_id))), confidence=float(conf), bbox_xyxy=(x1,y1,x2,y2) floats)`
       - No I/O; no ultralytics import required if duck-typed
       - Unit-test with fake Boxes-like objects (conftest factory `make_fake_yolo_result(...)`) — never download weights

    2. Implement `PerceptionStore` in `state/perception_store.py` (FrameBus-shaped, DET-04 single truth prep):
       - `@dataclass` `DetectionProduct` fields: frame_id, camera_id, t_capture, detections (list[Detection]), latency_ms, conf (float | None), model_name (str | None), error (str | None optional)
       - `set_detections(frame_id, camera_id, t_capture, detections, latency_ms, conf=None, model_name=None)` under lock; store copy of list
       - Optional metrics: det_fps via 1s monotonic window (mirror FrameBus); det_frames_dropped counter if loop reports skips (or compute skips only in loop — store at least latency + last product)
       - `snapshot() -> DetectionProduct | None` isolated copy
       - No numpy on store wire path

    3. Export public names from package `__init__` files.

    4. Fill `tests/test_detection_mapping.py` fully; add store tests either in same file or `tests/test_perception_store.py` if cleaner (prefer dedicated `tests/test_perception_store.py` only if needed — otherwise include store cases in test_detection_loop later; minimum: mapping file complete + store unit coverage in test_detection_mapping companion or new file listed in files_modified).

    Prefer adding `tests/test_perception_store.py` only if mapping file would exceed clear scope; if added, include it in files_modified for the task commit. Default: put store unit tests in `tests/test_detection_loop.py` Task 3 OR create `tests/test_perception_store.py` here for isolation — **create `tests/test_perception_store.py`** for FrameBus-parity clarity and add to files.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_detection_mapping.py tests/test_perception_store.py &amp;&amp; uv run ruff check src/sentry_ai/models/detection/mapping.py src/sentry_ai/state/perception_store.py tests/test_detection_mapping.py tests/test_perception_store.py</automated>
  </verify>
  <acceptance_criteria>
    - DET-02: mapping tests prove class_name, confidence, bbox_xyxy from fake YOLO boxes
    - Empty boxes yield [] not None
    - PerceptionStore keep-latest + isolated snapshot verified
    - No ultralytics/torch import required for these unit tests
  </acceptance_criteria>
  <done>Mapping + PerceptionStore ready for worker and loop consumers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: YoloDetectionWorker + DetectionLoop + plugin registration (DET-01)</name>
  <files>src/sentry_ai/models/detection/yolo_worker.py, src/sentry_ai/models/detection/loop.py, src/sentry_ai/models/detection/__init__.py, src/sentry_ai/plugins/registry.py, src/sentry_ai/config/models.py, src/sentry_ai/config/profiles/desktop-gpu.yaml, pyproject.toml, tests/test_detection_worker.py, tests/test_detection_loop.py, tests/test_plugins_registry.py</files>
  <read_first>
    - .planning/phases/03-fixed-class-detection/03-RESEARCH.md (Pattern 1 DetectionLoop, Pattern 5 conf lock, weight/tier mapping, predict BGR, Pitfall 1/5/9)
    - .planning/phases/03-fixed-class-detection/03-PATTERNS.md (yolo_worker, loop, plugin registration)
    - src/sentry_ai/capture/loop.py (start/stop/_run lifecycle template)
    - src/sentry_ai/plugins/builtins.py (NoopWorker)
    - src/sentry_ai/plugins/registry.py (register_builtins skip-if-present)
    - src/sentry_ai/plugins/protocols.py (ModelWorker)
    - tests/test_capture_loop_reconnect.py (_wait_until, start/stop finally)
  </read_first>
  <behavior>
    - Worker process with injectable fake model/predict returns list[Detection] from ImageFrame.image_bgr
    - set_conf(0.5) then process uses conf=0.5 on next predict (not frozen at load)
    - set_conf outside [0,1] raises ValueError
    - DetectionLoop: after bus.publish(frame), store eventually has that frame_id detections
    - DetectionLoop: same frame_id not reprocessed; newer frame overwrites product
    - DetectionLoop source has no VideoCapture / source.read (inspect or architecture assert)
    - Worker exceptions log / set store error path without killing thread (optional but preferred)
    - Registry lists yolo-fixed when importable or entry point registered
  </behavior>
  <action>
    1. Implement `YoloDetectionWorker` in `yolo_worker.py` (DET-01, ModelWorker):
       - `name: str = "yolo-fixed"`
       - `__init__(self, weights: str = "yolo26n.pt", conf: float = 0.25, device: str | None = None, model=None)` — injectable `model` for tests bypasses YOLO load
       - Thread-safe `_conf` with `set_conf` / `get_conf` (DET-03 foundation; validate [0,1])
       - Lazy load: if model is None, call `configure_model_cache()`, then `from ultralytics import YOLO` and `YOLO(weights)` — only on real path
       - Optional one-shot warm-up on zeros after load
       - `process(frame)`: read conf under lock; `model.predict(source=frame.image_bgr, conf=conf, imgsz=640, device=..., verbose=False, save=False)`; return `results_to_detections(results[0])`
       - Device resolve helper: if device None, pick cuda if available else mps else cpu (torch.cuda.is_available / backends.mps — guard imports)
       - Never open cameras / VideoCapture
       - Clear ImportError message if ultralytics missing: instruct `--extra detect`

    2. Implement `DetectionLoop` in `loop.py` (CaptureLoop structural copy):
       - `__init__(self, bus, worker, store)`
       - `start()` / `stop()` daemon thread name `detection`; join timeout 5.0; idempotent
       - `_run`: get_latest; if None or same frame_id → `_stop.wait(0.005)`; else process, measure latency_ms, set_detections with worker conf + model name; update `_last_frame_id`
       - Count skipped intermediate frames if desired (`det_frames_dropped` on store or loop metrics)
       - Broad except around process: log + optional store error detail; keep thread alive (T-3-04 isolation)
       - Never call source.read / VideoCapture

    3. Plugin registration:
       - pyproject entry point: `yolo-fixed = "sentry_ai.models.detection.yolo_worker:YoloDetectionWorker"`
       - `register_builtins`: register yolo-fixed only if import succeeds (graceful without torch); skip-if-present
       - Extend tests/test_plugins_registry.py accordingly (noop still present; yolo-fixed if importable)

    4. Config/profile (discretion):
       - Optionally add `detector_conf: float = 0.25` on ModelsConfig only if used now; otherwise keep conf runtime-only on worker
       - Change `desktop-gpu.yaml` `detector_tier: m` → `s` per RESEARCH recommendation
       - Document tier→weight via `tier_to_weight` for 03-02 serve wiring

    5. Tests (mock YOLO; no weight download):
       - `tests/test_detection_worker.py`: FakeModel with predict recording conf/source; runtime conf; empty detections; protocol isinstance if desired
       - `tests/test_detection_loop.py`: FakeDetectionWorker; FrameBus + store; _wait_until product; no VideoCapture in loop module source (grep/inspect pattern like preview tests)
       - Full suite + ruff at plan end

    6. Do **not** wire create_app or sentry serve here (03-02).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_detection_worker.py tests/test_detection_loop.py tests/test_detection_mapping.py tests/test_model_cache.py tests/test_perception_store.py tests/test_plugins_registry.py &amp;&amp; uv run ruff check src/sentry_ai/models src/sentry_ai/state src/sentry_ai/plugins/registry.py &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - DET-01: worker + loop process live bus frames into store without cameras
    - DET-02: end-to-end fake predict path yields schema-valid Detection list
    - MODEL-02: cache helper used on real load path; tests never network
    - Worker conf hot-update works under lock for next process()
    - yolo-fixed entry point declared; optional detect extra documented
    - Full pytest suite green; ruff clean; smoke path still no mandatory torch
  </acceptance_criteria>
  <done>Detection core (worker, loop, store, cache, mapping, plugin) ready for 03-02 API/UI surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → model load | Weight files resolved under Sentry cache root |
| Optional third-party code | Ultralytics/torch execution of model graphs |
| FrameBus → DetectionLoop | Trusted in-process ImageFrame BGR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | models/cache.py, yolo_worker weights | mitigate | Allowlist known filenames (yolo26n/s/m.pt); resolve under cache root; no arbitrary URL weights in v1 |
| T-03-02 | Elevation of Privilege | (conf API in 03-02) | accept deferred | Localhost default already (MODEL-03); PATCH surface lands 03-02 |
| T-03-03 | Tampering / Integrity | dual detection paths | mitigate | Single PerceptionStore product; worker is only producer |
| T-03-04 | Denial of Service | DetectionLoop vs capture | mitigate | Dedicated detection thread + keep-latest skip; never infer in capture/handlers |
| T-03-05 | Information Disclosure / Legal | THIRD_PARTY_MODELS, README | mitigate | Document Ultralytics AGPL; offline cache; sync=False |
| T-03-SC | Tampering | npm/pip install ultralytics-opencv-headless | mitigate | RESEARCH legitimacy Approved/[ASSUMED]; optional-extra only; no [SLOP] packages |
</threat_model>

<verification>
- `uv run pytest -q` full suite green
- `uv run ruff check src tests`
- Mapping/worker/loop/cache unit tests cover DET-01, DET-02, MODEL-02 without GPU or weight download
- `grep -R VideoCapture src/sentry_ai/models` returns no matches
</verification>

<success_criteria>
- YoloDetectionWorker + DetectionLoop publish Detection lists into PerceptionStore from FrameBus frames
- results_to_detections satisfies DET-02 field contract
- Model cache helper implements MODEL-02 path policy; docs state offline re-run after first download
- Optional `detect` extra isolates heavy deps; CI mocks YOLO
- Ready for 03-02 overlays, snapshot, PATCH conf, telemetry
</success_criteria>

<output>
Create `.planning/phases/03-fixed-class-detection/03-01-SUMMARY.md` when done
</output>
