---
phase: 07-edge-profiles-extension-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sentry_ai/config/profile_runtime.py
  - src/sentry_ai/config/models.py
  - src/sentry_ai/config/profiles/desktop-gpu.yaml
  - src/sentry_ai/config/profiles/jetson.yaml
  - src/sentry_ai/config/profiles/cpu-fallback.yaml
  - src/sentry_ai/models/cache.py
  - src/sentry_ai/models/depth/mapping.py
  - src/sentry_ai/backend/protocols.py
  - src/sentry_ai/cli.py
  - src/sentry_ai/api/app.py
  - src/sentry_ai/api/routes_preview.py
  - tests/test_profile_application.py
  - tests/test_headless_serve.py
  - tests/test_config_profiles.py
  - tests/test_model_cache.py
  - tests/test_backend_protocols.py
  - tests/test_cli_serve.py
  - tests/test_depth_mapping.py
autonomous: true
requirements:
  - EDGE-02
  - EDGE-05
user_setup: []
must_haves:
  truths:
    - "Runtime profiles select detector weights, open-vocab weights, depth tier, and device policy at serve construction (EDGE-02)"
    - "desktop-gpu maps detector_tier s → yolo26s.pt; jetson and cpu-fallback map n → yolo26n.pt"
    - "Open-vocab weights derive from detector_tier (n → yoloe-26n-seg.pt; s/m → yoloe-26s-seg.pt); mode remains off by default"
    - "preferred_backend cpu/onnxruntime forces worker device=cpu; torch/tensorrt apply profile device_id honestly without claiming live TRT inference"
    - "depth_tier small is enforced; Base/Large NC depth ids remain rejected"
    - "Serve default profile remains cpu-fallback; desktop-gpu is opt-in via --profile (no CUDA auto-switch)"
    - "Startup banner prints profile, detector/OV weights, depth model, preferred_backend, resolved device, probe summary"
    - "Headless mode via create_app(serve_ui=False) and sentry serve --no-ui serves /v1 and /api without Live Preview HTML (EDGE-05)"
    - "Headless does not remove auth risk on non-localhost bind; existing warning remains"
    - "probe_device never raises; light CUDA check when torch present; never hard-fails serve"
  artifacts:
    - path: "src/sentry_ai/config/profile_runtime.py"
      provides: "ProfileRuntime + profile_runtime(cfg) + device_for_backend"
      exports: ["ProfileRuntime", "profile_runtime", "device_for_backend"]
    - path: "src/sentry_ai/models/cache.py"
      provides: "tier_to_open_vocab_weight beside tier_to_weight"
      exports: ["tier_to_open_vocab_weight", "tier_to_weight"]
    - path: "src/sentry_ai/models/depth/mapping.py"
      provides: "depth_tier allowlist helper for Small-only"
      exports: ["assert_depth_tier_allowed", "tier_to_depth_model_id"]
    - path: "src/sentry_ai/backend/protocols.py"
      provides: "Light non-raising probe_device"
    - path: "src/sentry_ai/cli.py"
      provides: "Profile application at serve + --no-ui + banner"
    - path: "src/sentry_ai/api/app.py"
      provides: "create_app(serve_ui=...) flag"
    - path: "src/sentry_ai/api/routes_preview.py"
      provides: "GET / gated when serve_ui false"
    - path: "tests/test_profile_application.py"
      provides: "Profile→weights/device policy unit coverage"
    - path: "tests/test_headless_serve.py"
      provides: "Headless create_app + /v1 still works"
  key_links:
    - from: "src/sentry_ai/cli.py serve"
      to: "profile_runtime(cfg)"
      via: "construct YOLO/YOLOE/Depth workers with weights+device"
      pattern: "profile_runtime|tier_to_open_vocab_weight|device_for_backend"
    - from: "preferred_backend tensorrt"
      to: "live PyTorch device policy + honesty log"
      via: "device_for_backend + banner text"
      pattern: "tensorrt|live path|PyTorch"
    - from: "src/sentry_ai/cli.py --no-ui"
      to: "create_app(serve_ui=False)"
      via: "serve_ui=not no_ui"
      pattern: "no_ui|serve_ui"
    - from: "routes_preview.root_preview"
      to: "app.state.serve_ui"
      via: "404/JSON when UI disabled"
      pattern: "serve_ui"
---

<objective>
Make multi-target runtime profiles executable at serve time (detector/OV/depth tiers + preferred_backend device policy + honest startup banner) and ship headless serve so robots can consume perception APIs without Live Preview HTML.

Purpose: Close EDGE-02 and EDGE-05. Profiles stop being advisory-only; headless unlocks API-only deploy. Desktop E2E docs (EDGE-01) and export recipes (EDGE-03) land in later plans.

Output: `profile_runtime` helpers, wired `sentry serve`, light `probe_device`, `--no-ui` / `serve_ui`, and green unit tests without Jetson or weight downloads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-edge-profiles-extension-stubs/07-CONTEXT.md
@.planning/phases/07-edge-profiles-extension-stubs/07-RESEARCH.md
@.planning/phases/07-edge-profiles-extension-stubs/07-PATTERNS.md
@.planning/phases/07-edge-profiles-extension-stubs/07-VALIDATION.md
@.planning/phases/06-developer-controls-open-vocab/06-02-SUMMARY.md
@.planning/phases/01-foundations-contracts/01-03-SUMMARY.md

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| Profiles drive tiers/backends at serve | detector_tier, depth_tier, preferred_backend, device_id | CONTEXT D / EDGE-02 |
| Serve default profile | Keep `cpu-fallback` — no CUDA auto-switch to desktop-gpu | RESEARCH discretion lock |
| Primary maker path | Documented `--profile desktop-gpu` (docs in 07-03); wiring here | EDGE-01 wiring |
| Live inference | Stay on PyTorch Ultralytics/HF paths | CONTEXT |
| preferred_backend tensorrt/onnxruntime | Device policy honesty — not silent TRT/ORT runtime | RESEARCH |
| Open-vocab on edge | Mode off by default; n-tier weights on jetson/cpu via detector_tier | RESEARCH |
| Headless | `create_app(serve_ui=False)` + `sentry serve --no-ui` — not separate `sentry api` | RESEARCH lock |
| MJPEG in headless | Keep `/preview/mjpeg` and `/api/*` / `/v1/*`; gate only GET `/` HTML | RESEARCH A4 |
| Probe | Light torch.cuda check; never raise; never hard-fail serve | RESEARCH |
| Packages | No new required packages | RESEARCH package audit |
| Depth NC | Only Small HF ids; reject Base/Large | MODEL + depth mapping |

## Interfaces (shipped — use these)

From `src/sentry_ai/config/models.py`:
- `ModelsConfig.detector_tier`, `depth_tier`; `DeviceConfig.preferred_backend`, `device_id`
- All configs `extra="forbid"`

From `src/sentry_ai/config/profiles/*.yaml`:
- desktop-gpu: torch / cuda:0 / detector s / depth small
- jetson: tensorrt / 0 / detector n / depth small
- cpu-fallback: onnxruntime / cpu / detector n / depth small

From `src/sentry_ai/models/cache.py`:
- `tier_to_weight(tier) → yolo26{n,s,m}.pt`
- `KNOWN_WEIGHTS` includes yoloe-26n-seg.pt and yoloe-26s-seg.pt

From workers (all accept `device: str | None`):
- `YoloDetectionWorker(weights=..., conf=..., device=...)`
- `YoloeOpenVocabWorker(weights=..., conf=..., device=...)`
- `DepthAnythingWorker(depth_mode=..., model_id=..., device=...)`
- `resolve_device(None)` → cuda > mps > cpu when arg None

From `cli.serve` today:
- Only applies `tier_to_weight(cfg.models.detector_tier)` to fixed YOLO
- OV hard-codes `YOLOE_WEIGHTS` (s); depth always Small relative with no device override
- No `--no-ui`; `create_app` always mounts preview root HTML

From `probe_device`:
- Always `available=False` today — tests assert this; update when light probe lands

## Out of scope for 07-01

- ONNX/TensorRT export recipes/scripts (07-02)
- ROS2 / voice / multi-cam fusion stubs (07-03)
- Full desktop-gpu.md + safety docs polish (07-03 EDGE-01)
- Real ORT/TRT InferenceBackend implementations
- Auto-switching default profile when CUDA present
- Separate `sentry api` command
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProfileRuntime helpers — OV tier, depth tier, device policy</name>
  <files>src/sentry_ai/config/profile_runtime.py, src/sentry_ai/models/cache.py, src/sentry_ai/models/depth/mapping.py, src/sentry_ai/config/models.py, src/sentry_ai/config/profiles/desktop-gpu.yaml, src/sentry_ai/config/profiles/jetson.yaml, src/sentry_ai/config/profiles/cpu-fallback.yaml, tests/test_profile_application.py, tests/test_model_cache.py, tests/test_depth_mapping.py, tests/test_config_profiles.py</files>
  <read_first>
    - .planning/phases/07-edge-profiles-extension-stubs/07-RESEARCH.md (EDGE-02 section, ProfileRuntime pattern, device_for_backend table)
    - .planning/phases/07-edge-profiles-extension-stubs/07-PATTERNS.md (Pattern 1 profiles)
    - src/sentry_ai/models/cache.py
    - src/sentry_ai/models/depth/mapping.py
    - src/sentry_ai/config/models.py
    - src/sentry_ai/config/load.py
    - src/sentry_ai/config/profiles/desktop-gpu.yaml
    - src/sentry_ai/config/profiles/jetson.yaml
    - src/sentry_ai/config/profiles/cpu-fallback.yaml
    - tests/test_model_cache.py
    - tests/test_config_profiles.py
    - tests/test_depth_mapping.py
  </read_first>
  <behavior>
    - tier_to_open_vocab_weight("n") → yoloe-26n-seg.pt; "s"/"m"/None/unknown → yoloe-26s-seg.pt (m stays on s — no m OV weight)
    - profile_runtime(load_config("desktop-gpu")): detector yolo26s.pt, OV yoloe-26s-seg.pt, preferred_backend torch, device from device_id policy
    - profile_runtime(load_config("jetson")): detector yolo26n.pt, OV yoloe-26n-seg.pt, preferred_backend tensorrt, device policy cuda-like without claiming live TRT
    - profile_runtime(load_config("cpu-fallback")): detector yolo26n.pt, OV yoloe-26n-seg.pt, device forced "cpu"
    - device_for_backend("cpu"|"onnxruntime", any) → "cpu"
    - device_for_backend("torch", "cuda:0") → "cuda:0" (or explicit device_id)
    - device_for_backend("tensorrt", "0") → "cuda:0" (or cuda-like from device_id); never returns a fake "tensorrt" torch device string
    - device_for_backend("openvino", ...) → None (advisory only)
    - depth_tier small → MODE_TO_MODEL relative Small id path; base/large/unknown raise ValueError or refuse
    - All three profiles still allow_cloud false; detector_tier and depth_tier present
    - Optional ModelsConfig.open_vocab_tier: if present use it; else derive from detector_tier (A7 — prefer derive without requiring YAML field; only add field if comments need it)
  </behavior>
  <action>
    1. Add `tier_to_open_vocab_weight(tier: str | None) -> str` in `models/cache.py` per RESEARCH mapping. Export from `__all__`. Extend `tests/test_model_cache.py`.

    2. Add depth tier helpers in `models/depth/mapping.py` (per EDGE-02):
       - `ALLOWED_DEPTH_TIERS = frozenset({"small"})` or equivalent
       - `assert_depth_tier_allowed(tier)` / `tier_to_depth_model_id(tier, depth_mode="relative")` that returns existing MODE_TO_MODEL Small HF id for small only
       - Refuse base/large/NC tiers with clear ValueError mentioning commercial-friendly Small only
       - Do not add Base/Large model ids to MODE_TO_MODEL

    3. Create `src/sentry_ai/config/profile_runtime.py` with:
       - `@dataclass` `ProfileRuntime` fields: profile, detector_weights, open_vocab_weights, depth_model_id (str), depth_tier, preferred_backend (str), device (str | None), device_id (str)
       - `device_for_backend(backend, device_id) -> str | None` implementing RESEARCH table (cpu/onnxruntime → cpu; torch → explicit cuda-like device_id or None for auto; tensorrt → cuda device for live PyTorch path, not TRT runtime; openvino → None)
       - `profile_runtime(cfg: SentryConfig) -> ProfileRuntime` composing tier_to_weight, tier_to_open_vocab_weight, depth tier helper, device_for_backend
       - Pure functions only — no FastAPI, no torch import, no weight download

    4. Profile YAML: keep existing tiers; add short comments that preferred_backend is device policy / export target, live path remains PyTorch. Do not change serve default. Optional: document open_vocab derives from detector_tier. Do not set continuous OV true.

    5. Extend `tests/test_config_profiles.py` with parametrized assertions for detector_tier/depth_tier/preferred_backend matrix on all three profiles. Create `tests/test_profile_application.py` covering ProfileRuntime for each profile + device_for_backend cases (including tensorrt honesty: device is cuda/cpu string, not "tensorrt").

    6. Do not wire cli yet (Task 2). Do not implement headless yet (Task 3).
  </action>
  <verify>
    <automated>uv run pytest tests/test_profile_application.py tests/test_model_cache.py tests/test_depth_mapping.py tests/test_config_profiles.py -q</automated>
  </verify>
  <done>ProfileRuntime pure helpers map all three profiles to weights/device policy; depth_tier Small-only enforced; unit tests green without models or CUDA.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire serve profile application + light probe_device + startup banner</name>
  <files>src/sentry_ai/cli.py, src/sentry_ai/backend/protocols.py, tests/test_cli_serve.py, tests/test_backend_protocols.py, tests/test_profile_application.py</files>
  <read_first>
    - .planning/phases/07-edge-profiles-extension-stubs/07-RESEARCH.md (EDGE-02 serve banner, probe_device, preferred_backend honesty)
    - .planning/phases/07-edge-profiles-extension-stubs/07-PATTERNS.md (CLI flag + profile wiring)
    - src/sentry_ai/cli.py (serve block: worker construction + banner)
    - src/sentry_ai/backend/protocols.py
    - src/sentry_ai/models/detection/yolo_worker.py (device ctor)
    - src/sentry_ai/models/detection/yoloe_worker.py
    - src/sentry_ai/models/depth/worker.py
    - tests/test_cli_serve.py
    - tests/test_backend_protocols.py
  </read_first>
  <behavior>
    - serve constructs ProfileRuntime after load_config; passes detector_weights + device into YoloDetectionWorker; open_vocab_weights + device into YoloeOpenVocabWorker; depth uses Small model_id + device
    - Default --profile remains cpu-fallback (help + Option default)
    - Banner includes: profile name, detector weight, open-vocab weight, depth model id, preferred_backend, resolved device (or "auto"), probe available summary
    - When preferred_backend is tensorrt, banner or stderr log states live path is still PyTorch CUDA if available and engines are built via export recipes (not silent TRT claim)
    - When preferred_backend is onnxruntime, live path is PyTorch CPU (or forced cpu device) and log notes ORT is export target
    - probe_device: never raises; if torch importable and profile is desktop-gpu/jetson, available=torch.cuda.is_available(); cpu-fallback may report available=True with backend cpu/onnxruntime; if torch missing, available=False without importing torch hard-fail
    - Existing allow_cloud reject and soft ImportError for detect/depth extras unchanged
    - inspect.getsource(serve) shows profile_runtime / tier_to_open_vocab_weight / device wiring
  </behavior>
  <action>
    1. Upgrade `probe_device` in `backend/protocols.py` to light advisory probe per RESEARCH (discretion lock). Never raise. Do not import torch at module top if avoidable — try/except ImportError inside function. Update `tests/test_backend_protocols.py`: remove "always unavailable" absolute assertion; assert never raises; assert DeviceInfo shape; mock or branch when torch absent so CI without CUDA still passes.

    2. In `cli.serve` after successful load_config and allow_cloud check:
       - `rt = profile_runtime(cfg)`
       - Construct YOLO with `weights=rt.detector_weights, device=rt.device`
       - Construct YOLOE with `weights=rt.open_vocab_weights, device=rt.device` (not hard-coded DEFAULT_WEIGHTS alone)
       - Construct DepthAnythingWorker with default relative mode, model_id from rt.depth_model_id (or worker default after tier assert), device=rt.device
       - Call probe_device(cfg.profile) for banner only — do not exit if unavailable

    3. Expand startup banner lines: profile, weights, depth model, preferred_backend, device, probe. Update Live Preview bind line later in Task 3 for headless wording; for now keep UI wording if no_ui not yet added.

    4. Update profile Option help text from "loaded for consistency; no ML" to accurate "selects model tiers and device policy".

    5. Tests: extend test_cli_serve with inspect-source asserts for profile_runtime / tier_to_open_vocab_weight / --profile default cpu-fallback; unit-test profile_runtime integration already in test_profile_application; backend protocol tests for probe.

    6. Do not add --no-ui yet if cleaner to land in Task 3 — OR add the flag here and implement create_app in Task 3. Prefer Task 3 for full headless end-to-end; this task may leave a no_ui stub only if needed for compile. Best: complete profile wiring fully here; headless exclusively Task 3.
  </action>
  <verify>
    <automated>uv run pytest tests/test_profile_application.py tests/test_cli_serve.py tests/test_backend_protocols.py tests/test_config_profiles.py tests/test_model_cache.py -q</automated>
  </verify>
  <done>Serve applies profile tiers and device policy to all workers; banner is honest about backends; probe is light and non-fatal; tests green without weight download.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Headless serve — serve_ui flag, --no-ui, API without HTML</name>
  <files>src/sentry_ai/api/app.py, src/sentry_ai/api/routes_preview.py, src/sentry_ai/cli.py, tests/test_headless_serve.py, tests/test_cli_serve.py, tests/test_api_preview.py</files>
  <read_first>
    - .planning/phases/07-edge-profiles-extension-stubs/07-RESEARCH.md (EDGE-05)
    - .planning/phases/07-edge-profiles-extension-stubs/07-PATTERNS.md (Pattern 2/3 create_app headless)
    - src/sentry_ai/api/app.py
    - src/sentry_ai/api/routes_preview.py (root_preview, preview_mjpeg, api_status)
    - src/sentry_ai/cli.py (create_app call + banner)
    - tests/test_api_preview.py (TestClient patterns)
    - tests/test_api_v1.py (snapshot patterns)
  </read_first>
  <behavior>
    - create_app(..., serve_ui: bool = True) stores app.state.serve_ui
    - When serve_ui=True: GET / returns HTML 200 (existing behavior)
    - When serve_ui=False: GET / returns 404 JSON (or non-HTML) with detail mentioning headless / UI disabled and pointer to /v1/snapshot — not FileResponse HTML
    - serve_ui=False still serves /api/status 200, /v1/snapshot (200 or empty-product shape), /api/pipeline/config when injected, and keeps /preview/mjpeg available for debug
    - CLI: `--no_ui` / `--no-ui` Option default False; passes serve_ui=not no_ui
    - Optional env SENTRY_NO_UI=1 treated as no_ui true is nice-to-have; not required if flag works
    - Banner: when headless, bind line says headless API not Live Preview
    - Non-localhost warning still fires under --no-ui
    - serve --help shows --no-ui
    - No separate sentry api command
  </behavior>
  <action>
    1. Add `serve_ui: bool = True` to `create_app`. Set `app.state.serve_ui = serve_ui`. Always include preview_router (status + mjpeg live here) — gate only HTML root.

    2. Update `root_preview` to read serve_ui from request.app.state (default True if missing for backward compat). When False: return JSONResponse status 404 with body like `{"detail": "UI disabled (headless)", "v1": "/v1/snapshot"}` — do not serve index.html.

    3. CLI: add `no_ui: bool = typer.Option(False, "--no-ui", help="Serve perception API without Live Preview HTML (EDGE-05).")`. Pass `serve_ui=not no_ui` into create_app. Adjust banner bind line for headless vs Live Preview.

    4. Create `tests/test_headless_serve.py`:
       - SyntheticSource + CaptureLoop + create_app(serve_ui=False) TestClient
       - GET / not HTML 200 (status 404 and content-type json or body lacks html doctype)
       - GET /api/status 200
       - GET /v1/snapshot still works (200 with PerceptionFrame-shaped body or existing empty behavior)
       - create_app(serve_ui=True) still returns HTML at GET /
       - finally stop capture loop

    5. Extend test_cli_serve: help shows --no-ui; inspect source contains no_ui and serve_ui.

    6. Ensure existing test_api_preview GET / HTML tests still pass (default serve_ui True).
  </action>
  <verify>
    <automated>uv run pytest tests/test_headless_serve.py tests/test_cli_serve.py tests/test_api_preview.py tests/test_profile_application.py tests/test_backend_protocols.py -q</automated>
  </verify>
  <done>Headless API mode works without Live Preview HTML; profile wiring + headless complete for EDGE-02/EDGE-05; targeted tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI profile/device strings → worker construction | Config-derived device policy affects where inference runs |
| Headless API → LAN consumers | UI absence must not be mistaken for authentication |
| probe_device → health/banner | Advisory only; must not crash serve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information disclosure | serve --no-ui --host 0.0.0.0 | mitigate | Keep default 127.0.0.1; retain non-localhost warning; banner honesty |
| T-07-02 | Spoofing | preferred_backend=tensorrt | mitigate | Startup log clarifies live PyTorch path; no fake TRT backend |
| T-07-03 | Tampering | depth_tier base/large | mitigate | Small-only allowlist; ValueError; never load NC models |
| T-07-04 | Denial of service | probe_device torch import | mitigate | try/except; never raise; never block serve |
| T-07-05 | Elevation of privilege | Headless API surface | accept | Same perception-only routes as UI mode; no motor fields (API-05) |
| T-07-SC | Tampering | package installs | accept | No new packages in 07-01 |
</threat_model>

<verification>
- `uv run pytest tests/test_profile_application.py tests/test_headless_serve.py tests/test_cli_serve.py tests/test_backend_protocols.py tests/test_config_profiles.py tests/test_model_cache.py tests/test_depth_mapping.py -q` green
- Optional manual: `sentry serve --profile desktop-gpu --source synthetic` shows profile banner; `sentry serve --no-ui` → curl `/` not HTML, curl `/v1/snapshot` works
- No weight download required in CI
</verification>

<success_criteria>
1. EDGE-02: All three profiles drive detector/OV weights + device policy at serve
2. EDGE-05: `--no-ui` / `serve_ui=False` serves perception API without Live Preview HTML
3. Backend honesty: tensorrt/onnxruntime preferences never silent-claim live TRT/ORT inference
4. Serve default remains cpu-fallback; allow_cloud false unchanged
5. No export recipes, ROS2, or full desktop docs scope creep (later plans)
</success_criteria>

<output>
Create `.planning/phases/07-edge-profiles-extension-stubs/07-01-SUMMARY.md` when done
</output>
