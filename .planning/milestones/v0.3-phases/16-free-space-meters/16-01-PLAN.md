---
phase: 16-free-space-meters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sentry_ai/spatial/free_space.py
  - tests/test_free_space_bands.py
autonomous: true
requirements:
  - FS-01
  - FS-02
user_setup: []
must_haves:
  truths:
    - "compute_free_space(kind=METRIC_CALIBRATED) emits units='m' only because absolute meter cuts ran on the map (not a label flip of 0.72/0.45)"
    - "Default metric cuts are 1.5 m near / 3.0 m mid (constants); ordinal near_cut/mid_cut are ignored on the calibrated path"
    - "Calibrated path pins higher_is_farther and never min-max normalizes meters (does not call depth_to_nearness)"
    - "A 4.0-5.0 m scene with a 4.1 m blob is far on the metric path (near_frac~0, no near obstacles) while the same array with kind=RELATIVE may still emit a near blob"
    - "RELATIVE and METRIC_ESTIMATED still emit units='ordinal' via existing percentile nearness + auto polarity"
    - "nearness_mean/nearness_max stay in [0, 1] on the metric path (fixed 3.0 m horizon)"
    - "A 0.5 m blob on a 5 m background is occupied on the metric path; a uniform 2.0 m map is mid (not a fake near/far split)"
    - "No loop/assemble/validator/store/schema edits; no distance_m; no YAML; no DetectionLoop/FrameBus/ORT-TRT/kind_for_mode"
  artifacts:
    - path: "src/sentry_ai/spatial/free_space.py"
      provides: "Metric band branch + DEFAULT_METRIC_*_CUT_M + _meters_to_nearness"
      exports: ["DEFAULT_METRIC_NEAR_CUT_M", "DEFAULT_METRIC_MID_CUT_M", "compute_free_space"]
    - path: "tests/test_free_space_bands.py"
      provides: "FS-01/FS-02 honesty matrix including 4-5 m smoking-gun"
  key_links:
    - from: "compute_free_space kind=METRIC_CALIBRATED"
      to: "FreeSpaceResult.units='m'"
      via: "absolute meter cuts on scaled map (higher_is_farther)"
      pattern: "METRIC_CALIBRATED|metric_near_cut_m|_meters_to_nearness"
    - from: "compute_free_space kind=RELATIVE|METRIC_ESTIMATED"
      to: "FreeSpaceResult.units='ordinal'"
      via: "depth_to_nearness + 0.72/0.45"
      pattern: "units=.ordinal.|depth_to_nearness"
    - from: "ordinal near_cut/mid_cut"
      to: "ignored when kind is METRIC_CALIBRATED"
      via: "metric constants 1.5/3.0"
      pattern: "DEFAULT_METRIC_NEAR_CUT_M|DEFAULT_METRIC_MID_CUT_M"
---

<objective>
Ship a pure `compute_free_space` metric-band path so `units="m"` is honest: absolute meter cuts on an already-scaled depth map when `kind=metric_calibrated`, while relative and `metric_estimated` stay ordinal percentile nearness.

Purpose: FS-01 (`units="m"` only when calibrated) and FS-02 (never flip labels while still computing 0.72/0.45 nearness as if meters).

Output: `spatial/free_space.py` metric branch + synthetic honesty tests. Zero new pip packages. No FreeSpaceLoop/assemble/validator wiring (16-02). No `distance_m`. No re-scale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-free-space-meters/16-RESEARCH.md
@.planning/phases/16-free-space-meters/16-PATTERNS.md
@.planning/phases/16-free-space-meters/16-VALIDATION.md
@src/sentry_ai/spatial/free_space.py
@src/sentry_ai/spatial/smoothing.py
@src/sentry_ai/schemas/enums.py
@tests/test_free_space_bands.py
@src/sentry_ai/models/depth/loop.py

## Locked decisions (ROADMAP + RESEARCH — must honor)

| Decision | Value | Source |
|----------|-------|--------|
| When `units="m"` | Calibrated **iff** absolute meter cuts on scaled depth (`higher_is_farther`). Never label-only 0.72/0.45 flip | RESEARCH lock #1 |
| Relative + estimated | `units="ordinal"`; existing percentile + `auto` polarity | RESEARCH lock #2 |
| Cue nearness | `nearness_*` remain 0..1 (fixed 3.0 m horizon on metric path) | RESEARCH lock #3 |
| Default metric cuts | near 1.5 m / mid 3.0 m constants; do not reuse ordinal sliders as meters | RESEARCH lock #4 |
| Polarity / normalize | Pin `higher_is_farther`; **never min–max** meters; do not call `depth_to_nearness` on calibrated path | RESEARCH lock #5 |
| Consume, don't scale | Map is already DepthLoop-scaled; no `apply_map` here | RESEARCH lock #9 |
| Constraints | Zero new deps; freeze DetectionLoop/FrameBus/ORT-TRT/`kind_for_mode`; synthetic only; no YAML; no wizard redesign; no FSD/RANSAC | RESEARCH lock #10 |
| `distance_m` / loop / assemble / validator | 16-02 | Plan split |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| Meter→nearness | `_meters_to_nearness`: `clip((3.0 - d) / 3.0, 0, 1)` using `DEFAULT_METRIC_MID_CUT_M` as a **constant horizon** — not `dmin`/`dmax` |
| Occupied seed (metric) | Finite ROI pixels with `d < metric_near_cut_m` (1.5 m), then existing morphology/smoother |
| Band denominator | Finite ROI pixels (non-finite excluded from near/mid/far counts) |
| Invalid metric cuts | If `metric_near_cut_m >= metric_mid_cut_m` → `FreeSpaceResult(error=..., units="ordinal", depth_kind=kind)` |
| Error/exception path | Keep `units="ordinal"` (do not claim meters if compute failed) |
| `distance_m` | **Omitted** (16-02). Existing tests that assert no `distance_m` on relative cues stay green |
| Test file | Extend `tests/test_free_space_bands.py` (analog); do not add a new test module unless the file becomes unwieldy |
| `occupied_mask` override | Still honored on both paths (caller-provided mask + ROI) |

## Interfaces (shipped — consume, do not redesign)

From `spatial/free_space.py` today:
```python
DEFAULT_NEAR_CUT = 0.72          # ordinal nearness
DEFAULT_MID_CUT = 0.45
def depth_to_nearness(depth_map, *, nearness_polarity="auto") -> np.ndarray: ...
def compute_free_space(depth_map, *, kind: DepthKind, ...) -> FreeSpaceResult: ...
class ObstacleCue:  # bbox, nearness_mean/max, area_px, band — no distance_m yet
class FreeSpaceResult:  # units currently always "ordinal"
```

From DepthLoop (do not edit):
```python
# sole scale site — free-space receives the already-scaled map + promoted kind
kind, unit = calibration.promote_kind_unit(kind, unit)
depth_map = calibration.apply_map(depth_map)
store.set_depth(..., kind=kind, unit=unit, depth_map=depth_map)
```

## Target APIs (implement)

```python
DEFAULT_METRIC_NEAR_CUT_M = 1.5
DEFAULT_METRIC_MID_CUT_M = 3.0

def _meters_to_nearness(depth_m: np.ndarray) -> np.ndarray: ...

def compute_free_space(
    depth_map: np.ndarray,
    *,
    kind: DepthKind,
    nearness_polarity: NearnessPolarity = "auto",
    roi_bottom_frac: float = DEFAULT_ROI_BOTTOM_FRAC,
    near_cut: float = DEFAULT_NEAR_CUT,
    mid_cut: float = DEFAULT_MID_CUT,
    min_area_frac: float = DEFAULT_MIN_AREA_FRAC,
    smoother: OccupancySmoother | None = None,
    occupied_mask: np.ndarray | None = None,
    apply_morphology: bool = True,
    metric_near_cut_m: float = DEFAULT_METRIC_NEAR_CUT_M,
    metric_mid_cut_m: float = DEFAULT_METRIC_MID_CUT_M,
) -> FreeSpaceResult: ...
```

Export the new constants from `__all__`.

## Out of scope for 16-01

- `FreeSpaceLoop.reset_smoother` / kind tracking (16-02)
- `assemble._units_for_depth_kind` flip (16-02)
- `assert_free_space_units` tighten (16-02 — wire still allows calibrated+ordinal until assemble flips)
- `ObstacleCue.distance_m` / store `units` field (16-02)
- YAML, wizard REST, DetectionLoop/FrameBus/ORT-TRT/`kind_for_mode`
- Re-scale / RANSAC / FSD / motor
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Metric compute_free_space path + FS-01/FS-02 honesty tests</name>
  <files>src/sentry_ai/spatial/free_space.py, tests/test_free_space_bands.py</files>
  <read_first>
    - .planning/phases/16-free-space-meters/16-RESEARCH.md (Locked Decisions 1-5, 9-10; Code Examples; Pitfalls 1-2, 4, 6)
    - .planning/phases/16-free-space-meters/16-PATTERNS.md (free_space.py section)
    - .planning/phases/16-free-space-meters/16-VALIDATION.md
    - src/sentry_ai/spatial/free_space.py
    - tests/test_free_space_bands.py
    - src/sentry_ai/schemas/enums.py
  </read_first>
  <behavior>
    - kind=METRIC_CALIBRATED + map with 0.5 m lower-center blob on 5.0 m background → units=="m", depth_kind calibrated, ≥1 obstacle, occupied inside blob/ROI, method still "near_field_bands"
    - kind=RELATIVE or METRIC_ESTIMATED on the same 0.5/5.0 array → units=="ordinal" (existing tests remain green; estimated still ordinal)
    - 4.0–5.0 m map with a 4.1 m blob: calibrated → near_frac ≈ 0 (tol 0.02), no near-band obstacles (occupied empty or ~0); relative on the same array may still produce occupied/near blob (percentile). This is the FS-02 smoking gun — label-only 0.72/0.45 flip would fail it
    - Uniform 2.0 m calibrated map: mid_frac dominant, near_frac ≈ 0, far_frac ≈ 0; proves no per-frame min–max (min–max of a constant/near-constant map is undefined or splits noise into near/far)
    - Calibrated path ignores near_cut=0.99 and mid_cut=0.01: a 0.5 m blob still occupied; a 2.0 m region is still mid not near
    - nearness_mean/max on metric obstacles in [0, 1]; 0.5 m blob has higher nearness than a 2.0 m blob would
    - Calibrated path does not use auto polarity: inverting a meter map (5 m blob on 0.5 m bg) must NOT treat the 5 m blob as near
    - Existing relative polarity/auto/ROI/band-sum/no-ml-import tests stay green
    - Module still has no torch/transformers/ultralytics imports
    - No distance_m field required on ObstacleCue in this plan
  </behavior>
  <acceptance_criteria>
      - kind=METRIC_CALIBRATED + 0.5 m blob on 5 m bg → units=="m", occupied in blob, depth_kind calibrated
      - RELATIVE and METRIC_ESTIMATED on the same array → units=="ordinal"
      - 4.0–5.0 m + 4.1 m blob: metric near_frac≈0 and no near obstacles; relative may still occupy (FS-02)
      - Uniform 2.0 m calibrated → mid, not a fake near/far split (no min–max)
      - Ordinal near_cut/mid_cut ignored when calibrated (1.5/3.0 m still apply)
      - nearness_* in [0, 1] on metric path
      - Inverted meter map does not auto-flip polarity (5 m blob is far)
      - Existing relative tests + no-ml-import test stay green
      - No loop/assemble/validator/store/schema/distance_m edits
  </acceptance_criteria>
  <action>
    1. In `src/sentry_ai/spatial/free_space.py`:
       - Module docstring: two modes; meters only when calibrated via absolute cuts; consume scaled maps; never min–max meters.
       - Add `DEFAULT_METRIC_NEAR_CUT_M = 1.5`, `DEFAULT_METRIC_MID_CUT_M = 3.0` to `__all__`.
       - Add `_meters_to_nearness(depth_m)` using constant horizon `DEFAULT_METRIC_MID_CUT_M`. Finite-only; others 0.
       - Extend `compute_free_space` with `metric_near_cut_m` / `metric_mid_cut_m` kwargs (defaults = constants).
       - If `kind == DepthKind.METRIC_CALIBRATED`:
         - If `metric_near_cut_m >= metric_mid_cut_m` → error result, `units="ordinal"`.
         - Do **not** call `depth_to_nearness`. Pin farther-is-higher (smaller d = nearer).
         - `nearness = _meters_to_nearness(arr)`.
         - Finite mask: `np.isfinite(arr)`.
         - Bands over finite ROI: near `arr < near_m`; mid `near_m <= arr < mid_m`; far `arr >= mid_m`.
         - Occupied seed: `(finite & (arr < near_m) & roi)` unless `occupied_mask` provided (then existing mask & roi path).
         - Morphology/smoother unchanged.
         - `units = "m"`.
       - Else: existing ordinal path; ignore metric cut kwargs; `units = "ordinal"`.
       - Keep error/exception results at `units="ordinal"`.
       - Do not add `distance_m` to `ObstacleCue`.
    2. Extend `tests/test_free_space_bands.py` with the behavior bullets. Reuse the existing synthetic helper style (HxW float32, lower-center blob). Keep `test_metric_estimated_still_ordinal_units` and `test_relative_kind_units_ordinal_method_and_no_distance_m`.
    3. Do not edit `spatial/loop.py`, `api/assemble.py`, `schemas/validators.py`, `perception_store.py`, `routes_calibration.py`, `models/depth/loop.py`, or `kind_for_mode`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_free_space_bands.py tests/test_free_space_loop.py tests/test_free_space_smoothing.py tests/test_calibration_validators.py tests/test_assemble_perception_frame.py -q</automated>
  </verify>
  <done>Pure compute_free_space emits honest meters only when calibrated via 1.5/3.0 m cuts; ordinal path unchanged; FS-02 smoking-gun green; loop/wire not yet flipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scaled depth map → free-space units | Label-only `"m"` would lie to robots |
| Metric cuts vs ordinal sliders | Reusing 0.72/0.45 as meters mis-bands every scene |
| Polarity | `auto` on meters can invert a hallway into “near” |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Spoofing | units="m" without meter cuts | mitigate | Branch requires absolute cuts; FS-02 4–5 m test |
| T-16-02 | Tampering | Min–max of calibrated map | mitigate | Never call depth_to_nearness when calibrated; uniform 2.0 m test |
| T-16-03 | Tampering | Ordinal sliders used as meters | mitigate | Ignore near_cut/mid_cut on calibrated path; explicit test |
| T-16-04 | Spoofing | auto polarity on meters | mitigate | Pin higher_is_farther; inverted-map test |
| T-16-05 | Spoofing | metric_estimated as calibrated | mitigate | Estimated stays ordinal (existing + regression) |
| T-16-06 | Elevation | Re-scale / apply_map in free-space | mitigate | Out of scope; consume map as given |
| T-16-07 | Elevation | Loop/assemble/validator/YAML/FSD | mitigate | 16-01 file list; grep verify |
| T-16-SC | Tampering | Package installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `uv run pytest tests/test_free_space_bands.py tests/test_free_space_loop.py tests/test_free_space_smoothing.py tests/test_calibration_validators.py tests/test_assemble_perception_frame.py -q`
- Grep: no edits to `spatial/loop.py`, `api/assemble.py`, `schemas/validators.py`, `perception_store.py`, `models/depth/loop.py`, `cli.py`, routes, or `index.html`
- Confirm `compute_free_space` calibrated branch does not call `depth_to_nearness`
</verification>

<success_criteria>
1. Calibrated maps emit `units="m"` because 1.5/3.0 m cuts ran (FS-01).
2. A 4–5 m scene is far in metric and would be a lie if only the label flipped (FS-02).
3. Relative and `metric_estimated` remain ordinal percentile nearness.
4. Zero new deps; no loop/wire/validator/distance_m/YAML scope creep.
</success_criteria>

<output>
Create `.planning/phases/16-free-space-meters/16-01-SUMMARY.md` when done
</output>
