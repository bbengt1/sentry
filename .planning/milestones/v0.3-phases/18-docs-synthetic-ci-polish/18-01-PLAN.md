---
phase: 18-docs-synthetic-ci-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/calibration.md
  - docs/perception-frame.md
  - docs/safety-and-privacy.md
  - README.md
  - docs/README.md
  - docs/api-reference.md
  - docs/cli.md
  - docs/configuration.md
  - docs/architecture.md
  - docs/desktop-gpu.md
  - CHANGELOG.md
  - tests/test_calibration_docs.py
  - tests/test_safety_docs.py
autonomous: true
requirements:
  - OPS-02
user_setup: []
must_haves:
  truths:
    - "docs/calibration.md is a numbered operator hub: wizard → persist path → honesty triad → Cancel/Clear → persist status; approximate monocular scale, not vehicle-grade"
    - "Persist path documented as STACK $SENTRY_MODEL_CACHE/calibration/{safe_id}.yaml (or default_cache_root) plus optional SENTRY_CALIBRATION_DIR and --calibration-file — not ~/.config JSON"
    - "Honesty triad in hub + perception-frame: relative never m; metric_estimated ≠ calibrated; metric_calibrated + m only when applied+valid; draft never claims calibrated"
    - "Free-space hubs no longer say always ordinal; units=m iff metric_calibrated + 1.5/3.0 m cuts; optional distance_m when calibrated"
    - "Cancel = draft-only; Clear deletes YAML; persist status none|applied|ignored_mismatch|error separate from depth.kind"
    - "Keyword tests forbid stale always-ordinal / FSD-as-claim / precise meters / autonomous-as-claim on hub surfaces"
    - "README and docs/README link docs/calibration.md; CHANGELOG Unreleased notes v0.3 operator docs; pyproject stays 0.1.0"
    - "No src/sentry_ai product edits; no DetectionLoop/FrameBus/ORT-TRT/kind_for_mode; zero new deps"
  artifacts:
    - path: "docs/calibration.md"
      provides: "OPS-02 operator calibration hub"
      min_lines: 60
    - path: "tests/test_calibration_docs.py"
      provides: "TDD keyword lock for hub + stale-phrase forbid"
    - path: "docs/perception-frame.md"
      provides: "Wire contract: FS meters only when calibrated; optional distance_m"
    - path: "docs/safety-and-privacy.md"
      provides: "Non-FSD + FS not always-ordinal; link calibration hub"
    - path: "README.md"
      provides: "Root discoverability of calibration hub; no v1-always-ordinal"
  key_links:
    - from: "README.md + docs/README.md"
      to: "docs/calibration.md"
      via: "documentation table / start-here row"
      pattern: "calibration.md"
    - from: "docs/calibration.md"
      to: "STACK persist path"
      via: "SENTRY_MODEL_CACHE/calibration or default_cache_root"
      pattern: "SENTRY_MODEL_CACHE|calibration/"
    - from: "keyword tests"
      to: "hub surfaces"
      via: "Path.read_text asserts"
      pattern: "always ordinal|metric_calibrated|not vehicle-grade"
---

<objective>
Close the v0.3 docs honesty gap: operators get a guided non-FSD calibration flow, and hub surfaces stop claiming free-space is always ordinal.

Purpose: OPS-02 — operator docs describe the wizard, STACK persist path, and honesty rules without vehicle-grade / FSD claims.

Output: `docs/calibration.md` hub, refreshed stale hubs, TDD keyword tests green. Zero runtime product code. Zero new packages. Do not bump pyproject 0.1.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-docs-synthetic-ci-polish/18-RESEARCH.md
@.planning/phases/18-docs-synthetic-ci-polish/18-PATTERNS.md
@.planning/phases/18-docs-synthetic-ci-polish/18-VALIDATION.md
@.planning/milestones/v0.2-phases/12-docs-ci-packaging-polish/12-01-PLAN.md
@docs/perception-frame.md
@docs/safety-and-privacy.md
@README.md
@docs/README.md
@docs/api-reference.md
@docs/cli.md
@docs/configuration.md
@docs/architecture.md
@docs/desktop-gpu.md
@docs/edge-serve.md
@CHANGELOG.md
@tests/test_safety_docs.py
@tests/test_edge_serve_docs.py
@tests/test_export_docs.py

## Locked decisions (ROADMAP + RESEARCH + user brief — must honor)

| Decision | Value | Source |
|----------|-------|--------|
| Hub | New `docs/calibration.md` + refresh stale hubs listed in files_modified | Lock #user-1 |
| Persist path | STACK `$SENTRY_MODEL_CACHE/calibration/{safe_id}.yaml` or `default_cache_root`; optional `SENTRY_CALIBRATION_DIR` + `--calibration-file`. **Not** `~/.config` JSON | Lock #1 |
| Honesty triad | Relative never m; `metric_estimated` ≠ calibrated; `metric_calibrated` + m only when applied+valid; draft never calibrated | Lock #2 |
| Free-space | `units="m"` iff calibrated + 1.5/3.0 m cuts; else ordinal; optional `distance_m` when calibrated | Lock #3 |
| Cancel / Clear | Cancel = draft-only; Clear deletes YAML | Lock #4 |
| Persist status | `none\|applied\|ignored_mismatch\|error` separate from `depth.kind` | Lock #5 |
| Wizard copy | Approximate metric scale, monocular, not vehicle-grade | Lock #6 |
| Keyword tests | Forbid stale “always ordinal” / FSD-as-claim / precise meters / autonomous-as-claim | Lock #7 |
| Constraints | Zero new deps; freeze DetectionLoop/FrameBus/ORT-TRT/`kind_for_mode`; no pyproject bump; no product code | Lock #9 |
| CI extras / room | 18-02 | Plan split |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| Hub structure | Mirror `docs/edge-serve.md`: numbered steps + tables + outbound links; do not paste full REST schemas (link api-reference) |
| Keyword module | New `tests/test_calibration_docs.py` (Phase 12 `test_edge_serve_docs.py` shape) |
| Safety extend | Keep existing non-autonomy asserts; add always-ordinal forbid + calibration.md link |
| FSD / autonomous asserts | Forbid **claims** (`"sentry is fsd"`, `"precise meters"`, `"always ordinal"`). **Allow** “not FSD” / “not an autonomous driving stack” (already on safety page) |
| CHANGELOG | `## [Unreleased]` Added/Changed only; do **not** rewrite `[0.1.0]` historical “ordinal nearness” |
| Versioning note | `docs/README.md`: planning milestone v0.3 UX; package **stays 0.1.0** |
| Historical Phase 4/5 README sections | Update the **current** Free-space units sentence; do not delete Phase 4/5 scope lists |
| TDD | Task 1 RED (tests only) then Task 2 GREEN (docs). Prefer two commits |

## Interfaces (shipped — document, do not redesign)

```text
POST /api/depth/calibration/{freeze,sample,fit,apply,cancel,clear,save}
GET  /api/status  → calibration_active, scale, method, calibration_persist, calibration_persist_reason
sentry serve --calibration-file PATH
SENTRY_MODEL_CACHE / SENTRY_CALIBRATION_DIR
CalibrationState.apply / apply_params / clear_applied / clear_draft
try_reapply → none|applied|ignored_mismatch|error
Free-space units="m" iff DepthKind.METRIC_CALIBRATED + 1.5/3.0 m cuts
```

## Target APIs (docs + tests only)

```python
# tests/test_calibration_docs.py
def test_calibration_doc_exists() -> None: ...
def test_calibration_hub_wizard_and_honesty() -> None: ...
def test_calibration_hub_persist_stack_path() -> None: ...
def test_hubs_forbid_stale_always_ordinal_and_overclaim() -> None: ...
def test_readme_and_docs_index_link_calibration_hub() -> None: ...
```

## Out of scope for 18-01

- `tests/test_v03_honesty_matrix.py` and any `ci.yml` edit (18-02)
- Any file under `src/sentry_ai/`
- Wizard HTML / REST handler changes
- DetectionLoop / FrameBus / ORT-TRT / `kind_for_mode`
- New pip deps; `pyproject` version bump
- REQUIREMENTS checkbox closeout / complete-milestone
- Documenting `~/.config` JSON / platformdirs as the persist path
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — keyword tests for calibration hub + stale phrases</name>
  <files>tests/test_calibration_docs.py, tests/test_safety_docs.py</files>
  <read_first>
    - .planning/phases/18-docs-synthetic-ci-polish/18-RESEARCH.md (stale-phrase table; locks 1–7, 9)
    - .planning/phases/18-docs-synthetic-ci-polish/18-PATTERNS.md (keyword module shape)
    - tests/test_edge_serve_docs.py
    - tests/test_export_docs.py (forbid/require pattern)
    - tests/test_safety_docs.py
    - README.md (Free-space “v1 always ordinal”)
    - docs/perception-frame.md (No distance_m; v0.1 ordinal)
    - docs/safety-and-privacy.md
  </read_first>
  <behavior>
    - docs/calibration.md must exist
    - Hub text (lowered) must include: wizard, apply, cancel, clear, metric_calibrated, monocular, and (vehicle-grade or not vehicle-grade or approximate)
    - Hub must mention SENTRY_MODEL_CACHE or calibration/ yaml path; must mention SENTRY_CALIBRATION_DIR or --calibration-file
    - Hub must mention persist status tokens none or applied and ignored_mismatch (or fingerprint)
    - Hub must not contain ~/.config as the persist path; must not contain “always ordinal”; must not claim “precise meters”
    - README.md and docs/README.md must mention calibration.md
    - Combined hub surfaces (calibration.md, perception-frame, safety, README, docs/README, api-reference, cli, configuration, architecture, desktop-gpu) must not contain “always ordinal” or “v1 always ordinal”
    - Those hubs must not contain “precise meters” / “precise metres”
    - FSD-as-claim: lowered text must not contain “full self-driving” or “fsd-capable” or “autonomous vehicle” as a product capability; “not … fsd” / “not an autonomous” is allowed
    - Existing test_safety_docs.py non-autonomy / localhost / interlock asserts stay green
    - safety-and-privacy.md must not contain “always ordinal”; must mention calibration.md or metric_calibrated or “when calibrated”
  </behavior>
  <acceptance_criteria>
    - New test_calibration_docs.py exists with the functions named in Target APIs (or equivalent coverage)
    - `uv run pytest tests/test_calibration_docs.py tests/test_safety_docs.py -q --tb=line` is RED against current stale hubs / missing calibration.md
    - No files under src/sentry_ai/ or docs/*.md (except none) edited in this task — tests only
  </acceptance_criteria>
  <action>
    Add failing keyword tests first (TDD RED). Do not edit product docs in this task.

    1. Create tests/test_calibration_docs.py mirroring test_edge_serve_docs.py (REPO_ROOT Path.read_text).
    2. Implement the behavior bullets. Use a shared hub-path tuple so Task 2 can satisfy every assert by editing those files only.
    3. Extend tests/test_safety_docs.py: forbid “always ordinal” on the safety page; require calibration.md or “when calibrated” / metric_calibrated. Do not weaken existing perception-only asserts.
    4. Run the new/extended tests and confirm RED (missing hub + stale README/perception-frame/safety phrases).
    5. Commit tests-only if the workflow expects a RED commit.

    Avoid: editing markdown hubs; loading models; requiring Jetson/CUDA/depth extra; inventing asserts that ban legitimate “not FSD” / “not autonomous” language.
  </action>
  <verify>
    <automated>uv run pytest tests/test_calibration_docs.py tests/test_safety_docs.py -q --tb=line</automated>
  </verify>
  <done>Keyword suite encodes OPS-02 honesty and fails against pre-fix hub surfaces (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Calibration hub + stale-hub refresh (OPS-02 GREEN)</name>
  <files>docs/calibration.md, docs/perception-frame.md, docs/safety-and-privacy.md, README.md, docs/README.md, docs/api-reference.md, docs/cli.md, docs/configuration.md, docs/architecture.md, docs/desktop-gpu.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/18-docs-synthetic-ci-polish/18-PATTERNS.md (hub shape + refresh table)
    - .planning/phases/18-docs-synthetic-ci-polish/18-RESEARCH.md (stale-phrase table; locks 1–7)
    - docs/edge-serve.md (numbered hub template)
    - docs/perception-frame.md
    - docs/safety-and-privacy.md
    - README.md (Documentation table + Free-space units sentence)
    - docs/README.md (Start-here + Versioning)
    - docs/api-reference.md (Depth table — add calibration section after Depth)
    - docs/cli.md (serve options table)
    - docs/configuration.md (env + cache layout)
    - docs/architecture.md (spine + key modules)
    - docs/desktop-gpu.md (Expected stages)
    - CHANGELOG.md (Unreleased)
    - tests/test_calibration_docs.py (acceptance keywords from Task 1)
  </read_first>
  <action>
    Make OPS-02 true. No runtime code. Satisfy Task 1 tests.

    1. Create docs/calibration.md (PATTERNS target shape; ≥60 lines):
       - Framing: approximate monocular metric scale; **not vehicle-grade**; not FSD
       - Numbered operator path: (1) `uv sync --extra depth` on the maker machine (docs only — CI stays --extra dev) (2) `sentry serve` + Live Preview (3) known-distance samples (height optional/approximate) (4) Fit preview / residual (5) Apply vs Cancel (6) optional Save or apply `persist:true` (7) restart re-applies only on fingerprint match (8) Clear deletes YAML
       - Honesty triad table
       - Free-space meters iff calibrated + 1.5 m / 3.0 m cuts; else ordinal; optional `distance_m`
       - Persist: `$SENTRY_MODEL_CACHE/calibration/{safe_id}.yaml` or `default_cache_root()/calibration/`; `SENTRY_CALIBRATION_DIR`; `--calibration-file`
       - Persist status enum ≠ `depth.kind`; mismatch → `ignored_mismatch`, stay relative
       - Links to perception-frame, safety, api-reference, cli, configuration, desktop-gpu
       - Never document `~/.config` JSON / platformdirs as the path

    2. docs/perception-frame.md:
       - Depth table: `metric_calibrated` = meters after **applied+valid** calibration (wizard/persist), not the default path
       - Free-space: replace “v0.1 … not calibrated meters” and “No distance_m” with units m iff calibrated; nearness 0..1; optional `distance_m` when calibrated

    3. docs/safety-and-privacy.md:
       - Free-space is ordinal **unless** depth is `metric_calibrated`; still **not** a safety interlock
       - Keep “not an autonomous driving / FSD stack”
       - Related docs: add calibration.md
       - Operator checklist: treat calibrated meters as approximate monocular scale

    4. README.md:
       - Documentation table: add calibration.md row
       - Replace Free-space `units` (v1 always ordinal — not calibrated meters) with the locked triad sentence
       - Short pointer under Optional monocular depth: Live Preview wizard → docs/calibration.md

    5. docs/README.md: Start-here row for calibration.md; Versioning — planning milestone v0.3 Metric Depth Calibration UX; package remains 0.1.0 (do not imply a release cut)

    6. docs/api-reference.md: after Depth, add Calibration table (freeze/sample/fit/apply/cancel/clear/save; apply optional persist; status persist fields). extra=forbid. 503 if state missing. Link calibration.md

    7. docs/cli.md: `--calibration-file` on serve; banner `calibration: {status}`; note SENTRY_CALIBRATION_DIR

    8. docs/configuration.md: env rows for SENTRY_CALIBRATION_DIR; cache layout `calibration/{safe_id}.yaml`; one-liner --calibration-file

    9. docs/architecture.md: v0.3 plug-in — CalibrationState.apply_map after DepthAnythingWorker.process before set_depth; persist I/O in config/calibration_store.py; key modules rows; Boundaries: estimated ≠ calibrated; relative never sold as meters

    10. docs/desktop-gpu.md: Expected stages — relative default; optional wizard → metric_calibrated; free-space meters only when calibrated; link calibration.md; keep “Not robot control”

    11. CHANGELOG.md Unreleased: Added operator calibration guide; Changed hub honesty (FS meters when calibrated; persist path). Do not rewrite [0.1.0]. Do not bump version.

    Avoid: src/ edits; factory/CLI code; tensorrt extra; FPS; FSD claims; ~/.config JSON; pyproject version.
  </action>
  <acceptance_criteria>
    - docs/calibration.md exists and contains wizard + STACK persist path + honesty triad + Cancel/Clear + persist status
    - `rg -n "always ordinal|v1 always ordinal|precise meters" README.md docs/calibration.md docs/perception-frame.md docs/safety-and-privacy.md docs/README.md docs/api-reference.md docs/cli.md docs/configuration.md docs/architecture.md docs/desktop-gpu.md` returns no matches
    - `rg -n "calibration.md" README.md docs/README.md` matches
    - `rg -n "~/.config" docs/calibration.md` returns no persist-path claim (or only a “not this path” note)
    - `uv run pytest tests/test_calibration_docs.py tests/test_safety_docs.py tests/test_desktop_docs.py tests/test_edge_serve_docs.py tests/test_export_docs.py -q --tb=short` green
    - `rg -n "^version" pyproject.toml` still 0.1.0
    - No files under src/sentry_ai/ modified
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_calibration_docs.py tests/test_safety_docs.py tests/test_desktop_docs.py tests/test_edge_serve_docs.py tests/test_export_docs.py -q --tb=short</automated>
  </verify>
  <done>Operator hub + refreshed hubs satisfy OPS-02; keyword suite green; no product code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator | Stale “always ordinal” or FSD-as-capability mis-sets robot trust |
| Docs → persist path | Wrong path (`~/.config` JSON) causes operators to write files the runtime never reads |
| Docs → safety users | Vehicle-grade / precise-meter claims would over-sell monocular scale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Spoofing | Hub claims FSD / precise meters / always-ordinal | mitigate | Keyword forbid + calibration.md copy lock |
| T-18-02 | Spoofing | Persist path documented as ~/.config JSON | mitigate | STACK path required; ~/.config forbidden as the path |
| T-18-03 | Tampering | Draft described as calibrated | mitigate | Honesty triad + draft-never-calibrated sentence |
| T-18-04 | Elevation | Docs imply CI needs a room / --extra depth | mitigate | Hub may mention maker `--extra depth`; do not change ci.yml (18-02) |
| T-18-05 | Tampering | Product code / spine edits smuggled into docs PR | mitigate | files_modified is docs+tests only; grep src/ |
| T-18-SC | Tampering | Package installs / version bump | accept | No installs; pyproject stays 0.1.0 |
</threat_model>

<verification>
- Keyword suites for calibration + safety + existing edge docs green
- Stale always-ordinal / precise-meters absent from listed hubs
- No `src/sentry_ai` diffs; pyproject version unchanged
- Persist path in hub is STACK YAML, not ~/.config JSON
</verification>

<success_criteria>
1. OPS-02: Operator docs describe wizard, STACK persist path, and honesty rules without vehicle-grade / FSD claims.
2. perception-frame / safety / README no longer drift to “always ordinal”.
3. Keyword locks prevent regression.
4. Zero new deps; no product-feature creep; package 0.1.0 unchanged.
</success_criteria>

<output>
Create `.planning/phases/18-docs-synthetic-ci-polish/18-01-SUMMARY.md` when done
</output>
