---
phase: 18-docs-synthetic-ci-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - tests/test_v03_honesty_matrix.py
  - tests/test_edge_ci_workflow.py
autonomous: true
requirements:
  - OPS-03
user_setup: []
must_haves:
  truths:
    - "tests/test_v03_honesty_matrix.py documents existing fit/apply/honesty/persist suites and asserts those files exist — no new product code"
    - "Default CI remains ubuntu-latest + uv sync --extra dev + ruff + pytest + sentry health"
    - "ci.yml does not gain --extra depth, --extra detect, --extra onnx, jetson, cuda, self-hosted, or a physical-room step"
    - "OPS-03 coverage is the existing synthetic suites (Phases 13–17), not a new E2E that loads DAV2 weights"
    - "No src/sentry_ai edits; no DetectionLoop/FrameBus/ORT-TRT/kind_for_mode; zero new deps; pyproject stays 0.1.0"
  artifacts:
    - path: "tests/test_v03_honesty_matrix.py"
      provides: "OPS-03 living inventory + file-existence lock"
    - path: "tests/test_edge_ci_workflow.py"
      provides: "EDGE-CI-02 + v0.3: still no --extra depth / room / Jetson / CUDA"
    - path: ".github/workflows/ci.yml"
      provides: "Unchanged contract (edit only if a lock fails — expected byte-identical)"
  key_links:
    - from: "test_v03_honesty_matrix.py"
      to: "existing Phase 13–17 test modules"
      via: "path existence + module docstring table"
      pattern: "test_calibration_fit|test_calibration_store|test_free_space_bands"
    - from: "test_edge_ci_workflow.py"
      to: ".github/workflows/ci.yml"
      via: "Path.read_text"
      pattern: "uv sync --extra dev|--extra depth"
---

<objective>
Lock OPS-03: fit / apply / honesty / persist stay proven with synthetic data, and default GitHub Actions never requires a room, Jetson, CUDA, or `--extra depth`.

Purpose: OPS-03 — automated tests cover the v0.3 matrix without physical hardware. Behavior already shipped in Phases 13–17; this plan **inventories and locks**, it does not re-implement.

Output: thin `tests/test_v03_honesty_matrix.py` + confirm `ci.yml` / `test_edge_ci_workflow.py`. Depends on 18-01 (keyword suite is part of the CI inventory). No runtime product code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-docs-synthetic-ci-polish/18-RESEARCH.md
@.planning/phases/18-docs-synthetic-ci-polish/18-PATTERNS.md
@.planning/phases/18-docs-synthetic-ci-polish/18-VALIDATION.md
@.planning/phases/18-docs-synthetic-ci-polish/18-01-PLAN.md
@.planning/milestones/v0.2-phases/12-docs-ci-packaging-polish/12-02-PLAN.md
@.github/workflows/ci.yml
@tests/test_edge_ci_workflow.py
@tests/test_calibration_fit.py
@tests/test_calibration_store.py
@tests/test_calibration_persist.py
@tests/test_calibration_validators.py
@tests/test_free_space_bands.py
@tests/test_calibration_docs.py

## Locked decisions (must honor)

| Decision | Value | Source |
|----------|-------|--------|
| CI contract | Keep `ci.yml` as `uv sync --extra dev` + ruff + pytest + `sentry health` | Lock #8 |
| Hardware | Do **not** require room, Jetson, CUDA, `--extra depth` | Lock #8 |
| Matrix test | Optional thin `tests/test_v03_honesty_matrix.py` documenting **existing** suites | Lock #user-10 |
| Product code | None. Freeze DetectionLoop/FrameBus/ORT-TRT/`kind_for_mode` | Lock #9 |
| Deps / version | Zero new deps; do not bump pyproject 0.1.0 | Lock #9 |
| Milestone | Do not close REQUIREMENTS or run complete-milestone | Lock #10 |
| Docs hubs | Already done in 18-01 — do not reopen copy except if a keyword test broke | Plan split |

## Claude's discretion

| Item | Choice |
|------|--------|
| Matrix module | New file (do not cram into test_edge_ci_workflow.py) so v0.3 inventory is discoverable |
| Inventory list | Fit, apply/state, kind/unit honesty, free-space meters, persist/serve, plus 18-01 `test_calibration_docs.py` |
| Existence vs collect | Assert each path `is_file()`; optional `pytest --collect-only` on the list (no new cases required) |
| CI lock extend | Add `test_ci_no_extra_depth_or_room` **only if** current tests do not already forbid `--extra depth` (they do — keep them; add a one-line comment that v0.3 OPS-03 relies on the same lock) |
| ci.yml | Prefer **zero bytes changed**. Edit only if a static test fails due to drift |
| Verify command | Run matrix file + edge CI lock + a representative subset of inventoried suites (not a second full copy of `pytest -q` unless time allows) |

## Interfaces (shipped — consume)

```text
.github/workflows/ci.yml     # ubuntu-latest; uv sync --extra dev; ruff; pytest; sentry health
tests/test_edge_ci_workflow.py
# Existing synthetic suites — see RESEARCH inventory table
```

## Target APIs (tests only)

```python
# tests/test_v03_honesty_matrix.py
V03_INVENTORY = (
    "tests/test_calibration_fit.py",           # fit / reject
    "tests/test_calibration_state.py",         # apply / draft
    "tests/test_depth_loop.py",                # apply_map + late W×H
    "tests/test_calibration_validators.py",    # kind/unit + FS units
    "tests/test_depth_kind_honesty.py",
    "tests/test_perception_store_depth_honesty.py",
    "tests/test_free_space_bands.py",          # FS-01/02
    "tests/test_free_space_loop.py",           # FS-03
    "tests/test_assemble_perception_frame.py",
    "tests/test_calibration_store.py",         # persist I/O
    "tests/test_calibration_persist.py",       # try_reapply
    "tests/test_api_calibration.py",           # save/clear/cancel
    "tests/test_cli_calibration_inject.py",
    "tests/test_calibration_docs.py",          # 18-01 OPS-02
)

def test_v03_inventory_files_exist() -> None: ...
def test_v03_ci_stays_dev_extra_only() -> None:
    # read ci.yml; assert uv sync --extra dev; forbid --extra depth
    ...
```

## Out of scope for 18-02

- New FakeDepthWorker / ASGI product tests
- Loading real DAV2 / YOLO weights
- Adding `--extra depth` (or detect/onnx/tensorrt) to GHA
- Self-hosted / Jetson / CUDA runners
- `src/sentry_ai/**` edits
- pyproject version bump
- complete-milestone / REQUIREMENTS checkbox sweep
- Re-opening 18-01 prose unless a test proves drift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OPS-03 honesty-matrix inventory + CI lock confirm</name>
  <files>tests/test_v03_honesty_matrix.py, tests/test_edge_ci_workflow.py</files>
  <read_first>
    - .planning/phases/18-docs-synthetic-ci-polish/18-RESEARCH.md (inventory table; locks 8–10)
    - .planning/phases/18-docs-synthetic-ci-polish/18-PATTERNS.md (matrix module)
    - .planning/phases/18-docs-synthetic-ci-polish/18-01-PLAN.md
    - tests/test_edge_ci_workflow.py (already forbids --extra depth / jetson / cuda)
    - .github/workflows/ci.yml
    - tests/test_calibration_docs.py (must exist after 18-01)
  </read_first>
  <behavior>
    - tests/test_v03_honesty_matrix.py exists with a module docstring table mapping fit / apply / honesty / persist → files
    - Every path in V03_INVENTORY exists on disk
    - ci.yml contains ubuntu-latest, uv sync --extra dev, ruff, pytest, sentry health
    - ci.yml does not contain --extra depth, --extra detect, --extra onnx, self-hosted, tensorrt, jetson, cuda (same contract as test_edge_ci_workflow.py)
    - Existing test_edge_ci_workflow.py tests stay green
    - No new product assertions (no CalibrationState / apply_map / YAML round-trip re-implementation)
    - Representative inventoried suites still collect and pass
  </behavior>
  <acceptance_criteria>
    - `uv run pytest tests/test_v03_honesty_matrix.py tests/test_edge_ci_workflow.py -q --tb=short` green
    - `uv run pytest tests/test_calibration_fit.py tests/test_calibration_store.py tests/test_calibration_persist.py tests/test_calibration_validators.py tests/test_free_space_bands.py tests/test_calibration_docs.py -q --tb=short` green
    - `rg -n "--extra depth|--extra detect|self-hosted|tensorrt|jetson|cuda" .github/workflows/ci.yml` returns no matches
    - `rg -n "uv sync --extra dev|sentry health" .github/workflows/ci.yml` matches
    - `git diff --name-only` for this plan does not include `src/sentry_ai/` or `pyproject.toml`
  </acceptance_criteria>
  <action>
    1. Create tests/test_v03_honesty_matrix.py with the living docstring + V03_INVENTORY + existence test + ci.yml `--extra depth` forbid (thin; may read the same file as EDGE-CI-02).
    2. In tests/test_edge_ci_workflow.py, add a short comment (or docstring line) that v0.3 OPS-03 reuses this lock — do **not** rewrite the three existing tests. Only add a new test if a lock is missing (it is not).
    3. Confirm .github/workflows/ci.yml is unchanged and still matches the contract. Do not add extras, runners, or a “room” job.
    4. Run the verify command. If an inventoried suite fails, **do not** redesign product code — escalate as a Phase 13–17 regression.
    5. Optionally `uv run ruff check tests/test_v03_honesty_matrix.py tests/test_edge_ci_workflow.py`.

    Avoid: src/ edits; new pip packages; real weight downloads; GHA GPU labels; complete-milestone.
  </action>
  <verify>
    <automated>uv run pytest tests/test_v03_honesty_matrix.py tests/test_edge_ci_workflow.py tests/test_calibration_fit.py tests/test_calibration_store.py tests/test_calibration_persist.py tests/test_calibration_validators.py tests/test_free_space_bands.py tests/test_calibration_docs.py tests/test_safety_docs.py -q --tb=short</automated>
  </verify>
  <done>OPS-03 inventory locked; default CI stays hardware-free; no product code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GHA → contributor machines | Adding `--extra depth` or CUDA runners would make default CI flaky/expensive |
| Inventory test → future editors | Deleting a Phase 14–17 suite without replacing coverage would silently drop OPS-03 |
| Docs vs CI | Maker docs may mention `--extra depth`; CI must not require it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Denial of service | ci.yml gains depth/CUDA/Jetson | mitigate | Static forbid + existing EDGE-CI-02 |
| T-18-12 | Tampering | Honesty suite deleted, CI still green | mitigate | V03_INVENTORY file-existence lock |
| T-18-13 | Spoofing | New “E2E” that loads real DAV2 in default pytest | mitigate | No new product tests; inventory only |
| T-18-14 | Elevation | src/ or factory edits in a docs/CI plan | mitigate | files_modified tests-only; grep |
| T-18-15 | Tampering | complete-milestone / version bump smuggled | mitigate | Out of scope; pyproject untouched |
| T-18-SC | Tampering | Package installs | accept | No package installs |
</threat_model>

<verification>
- Matrix + EDGE-CI-02 + representative v0.3 suites green
- ci.yml still ubuntu-latest + `--extra dev` only
- No src/sentry_ai or pyproject diffs
</verification>

<success_criteria>
1. OPS-03: Automated tests cover fit / apply / honesty / persist with synthetic data (inventory + existing suites).
2. Default GHA does not require a room, Jetson, CUDA, or `--extra depth`.
3. Zero new product code and zero new deps.
4. After this plan merges (with 18-01), v0.3 requirements are **closable** — complete-milestone is a later step, not this PR.
</success_criteria>

<output>
Create `.planning/phases/18-docs-synthetic-ci-polish/18-02-SUMMARY.md` when done
</output>
