---
phase: 17-persist-reapply-on-serve
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - src/sentry_ai/cli.py
  - src/sentry_ai/api/app.py
  - src/sentry_ai/api/deps.py
  - src/sentry_ai/api/routes_calibration.py
  - src/sentry_ai/api/routes_preview.py
  - src/sentry_ai/models/depth/loop.py
  - tests/test_api_calibration.py
  - tests/test_cli_calibration_inject.py
  - tests/test_depth_loop.py
  - tests/test_api_calibration_smoother.py
autonomous: true
requirements:
  - PER-02
  - PER-04
user_setup: []
must_haves:
  truths:
    - "sentry serve calls try_reapply on the resolved YAML path after source + depth worker exist; matching file apply_params without wizard samples"
    - "--calibration-file overrides directory/camera_id stem; SENTRY_CALIBRATION_DIR still used when the flag is omitted"
    - "Serve banner prints calibration: none|applied|ignored_mismatch|error (plus reason when set)"
    - "POST /api/depth/calibration/save writes applied params; apply without persist writes nothing; apply persist:true writes"
    - "POST clear deletes the YAML so a later try_reapply is none; POST cancel does not delete the file and does not clear_applied"
    - "GET /api/status exposes calibration_persist (and reason) additive; does not invent depth_kind from persist"
    - "DepthLoop refuse_if_mismatch before apply_map: file/applied W×H vs later live W×H mismatch clears applied (unscaled, not metric_calibrated)"
    - "DepthLoop remains sole apply_map site; no DetectionLoop/FrameBus/ORT-TRT/kind_for_mode; no wizard HTML redesign; no FSD"
  artifacts:
    - path: "src/sentry_ai/cli.py"
      provides: "--calibration-file + try_reapply + banner + create_app calibration_path"
    - path: "src/sentry_ai/api/routes_calibration.py"
      provides: "POST save; apply persist:true; clear_persisted"
    - path: "src/sentry_ai/api/routes_preview.py"
      provides: "calibration_persist on /api/status"
    - path: "src/sentry_ai/models/depth/loop.py"
      provides: "late W×H refuse_if_mismatch before apply_map"
    - path: "tests/test_api_calibration.py"
      provides: "PER-04 clear-deletes-file + save/persist:true"
  key_links:
    - from: "cli.serve"
      to: "try_reapply(state, path, live)"
      via: "live fingerprint from source camera_id + worker mode/model; W×H None at start"
      pattern: "try_reapply|--calibration-file"
    - from: "POST .../save and apply persist:true"
      to: "persist_applied"
      via: "app.state.calibration_path"
      pattern: "persist_applied|/save"
    - from: "POST .../clear"
      to: "clear_persisted"
      via: "unlink YAML"
      pattern: "clear_persisted"
    - from: "POST .../cancel"
      to: "clear_draft only"
      via: "no delete_params"
      pattern: "clear_draft"
    - from: "DepthLoop success path"
      to: "refuse_if_mismatch then apply_map"
      via: "live W×H from depth_map shape"
      pattern: "refuse_if_mismatch"
---

<objective>
Wire 17-01 store + `try_reapply` through `sentry serve`, REST save/clear, additive persist status, and DepthLoop late-size refuse so a matching saved calibration survives restart and a Clear cannot be resurrected.

Purpose: PER-02 (re-apply on serve) and PER-04 (clear/invalidate stored calibration).

Output: CLI load + banner, REST save / `persist:true` / clear-file, `/api/status` persist field, DepthLoop `refuse_if_mismatch`. Depends on 17-01. Zero new packages. No wizard HTML redesign. No second `apply_map`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-persist-reapply-on-serve/17-RESEARCH.md
@.planning/phases/17-persist-reapply-on-serve/17-PATTERNS.md
@.planning/phases/17-persist-reapply-on-serve/17-VALIDATION.md
@.planning/phases/17-persist-reapply-on-serve/17-01-PLAN.md
@src/sentry_ai/cli.py
@src/sentry_ai/api/app.py
@src/sentry_ai/api/deps.py
@src/sentry_ai/api/routes_calibration.py
@src/sentry_ai/api/routes_preview.py
@src/sentry_ai/models/depth/loop.py
@src/sentry_ai/control/calibration_persist.py
@src/sentry_ai/config/calibration_store.py
@tests/test_api_calibration.py
@tests/test_cli_calibration_inject.py
@tests/test_depth_loop.py
@tests/test_api_calibration_smoother.py

## Locked decisions (must honor)

| Decision | Value | Source |
|----------|-------|--------|
| Serve auto-apply | `try_reapply` when file present AND fingerprints_match; W×H may be None at start | RESEARCH #6 #7 |
| `--calibration-file` | Explicit file override | RESEARCH #2 |
| Persist trigger | POST save **and** optional `persist:true` on apply; apply-without-persist stays session-only | RESEARCH #9 |
| Clear vs Cancel | Clear deletes file (`clear_persisted`); Cancel draft-only | RESEARCH #10 |
| Status | Additive `calibration_persist` = none\|applied\|ignored_mismatch\|error; separate from `depth.kind`; serve banner | RESEARCH #11 |
| Late size | If file/applied has W×H and live product later mismatches → refuse/clear | RESEARCH #6 |
| Apply site | DepthLoop sole `apply_map`; persist only loads state | RESEARCH #12 |
| Constraints | Zero new deps; freeze DetectionLoop/FrameBus/ORT-TRT/`kind_for_mode`; no wizard redesign; no FSD; docs Phase 18 | RESEARCH #13 |

## Claude's discretion

| Item | Choice |
|------|--------|
| Live fingerprint at serve | `camera_id` from source (or `--camera-id`); `depth_mode`/`model_id` from `depth_worker` when present; `width`/`height` None |
| Banner | `calibration: {status}` and ` reason={reason}` when reason set; optional ` path=` when useful — keep one line |
| `create_app` | Add `calibration_path: Path \| str \| None = None` → `app.state.calibration_path`; add to `AppState` if the dataclass lists injects |
| Apply body | Optional `CalibrationApplyBody` with `persist: bool = False`; `None` body ⇒ persist false (existing clients) |
| Save 422 | Not applied, or no `calibration_path` and cannot resolve from applied fingerprint |
| Path fallback in routes | Prefer `app.state.calibration_path`; else `calibration_path(applied.fingerprint.camera_id)` |
| DepthLoop live size | `H, W = depth_map.shape[:2]` when map is 2-D; camera_id from frame meta if present else applied camera_id; mode/model from worker |
| Belt-and-suspenders | Keep 16-02 `reset_smoother` on apply/clear; Clear still resets smoother **and** deletes file |
| Inspect tests | `test_cli_calibration_inject.py` asserts `try_reapply`, `--calibration-file`, banner string `calibration:` |
| Functional serve | Do **not** boot uvicorn; `try_reapply` already covered in 17-01. 17-02 proves CLI source contains the call + REST + DepthLoop |

## Interfaces (shipped — consume)

```python
# 17-01
try_reapply(state, path, live) -> ReapplyResult
persist_applied(state, path) -> Path
clear_persisted(state, path) -> None
refuse_if_mismatch(state, live) -> str | None
calibration_path(camera_id, *, explicit_file=None) -> Path

# Phase 15
POST /api/depth/calibration/apply    # no body today
POST /api/depth/calibration/cancel   # clear_draft
POST /api/depth/calibration/clear    # clear_applied + draft; no file
GET  /api/status                     # calibration_active, scale, method, camera_id

# cli.serve
calibration_state = CalibrationState()  # empty today
```

## Target APIs (implement)

```python
# cli.serve
calibration_file: Path | None = typer.Option(None, "--calibration-file", ...)

# create_app(..., calibration_path: Path | str | None = None)

# routes
class CalibrationApplyBody(BaseModel):
    model_config = ConfigDict(extra="forbid")
    persist: bool = False

POST /api/depth/calibration/save     # persist_applied
POST /api/depth/calibration/apply    # optional body persist
POST /api/depth/calibration/clear    # clear_persisted when path known

# status
data["calibration_persist"] = snap.persist_status
if snap.persist_reason:
    data["calibration_persist_reason"] = snap.persist_reason
```

## Out of scope for 17-02

- Changing 17-01 match math / YAML shape / `apply_params` rules
- Capture uniqueID / RTSP fingerprint fields
- Wizard HTML Save button / docs (18)
- DetectionLoop / FrameBus / ORT-TRT / `kind_for_mode`
- FSD / interlock / motor / platformdirs / profile YAML
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serve re-apply, REST save/clear-file, status, late W×H (PER-02 / PER-04)</name>
  <files>src/sentry_ai/cli.py, src/sentry_ai/api/app.py, src/sentry_ai/api/deps.py, src/sentry_ai/api/routes_calibration.py, src/sentry_ai/api/routes_preview.py, src/sentry_ai/models/depth/loop.py, tests/test_api_calibration.py, tests/test_cli_calibration_inject.py, tests/test_depth_loop.py, tests/test_api_calibration_smoother.py</files>
  <read_first>
    - .planning/phases/17-persist-reapply-on-serve/17-RESEARCH.md (locks 2, 6-7, 9-13; Pitfalls 4-5, 7, 10)
    - .planning/phases/17-persist-reapply-on-serve/17-PATTERNS.md (cli, routes, status, DepthLoop)
    - .planning/phases/17-persist-reapply-on-serve/17-01-PLAN.md
    - src/sentry_ai/cli.py (serve CalibrationState hoist + banner)
    - src/sentry_ai/api/app.py
    - src/sentry_ai/api/deps.py
    - src/sentry_ai/api/routes_calibration.py (apply/clear/cancel)
    - src/sentry_ai/api/routes_preview.py (calibration_active block)
    - src/sentry_ai/models/depth/loop.py
    - src/sentry_ai/control/calibration_persist.py
    - tests/test_api_calibration.py
    - tests/test_cli_calibration_inject.py
    - tests/test_depth_loop.py
  </read_first>
  <behavior>
    - inspect cli.serve: `--calibration-file`, `try_reapply(`, `calibration:`, `calibration_path=` passed to create_app; still constructs one CalibrationState injected into DepthLoop and create_app
    - try_reapply is invoked after source exists and after depth_worker construction attempt (so live mode/model can be filled when depth extra is present)
    - POST apply (no persist) after a tmp calibration_path on app.state does not create/write YAML
    - POST apply `{"persist": true}` writes loadable YAML at calibration_path
    - POST `/api/depth/calibration/save` after in-memory apply writes YAML; 422 if not applied
    - extra=forbid on apply/save bodies
    - POST clear after a saved file: file gone; state not applied; subsequent try_reapply(same path, matching live) status=="none"
    - POST cancel after a saved file: file still exists; applied stays if it was applied
    - GET /api/status includes calibration_persist; after try_reapply mismatch (via state.set_persist_status or helper) status shows ignored_mismatch and depth_kind is not forced to metric_calibrated
    - Existing apply/cancel/clear/sample/409 tests stay green; smoother reset on clear still happens
    - DepthLoop: applied params with fingerprint 640×480; first FakeDepthWorker map 320×240 → after tick, not applied / kind not metric_calibrated / map not scaled (refuse_if_mismatch ran before apply_map)
    - DepthLoop: applied 640×480 and map 640×480 stays applied and scaled (no false refuse)
    - DepthLoop: applied with W/H None and map 640×480 stays applied (sizes skipped)
    - DepthLoop source still has no YAML load; grep calibration_store out of loop.py (only refuse_if_mismatch / persist helper)
    - No torch/FrameBus imports added to routes; extra=forbid; no motor/FSD fields
    - 16-02 apply/clear still call reset_smoother
  </behavior>
  <acceptance_criteria>
      - Serve source calls try_reapply + banner + --calibration-file (PER-02)
      - Matching re-apply path remains apply_params (no wizard) — proven by 17-01 helper + CLI inspect
      - Save and persist:true write YAML; apply-without-persist does not
      - Clear deletes file so restart load is none (PER-04)
      - Cancel does not delete file
      - Status calibration_persist additive; separate from depth.kind
      - Late W×H mismatch clears applied before apply_map
      - Zero new deps; no DetectionLoop/FrameBus/ORT/kind_for_mode/wizard HTML/FSD
  </acceptance_criteria>
  <action>
    1. `cli.serve`: add `--calibration-file`; resolve path via `calibration_path(..., explicit_file=...)`; build live fingerprint; `try_reapply`; echo banner; pass `calibration_path` into `create_app`.
    2. `create_app` + `AppState`: optional `calibration_path`; stash on `app.state`.
    3. Routes: POST save; apply optional `{persist}`; clear uses `clear_persisted` when a path is known (else existing in-memory clear). Cancel unchanged. Keep `_reset_free_space_smoother` on apply/clear.
    4. `/api/status`: copy `persist_status` / `persist_reason` from snapshot. Do not set `depth_kind` from persist.
    5. DepthLoop: after a usable `worker.process` result with a depth_map, if calibration is not None, build live fingerprint (camera_id from frame meta if available, HxW from map, worker mode/model) and `refuse_if_mismatch` **before** `promote_kind_unit` + `apply_map`.
    6. Tests per behavior. Extend API tests with `tmp_path` file assigned to `app.state.calibration_path`. Extend FakeDepthWorker loop tests with applied fingerprint sizes.
    7. Grep guard: no DetectionLoop/FrameBus/ORT factory/`kind_for_mode`/index.html algorithm changes. No `apply_map` outside DepthLoop + CalibrationState.
  </action>
  <verify>
    <automated>uv run pytest tests/test_calibration_store.py tests/test_calibration_persist.py tests/test_calibration_state.py tests/test_api_calibration.py tests/test_api_calibration_smoother.py tests/test_cli_calibration_inject.py tests/test_depth_loop.py tests/test_cli_serve.py -q</automated>
  </verify>
  <done>Matching YAML re-applies on serve; Clear deletes the file; late size mismatch refuses; persist status is visible and separate from depth.kind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Serve start → CalibrationState | Auto-apply without match is a permanent unit lie |
| REST Clear → disk | Leaving the file resurrects scale on restart |
| REST Cancel → disk | Deleting the file on Cancel violates WIZ-02 |
| Live product size → applied fingerprint | Stale W×H would scale the wrong geometry |
| Persist status → depth.kind | Status must not relabel relative as calibrated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Spoofing | Serve load without try_reapply match | mitigate | CLI inspect + 17-01 helper tests |
| T-17-12 | Tampering | Clear leaves YAML | mitigate | clear_persisted + restart none test |
| T-17-13 | Tampering | Cancel deletes YAML | mitigate | cancel-leaves-file test |
| T-17-14 | Tampering | Auto-save on apply | mitigate | apply-without-persist writes nothing |
| T-17-15 | Spoofing | Late resolution ignored | mitigate | DepthLoop size-mismatch test |
| T-17-16 | Spoofing | Status sets depth_kind | mitigate | persist field additive only |
| T-17-17 | Elevation | Second apply_map in routes/CLI | mitigate | grep apply_map |
| T-17-18 | Elevation | Wizard/FSD/DetectionLoop | mitigate | out of scope; grep |
| T-17-SC | Tampering | Package installs | accept | No package installs |
</threat_model>

<verification>
- `uv run pytest tests/test_calibration_store.py tests/test_calibration_persist.py tests/test_calibration_state.py tests/test_api_calibration.py tests/test_api_calibration_smoother.py tests/test_cli_calibration_inject.py tests/test_depth_loop.py tests/test_cli_serve.py -q`
- Confirm serve source contains `try_reapply` and `calibration:`
- Confirm POST cancel does not call `delete_params` / `clear_persisted`
- Grep: no `apply_map` in `cli.py` or `routes_calibration.py`; no DetectionLoop/FrameBus/ORT/`kind_for_mode`/index.html edits
</verification>

<success_criteria>
1. Matching saved calibration re-applies on serve without the wizard (PER-02).
2. Clear deletes the file so restart cannot resurrect; Cancel stays draft-only (PER-04).
3. Persist status is visible (`none|applied|ignored_mismatch|error`) and separate from `depth.kind`.
4. Late W×H mismatch refuses/clears before maps are scaled.
5. Zero new deps; DepthLoop remains the sole scale site.
</success_criteria>

<output>
Create `.planning/phases/17-persist-reapply-on-serve/17-02-SUMMARY.md` when done
</output>
