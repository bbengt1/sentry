---
phase: 11-sticky-fallback-dual-model-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/sentry_ai/capture/status.py
  - src/sentry_ai/api/app.py
  - src/sentry_ai/api/deps.py
  - src/sentry_ai/api/routes_preview.py
  - src/sentry_ai/cli.py
  - src/sentry_ai/ui/static/index.html
  - tests/test_backend_honesty_status.py
  - tests/test_edge_rt04_torch_only.py
  - tests/test_export_docs.py
  - docs/export/yolo26-onnx-tensorrt.md
  - docs/export/jetson-packaging.md
  - docs/export/README.md
autonomous: true
requirements:
  - EDGE-RT-04
  - BACK-03
user_setup: []
must_haves:
  truths:
    - "Depth continues on DepthAnythingWorker torch/HF path; serve never routes depth through build_detection_worker"
    - "Open-vocab continues on YoloeOpenVocabWorker with profile .pt YOLOE weights; default mode remains off"
    - "No live ORT/TRT backend claim exists for depth or open-vocab stages this milestone"
    - "Operator status/banner/UI still show factory-authored backend_requested / backend_live / backend_reason; fallback_to_torch is pass-through when set"
    - "Dual-model guidance states TRT (or torch) fixed-class YOLO + torch DAV2 may share GPU and must be measured on device — no published dual-model FPS"
    - "Continuous open-vocab + TRT YOLO + DAV2 is explicitly not a first-class supported configuration"
    - "Export/docs no longer say dual-model guardrails or sticky policy are deferred to Phase 11"
    - "Status route never recomputes live from preferred_backend"
  artifacts:
    - path: "src/sentry_ai/capture/status.py"
      provides: "StatusSnapshot.fallback_to_torch optional field"
      contains: "fallback_to_torch"
    - path: "src/sentry_ai/api/app.py"
      provides: "create_app pass-through for fallback_to_torch"
    - path: "src/sentry_ai/api/deps.py"
      provides: "AppState.fallback_to_torch"
    - path: "src/sentry_ai/api/routes_preview.py"
      provides: "status pass-through includes fallback_to_torch (None-safe for False)"
    - path: "src/sentry_ai/cli.py"
      provides: "inject rt.fallback_to_torch into create_app + banner"
    - path: "src/sentry_ai/ui/static/index.html"
      provides: "footer honesty for requested→live(+reason) including live-null strict case"
    - path: "tests/test_edge_rt04_torch_only.py"
      provides: "EDGE-RT-04 construction and non-claim proofs"
    - path: "tests/test_export_docs.py"
      provides: "dual-model + sticky + soft/strict keyword honesty"
    - path: "docs/export/yolo26-onnx-tensorrt.md"
      provides: "shipped dual-model guardrails language (no Phase 11 deferral)"
    - path: "docs/export/jetson-packaging.md"
      provides: "measure-on-device dual-model knobs; non-claim continuous OV+TRT+DAV2"
  key_links:
    - from: "cli.serve"
      to: "create_app(fallback_to_torch=rt.fallback_to_torch)"
      via: "pass-through only"
      pattern: "fallback_to_torch"
    - from: "routes_preview /api/status"
      to: "app.state.fallback_to_torch"
      via: "field tuple pass-through if value is not None"
      pattern: "fallback_to_torch"
    - from: "cli.serve depth construction"
      to: "DepthAnythingWorker"
      via: "separate from build_detection_worker"
      pattern: "DepthAnythingWorker"
    - from: "cli.serve OV construction"
      to: "YoloeOpenVocabWorker(weights=rt.open_vocab_weights)"
      via: ".pt path only"
      pattern: "YoloeOpenVocabWorker"
    - from: "export dual-model docs"
      to: "measure-on-device + non-claim continuous OV"
      via: "keyword tests"
      pattern: "measure on device|not a first-class|continuous open-vocab"
---

<objective>
Lock depth and open-vocab to existing PyTorch paths, surface soft/strict policy on operator status, and ship dual-model honesty docs (measure-on-device; no continuous OV+TRT+DAV2 claim).

Purpose: Close EDGE-RT-04 and complete BACK-03 operator visibility — robots/makers see fallback mode + honest dual-model scope without inventing FPS or routing depth/OV through the detection factory.

Output: `fallback_to_torch` on StatusSnapshot/status/banner/UI, EDGE-RT-04 unit/static tests, dual-model docs with Phase 11 deferral language retired, keyword tests green without Jetson.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-sticky-fallback-dual-model-guardrails/11-RESEARCH.md
@.planning/phases/11-sticky-fallback-dual-model-guardrails/11-PATTERNS.md
@.planning/phases/11-sticky-fallback-dual-model-guardrails/11-VALIDATION.md
@.planning/phases/11-sticky-fallback-dual-model-guardrails/11-01-PLAN.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-02-SUMMARY.md
@src/sentry_ai/cli.py
@src/sentry_ai/capture/status.py
@src/sentry_ai/api/app.py
@src/sentry_ai/api/deps.py
@src/sentry_ai/api/routes_preview.py
@src/sentry_ai/ui/static/index.html
@docs/export/yolo26-onnx-tensorrt.md
@docs/export/jetson-packaging.md
@tests/test_backend_honesty_status.py
@tests/test_export_docs.py

## Depends on 11-01

Plan 11-01 delivers: `ProfileRuntime.fallback_to_torch`, factory soft/strict, serve Exit(1) on strict miss, sticky/soft-strict docs in configuration/architecture.

This plan must not re-implement factory policy. It wires operator surface + EDGE-RT-04 lock + dual-model docs.

## Locked decisions

| Decision | Value | Source |
|----------|-------|--------|
| Depth path | DepthAnythingWorker torch/HF only — no factory | EDGE-RT-04 |
| OV path | YoloeOpenVocabWorker `.pt` YOLOE; mode off default | EDGE-RT-04 / RESEARCH |
| Status field | `fallback_to_torch: bool \| None = None` on StatusSnapshot (not string enum) | RESEARCH + PATTERNS |
| Pass-through | Never recompute backend_live from preferred_backend | Phase 8 invariant |
| Dual-model claim | TRT/torch fixed-class YOLO + torch DAV2 Small may share GPU — measure on device | ROADMAP SC4 |
| Dual-model non-claim | Continuous open-vocab + TRT YOLO + DAV2 not first-class | ROADMAP SC4 / PITFALLS |
| FPS | No published dual-model FPS tables | STATE / RESEARCH |
| Phase 11 deferred language | Retire from export docs once this ships | RESEARCH State of the Art |
| Residual | No VRAM governor / dual-model scheduler rewrite | Deferred Ideas |

## Interfaces to extend (mirror all four places)

StatusSnapshot (`capture/status.py`):
```python
backend_requested: str | None = None
backend_live: str | None = None
backend_reason: str | None = None
# ADD: fallback_to_torch: bool | None = None
```

create_app / AppState / routes_preview field loop: add `fallback_to_torch` alongside backend_* .

Pass-through rule for bool False: use `if value is not None` — never truthiness checks that drop False.

UI footer: when backend_live is null/absent but reason is set (strict miss if process were up — rare after Exit(1); still harden display), show reason. Keep requested → live pair.

## Out of scope

- Factory soft/strict policy changes (11-01)
- Live ORT/TRT for depth/OV
- CI Jetson matrix (Phase 12 EDGE-CI-*)
- Full edge-serve narrative polish (Phase 12 EDGE-DOC-*)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Operator surface — fallback_to_torch status/banner/UI</name>
  <files>src/sentry_ai/capture/status.py, src/sentry_ai/api/app.py, src/sentry_ai/api/deps.py, src/sentry_ai/api/routes_preview.py, src/sentry_ai/cli.py, src/sentry_ai/ui/static/index.html, tests/test_backend_honesty_status.py</files>
  <read_first>
    - .planning/phases/11-sticky-fallback-dual-model-guardrails/11-PATTERNS.md (StatusSnapshot, create_app, deps, routes pass-through, UI footer strict-null note)
    - src/sentry_ai/capture/status.py (backend_* optional fields, extra=forbid)
    - src/sentry_ai/api/app.py (create_app kwargs → app.state + AppState)
    - src/sentry_ai/api/deps.py (AppState backend_*)
    - src/sentry_ai/api/routes_preview.py (field tuple pass-through)
    - src/sentry_ai/cli.py (create_app call + banner backend_* block after 11-01)
    - src/sentry_ai/ui/static/index.html (footer req → live (reason))
    - tests/test_backend_honesty_status.py (pass-through triples; retire ort_loader_not_implemented fixture)
  </read_first>
  <action>
    Surface soft/strict policy to operators without inventing live backends (BACK-03 visibility + EDGE-RT-04 operator honesty).

    1. Add `fallback_to_torch: bool | None = None` to `StatusSnapshot` (default None for backward compatibility; extra=forbid remains).

    2. Thread the field through create_app signature → `app.state.fallback_to_torch` → AppState → routes_preview status builder. Extend the honesty field loop to include `fallback_to_torch`. Critical: include the field when value is `False` (strict) — check `is not None`, not truthiness.

    3. In cli.serve create_app call, pass `fallback_to_torch=getattr(rt, "fallback_to_torch", True)` (or `rt.fallback_to_torch` after 11-01). Banner: echo `fallback_to_torch: true|false` near backend_* lines when detection factory ran (even if soft). On ImportError detection-disabled path, leave field None unless rt is available (prefer still pass rt value when profile loaded).

    4. UI footer (`index.html`): keep requested → live display. When reason is present and (live is missing OR live differs from requested), append `(reason)` so strict-style null live still shows reason if status is polled. Optional: append soft/strict hint from `data.fallback_to_torch` (e.g. title attribute or short suffix) — keep compact; do not invent FPS.

    5. Tests in `tests/test_backend_honesty_status.py`:
       - Pass-through True and False for fallback_to_torch on /api/status
       - StatusSnapshot dump includes field when set
       - create_app app.state + deps carry field
       - Retire fixture reason `ort_loader_not_implemented` → use current soft vocabulary (`ort_artifact_missing` or `trt_artifact_missing`)
       - Existing live ORT/TRT honesty triples remain green
       - Assert route does not import factory or preferred_backend for live computation (source inspect optional)

    Avoid: recomputing backend_live in the route; adding depth/OV fake backend_live fields; changing factory policy.
  </action>
  <acceptance_criteria>
    - `rg -n "fallback_to_torch" src/sentry_ai/capture/status.py src/sentry_ai/api/app.py src/sentry_ai/api/deps.py src/sentry_ai/api/routes_preview.py src/sentry_ai/cli.py` all match
    - `rg -n "ort_loader_not_implemented" tests/test_backend_honesty_status.py` returns no matches
    - `uv run pytest tests/test_backend_honesty_status.py -q --tb=short` green
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_backend_honesty_status.py -q --tb=short</automated>
  </verify>
  <done>Operators see fallback_to_torch on status/banner with pass-through False preserved; honesty fixtures use current reason codes; UI shows reason when live differs or is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EDGE-RT-04 torch-only lock + dual-model docs honesty</name>
  <files>tests/test_edge_rt04_torch_only.py, tests/test_export_docs.py, docs/export/yolo26-onnx-tensorrt.md, docs/export/jetson-packaging.md, docs/export/README.md</files>
  <read_first>
    - .planning/phases/11-sticky-fallback-dual-model-guardrails/11-RESEARCH.md (EDGE-RT-04 section, dual-model SC4, deferred language to retire)
    - .planning/phases/11-sticky-fallback-dual-model-guardrails/11-PATTERNS.md (docs honesty table, Phase 11 deferred language locations)
    - src/sentry_ai/cli.py (depth + OV construction blocks — already correct; tests must lock)
    - src/sentry_ai/models/depth/worker.py (torch/HF path markers)
    - src/sentry_ai/models/detection/yoloe_worker.py (YOLOE .pt path)
    - docs/export/yolo26-onnx-tensorrt.md (dual-model + Phase 11 deferred lines)
    - docs/export/jetson-packaging.md (dual-model honesty + Phase 11 deferral)
    - tests/test_export_docs.py (keyword test style from Phase 9–10)
  </read_first>
  <behavior>
    - Serve source constructs DepthAnythingWorker and YoloeOpenVocabWorker without routing those stages through build_detection_worker
    - Depth worker module does not claim backend_live onnxruntime/tensorrt
    - Open-vocab worker uses weights path / YOLOE .pt pattern; not preferred_backend factory
    - Export docs include measure-on-device dual-model language for TRT/torch YOLO + torch depth
    - Export docs state continuous open-vocab + TRT + DAV2 is not first-class
    - Export docs do not claim published dual-model FPS guarantees
    - Sticky / soft / strict language present; no “deferred to Phase 11” for sticky or dual-model guardrails after ship
  </behavior>
  <action>
    Prove EDGE-RT-04 and ship dual-model guardrail documentation (ROADMAP SC3–SC4).

    1. Create `tests/test_edge_rt04_torch_only.py`:
       - Inspect `cli.py` serve source: contains `DepthAnythingWorker` and `YoloeOpenVocabWorker`; depth/OV construction is not via `build_detection_worker` (factory appears only for fixed-class detection block).
       - Inspect depth worker source: no `backend_live` ORT/TRT claims; no `preferred_backend` loader branch for depth.
       - Inspect yoloe worker: loads via YOLOE / weights string; not `resolve_detector_artifact` for OV.
       - Optional: OpenVocabLoop default mode off if cheap to assert from existing types.
       - No GPU, no weight download, no Jetson.

    2. Update export docs:
       - `yolo26-onnx-tensorrt.md`: Replace “Phase 11 owns first-class dual-model guardrails” and sticky “deferred to Phase 11” bullets with shipped language:
         - Sticky: factory once at serve; soft default torch+reason; strict via `fallback_to_torch=false` / `SENTRY_FALLBACK_TO_TORCH=false`.
         - Supported: fixed-class TRT or torch YOLO + torch DAV2 Small may share GPU — measure VRAM/latency on device.
         - Not first-class: continuous open-vocab + TRT YOLO + DAV2 this milestone.
         - Knobs: disable depth, OV off/on_demand, nano tier, `--no-ui`, `nvidia-smi`.
         - No FPS fiction.
       - `jetson-packaging.md`: Same dual-model honesty; remove “guardrails are Phase 11” / sticky Phase 11 deferral; keep JetPack/system TRT and no pip tensorrt pin.
       - `docs/export/README.md`: Short dual-model / measure-on-device pointer consistent with above; no Phase 11 deferral for these items.

    3. Extend `tests/test_export_docs.py` keyword asserts:
       - Soft/strict or fallback_to_torch / SENTRY_FALLBACK_TO_TORCH mentioned in configuration or export blob (coordinate with 11-01 configuration docs — may already exist; assert export dual-model files include sticky or soft/strict if documented there).
       - Dual-model: “measure” + on device language present.
       - Non-claim: continuous open-vocab (or open-vocab + TRT + depth) not first-class.
       - Forbidden: inventing guaranteed dual-model FPS tables (assert no bare “30 FPS dual-model” style claims if such appear; keep existing no-FPS hygiene).
       - Assert Phase 11 deferred phrases for sticky/dual-model are gone from yolo26 + jetson-packaging (e.g. no `Phase 11 owns` / `deferred to Phase 11` for those topics).

    Avoid: sequential GPU scheduler code; VRAM measurement in CI; changing jetson.yaml field values; FPS numbers without methodology; touching DetectionLoop scheduling.
  </action>
  <acceptance_criteria>
    - `tests/test_edge_rt04_torch_only.py` exists and passes
    - `rg -n "Phase 11 owns|deferred to Phase 11|Sticky thrash-free fallback policy \\(Phase 11\\)" docs/export/yolo26-onnx-tensorrt.md docs/export/jetson-packaging.md` returns no matches (or only historical changelog if any — prefer zero)
    - `rg -ni "measure on device|not a first-class|continuous open-vocab|fallback_to_torch|sticky" docs/export/yolo26-onnx-tensorrt.md docs/export/jetson-packaging.md` shows shipped honesty
    - `uv run pytest tests/test_edge_rt04_torch_only.py tests/test_export_docs.py tests/test_backend_honesty_status.py -q --tb=short` green
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_edge_rt04_torch_only.py tests/test_export_docs.py tests/test_backend_honesty_status.py tests/test_detection_factory.py -q --tb=short</automated>
  </verify>
  <done>EDGE-RT-04 locked by tests; dual-model docs claim measure-on-device YOLO+DAV2 only and non-claim continuous OV+TRT+DAV2; Phase 11 deferral language retired; full phase-related suite green without Jetson.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Status API → robot clients | Unauthenticated LAN status can expose backend policy; must not lie about live backend |
| Docs → operator expectations | Dual-model wording shapes resource planning; false FPS is operational harm |
| Config injection of fallback_to_torch | Already mitigated in 11-01; status must reflect true mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Spoofing | /api/status backend_* | mitigate | Pass-through only from factory-authored app.state; never recompute live from preferred_backend; tests assert honesty triples |
| T-11-08 | Spoofing | dual-model docs | mitigate | Explicit non-claim continuous OV+TRT+DAV2; measure-on-device only; keyword tests forbid deferred-lie language and FPS fiction |
| T-11-09 | Information disclosure | fallback_to_torch on status | accept | Operational mode flag, not a secret; helps robots detect misconfig |
| T-11-10 | Denial of Service | dual-model OOM | mitigate | Docs knobs (OV off, nano, disable depth, --no-ui); no first-class continuous OV+TRT+DAV2; no runtime VRAM governor this phase |
| T-11-11 | Tampering | UI footer display | mitigate | Footer only renders status fields; does not invent live ORT/TRT; reason shown when live differs/absent |
| T-11-SC | Tampering | package installs | accept | No new packages this plan |
</threat_model>

<verification>
- Status pass-through for backend_* + fallback_to_torch True/False
- EDGE-RT-04 tests prove depth/OV not factory-routed
- Export docs dual-model honesty + retired Phase 11 deferral
- Combined gate: factory + honesty + edge_rt04 + export_docs green
- No Jetson / real engines / weight downloads in default tests
</verification>

<success_criteria>
- EDGE-RT-04: depth and open-vocab remain PyTorch paths with automated proof
- BACK-03 operator surface: fallback_to_torch visible on status when serve injects it
- Dual-model guidance shipped: measure-on-device YOLO+depth; continuous OV+TRT+DAV2 not first-class
- Roadmap Phase 11 SC 1–4 satisfiable after 11-01 + 11-02
</success_criteria>

<output>
Create `.planning/phases/11-sticky-fallback-dual-model-guardrails/11-02-SUMMARY.md` when done
</output>
