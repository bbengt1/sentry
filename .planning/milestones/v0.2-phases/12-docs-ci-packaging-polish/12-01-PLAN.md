---
phase: 12-docs-ci-packaging-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/edge-serve.md
  - README.md
  - docs/desktop-gpu.md
  - docs/export/README.md
  - scripts/export/README.md
  - docs/README.md
  - THIRD_PARTY_MODELS.md
  - CHANGELOG.md
  - tests/test_export_docs.py
  - tests/test_desktop_docs.py
  - tests/test_third_party_models_doc.py
  - tests/test_edge_serve_docs.py
autonomous: true
requirements:
  - EDGE-DOC-01
  - EDGE-DOC-02
user_setup: []
must_haves:
  truths:
    - "Makers can follow a numbered export → place artifact → sentry serve --profile path (with and without UI) from a discoverable hub"
    - "Root README, desktop-gpu, and scripts/export README no longer claim TensorRT is export-only or jetson is still-PyTorch-only"
    - "Live fixed-class ORT/TRT conditions (preferred + artifact + dep) are discoverable from root README and the edge hub"
    - "AGPL Ultralytics commercial caution explicitly covers YOLO-derived .onnx and .engine artifacts in THIRD_PARTY_MODELS"
    - "No invented dual-model FPS or guaranteed realtime claims on edge hub surfaces"
    - "Export index no longer defers desktop-gpu walkthrough to Phase 7 plan 07-03"
  artifacts:
    - path: "docs/edge-serve.md"
      provides: "Numbered export→serve e2e hub for desktop/Jetson"
      min_lines: 40
    - path: "README.md"
      provides: "Honest live ORT/TRT Export section + jetson profile row + edge hub link"
    - path: "docs/desktop-gpu.md"
      provides: "Jetson/TRT language matches live factory; FPS honesty retained"
    - path: "docs/export/README.md"
      provides: "Links to desktop-gpu + edge-serve; Phase 7 deferral removed"
    - path: "scripts/export/README.md"
      provides: "Live serve conditions pointer; not PyTorch-only"
    - path: "docs/README.md"
      provides: "Start-here link to edge-serve hub"
    - path: "THIRD_PARTY_MODELS.md"
      provides: "Derived ORT/TRT AGPL lineage section"
      contains: "Derived"
    - path: "tests/test_export_docs.py"
      provides: "Root/export honesty keyword locks for EDGE-DOC-01"
    - path: "tests/test_desktop_docs.py"
      provides: "No stale non-live TRT claims on desktop-gpu"
    - path: "tests/test_third_party_models_doc.py"
      provides: "AGPL lineage keyword lock for EDGE-DOC-02"
    - path: "tests/test_edge_serve_docs.py"
      provides: "Edge hub e2e narrative keyword lock"
  key_links:
    - from: "README.md"
      to: "docs/edge-serve.md"
      via: "documentation table + Export section link"
      pattern: "edge-serve"
    - from: "docs/edge-serve.md"
      to: "docs/export/* + sentry serve --profile"
      via: "numbered steps"
      pattern: "export|sentry serve|--profile"
    - from: "THIRD_PARTY_MODELS.md"
      to: "derived .onnx/.engine AGPL caution"
      via: "lineage section + Ultralytics license reference"
      pattern: "derived|\\.onnx|\\.engine|agpl"
    - from: "keyword tests"
      to: "hub surfaces"
      via: "Path.read_text asserts"
      pattern: "not a live tensorrt runtime|still pytorch live"
---

<objective>
Close the v0.2 docs honesty gap: makers get a followable export → onnx/engine → `sentry serve --profile …` (± `--no-ui`) narrative, and AGPL lineage covers YOLO-derived ORT/TRT artifacts.

Purpose: Fix split-brain between accurate `docs/export/*` and stale root/desktop/scripts hubs so Phases 9–11 live ORT/TRT are discoverable without inventing FPS.

Output: `docs/edge-serve.md` hub, refreshed root/desktop/export surfaces, THIRD_PARTY derived AGPL lineage, keyword tests green without hardware.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-docs-ci-packaging-polish/12-RESEARCH.md
@.planning/phases/12-docs-ci-packaging-polish/12-PATTERNS.md
@.planning/phases/12-docs-ci-packaging-polish/12-VALIDATION.md
@.planning/phases/11-sticky-fallback-dual-model-guardrails/11-02-SUMMARY.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-02-SUMMARY.md
@README.md
@docs/desktop-gpu.md
@docs/export/README.md
@docs/export/yolo26-onnx-tensorrt.md
@docs/export/jetson-packaging.md
@scripts/export/README.md
@docs/README.md
@THIRD_PARTY_MODELS.md
@CHANGELOG.md
@tests/test_export_docs.py
@tests/test_desktop_docs.py
@tests/test_third_party_models_doc.py

## Research locks (no CONTEXT.md — honor these)

| Lock | Value |
|------|-------|
| Split-brain fix | Root README, docs/desktop-gpu.md, scripts/export/README.md still claim non-live TRT / torch-only — must fix |
| Hub shape | Prefer thin `docs/edge-serve.md` + links into `docs/export/*` (desktop-gpu numbered pattern) |
| AGPL | Derived `.onnx`/`.engine` lineage in THIRD_PARTY_MODELS — policy caution, not legal certification |
| FPS | No fake dual-model FPS / guaranteed realtime |
| Packages | No new runtime packages; no factory/runtime rewrites |
| CHANGELOG | Optional Unreleased notes; do **not** bump package 0.1.0 → 0.2.0 |

## Stale phrases that must disappear (hub surfaces)

- `not a live TensorRT runtime` / `not a live tensorrt runtime`
- `still PyTorch live` / `still pytorch live`
- `Live sentry serve stays on PyTorch profiles` / `stays on pytorch profiles`
- Export index: `Phase 7 plan` / `07-03` deferral

## Live conditions triad (copy honesty, do not invent)

| Backend | Live when |
|---------|-----------|
| Torch | Default with detect extra |
| ORT | preferred=onnxruntime + allowlisted `.onnx` + `uv sync --extra onnx` |
| TRT | preferred=tensorrt + allowlisted `.engine` + system/JetPack TensorRT (no pip extra) |
| Miss | Soft torch + reason (default); strict fail-closed via `SENTRY_FALLBACK_TO_TORCH=false` |

## Out of scope for 12-01

- CI workflow / gitignore / packaging static locks (12-02)
- Factory, DetectionLoop, FrameBus, `/v1` changes
- Real Jetson hardware validation as a gate
- New pip dependencies
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — keyword tests for edge narrative + AGPL lineage</name>
  <files>tests/test_export_docs.py, tests/test_desktop_docs.py, tests/test_third_party_models_doc.py, tests/test_edge_serve_docs.py</files>
  <read_first>
    - .planning/phases/12-docs-ci-packaging-polish/12-RESEARCH.md (Wave 0 gaps, Code Examples for keyword asserts)
    - .planning/phases/12-docs-ci-packaging-polish/12-PATTERNS.md (forbid/require phrase tables; test module shapes)
    - tests/test_export_docs.py (existing live ORT/TRT + dual-model + no-FPS patterns)
    - tests/test_desktop_docs.py (primary-path pattern)
    - tests/test_third_party_models_doc.py (AGPL YOLO row pattern)
    - README.md (lines ~76 jetson row, ~285–298 Export section — stale claims)
    - docs/desktop-gpu.md (lines ~113–125 stale TRT)
    - scripts/export/README.md (opener “stays on PyTorch profiles”)
    - docs/export/README.md (lines 88–89 Phase 7 deferral)
    - THIRD_PARTY_MODELS.md (no derived .onnx/.engine lineage yet)
  </read_first>
  <behavior>
    - Root README must not contain “not a live tensorrt runtime” or “still pytorch live”; must mention docs/export, sentry serve, --profile, onnx, and tensorrt or .engine
    - scripts/export/README must not say serve “stays on pytorch profiles”; must mention live and/or onnx/engine
    - docs/export/README must not mention Phase 7 plan / 07-03; must link desktop-gpu or edge-serve
    - docs/desktop-gpu must not claim “not a live tensorrt runtime” or “still pytorch live”
    - docs/edge-serve.md must exist with export → sentry serve --profile → headless/--no-ui → backend_live or backend_requested → measure language; no “30 fps dual-model”
    - README must link edge-serve hub
    - THIRD_PARTY_MODELS must mention AGPL plus .onnx and .engine with derived/export/lineage/same AGPL/commercial caution language and caution/evaluate/obligations tone
  </behavior>
  <action>
    Add failing keyword tests first (TDD RED). Do not edit product docs in this task.

    1. Extend tests/test_export_docs.py with tests matching 12-PATTERNS shapes:
       - test_root_readme_edge_live_path_honesty — read REPO_ROOT/README.md; forbid stale non-live phrases; require docs/export, sentry serve, --profile, onnx + (tensorrt or .engine)
       - test_scripts_export_readme_not_pytorch_only — forbid “stays on pytorch profiles”; require live or onnx or engine language
       - test_export_index_no_phase7_deferral — forbid Phase 7 plan / 07-03; require desktop-gpu or edge-serve reference
       Keep all existing export dual-model / sticky / no-FPS tests green without weakening them.

    2. Extend tests/test_desktop_docs.py:
       - test_desktop_doc_no_stale_non_live_trt_claim — forbid “not a live tensorrt runtime” and “still pytorch live”; require tensorrt or export or engine discoverability

    3. Create tests/test_edge_serve_docs.py (mirror test_desktop_docs.py):
       - test_edge_serve_doc_exists
       - test_edge_serve_numbered_export_to_serve_path — require export, sentry serve, --profile, onnx, tensorrt or .engine, --no-ui or headless, backend_live or backend_requested, measure; forbid “30 fps dual-model”
       - test_readme_links_edge_serve_doc — README mentions docs/edge-serve.md or edge-serve

    4. Extend tests/test_third_party_models_doc.py:
       - test_doc_agpl_lineage_for_derived_onnx_engine — AGPL + onnx + engine + derived/export/lineage/same agpl/commercial caution + caution|obligations|evaluate

    5. Run the new/extended tests and confirm they FAIL (RED) against current stale docs. Commit as test-only.

    Avoid: editing markdown product docs in this task; loading real models; requiring Jetson; inventing FPS assert strings that ban legitimate “measure FPS on device” language (forbid dual-model guarantees only).
  </action>
  <acceptance_criteria>
    - New/extended test functions exist in the four test modules listed in files
    - `uv run pytest tests/test_export_docs.py tests/test_desktop_docs.py tests/test_third_party_models_doc.py tests/test_edge_serve_docs.py -q --tb=line` shows RED failures on the new EDGE-DOC asserts (stale docs / missing hub / missing lineage)
    - Existing dual-model / sticky / no Phase-11-deferral tests in test_export_docs.py still collect
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_export_docs.py tests/test_desktop_docs.py tests/test_third_party_models_doc.py tests/test_edge_serve_docs.py -q --tb=line</automated>
  </verify>
  <done>Keyword suite encodes EDGE-DOC-01/02 honesty and fails against pre-fix hub surfaces (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Edge hub + split-brain doc honesty (EDGE-DOC-01)</name>
  <files>docs/edge-serve.md, README.md, docs/desktop-gpu.md, docs/export/README.md, scripts/export/README.md, docs/README.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/12-docs-ci-packaging-polish/12-PATTERNS.md (edge-serve module shape; README/desktop/export replacement prose)
    - docs/desktop-gpu.md (numbered e2e hub template to mirror)
    - docs/export/README.md (accurate live ORT/TRT conditions — source of truth to link, not duplicate wholesale)
    - docs/export/yolo26-onnx-tensorrt.md (live condition table)
    - docs/export/jetson-packaging.md (jetson profile + dual-model measure-on-device)
    - README.md (profiles table + Export section + docs table)
    - scripts/export/README.md (stale opener)
    - docs/README.md (Start-here table + versioning)
    - CHANGELOG.md (Keep-a-Changelog; optional Unreleased)
    - tests/test_edge_serve_docs.py (acceptance keywords from Task 1)
  </read_first>
  <action>
    Make EDGE-DOC-01 true for makers: one numbered path, fix all split-brain hubs. No runtime code.

    1. Create docs/edge-serve.md as a thin hub (desktop-gpu structure, not a second export recipe book):
       - Title framing: export → artifact → sentry serve for live fixed-class ORT/TRT
       - Numbered steps per RESEARCH/PATTERNS: (1) install extras — detect, optional onnx/depth; explicitly NO --extra tensorrt (2) export onnx|engine via scripts/export/export_yolo.py; engines on-device only (3) place artifact / SENTRY_DETECTOR_ONNX | SENTRY_DETECTOR_ENGINE / allowlisted cache stems (4) sentry serve --profile cpu-fallback|jetson|desktop-gpu (5) optional --no-ui headless (6) confirm banner or GET /api/status backend_requested vs backend_live (+ backend_reason) (7) soft default vs strict SENTRY_FALLBACK_TO_TORCH=false (8) dual-model measure-on-device YOLO+DAV2; continuous OV+TRT+DAV2 not first-class; no FPS claims
       - Short on-device validation checklist (manual): export engine on SKU → import tensorrt → serve --profile jetson → confirm backend_live
       - Link out to docs/export/README.md, yolo26-onnx-tensorrt.md, jetson-packaging.md, desktop-gpu.md, THIRD_PARTY_MODELS.md
       - Never invent dual-model FPS tables

    2. Fix README.md:
       - Profiles table: jetson cell must not say “still PyTorch live”; state live TRT when .engine + system TensorRT else soft torch
       - Replace Export section that says “not a live TensorRT runtime in Sentry v1” / “Live sentry serve stays on PyTorch profiles” with live ORT/TRT conditions triad + soft-fall honesty + on-device engines + links to docs/edge-serve.md and docs/export/
       - Documentation table: add edge-serve row next to export

    3. Fix docs/desktop-gpu.md:
       - Jetson comment/command block: remove “still PyTorch live path; TRT via export recipes”
       - “What this path is not”: keep “Not a measured FPS guarantee”; replace “Not a live TensorRT runtime…” with on-device multi-SKU honesty; point to edge-serve + export

    4. Fix docs/export/README.md lines that defer to “later release doc (Phase 7 plan 07-03)”:
       - Point to ../desktop-gpu.md and ../edge-serve.md and jetson-packaging.md
       - Keep existing live ORT/TRT sections and hard rules intact

    5. Fix scripts/export/README.md opener:
       - Remove “Live sentry serve stays on PyTorch profiles”
       - State live fixed-class serve can use exported .onnx/.engine when preferred+artifact+dep met; else soft-fall; link docs/export/README.md and docs/edge-serve.md
       - Keep allowlist safety, on-device engine rule, AGPL pointer, command examples

    6. Fix docs/README.md Start-here table: add edge-serve.md row for export→serve edge path; optional versioning note that package may still be 0.1.0 while planning milestone is v0.2 Edge Runtime (do not bump pyproject version)

    7. CHANGELOG.md: add ## [Unreleased] Changed notes for live ORT/TRT serve docs honesty, hub split-brain fix, AGPL derived lineage (lineage lands in Task 3 — either mention both here or leave AGPL bullet for Task 3). Do not rewrite [0.1.0] history as if it always had live ORT/TRT; optional soft note under Known limitations is OK but prefer Unreleased.

    Avoid: factory/CLI edits; tensorrt pip extra; prebuilt engines; FPS numbers; deep-copying entire export recipes into the hub.
  </action>
  <acceptance_criteria>
    - docs/edge-serve.md exists and contains numbered export→serve steps including --profile and --no-ui or headless
    - `rg -n "not a live [Tt]ensor[Rr][Tt]|still [Pp]y[Tt]orch live|stays on [Pp]y[Tt]orch profiles|Phase 7 plan|07-03" README.md docs/desktop-gpu.md docs/export/README.md scripts/export/README.md docs/edge-serve.md` returns no matches on those stale phrases
    - `rg -n "edge-serve" README.md docs/README.md docs/export/README.md` matches
    - `uv run pytest tests/test_export_docs.py tests/test_desktop_docs.py tests/test_edge_serve_docs.py -q --tb=short` passes EDGE-DOC-01 tests (THIRD_PARTY may still RED until Task 3)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_export_docs.py tests/test_desktop_docs.py tests/test_edge_serve_docs.py -q --tb=short</automated>
  </verify>
  <done>Export→serve narrative is discoverable from root + edge hub; split-brain non-live TRT language is gone from hub surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: AGPL derived-artifact lineage (EDGE-DOC-02) + suite green</name>
  <files>THIRD_PARTY_MODELS.md, CHANGELOG.md, tests/test_third_party_models_doc.py</files>
  <read_first>
    - .planning/phases/12-docs-ci-packaging-polish/12-PATTERNS.md (Derived ORT/TRT AGPL section pattern)
    - THIRD_PARTY_MODELS.md (YOLO/YOLOE AGPL rows + Default selection rules + Ultralytics license URL)
    - docs/export/README.md (existing AGPL caution hard rule)
    - tests/test_third_party_models_doc.py (new lineage test from Task 1 + existing AGPL/Apache tests)
  </read_first>
  <action>
    Close EDGE-DOC-02 with project policy documentation (not legal advice).

    1. Add a “Derived ORT / TRT artifacts (AGPL lineage)” section to THIRD_PARTY_MODELS.md after the license table or under Default selection rules:
       - Artifacts exported from AGPL Ultralytics YOLO/YOLOE weights (including .onnx graphs and TensorRT .engine files from model.export / scripts/export/export_yolo.py) remain subject to the same AGPL commercial caution as source weights
       - Operators must evaluate AGPL obligations before redistributing those artifacts — this is project policy documentation, not legal advice
       - Small table: yolo26{n,s,m}.onnx / .engine → YOLO26 AGPL .pt; YOLOE .onnx experimental → YOLOE AGPL .pt
       - Keep Ultralytics license link in References
       - Do not claim “we certify compliance”

    2. Ensure export docs already reference THIRD_PARTY_MODELS (they do) — only add a one-line pointer from edge-serve or export hard rules if needed for discoverability; do not duplicate legal essays.

    3. Finish CHANGELOG Unreleased AGPL lineage bullet if not done in Task 2.

    4. Run full EDGE-DOC keyword suite and keep existing Apache Small / NC / YOLOE tests green.

    Avoid: claiming legal certification; removing Apache DAV2 default; adding packages; shipping engines in repo.
  </action>
  <acceptance_criteria>
    - `rg -n "Derived|\\.onnx|\\.engine|commercial caution|evaluate" THIRD_PARTY_MODELS.md` shows lineage section
    - `uv run pytest tests/test_third_party_models_doc.py tests/test_export_docs.py tests/test_desktop_docs.py tests/test_edge_serve_docs.py -q --tb=short` all green
    - Existing tests for Apache Small default, NC non-default, YOLO/YOLOE AGPL still pass
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_third_party_models_doc.py tests/test_export_docs.py tests/test_desktop_docs.py tests/test_edge_serve_docs.py -q --tb=short</automated>
  </verify>
  <done>AGPL lineage covers derived .onnx/.engine; full EDGE-DOC keyword suite green without hardware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → maker machine | Untrusted operators follow docs to place artifacts and set env paths |
| Docs → redistributors | License policy text shapes redistribution behavior for AGPL-derived artifacts |
| Doc claims → safety users | Performance/honesty language must not imply certified realtime dual-model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | docs/edge-serve.md artifact path guidance | mitigate | Document only allowlisted env/cache stems (SENTRY_DETECTOR_*, SENTRY_ARTIFACT_ROOT); never arbitrary download URLs or path traversal recipes |
| T-12-02 | Information disclosure | THIRD_PARTY / export AGPL silence | mitigate | Explicit derived .onnx/.engine AGPL commercial caution + Ultralytics license link; keyword-tested |
| T-12-03 | Spoofing | Fake FPS / dual-model realtime claims | mitigate | Measure-on-device language only; keyword forbid “30 fps dual-model” and dual-model guarantees on hub |
| T-12-04 | Elevation of privilege | Docs suggesting pip tensorrt extra | mitigate | Hub explicitly NO --extra tensorrt; system/JetPack only; existing packaging tests remain in 12-02 |
| T-12-05 | Denial of service | Encouraging multi-SKU engine copies | mitigate | Keep on-device / never-copy hard rules linked from hub |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- Keyword suites for export, desktop, edge hub, THIRD_PARTY all green
- Stale non-live TRT phrases absent from README, desktop-gpu, scripts/export README, edge-serve, export index
- No factory/runtime files modified
- No dual-model FPS invention
</verification>

<success_criteria>
1. EDGE-DOC-01: Numbered export → engine/onnx → `sentry serve --profile …` (± UI) path exists and is linked from root README
2. EDGE-DOC-02: THIRD_PARTY_MODELS documents AGPL caution for YOLO-derived .onnx and .engine
3. Split-brain hub language removed; keyword locks prevent regression
4. Full EDGE-DOC test subset green without Jetson/GPU
</success_criteria>

<output>
Create `.planning/phases/12-docs-ci-packaging-polish/12-01-SUMMARY.md` when done
</output>
