---
phase: 10-live-tensorrt-fixed-class-yolo
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - docs/export/yolo26-onnx-tensorrt.md
  - docs/export/jetson-packaging.md
  - docs/export/README.md
  - docs/architecture.md
  - docs/configuration.md
  - src/sentry_ai/config/profiles/jetson.yaml
  - tests/test_export_docs.py
  - tests/test_pyproject_onnx_extra.py
autonomous: true
requirements:
  - TRT-02
  - TRT-03
user_setup: []
must_haves:
  truths:
    - "Docs require on-device engine build; never-copy across SKUs; no multi-SKU prebuilt engines in wheel/repo (TRT-02)"
    - "Docs state live TRT conditions: preferred_backend=tensorrt + allowlisted .engine + system tensorrt importable → backend_live=tensorrt"
    - "Docs describe soft torch fallback with trt_artifact_missing / trt_dep_missing / path_rejected when conditions fail"
    - "Jetson packaging notes cover JetPack/system TensorRT and explicitly forbid a project tensorrt pip pin as required app dep (TRT-03)"
    - "pyproject.toml still has no tensorrt optional-extra; test_no_tensorrt_optional_extra stays green"
    - "Export/serve recipe documents: export_yolo.py --format engine on-device → place under allowlist / SENTRY_DETECTOR_ENGINE → sentry serve --profile jetson"
    - "Absolute 'TRT not live yet / future phase' language is removed or qualified for fixed-class live TRT"
    - "No invented FPS; dual-model VRAM remains measure-on-device (Phase 11 owns first-class guardrails)"
    - "CI still does not require Jetson or system TensorRT"
    - "DetectionLoop / FrameBus / PerceptionStore / /v1 remain unmodified"
  artifacts:
    - path: "docs/export/yolo26-onnx-tensorrt.md"
      provides: "Live TRT conditions + on-device engine lifecycle honesty"
    - path: "docs/export/jetson-packaging.md"
      provides: "JetPack/system TensorRT packaging notes; no pip pin (TRT-03)"
    - path: "docs/export/README.md"
      provides: "Live ORT+TRT vs offline export matrix"
    - path: "docs/architecture.md"
      provides: "Profiles vs live inference honesty includes live TRT"
    - path: "docs/configuration.md"
      provides: "preferred_backend tensorrt live conditions"
    - path: "src/sentry_ai/config/profiles/jetson.yaml"
      provides: "Comment honesty for live TRT when .engine + system tensorrt"
    - path: "tests/test_export_docs.py"
      provides: "Keyword asserts for live TRT + on-device + no pip pin"
    - path: "tests/test_pyproject_onnx_extra.py"
      provides: "Static assert no tensorrt optional extra (keep)"
  key_links:
    - from: "docs live TRT conditions"
      to: "factory live TRT path (10-01)"
      via: "documented preferred + .engine + system tensorrt → backend_live=tensorrt"
      pattern: "backend_live|tensorrt|\\.engine"
    - from: "docs on-device rules"
      to: "scripts/export/export_yolo.py --format engine"
      via: "maker recipe without shipping engines"
      pattern: "on-device|never copy|export_yolo|--format engine"
    - from: "jetson-packaging.md"
      to: "system/JetPack TensorRT"
      via: "no project tensorrt pip extra"
      pattern: "JetPack|system.*TensorRT|no.*tensorrt.*pip|pip extra"
    - from: "test_no_tensorrt_optional_extra"
      to: "pyproject.toml optional-dependencies"
      via: "static absence of tensorrt extra"
      pattern: "tensorrt"
---

<objective>
Update operator docs, jetson profile comments, and keyword tests so makers see honest live TensorRT conditions, on-device engine lifecycle rules, and JetPack/system TensorRT packaging — without a project `tensorrt` pip pin and without multi-SKU prebuilt engines.

Purpose: Close TRT-02 (on-device build required; no multi-SKU engines in wheel/repo) and TRT-03 (Jetson packaging notes; no generic tensorrt pip pin as app dep). Live factory path is assumed shipped by 10-01.

Output: Revised export/architecture/configuration/jetson docs + keyword/static tests green; packaging hygiene preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-RESEARCH.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-PATTERNS.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-VALIDATION.md
@.planning/phases/10-live-tensorrt-fixed-class-yolo/10-01-PLAN.md
@docs/export/yolo26-onnx-tensorrt.md
@docs/export/jetson-packaging.md
@docs/export/README.md
@docs/architecture.md
@docs/configuration.md
@src/sentry_ai/config/profiles/jetson.yaml
@scripts/export/export_yolo.py
@tests/test_export_docs.py
@tests/test_pyproject_onnx_extra.py
@pyproject.toml

## Locked decisions (do not re-litigate)

| Decision | Value | Source |
|----------|-------|--------|
| On-device engines | Build on same GPU arch + TRT/JetPack as production; never copy across SKUs | TRT-02 / RESEARCH |
| No multi-SKU ship | Do not put `.engine` in repo, wheel, or multi-SKU Releases | TRT-02 / STATE |
| No pip tensorrt | No project optional-extra or required pin for `tensorrt` / `tensorrt-cu*` | TRT-03 |
| Live conditions | preferred_backend=tensorrt + allowlisted `.engine` + system tensorrt importable → backend_live=tensorrt | 10-01 / TRT-01 |
| Soft fallback honesty | trt_artifact_missing \| trt_dep_missing \| path_rejected | VALIDATION |
| Env | Document `SENTRY_DETECTOR_ENGINE` + optional `SENTRY_ARTIFACT_ROOT` | Phase 8 |
| Export tool | Existing `scripts/export/export_yolo.py --format engine` (no new exporter) | RESEARCH |
| CI | Docs/tests never require Jetson or system TensorRT in GHA | EDGE-CI-02 spirit |
| Spine freeze | No DetectionLoop / bus / store / `/v1` edits | EDGE-RT-01 |
| Factory code | Do not re-implement factory in this plan (10-01 owns code path) | ROADMAP split |

## Claude's discretion (chosen for this plan)

| Item | Choice |
|------|--------|
| JetPack matrix depth | “Verify on device” + system/JetPack TRT path; do **not** invent apt package pins or FPS tables without hardware |
| Doc surface split | Primary live TRT table in `yolo26-onnx-tensorrt.md`; JetPack/system notes + no-pip in `jetson-packaging.md`; matrix in export README; short honesty in architecture/configuration; jetson.yaml comments only |
| Conf caveat | Document that runtime conf works via Ultralytics postprocess NMS for default (non-`nms=True` baked) engines; “when supported” matches TRT-04 wording |
| Dual-model | One honesty sentence: measure on device; Phase 11 owns first-class dual-model guardrails — no FPS claims |
| profile_runtime docstring | Optional one-line honesty if it still says tensorrt is never live runtime; not required if architecture/configuration cover it |
| pyproject | **Do not** add tensorrt extra; only re-run / keep static test (no production pyproject change expected) |

## Interfaces from 10-01 (docs must match)

```text
Live TRT when:
  preferred_backend == tensorrt
  + allowlisted existing .engine (resolve / SENTRY_DETECTOR_ENGINE)
  + importlib find_spec("tensorrt") succeeds (system/JetPack)
  → backend_live=tensorrt, worker weights end with .engine

Soft fall:
  trt_artifact_missing | trt_dep_missing | path_rejected → backend_live=torch
```

Maker recipe (docs):
```bash
uv sync --extra detect   # (+ depth if dual-model); NO --extra tensorrt
# verify: python -c "import tensorrt"
uv run python scripts/export/export_yolo.py \
  --weights yolo26n.pt --format engine --imgsz 640 --device 0
# place under allowlist or:
export SENTRY_DETECTOR_ENGINE=/allowlisted/path/yolo26n.engine
uv run sentry serve --profile jetson --source usb --device 0
```

## Out of scope for 10-02

- Re-implementing factory live branch (10-01)
- Sticky fallback policy (Phase 11)
- Full edge-serve narrative polish + AGPL lineage refresh (Phase 12 EDGE-DOC-*)
- CI selection matrix hardening beyond TRT docs keywords (Phase 12 EDGE-CI-*)
- Shipping or generating real `.engine` artifacts
- Changing YAML profile field values (comments only for jetson.yaml)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Docs honesty for live TRT + on-device lifecycle (TRT-02, TRT-03)</name>
  <files>docs/export/yolo26-onnx-tensorrt.md, docs/export/jetson-packaging.md, docs/export/README.md, docs/architecture.md, docs/configuration.md, src/sentry_ai/config/profiles/jetson.yaml</files>
  <read_first>
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-RESEARCH.md (docs debt, on-device lifecycle, pitfalls 2/5/8, recommended plan 10-02)
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-PATTERNS.md (export + product docs update pattern + keyword-friendly table)
    - docs/export/yolo26-onnx-tensorrt.md (intro table still saying TRT non-live; profile section; deferred list)
    - docs/export/jetson-packaging.md (preferred_backend row; TensorRT/JetPack section; no-pip note)
    - docs/export/README.md (live matrix row “TRT not live yet”)
    - docs/architecture.md (Profiles vs live inference — TRT future-phase language)
    - docs/configuration.md (preferred_backend tensorrt soft torch until future phase)
    - src/sentry_ai/config/profiles/jetson.yaml (header comments)
    - scripts/export/export_yolo.py (confirm --format engine still present; docs only)
  </read_first>
  <behavior>
    - Export docs state live fixed-class TRT when preferred_backend=tensorrt + valid allowlisted .engine + system tensorrt importable
    - Soft-fallback honesty: missing artifact/deps/path → torch + trt_* / path_rejected reasons (not silent TRT claim)
    - On-device / never-copy / no-prebuilt multi-SKU engines remain explicit (TRT-02)
    - Jetson packaging states JetPack/system TensorRT and no project tensorrt pip extra (TRT-03)
    - Env SENTRY_DETECTOR_ENGINE / SENTRY_ARTIFACT_ROOT documented for engine placement
    - Serve recipe: on-device export → allowlist → sentry serve --profile jetson
    - Absolute “live TensorRT is not claimed until a future phase” / “TRT not live yet” language removed or replaced for fixed-class
    - Live ORT honesty from Phase 9 preserved
    - jetson.yaml comments describe live TRT conditions; YAML keys/values unchanged
    - No invented FPS; no multi-SKU engine ship; CI does not require Jetson
  </behavior>
  <action>
    1. Update `docs/export/yolo26-onnx-tensorrt.md`:
       - Add live TRT row alongside live ORT (PATTERNS recommended table): preferred_backend=tensorrt + allowlisted `.engine` + system `tensorrt` importable → backend_live=tensorrt (Ultralytics-native YOLO("*.engine"))
       - Soft torch fallback row for TRT reasons: trt_artifact_missing / trt_dep_missing / path_rejected
       - Replace “TensorRT still non-live / future phase” language with live conditions + on-device build still required
       - Profile section: jetson preferred tensorrt **can be live** with artifact+system TRT; keep desktop-gpu torch default; rebuild engines on edge
       - Keep AGPL, on-device rules, never-copy, no prebuilt multi-SKU, no invented FPS
       - Document conf caveat briefly: runtime conf via Ultralytics postprocess NMS for default engines (export without baked nms=True); “when supported”
       - Document env `SENTRY_DETECTOR_ENGINE` / optional `SENTRY_ARTIFACT_ROOT`
       - Clarify deferred items: custom TRT InferenceBackend class is not the Phase 10 path (factory Ultralytics path is); sticky policy Phase 11; dual-model first-class Phase 11

    2. Update `docs/export/jetson-packaging.md` (TRT-03 primary surface):
       - Flip “Live Sentry inference remains PyTorch / TensorRT is export only” to live TRT when conditions met
       - preferred_backend row: tensorrt can be live with .engine + system TRT; else soft torch + reason
       - Strengthen JetPack/system TensorRT section: use JetPack-bundled or NVIDIA system TRT; verify with `python -c "import tensorrt"`; **no** project `tensorrt` pip extra / pin
       - Keep on-device build, never copy, no prebuilt engines, rebuild after JetPack upgrades
       - Point to export_yolo.py --format engine and yolo26 doc
       - Optional dual-model honesty: TRT YOLO + torch depth may share GPU — measure on device; no FPS claims this milestone
       - Do not invent JetPack apt package version pins without hardware verification

    3. Update `docs/export/README.md`:
       - Live matrix: fixed-class ORT live (Phase 9) **and** fixed-class TRT live (Phase 10) under documented conditions; torch default elsewhere
       - Remove “TRT not live yet / future live-TRT phase” absolute language
       - Keep offline export rules; no tensorrt pip extra; CI does not require Jetson

    4. Update `docs/architecture.md` and `docs/configuration.md`:
       - preferred_backend tensorrt may be live when .engine + system tensorrt present; otherwise soft torch with reason
       - Preserve ORT live honesty; preserve desktop-gpu torch default
       - Link export docs for engine lifecycle

    5. Update `src/sentry_ai/config/profiles/jetson.yaml` header comments only (do **not** change YAML keys/values):
       - preferred_backend=tensorrt → live TRT when allowlisted .engine + system tensorrt importable; else soft torch fallback with honest reason
       - Build engines on-device only

    6. Do not modify factory.py, pyproject extras, DetectionLoop, or add `.engine` files to the repo.
  </action>
  <acceptance_criteria>
    - yolo26-onnx-tensorrt.md mentions live tensorrt conditions with .engine and system tensorrt / backend_live
    - yolo26 + jetson-packaging still contain on-device, never-copy (or equivalent), and no-prebuilt engine rules
    - jetson-packaging states no project tensorrt pip extra / use JetPack or system TensorRT
    - export README no longer claims TRT is not live yet as absolute future work for fixed-class
    - architecture.md and configuration.md describe live TRT possibility for preferred tensorrt
    - jetson.yaml comments updated; preferred_backend value still `tensorrt` and other fields unchanged
    - No new `.engine` files added under repo; pyproject optional-dependencies still has no tensorrt key
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_export_docs.py tests/test_pyproject_onnx_extra.py -q</automated>
  </verify>
  <done>Docs and jetson profile comments honestly describe live TRT conditions, on-device lifecycle, and system/JetPack TRT with no pip pin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keyword/static tests for TRT-02/03 + phase suite gate</name>
  <files>tests/test_export_docs.py, tests/test_pyproject_onnx_extra.py</files>
  <read_first>
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-VALIDATION.md (TRT-02/03 rows; Wave 0 docs tests)
    - .planning/phases/10-live-tensorrt-fixed-class-yolo/10-PATTERNS.md (keyword-test friendly honesty table)
    - tests/test_export_docs.py (existing live ORT + on-device TRT keyword tests)
    - tests/test_pyproject_onnx_extra.py (test_no_tensorrt_optional_extra)
    - docs/export/yolo26-onnx-tensorrt.md (post-Task-1 content)
    - docs/export/jetson-packaging.md (post-Task-1 content)
    - docs/export/README.md (post-Task-1 content)
  </read_first>
  <behavior>
    - Keyword tests assert live TRT conditions appear in export docs (tensorrt + .engine + live / backend_live language)
    - On-device / never-copy / no-prebuilt asserts remain green (TRT-02)
    - Jetson packaging keywords assert system/JetPack TensorRT and no project pip pin (TRT-03)
    - Soft-fallback honesty for TRT reasons appears in docs somewhere (artifact/dep/path)
    - test_no_tensorrt_optional_extra remains green
    - Existing live ORT keyword tests remain green
    - Absolute “TRT never live / future phase only” is not required; do not assert pytorch-only forever for fixed-class TRT
  </behavior>
  <action>
    1. Extend `tests/test_export_docs.py`:
       - Assert yolo26 and/or README text mentions live tensorrt conditions (keywords flexible but must couple: `tensorrt` or `TensorRT`, live language, and `.engine` / engine artifact)
       - Assert soft-fallback / missing artifact or dependency honesty appears for TRT (reason names or clear prose: artifact missing / system tensorrt missing / path rejected)
       - Assert jetson-packaging (or yolo26) mentions JetPack or system TensorRT and that project does not use a tensorrt pip extra / pin
       - Keep on-device / never-copy / no prebuilt engine asserts green
       - Keep live ORT keyword coverage green
       - Do not require Jetson hardware or real engine files

    2. Keep `tests/test_pyproject_onnx_extra.py::test_no_tensorrt_optional_extra` unchanged unless a docstring tweak is needed; **must stay green**. Do not add tensorrt to pyproject.

    3. Phase suite gate for this plan + dependency:
       - Run: factory, trt parity, backend honesty, export docs, pyproject no-tensorrt, artifact paths
       - Confirm spine freeze with `rg` / git path check: no modifications to loop.py, frame_bus, perception_store, routes_v1
       - Confirm no `tensorrt` key under optional-dependencies in pyproject.toml
       - Confirm factory still has live TRT path from 10-01 (backend_live tensorrt string present)

    4. Do not edit production factory code in this task unless a docs test reveals a 10-01 regression (prefer report-back).
  </action>
  <acceptance_criteria>
    - tests/test_export_docs.py has automated asserts covering live TRT conditions, on-device rules, and no pip tensorrt / system-or-JetPack packaging
    - test_no_tensorrt_optional_extra passes
    - Combined phase gate suite green without Jetson/system TensorRT
    - pyproject.toml has no tensorrt optional-extra
    - Spine freeze intact
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_detection_factory.py tests/test_trt_parity.py tests/test_backend_honesty_status.py tests/test_export_docs.py tests/test_pyproject_onnx_extra.py tests/test_artifact_paths.py -q</automated>
  </verify>
  <done>Keyword/static tests prove TRT-02/03 docs honesty; full Phase 10 automated dimension set green without Jetson/system TensorRT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator trust | Docs that claim live TRT without path+dep conditions enable spoofed operational expectations |
| Packaging → supply chain | Adding pip `tensorrt` would pull CUDA-SKU-specific wheels and violate TRT-03 |
| Repo/wheel contents | Shipping `.engine` files enables SKU-mismatched loads / supply of untrusted binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Spoofing | Export/architecture docs | mitigate | Keyword tests require live TRT conditions + soft-fallback honesty; remove absolute future-phase-only lies |
| T-10-10 | Tampering | Project packaging extras | mitigate | Keep `test_no_tensorrt_optional_extra`; never add tensorrt/tensorrt-cu* to pyproject |
| T-10-11 | Tampering | Prebuilt multi-SKU `.engine` in repo/wheel | mitigate | Docs + keyword tests: on-device only, never copy, no prebuilt; hatch force-include already excludes engines — do not add engines |
| T-10-12 | Information disclosure | Invented FPS / dual-model claims | mitigate | Explicit no-FPS / measure-on-device language; dual-model first-class deferred to Phase 11 |
| T-10-13 | Elevation of privilege | Docs suggesting arbitrary path download of engines | mitigate | Document only allowlisted env/cache/cwd paths (`SENTRY_DETECTOR_ENGINE`, `SENTRY_ARTIFACT_ROOT`); no arbitrary weight download |
| T-10-SC | Tampering | pip install of tensorrt mid-docs guidance | mitigate | Docs forbid project pip pin; direct makers to JetPack/system TRT only; no new package installs this plan |
</threat_model>

<verification>
- `uv run pytest tests/test_export_docs.py tests/test_pyproject_onnx_extra.py tests/test_detection_factory.py tests/test_trt_parity.py tests/test_backend_honesty_status.py tests/test_artifact_paths.py -q` green
- `uv run ruff check tests/test_export_docs.py` green (if available)
- `rg -n 'tensorrt' docs/export/yolo26-onnx-tensorrt.md docs/export/jetson-packaging.md` shows live conditions + system/JetPack + no pip extra language
- `rg -n 'on-device|never copy|prebuilt' docs/export -i` matches
- `rg -n 'TRT not live yet|live TensorRT is not claimed until a future' docs/` is empty (or only historical notes clearly superseded)
- `python -c "import tomllib,pathlib; d=tomllib.loads(pathlib.Path('pyproject.toml').read_text()); assert 'tensorrt' not in d.get('project',{}).get('optional-dependencies',{})"`
- No edits to `models/detection/loop.py`, `bus/frame_bus.py`, `state/perception_store.py`, `api/routes_v1.py`
- No `*.engine` files committed
</verification>

<success_criteria>
1. Docs require on-device engine build and forbid multi-SKU prebuilt engines in wheel/repo (TRT-02).
2. Jetson packaging notes cover JetPack/system TensorRT and forbid project tensorrt pip pin (TRT-03).
3. Docs state live TRT conditions consistent with 10-01 factory behavior + soft-fallback reasons.
4. Keyword/static tests encode TRT-02/03 honesty; no-tensorrt extra test green.
5. Export → place engine → serve recipe is discoverable without inventing FPS or shipping engines.
6. Phase 10 automated validation map (factory + parity + honesty + docs + packaging) green without Jetson.
</success_criteria>

<output>
Create `.planning/phases/10-live-tensorrt-fixed-class-yolo/10-02-SUMMARY.md` when done
</output>
