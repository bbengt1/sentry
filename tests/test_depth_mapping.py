"""DEPTH-01/04: depth mode → model id + kind/unit mapping (filled in Task 2)."""

from __future__ import annotations

import pytest

# Implemented in Task 2 (mapping honesty tests).
pytestmark = pytest.mark.skip(reason="04-01 Task 2 — mapping not yet implemented")


def test_kind_for_mode_placeholder() -> None:
    raise AssertionError("should be skipped")
